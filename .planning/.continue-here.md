---
context: default
phase: null
milestone: null
task: null
total_tasks: null
status: paused
last_updated: 2026-06-09T06:49:53.054Z
---

# Continue Here — VAC-crew unit duplication fix (DONE + merged) + poc PDF

<current_state>
A standalone debug+fix session, not phase execution. The VAC-crew
duplicate-billing bug is **fully fixed, verified on real data, and merged to
`origin/master`** (squash commit `47fe16d`, PR #274). The next scheduled
GitHub Actions run will use the fixed code. A boss-ready print PDF for
WR 91029362 was also generated. Nothing is left to do on the fix itself; the
only open items are operational (rotate a token, optionally tidy local git).
</current_state>

## Infrastructure / Git State (read before touching git)

- **`origin/master` is correct** — contains the merged fix (`47fe16d`) +
  auto docs-changelog (`a0ba96e`). The scheduled run pulls this. ✅
- **Local `master` is DIVERGED / stale** — it sits on pre-session WIP commits
  `0ae1427` + `86413d8` (never pushed; their content was swept into the
  squash, so they are now redundant) and still has the pre-existing
  uncommitted `.github/prompts/*.md` edits. gh's post-merge auto-pull could
  not fast-forward — this is expected and harmless.
- **To sync local (do deliberately, not blindly):**
  ```bash
  git stash push -- .github/prompts/      # preserve pre-existing prompt edits
  git reset --hard origin/master          # untracked poc/, generated_docs, .planning/debug survive
  git stash pop                            # restore prompt edits
  ```
- Untracked artifacts that must survive any sync: `poc/` (incl. the new PDF),
  `generated_docs/` (ephemeral run output), `.planning/debug/*.md`.

<completed_work>

- VAC-crew duplication diagnosed via `/gsd-debug` (session
  `.planning/debug/vac-crew-leak-foreman-sheet.md`, status **resolved**).
- Fix implemented, reviewed (Codex), finalized, and squash-merged as PR #274
  (`47fe16d`). Net production change vs baseline:
  1. **Unit-level cross-row reconciliation** in `group_source_rows`: a
     pre-pass collects `(WR, week, Pole #, CU)` of every `__is_vac_crew` row;
     any non-VAC row with a matching unit identity is dropped (`continue`)
     from all non-VAC variants, so a VAC-claimed unit appears ONLY on the
     VacCrew sheet. Per-unit, not per-pole.
  2. `"EXCLUDING from foreman/helper"` added to `_PII_LOG_MARKERS` (+ test).
- Tests: `tests/test_vac_crew_exclusion_leak.py` (rewritten),
  `tests/test_sentry_log_sanitizer.py`. Suite **1055 passed, 0 failures**.
- Verified on real WR 90922617 (read-only `SKIP_UPLOAD` dry run): foreman
  total $30,023.63 → $26,098.52 (exactly the 3 VAC-claimed units), VacCrew
  total unchanged, zero cross-sheet duplicates, foreman's other Point 11
  units retained.
- Living Ledger entry recorded (final units-required contract).
- **poc PDF generated:** `poc/out/WR_91029362_WeekEnding_060726_Leslie_Maybin_PRINT.pdf`
  (4 days, 8 pages) via `poc/build_wr_pdf.py` from the existing billing Excel.
</completed_work>

<remaining_work>

- (Operational) Rotate the Smartsheet API token — see Pending Human Actions.
- (Optional) Sync local `master` to `origin/master` — see Git State above.
- No code work remains on the VAC fix; it is merged and live on master.
</remaining_work>

<decisions_made>

- VAC-crew billing **requires `Units Completed?` checked** (same rule as the
  helper foreman). Operator confirmed VAC rows always have it checked in
  practice. This avoided propagating a widened "billable" definition through
  the Subproject-C attribution chain (bulk prefetch / claimer freeze /
  `freeze_row`) — an earlier "credit VAC even when unchecked" widening was
  **reverted** for exactly this reason (Codex correctly flagged the chain).
- Exclusion grain is **per-UNIT (WR + week + Pole # + CU), not per-pole** — a
  pole can hold both VAC-claimed and genuinely foreman-completed units.
- Squash-merge to master so the next scheduled run picks up the fix.
</decisions_made>

<blockers>
- Local master diverged from origin/master (cosmetic; see Git State for the
  safe sync). Remote is correct.
</blockers>

## Pending Human Actions

- [ ] **Rotate the Smartsheet API token** (BLOCKING/security) — a live token
      was pasted into the chat to run the read-only verification dry run;
      treat it as compromised and rotate it in Smartsheet.
- [ ] (Optional) Sync local `master` to `origin/master` (see Git State).

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Description | Severity | Prevention |
|---|---|---|---|
| Trusting green unit tests as proof of a prod fix | The first (row-local) fix passed all unit tests but did NOT fix the real bug — the real mechanism (two source rows across sheets) only showed in a real-WR dry run. | advisory | For billing-logic fixes, verify against a real WR via `SKIP_UPLOAD` dry run, not just unit tests. |
| Letting a bug report's framing override the code | The report said "totals exclude but line item stays"; the real mechanism was different (cross-sheet duplicate membership). | advisory | Cross-check report wording against actual code; keep system knowledge in `.planning`/ledger, not assumptions. |
| Expecting subagents to run commands | Subagents in this harness are **read-only** — all PowerShell/Bash/python calls are denied for them. | advisory | Run pipeline/poc/pytest commands in the MAIN context; use subagents only for read-only analysis. |

## Required Reading (in order)
1. `.planning/debug/vac-crew-leak-foreman-sheet.md` — full debug record + final outcome.
2. `memory-bank/living-ledger.md` (latest entry) — the established VAC-crew rule.
3. This file's Git State section — before any git reconciliation.

<context>
The fix is done and shipped. Mentally: the only thing a resuming agent should
do is (1) confirm the token was rotated, (2) optionally tidy local git to
match origin. Do NOT re-open the VAC logic — it is merged and verified. If new
VAC issues arise, start a fresh `/gsd-debug` session.
</context>

<next_action>
Start with: confirm the Smartsheet token has been rotated. Then, if desired,
sync local master to origin/master using the Git State commands. No further
work is required on the VAC fix.
</next_action>
