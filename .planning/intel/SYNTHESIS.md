# Synthesis Summary

Generated by `gsd-doc-synthesizer` on 2026-05-14.

Mode: `new` (no pre-existing `.planning/` context to merge against).

## Doc counts by type

- DOC: 46 (45 high-confidence + 1 medium = `CLAUDE.md`)
- SPEC: 2 (both medium):
  - `.github/instructions/subcontractor-pricing-folder-discovery.instructions.md`
  - `docs/railway-to-render-transition-plan.md`
- ADR: 0
- PRD: 0
- UNKNOWN: 0
- Locked (`locked: true`): 0
- Per-doc `precedence` overrides: 0

## Decisions extracted

- 0 canonical ADRs (none in ingest)
- ~30 ADR-equivalent rules extracted from the `CLAUDE.md`
  Living Ledger, dates preserved. These are the operative
  locked rules the upcoming Subcontractor Rate Logic phase
  must respect.
- 8 SPEC-level decisions extracted from the two SPECs.

Notable locked-equivalent rules:

- 2026-04-20 — Sentry release naming guardrail (slash-free)
- 2026-04-20 — `SENTRY_ENABLE_LOGS` gate + `before_send_log`
- 2026-04-21 — VAC crew CU-direct fallback rule
- 2026-04-22 — Hash key MUST capture per-row VAC crew fields
- 2026-04-22 — Attachment pre-fetch sub-budget + non-blocking
  executor shutdown
- 2026-04-22 — Time-budget proportions (180 / 195 / 10 / 45 / 2)
- 2026-04-23 — Security audit: WR# sanitization at every
  derivation site; `_redact_exception_message` for Sentry
  contexts
- 2026-04-23 — `RATE_RECALC_WEEKLY_FALLBACK` env var
- 2026-04-24 — `RATE_RECALC_SKIP_ORIGINAL_CONTRACT` guard
- 2026-04-24 — CSV-side rate recalc retired at workflow
  layer (LEGACY pin); retention policy documented
- 2026-04-25 — PostgREST SQLSTATE classification
  (`42`/`22`/`23` permanent; `08`/`40`/`53`/`57` transient)
- 2026-04-25 — `freeze_row` parallelization via
  `ThreadPoolExecutor(max_workers=min(PARALLEL_WORKERS,
  len(group_rows)))`

## Requirements extracted

- 7 requirements (REQ-*) extracted exclusively from
  `docs/railway-to-render-transition-plan.md`. No other doc
  in the ingest set carries PRD-style scope + acceptance
  criteria pairs.
- IDs:
  - REQ-railway-render-migration
  - REQ-artifact-explorer-v1
  - REQ-excel-styled-renderer
  - REQ-cross-artifact-search
  - REQ-backend-routes-for-explorer
  - REQ-migration-staging-verification
  - REQ-migration-decommission
- Plus an explicit "deferred-from-v1" list (CSV per-sheet,
  PDF, parsed-JSON export, Supabase-backed search, `RunCard`
  thumbnails).

## Constraints extracted

- 15 constraint blocks covering: Subcontractor pricing
  contract (business-rule), folder-based discovery
  contract (api-contract), Smartsheet API constraints (nfr/
  protocol), Excel generation constraints (api-contract),
  change-detection key composition (business-rule),
  time-budget contract (nfr), discovery-cache invariants
  (schema), Render service configuration (protocol/infra),
  Render env-var contract (api-contract), Render risks
  & mitigations (nfr), in-memory cache memory budget (nfr),
  Express backend conventions (protocol), Sentry tagging
  contract (protocol), attachment upload pattern (api-
  contract), `advanced_options` parser contract (api-
  contract), schedule contract (nfr), Supabase / billing_audit
  schema contract (schema).

## Context topics extracted

- 21 topic blocks: project overview, repo layout, data
  pipeline architecture, env-var inventory, variant
  detection, CI/CD, Sentry, Azure DevOps mirror, portal-v2,
  Docusaurus runbook, memory-bank conventions, active
  in-flight work, critical pitfalls, coding conventions,
  testing strategy, smartsheet-debugger agent, helper
  scripts, artifact preservation and dashboard surface.

## Conflicts

See `.planning/INGEST-CONFLICTS.md` for the three-bucket
detail.

- BLOCKERS: 0
- WARNINGS: 1 (Living Ledger embedded-ADR extraction —
  user confirmation requested but synthesizer has proceeded
  on the default-yes assumption)
- INFO: 8 (no-ADR-in-set acknowledgement, 4 DOC↔DOC peer-
  citation cycles, 1 SPEC-reinforces-Ledger alignment,
  1 internal supersession recorded, 1 missing pre-migration
  ADR deliverable surfaced)

## File pointers

- `.planning/intel/decisions.md`
- `.planning/intel/requirements.md`
- `.planning/intel/constraints.md`
- `.planning/intel/context.md`
- `.planning/INGEST-CONFLICTS.md`

## Routing readiness

Safe to route to `gsd-roadmapper`, with the one WARNING
acknowledged. The roadmapper should:

1. Treat every entry under `decisions.md` "ADR-equivalent
   rules from CLAUDE.md Living Ledger" as operative-locked
   when scheduling the upcoming Subcontractor Rate Logic
   phase.
2. Treat the Railway→Render plan's Phase 0 step 3
   pre-migration ADR (currently missing under
   `memory-bank/adr/`) as a scheduled deliverable, not a
   blocker.
3. Honor the `decisions.md` 2026-04-24 14:30 retention
   policy if any future plan re-introduces CSV-side rate
   recalc — the env vars `RATE_CUTOFF_DATE`/`NEW_RATES_CSV`/
   `OLD_RATES_CSV` are pinned empty in the weekly workflow
   and the un-retire pre-condition is documented.
