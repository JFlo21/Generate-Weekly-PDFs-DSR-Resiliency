---
phase: 1.1
slug: subcontractor-helper-shadow-rescue
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-19
---

# Phase 1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x (existing) |
| **Config file** | none — Wave 0 not required (existing infrastructure covers all phase requirements) |
| **Quick run command** | `pytest tests/test_subcontractor_pricing.py -v` |
| **Full suite command** | `pytest tests/ -v` |
| **Estimated runtime** | ~25-40 seconds (quick) / ~90 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/test_subcontractor_pricing.py -v` (or the focused file modified by the task)
- **After every plan wave:** Run `pytest tests/ -v`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 40 seconds (quick) / 90 seconds (full)

---

## Per-Task Verification Map

*Populated during planning — each plan's tasks land here with Test Type + Automated Command + File Exists. Filled in by the planner via per-task `<automated>` blocks and back-referenced from PLAN.md frontmatter.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD     | TBD  | TBD  | SUB-08..12  | —          | TBD             | unit/e2e  | `pytest tests/... -v` | ✅          | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements. No Wave 0 stubs needed — the pytest harness, `tests/test_subcontractor_pricing.py`, `tests/test_billing_audit_shadow.py`, `tests/test_security_audit_followup.py`, `tests/test_performance_optimizations.py`, and `tests/validate_production_safety.py` are all already in place from Phase 1.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Data-team coordination for `billing_audit.attribution_snapshot` access | SUB-11 (Bug C) | RPC creation OR PostgREST exposed-schemas change is owned by the data team, not this codebase. Must be confirmed at merge time. | Confirm `lookup_attribution(p_wr, p_week_ending, p_smartsheet_row_id)` RPC exists in production Supabase (or that direct `SELECT` on `billing_audit.attribution_snapshot` returns) before flipping `SUBCONTRACTOR_HELPER_CLAIM_ATTRIBUTION_ENABLED=1`. The code's fall-back-to-current-helper path (D-12) makes Bug C safe-to-merge before this confirmation, but the feature only activates after the data team's side ships. |
| Cron-run byte-identical guarantee (ROADMAP success criterion #5) | SUB-08..12 | Requires a real scheduled GHA run against production Smartsheet (the synthetic-mock unit tests cannot exercise the real source-sheet diversity). | After merge, watch the first 2 cron runs and verify that primary / helper / vac_crew / ORIG-folder Excel attachments produce the same hashes as the pre-merge baseline (hash-history diff in `generated_docs/hash_history.json`). Only subcontractor variant outputs should change. |
| Hash-history one-time prune fires once and then no-ops | SUB-12 | The version-advance trigger is observable across a session boundary, not within a single test run. | Verify the first post-merge cron run logs ONE INFO line naming the orphan count + WR list; subsequent runs in the same `PHASE_1_1_HASH_PRUNE_VERSION` band emit zero such lines. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies *(planner-populated)*
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify *(planner-populated)*
- [ ] Wave 0 covers all MISSING references *(N/A — no Wave 0 needed)*
- [ ] No watch-mode flags *(verified by quick-run command above — `pytest -v` is one-shot, not watch)*
- [ ] Feedback latency < 90s *(verified — full suite ~90s)*
- [ ] `nyquist_compliant: true` set in frontmatter *(set by the planner after Per-Task Verification Map is filled)*

**Approval:** pending
