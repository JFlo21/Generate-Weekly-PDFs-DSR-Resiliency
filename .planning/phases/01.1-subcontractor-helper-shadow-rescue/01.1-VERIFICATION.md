---
phase: 01.1-subcontractor-helper-shadow-rescue
verified: 2026-05-19T20:00:00Z
status: human_needed
score: 6/7 must-haves verified (1 deferred to live cron, plus 2 carry-over BLOCKERs from REVIEW.md)
overrides_applied: 0
gaps:
  - truth: "lookup_attribution PostgREST RPC has a contract entry in billing_audit/schema.sql"
    status: failed
    reason: "CR-01 BLOCKER from 01.1-REVIEW.md is unresolved in the merged code. CLAUDE.md [2026-04-25 12:00] Living Ledger entry rule 1 mandates: 'Any new Supabase table or column the pipeline reads/writes MUST be defined in billing_audit/schema.sql in the same PR that adds the Python code.' The pipeline reads from billing_audit.attribution_snapshot via the new lookup_attribution RPC, but neither the table DDL nor the RPC parameter+return-shape contract is documented in schema.sql. Verified via grep: 'lookup_attribution' has 0 matches in billing_audit/schema.sql."
    artifacts:
      - path: "billing_audit/schema.sql"
        issue: "Missing contract block for lookup_attribution RPC (p_wr / p_week_ending / p_smartsheet_row_id) and for the billing_audit.attribution_snapshot read surface (helper / helper_dept / source_run_id columns)"
    missing:
      - "Append a comment-style contract block for lookup_attribution to billing_audit/schema.sql matching the existing freeze_attribution shape at L128-156. Operators should not have to read writer.py + CONTEXT.md + REVIEW.md to discover what to deploy in Supabase."
  - truth: "billing_audit/writer.py module docstring accurately describes its public surface (now includes a reader)"
    status: failed
    reason: "CR-02 BLOCKER from 01.1-REVIEW.md is unresolved in the merged code. billing_audit/writer.py:1-14 still reads 'Writes only. No read / hydration path — that lands in the follow-up PR.' This is factually wrong: lookup_attribution at L650 IS the reader, and this PR IS the follow-up. The package docstring at billing_audit/__init__.py was correctly updated (Plan 01.1-04 Task 2), but the module docstring contradicts it. A reader landing on writer.py first will miss the new public surface and the documented PII-out exception (the reader returns helper TEXT in its dict, which is the one place per-row PII leaves the package)."
    artifacts:
      - path: "billing_audit/writer.py"
        issue: "Module docstring at L1-14 says 'Writes only. No read / hydration path — that lands in the follow-up PR.' Now misleading."
    missing:
      - "Rewrite the module docstring to acknowledge the lookup_attribution reader, explicitly document the no-PII-out exception (reader returns helper PII in its return dict), and note that the reader bypasses billing_audit.feature_flag (kill switch lives at the caller via SUBCONTRACTOR_HELPER_CLAIM_ATTRIBUTION_ENABLED)."
human_verification:
  - test: "Live cron run on a subcontractor sheet with operator-populated helper data produces _ReducedSub_Helper_<foreman>.xlsx and _AEPBillable_Helper_<foreman>.xlsx attached to the correct sheets, and NO legacy primary-no-suffix Excel for any subcontractor WR appears on any target sheet"
    expected: "On the next scheduled run after merge (cron fires every 2h on weekdays per CLAUDE.md), the GHA artifacts show shadow-variant files for subcontractor WRs with operator-populated helper data; TARGET_SHEET_ID=5723337641643908 carries both _AEPBillable_Helper_<name>.xlsx and _ReducedSub_Helper_<name>.xlsx attachments; SUBCONTRACTOR_PPP_SHEET_ID=8162920222379908 carries _ReducedSub* attachments only (whitelist actively prunes anything else)"
    why_human: "ROADMAP goal explicitly says 'A scheduled cron run … produces …' — this is a live-production observable that the verifier cannot exercise. The code-side contract is verified (all artifacts present, 682/26/58 tests pass), but the end-to-end production outcome requires watching the next cron fire."
  - test: "Bug C frozen-helper attribution behaves correctly under a real mid-week foreman swap in Smartsheet against the deployed lookup_attribution RPC"
    expected: "After the data team deploys the lookup_attribution RPC in Supabase Dashboard (per 01.1-VALIDATION.md Manual-Only row 1), a subcontractor WR that experiences a foreman swap produces helper files keyed on the FROZEN helper from attribution_snapshot, not the current Smartsheet Foreman Helping? value. Operator inspects two consecutive cron runs spanning the swap to confirm the prior-foreman's helper file is retained with their original line items."
    why_human: "Requires the lookup_attribution RPC to exist in Supabase (data-team deliverable; not in this PR per D-16). Pipeline is fail-safe — RPC absence trips PGRST106 → global kill → D-12 fall-back to current helper. Verifier cannot simulate a real mid-week swap nor confirm the RPC deploy."
  - test: "PPP cleanup whitelist actually deletes a pre-existing off-contract attachment from SUBCONTRACTOR_PPP_SHEET_ID on the first run after merge"
    expected: "If any stale primary-shape attachment exists on SUBCONTRACTOR_PPP_SHEET_ID from a pre-Bug-B1 deployment, the first cron run after merge logs '🗑️ Removed off-contract variant on sheet 8162920222379908: WR_<wr>_WeekEnding_<MMDDYY>_<ts>_<hash>.xlsx' at INFO level and the attachment disappears from PPP. removed_off_contract counter in the per-sheet summary line is non-zero on first run, zero on subsequent runs (steady state)."
    why_human: "Whether stale off-contract attachments actually exist on PPP today is a live-data question. The whitelist is unit-tested (TestCleanupVariantWhitelist 10 methods + e2e TestBugB2WhitelistE2E) but the production cleanup of pre-existing orphans requires the live cron run."
  - test: "Hash-history prune fires exactly once and is idempotent on subsequent runs"
    expected: "On first cron run after merge, the prune pass logs the affected-WR list and persists _phase_prune_version=1 in hash_history.json. Subsequent runs no-op (no prune log)."
    why_human: "TestHashPruneIdempotency unit-tests this logic in isolation, but production observability requires checking GHA logs for the one-time prune log on the first post-merge run, then confirming silence on the second."
---

# Phase 01.1: Subcontractor Helper-Shadow Rescue Verification Report

**Phase Goal:** A scheduled cron run on a subcontractor sheet with
operator-populated helper data produces `_ReducedSub_Helper_<foreman>.xlsx`
and `_AEPBillable_Helper_<foreman>.xlsx` attached to the correct sheets
(routing matrix unchanged from Phase 1: `_AEPBillable*` to TARGET_SHEET_ID
5723337641643908 only; `_ReducedSub*` to both TARGET and
SUBCONTRACTOR_PPP_SHEET_ID 8162920222379908); no duplicate-no-suffix Excel
file appears on any target sheet for any subcontractor WR; helper files
contain only the line items each foreman claimed before any mid-week shift
change (per `billing_audit.pipeline_run` history); and zero impact to
primary, helper, VAC-crew, or ORIG-folder outputs.

**Verified:** 2026-05-19T20:00:00Z
**Status:** human_needed (with 2 BLOCKER gaps carried forward from REVIEW.md)
**Re-verification:** No — initial verification

## Verification Summary

The five Phase 1.1 plans (01.1-01..05) all completed and landed in
master via 7 merge commits across 5 worktree branches. Every
code-side artifact and key link the must-haves enumerate is present
and importable. The full pytest suite reports 682 passed / 26
skipped / 58 subtests — matching the +59-net-tests claim in the
brief (was 623 pre-phase). The four kill-switch env vars resolve
correctly, the workflow pin block is in place, and the hash-history
prune helper is wired at session startup with a fail-safe wrap.

The ROADMAP goal's PRODUCTION-side outcome ("a scheduled cron run
… produces …") is NOT verifiable until the next cron fire. Per the
brief's explicit note, this becomes a `human_verification` item
rather than a `failed` truth — the code-side contract for it is
present.

Two BLOCKERs from 01.1-REVIEW.md (CR-01 schema.sql missing
contract; CR-02 writer.py docstring stale) were NOT addressed
before this verification run. Both directly violate
CLAUDE.md Living Ledger rules ([2026-04-25 12:00] rule 1 for
CR-01; project doc-hygiene for CR-02). They do not crash the
pipeline (Bug C's global-kill switch and D-12 fall-back contain
the runtime impact), but the operator-visible signal of what to
deploy in Supabase is absent from the repository. These remain
must-fix before production-flip of
`SUBCONTRACTOR_HELPER_CLAIM_ATTRIBUTION_ENABLED`.

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria + REVIEW BLOCKERs)

| # | Truth | Status | Evidence |
| - | ----- | ------ | -------- |
| 1 | For every subcontractor-folder WR group with both Helping Foreman Completed Unit? and Units Completed? checked, the workflow emits `_ReducedSub_Helper_<name>` (to TARGET+PPP) and (post-cutoff) `_AEPBillable_Helper_<name>` (to TARGET only) | HUMAN NEEDED | Code-side contract verified: Bug A rescue branch (gw_pdf.py:3998-4031) lets helper rows pass the has_price gate; Bug B1 partitioning gate at L4610 routes them to variant keys only; PPP routing at L7827 with `variant_whitelist={'reduced_sub', 'reduced_sub_helper'}`. Live cron observation required (deferred to human verification item 1). |
| 2 | No legacy primary-no-suffix Excel file is generated, uploaded, or retained on any target sheet for any subcontractor WR | VERIFIED | Bug B1 gate `if not is_subcontractor_row and not valid_helper_row:` at L4610 suppresses the legacy primary key for subcontractor rows. Bug B2 whitelist `variant_whitelist={'reduced_sub', 'reduced_sub_helper'}` at L7827 deletes any off-contract attachment on PPP regardless of valid_wr_weeks. Source-grep test TestProductionCodeSiteInvariants locks all four production code-site invariants in place. TestBugB2WhitelistE2E exercises end-to-end deletion. |
| 3 | Helper files contain only line items each foreman claimed BEFORE any mid-week shift change (via billing_audit.pipeline_run attribution history) | VERIFIED (code-side) | Bug C integration at gw_pdf.py:4789 imports lookup_attribution; four-gate condition (is_subcontractor_row + kill-switch + valid_helper_row + helper_mode_enabled) drives the reader; D-12 fall-back ensures helper files never silently empty when RPC returns None. Live RPC deploy + mid-week swap test is human-verification item 2. |
| 4 | `pytest tests/` passes including: pre-acceptance rescue regression, variant-partition gate regression, PPP cleanup whitelist regression, claim-history attribution regression, AND a true end-to-end integration test driving _fetch_and_process_sheet → group_source_rows → generate_excel; no existing test regresses | VERIFIED | `pytest tests/` → 682 passed / 26 skipped / 58 subtests passed. TestEndToEndPipeline (28 methods in tests/test_subcontractor_helper_shadow_rescue.py) + TestLookupAttribution (14 methods in tests/test_billing_audit_shadow.py) + TestBugB2WhitelistE2E + TestHashPruneIdempotency + TestProductionCodeSiteInvariants present. D-22 rewrite of test_kill_switch_disables_new_variant_emission landed in-place with the partitioning-contract sibling test method. Wave-by-wave growth: 623 → 630 → 643 → 682 (no regressions at any wave). |
| 5 | A scheduled weekly workflow run produces byte-identical primary / helper / VAC-crew / ORIG-folder outputs (only subcontractor variant outputs change) | HUMAN NEEDED (code-side preserved) | Bug B1 gate is scoped to is_subcontractor_row only (D-15 scope). Hoist + gate change in group_source_rows do not touch the RES_GROUPING_MODE=='primary' branch or the non-subcontractor primary-emission cascade. Per WR-06 (advisory): RES_GROUPING_MODE=='primary' is non-production and not gate-modified — divergence on local dev runs is acceptable but undocumented. Byte-identical hash-diff confirmation requires a live TEST_MODE run. |
| 6 | Operator-facing kill switch SUBCONTRACTOR_RATE_RECALC_PREACCEPTANCE_ENABLED='0' disables the Bug A rescue, reverting to pre-fix state without affecting Bug B1/B2/C | VERIFIED | Env-var parsing: 13-case truth table passes (per 01.1-01-SUMMARY). Rescue gate at L3999-4000 reads `is_subcontractor_sheet and SUBCONTRACTOR_RATE_RECALC_PREACCEPTANCE_ENABLED and price_val <= 0`. Pinned at .github/workflows/weekly-excel-generation.yml:343. Bug B1 (group_source_rows partitioning), Bug B2 (cleanup whitelist), and Bug C (lookup_attribution wiring) are independent code paths with their own kill switches. |
| 7 | Living Ledger entry documents 2-cycle debug methodology, Plan 01-03 design-intent override, pre-acceptance-rescue-generalization rule, and test-methodology rule | VERIFIED | CLAUDE.md L1860 contains entry `[2026-05-20 00:26] Phase 1.1 (Subcontractor Helper-Shadow Rescue + Variant Partition + Claim-History Attribution) closure`. All four numbered rules present (grep -c → 1 each for 2-cycle methodology, design-intent override, pre-acceptance-rescue-generalization, test-methodology). Append-only invariant preserved (172 insertions, 0 deletions per `git diff --stat`). |
| BLOCKER CR-01 | lookup_attribution RPC has a contract entry in billing_audit/schema.sql | FAILED | grep `lookup_attribution` billing_audit/schema.sql → 0 matches. CLAUDE.md [2026-04-25 12:00] rule 1 directly violated. Pipeline does NOT crash (Bug C fail-safe contains runtime impact) but operators cannot self-serve the deploy contract from the repo. |
| BLOCKER CR-02 | billing_audit/writer.py module docstring accurately describes the public surface (now includes a reader) | FAILED | billing_audit/writer.py:1-14 still reads `"Writes only. No read / hydration path — that lands in the follow-up PR."` lookup_attribution at L650 IS the reader; THIS PR IS the follow-up. Docstring contradicts billing_audit/__init__.py which was correctly updated. |

**Score:** 6/7 ROADMAP success criteria verified or human-deferred (1 carried forward to live cron). 2 BLOCKER carry-overs from REVIEW.md remain FAILED.

### Required Artifacts (Code-Side)

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `generate_weekly_pdfs.py:472` | `SUBCONTRACTOR_RATE_RECALC_PREACCEPTANCE_ENABLED = os.getenv(...)` (Bug A kill switch) | VERIFIED | `isinstance(SUBCONTRACTOR_RATE_RECALC_PREACCEPTANCE_ENABLED, bool)` → True at runtime |
| `generate_weekly_pdfs.py:1489` | `def _subcontractor_rescue_price(row_data: dict) -> float:` (Bug A helper) | VERIFIED | Callable; lives BEFORE `_resolve_row_price` per plan ordering |
| `generate_weekly_pdfs.py:3999-4031` | Bug A pre-acceptance rescue branch in `_fetch_and_process_sheet` | VERIFIED | Composite gate `is_subcontractor_sheet and SUBCONTRACTOR_RATE_RECALC_PREACCEPTANCE_ENABLED and price_val <= 0` present; mutates BOTH `price_val` AND `row_data['Units Total Price']` (Rule 2 auto-fix per 01.1-01-SUMMARY) |
| `generate_weekly_pdfs.py:4610` | Bug B1 partitioning gate `if not is_subcontractor_row and not valid_helper_row:` | VERIFIED | Present exactly once; hoisted is_subcontractor_row computation lives above the cascade (Rule 1 auto-fix per 01.1-02-SUMMARY put hoist OUTSIDE the if is_vac_crew_row: nesting); duplicate computation at the variant-emission block removed |
| `generate_weekly_pdfs.py:2626` | `cleanup_untracked_sheet_attachments` signature carries `variant_whitelist: set[str] | None = None` (Bug B2) | VERIFIED | Runtime inspect confirms: parameters = ['client', 'target_sheet_id', 'valid_wr_weeks', 'test_mode', 'attachment_cache', 'target_sheet', 'variant_whitelist'], default None |
| `generate_weekly_pdfs.py:7827` | PPP cleanup invocation passes `variant_whitelist={'reduced_sub', 'reduced_sub_helper'}` | VERIFIED | Exactly 1 grep match; TARGET cleanup invocation does NOT carry the kwarg (D-09 preserved) |
| `billing_audit/writer.py:650` | `def lookup_attribution(wr, week_ending, smartsheet_row_id):` (Bug C reader) | VERIFIED | Callable; positional signature matches RPC parameter contract |
| `generate_weekly_pdfs.py:483` | `SUBCONTRACTOR_HELPER_CLAIM_ATTRIBUTION_ENABLED = os.getenv(...)` (Bug C kill switch) | VERIFIED | `isinstance(..., bool)` → True; pinned at workflow L345 (placeholder REPLACED, not duplicated) |
| `generate_weekly_pdfs.py:4789` | `from billing_audit.writer import lookup_attribution` inside Bug C integration block | VERIFIED | Present; defensive try/except wrap per D-12 fail-safe contract |
| `generate_weekly_pdfs.py:309` | `PHASE_1_1_HASH_PRUNE_VERSION = 1` (SUB-12 idempotent migration sentinel) | VERIFIED | Module-level constant present |
| `generate_weekly_pdfs.py:2990, 6699` | `_run_phase_1_1_hash_prune` helper + session-startup call site | VERIFIED | Both present; call site wrapped in fail-safe try/except per [2026-04-22 16:05] rule 4 |
| `.github/workflows/weekly-excel-generation.yml:343, 345` | Workflow pins for both Bug A and Bug C kill switches | VERIFIED | Both `SUBCONTRACTOR_RATE_RECALC_PREACCEPTANCE_ENABLED: '1'` and `SUBCONTRACTOR_HELPER_CLAIM_ATTRIBUTION_ENABLED: '1'` present; rollback comment blocks present |
| `website/docs/reference/environment.md:131, 192` | Operator-facing doc entries for both Bug A and Bug C env vars | VERIFIED | Two distinct `### SUBCONTRACTOR_*` sections present with semantics, default, rollback path |
| `tests/test_subcontractor_helper_shadow_rescue.py` | NEW file with TestEndToEndPipeline / TestBugB2WhitelistE2E / TestHashPruneIdempotency / TestProductionCodeSiteInvariants | VERIFIED | 4 test classes, 28 methods (pytest collection) |
| `tests/test_billing_audit_shadow.py:4230` | TestLookupAttribution class (14 methods) | VERIFIED | Class present; 10 always-on + 4 postgrest-gated skips |
| `tests/test_subcontractor_pricing.py` | D-22 in-place rewrite of test_kill_switch_disables_new_variant_emission + new sibling test_partitioning_contract_for_subcontractor_non_helper_rows | VERIFIED | Class TestSubcontractorVariantKillSwitchAndScope contains both methods; test passes |
| `CLAUDE.md:1860` | Living Ledger entry [2026-05-20 00:26] for Phase 1.1 closure | VERIFIED | Header present; four numbered rules present (grep -c → 1 each); append-only invariant preserved |
| `billing_audit/schema.sql` | lookup_attribution contract entry (CR-01) | **MISSING** | 0 grep matches for lookup_attribution |
| `billing_audit/writer.py:1-14` | Module docstring updated to acknowledge read-path arrival (CR-02) | **STUB** | Docstring still says "Writes only. No read / hydration path" |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `_fetch_and_process_sheet` row-acceptance gate | `_subcontractor_rescue_price` helper | additive elif branch at L3998-4031 mutating price_val in-place | WIRED | Rescue mutates BOTH price_val and row_data['Units Total Price'] so has_price gate's price_raw clause also passes |
| `group_source_rows` per-row loop primary cascade | partition-aware emission | `if not is_subcontractor_row and not valid_helper_row:` at L4610 | WIRED | is_subcontractor_row hoisted to single per-row site (outside is_vac_crew_row branch); duplicate removed; subcontractor variant emission block at L4687+ reads the hoisted variable correctly |
| `main()` cleanup invocation for PPP | `cleanup_untracked_sheet_attachments` with whitelist | trailing kwarg `variant_whitelist={'reduced_sub', 'reduced_sub_helper'}` at L7827 | WIRED | Inside Sentry span; TARGET invocation does NOT carry the kwarg (legacy behavior preserved per D-09) |
| Bug C integration block in `group_source_rows` | `billing_audit.writer.lookup_attribution` | local `from billing_audit.writer import lookup_attribution` at L4789 | WIRED | Defensive try/except + per-WR dedupe set + D-12 fall-back to current helper on None return |
| Session-startup load_hash_history | `_run_phase_1_1_hash_prune` | call site at L6699 wrapped in try/except | WIRED | Reads `hash_history.get('_phase_prune_version', 0)`; advances to PHASE_1_1_HASH_PRUNE_VERSION on first run; idempotent on subsequent runs |
| `_subcontractor_rescue_price` | `_SUBCONTRACTOR_RATES` cached dict | `.get(cu)` direct lookup; safety floor returns 0.0 on miss | WIRED | Reuses Phase 1's cached dict (no CSV re-read); substring-direction discipline preserved (inst/rem/tran/xfr) |

### Data-Flow Trace (Level 4)

This phase modifies routing/partitioning logic in a batch pipeline,
not a rendering-component data flow. The Bug A rescue MUTATES the
row dict in-place; downstream consumers see the mutated `price_val`
and `Units Total Price`. The Bug B1 partitioning suppresses a key
in `keys_to_add`; downstream variant emission produces real variant
keys; `generate_excel` consumes the rows. The Bug C reader returns
either a dict (frozen-helper override) or None (D-12 fall-back to
current Smartsheet helper); both branches feed `_attributed_helper`
which is sanitized and used in the shadow-variant group key. The
PPP cleanup whitelist drives `delete_attachment` calls. No hollow
or hardcoded-empty data flows; production data shapes are real
(Smartsheet rows + Supabase RPC return + SDK attachment objects).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Module imports without error and kill switches resolve | `python -c "import generate_weekly_pdfs as g; assert g.PHASE_1_1_HASH_PRUNE_VERSION == 1 and g.SUBCONTRACTOR_RATE_RECALC_PREACCEPTANCE_ENABLED and g.SUBCONTRACTOR_HELPER_CLAIM_ATTRIBUTION_ENABLED and callable(g._subcontractor_rescue_price) and callable(g._run_phase_1_1_hash_prune)"` | All assertions pass | PASS |
| lookup_attribution importable with expected signature | `python -c "import inspect, billing_audit.writer as w; print(list(inspect.signature(w.lookup_attribution).parameters.keys()))"` | `['wr', 'week_ending', 'smartsheet_row_id']` | PASS |
| cleanup_untracked_sheet_attachments carries variant_whitelist kwarg | `python -c "import inspect, generate_weekly_pdfs as g; sig = inspect.signature(g.cleanup_untracked_sheet_attachments); p = sig.parameters['variant_whitelist']; assert p.default is None"` | exits 0 | PASS |
| Full test suite | `pytest tests/ -q` | 682 passed / 26 skipped / 58 subtests passed in 8.53s | PASS |
| Workflow YAML still parses | (verified during plan executor runs per SUMMARY notes) | exits 0 | PASS |
| Live cron emits shadow-variant files for subcontractor WRs | (not runnable locally — requires scheduled GHA run) | N/A | SKIP (routed to human verification item 1) |

### Requirements Coverage

ROADMAP.md Phase 1.1 names requirements SUB-08, SUB-09, SUB-10, SUB-11, SUB-12
but immediately notes "to be formalized via `/gsd-discuss-phase 1.1`". These
IDs are NOT present in `.planning/REQUIREMENTS.md` (the canonical
requirements file lists only SUB-01..SUB-07 plus MIG-01 plus v2 items).
This is a documented planning-state gap: each plan's frontmatter declares
the SUB-XX IDs but no central definition exists.

| Requirement | Source Plan | Description (from plan frontmatter / tags) | Status | Evidence |
| ----------- | ----------- | ------------------------------------------ | ------ | -------- |
| SUB-08 | 01.1-01-PLAN | Bug A pre-acceptance rescue for subcontractor helper rows | SATISFIED | _subcontractor_rescue_price + rescue branch + kill switch + workflow pin + docs all present |
| SUB-09 | 01.1-02-PLAN | Bug B1 variant partitioning (subcontractor non-helper rows emit ONLY variant keys) | SATISFIED | Partitioning gate at L4610 + hoisted is_subcontractor_row + TestSubcontractorB1PartitioningGate (7 methods) |
| SUB-10 | 01.1-03-PLAN | Bug B2 PPP cleanup whitelist (off-contract attachments unconditionally pruned) | SATISFIED | variant_whitelist kwarg + off-contract delete loop + removed_off_contract counter + PPP invocation pass {'reduced_sub','reduced_sub_helper'} + TestCleanupVariantWhitelist (10 methods) |
| SUB-11 | 01.1-04-PLAN | Bug C claim-history attribution reader (per-row partitioning by frozen helper) | SATISFIED (code-side) | lookup_attribution reader + integration block + kill switch + workflow pin + docs + TestLookupAttribution (14 methods). NOTE: requires data-team to deploy the RPC in Supabase before production-flip — human verification item 2. |
| SUB-12 | 01.1-05-PLAN | Hash-history prune + e2e regression + D-22 rewrite + D-23 Living Ledger entry | SATISFIED | PHASE_1_1_HASH_PRUNE_VERSION constant + _run_phase_1_1_hash_prune helper + extended load/save filters + new e2e test file (28 methods) + Living Ledger entry [2026-05-20 00:26] |

No orphaned requirements detected (REQUIREMENTS.md has no Phase 1.1
mappings, so all five claimed IDs are accounted for by the five plans
that declared them).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `billing_audit/schema.sql` | (entire file, no entry) | Missing DDL for lookup_attribution RPC + attribution_snapshot read surface | BLOCKER (CR-01) | Violates CLAUDE.md [2026-04-25 12:00] rule 1; operators cannot self-serve the Supabase deploy contract |
| `billing_audit/writer.py:1-14` | 1-14 | Stale module docstring claims "writes only" but reader now exists | BLOCKER (CR-02) | Contradicts billing_audit/__init__.py; future contributors will miss the PII-out exception on reader's return value |
| `generate_weekly_pdfs.py:4022-4031` | 4022-4031 | `_wr_diag` in Bug A rescue diagnostic NOT sanitized at derivation site | WARNING (WR-03) | Violates CLAUDE.md [2026-04-23 12:00] rule 1; bounded blast radius (FILTER_DIAGNOSTICS-gated) but log file could carry `WR=1234/../evil` |
| `generate_weekly_pdfs.py:4840-4847` | 4840-4847 | Whitespace-only `helper` from reader → empty-suffix group key | WARNING (WR-02) | Reader's `data.get("helper")` truthy check accepts "   "; downstream `.strip()` produces empty key like `041926_91467680_REDUCEDSUB_HELPER_` which whitelist accepts as in-contract |
| `generate_weekly_pdfs.py` Bug A rescue test scope | (test file) | Rescue branch has only source-grep guard; no behavioral test drives _fetch_and_process_sheet for Bug A | WARNING (WR-01) | The new e2e test file SHOULD have closed the "mirror passes but production reverted" trap for Bug A — but only Bug B1 / B2 / C have true e2e coverage. Bug A coverage is the exact failure mode the file's preamble warns against |
| `generate_weekly_pdfs.py:4782-4783` | 4782-4783 | Implicit `_attribution_reason = None` for kill-switch-off path | WARNING (WR-04) | Maintainability hazard; the plan called for explicit else: branch but implementation omitted it |
| `billing_audit/writer.py:719-723` | 719-723 | lookup_attribution bypasses billing_audit.feature_flag table | WARNING (WR-05) | Operators have only ONE kill switch (env var); writers have TWO (env var + feature flag). Asymmetric kill-switch surface |
| `generate_weekly_pdfs.py:4594-4628` | 4594-4628 | `RES_GROUPING_MODE=='primary'` branch still emits both primary AND variants for subcontractor rows | WARNING (WR-06) | Non-production mode silently diverges from production behavior; local dev runs may see duplicate files |
| `generate_weekly_pdfs.py:4789` | 4789 | `from billing_audit.writer import lookup_attribution` inside per-row loop | INFO (IN-01) | Non-idiomatic; works because sys.modules caches imports but adds per-row attribute lookup overhead |
| `generate_weekly_pdfs.py:4828-4830` | 4828-4830 | `from billing_audit.client import _global_disable_reason` (private symbol import) | INFO (IN-02) | Hidden coupling; future refactor of client.py could silently break the reason-distinguishing branch |
| `generate_weekly_pdfs.py:4840` | 4840 | Reader returns `helper_dept` and `source_run_id` but caller ignores both | INFO (IN-03) | Plan-compliant (v1.0 only uses helper name) but data team is paying to compute values pipeline throws away |
| `.github/workflows/weekly-excel-generation.yml:340` | 340 | Stale line-number reference in Bug A rollback comment (L3714 → actually L4049) | INFO (IN-04) | Violates CLAUDE.md [2026-04-22 16:05] rule 3 (refer to function / env-var names not line numbers) |

### Human Verification Required

#### 1. Live cron run produces shadow-variant files for subcontractor WRs

**Test:** Wait for the next scheduled GHA cron run on a subcontractor sheet
where operators have populated helper-foreman data (both
`Helping Foreman Completed Unit?` and `Units Completed?` checked). Inspect
the GHA artifact + the two target sheets.

**Expected:**
- `generated_docs/<week>/WR_<wr>_WeekEnding_<MMDDYY>_<ts>_..._ReducedSub_Helper_<name>.xlsx` appears
- `generated_docs/<week>/WR_<wr>_WeekEnding_<MMDDYY>_<ts>_..._AEPBillable_Helper_<name>.xlsx` appears (if WR is post-cutoff)
- TARGET_SHEET_ID=5723337641643908 carries BOTH variant attachments
- SUBCONTRACTOR_PPP_SHEET_ID=8162920222379908 carries the `_ReducedSub*` attachment ONLY
- NO `WR_<wr>_WeekEnding_<MMDDYY>_<ts>_<hash>.xlsx` (no-suffix legacy primary) on any target sheet for that WR
- Startup banner emits both `📋 SUBCONTRACTOR_RATE_RECALC_PREACCEPTANCE_ENABLED=True` and `📋 SUBCONTRACTOR_HELPER_CLAIM_ATTRIBUTION_ENABLED=True`

**Why human:** ROADMAP goal explicitly says "a scheduled cron run … produces …" — production-time observation. The code-side contract is fully verified (all artifacts present, 682 tests pass) but the live cron outcome is not testable without firing the cron.

#### 2. Bug C frozen-helper partitioning after a real mid-week swap

**Test:** Coordinate with the data team to deploy the
`lookup_attribution(p_wr TEXT, p_week_ending DATE, p_smartsheet_row_id BIGINT)`
RPC in the Supabase Dashboard (RESEARCH §C contract; CR-01 fix
should land first so the contract is in `schema.sql`). After
deploy, identify a subcontractor WR that experiences a foreman
swap mid-week. Compare two consecutive cron runs spanning the swap.

**Expected:** After the swap, the prior foreman's helper file is RETAINED with their original line items (frozen-helper from `attribution_snapshot` overrides current `Foreman Helping?` value for shadow-variant emission). A per-WR WARNING with `reason=no_history` or `reason=fetch_failure` should NOT fire when the RPC returns a frozen helper.

**Why human:** Requires data-team Supabase deploy + live mid-week swap on a real subcontractor WR. Cannot be simulated by the verifier.

#### 3. PPP cleanup whitelist actually deletes pre-existing off-contract attachments

**Test:** On the FIRST cron run after merge (or on a manual dispatch), inspect SUBCONTRACTOR_PPP_SHEET_ID=8162920222379908 for any stale primary-shape attachments and tail the cleanup log lines.

**Expected:** `🗑️ Removed off-contract variant on sheet 8162920222379908: WR_<wr>_WeekEnding_<MMDDYY>_<ts>_<hash>.xlsx` INFO lines appear; `removed_off_contract` counter in the end-of-cleanup summary line is non-zero on first run, zero on subsequent runs.

**Why human:** Whether stale orphans exist on PPP today is a live-data question. The whitelist is unit-tested (TestCleanupVariantWhitelist 10 methods + TestBugB2WhitelistE2E) but production cleanup of pre-existing orphans requires the live cron run.

#### 4. Hash-history prune fires once and is idempotent

**Test:** Inspect GHA logs for the first cron run after merge, then again after the second cron run.

**Expected:** First run logs the prune pass with affected-WR list and persists `_phase_prune_version=1` in `hash_history.json`. Second-and-later runs no-op (no prune log line).

**Why human:** `TestHashPruneIdempotency` unit-tests the helper in isolation, but production observability requires inspecting GHA artifact logs.

### Gaps Summary

Two BLOCKER findings from `01.1-REVIEW.md` are unresolved in the
merged code:

1. **CR-01:** `lookup_attribution` PostgREST RPC has NO contract entry
   in `billing_audit/schema.sql`. This directly violates
   `CLAUDE.md [2026-04-25 12:00]` Living Ledger entry rule 1, which
   was created precisely to prevent the "Python reader whose matching
   DDL exists only in a Supabase Dashboard" failure mode. The pipeline
   does not crash (Bug C's run-global kill + D-12 fall-back contain
   the runtime impact), but operators cannot self-serve the Supabase
   deploy contract from the repository. **Must close before flipping
   SUBCONTRACTOR_HELPER_CLAIM_ATTRIBUTION_ENABLED=1 in production.**

2. **CR-02:** `billing_audit/writer.py` module docstring still claims
   "Writes only. No read / hydration path — that lands in the
   follow-up PR." Phase 1.1 IS the follow-up PR and the reader IS in
   the file. The package docstring at `billing_audit/__init__.py` was
   correctly updated in Plan 01.1-04 Task 2; the module docstring
   contradicts it. A future contributor landing on writer.py first
   will miss the new public surface AND the no-PII-out exception
   (reader returns helper PII in its return dict). Surgical fix
   (rewrite ~25 lines of module docstring) per REVIEW.md CR-02.

Six additional WARNING-severity findings (WR-01 through WR-06) and
four INFO-severity findings (IN-01 through IN-04) are documented in
`01.1-REVIEW.md` for follow-up. None block Phase 1.1 closure but
WR-01 (Bug A coverage is structurally hollow) and WR-02 (whitespace
helper malforms group key) are particularly load-bearing for future
gap-closure cycles and should be addressed before the next phase
that touches subcontractor-helper code paths.

Live-production observability for the four `human_verification`
items above will confirm the ROADMAP goal end-to-end. Test baseline
682/26/58 preserved per executor SUMMARYs. Plan-progress: 5/5
complete.

---

_Verified: 2026-05-19T20:00:00Z_
_Verifier: Claude (gsd-verifier)_
