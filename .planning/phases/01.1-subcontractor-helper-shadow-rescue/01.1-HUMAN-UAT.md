---
status: partial
phase: 01.1-subcontractor-helper-shadow-rescue
source: [01.1-VERIFICATION.md]
started: 2026-05-19T23:35:00Z
updated: 2026-05-20T00:05:00Z
---

## Current Test

[code-side fix for the diagnosed duplicate-leak gap is implemented and
verified (Plan 01.1-06 + WR-01 review follow-up); Test 1 pending
live-cron re-confirmation, Test 2 blocked on external RPC deploy]

## Tests

### 1. Live cron emits shadow-variant files for subcontractor WRs
expected: On the next scheduled run after merge (cron fires every 2h on weekdays per CLAUDE.md), the GHA artifacts show shadow-variant files for subcontractor WRs with operator-populated helper data; `TARGET_SHEET_ID=5723337641643908` carries both `_AEPBillable_Helper_<name>.xlsx` and `_ReducedSub_Helper_<name>.xlsx` attachments; `SUBCONTRACTOR_PPP_SHEET_ID=8162920222379908` carries `_ReducedSub*` attachments only (whitelist actively prunes anything else); NO legacy primary-no-suffix Excel for any subcontractor WR appears on any target sheet.
result: pending
fix: "RESOLVED code-side by Plan 01.1-06 (commits 2805a63..59d2f8d) + WR-01 review follow-up (commit 6b2ad31). The `if not is_subcontractor_row:` guard now stops subcontractor helper rows from emitting the legacy ('helper', ...) key; PHASE_1_1_HASH_PRUNE_VERSION=2 drops legacy _HELPER_ orphans; the SUBCONTRACTOR_LEGACY_HELPER_CLEANUP_ENABLED TARGET cleanup actively removes pre-existing legacy _Helper_ subcontractor attachments (with the WR-01 valid_wr_weeks exemption so cross-sheet-overlap WRs keep their legitimate non-sub _Helper_ files). 9/9 code-side must-haves verified in 01.1-VERIFICATION.md; pytest 689 passed. PENDING: live-cron re-confirmation on the next scheduled run that the WR_90773033 wk 041226 Chris_Lopez legacy attachment is gone and no new legacy _Helper_ appears for any subcontractor WR on TARGET."
reported: "Duplicate leak — a legacy `_Helper_<name>` file (no variant prefix) is emitted for subcontractor helper rows alongside the `_ReducedSub_Helper_<name>` shadow. Subcontractor helper rows should partition to ONLY shadow variants, like Bug B1 did for non-helper rows."
severity: major
evidence_run: 26138204743 (workflow_dispatch, commit 1b6881c — first run on merged Phase 1.1 code, completed success 2026-05-20T03:07Z)
evidence: |
  Confirmed working: subcontractor variants + PPP routing ENABLED; 5 sub-sheets
  discovered. For the 5 sub-WRs (90727774/90773033/90851321/90948056/90775883):
  34 _ReducedSub files (all weeks) + 22 _AEPBillable files (post-cutoff only) +
  1 _ReducedSub_Helper_Chris_Lopez shadow. ZERO bare no-suffix primary files for
  any sub-WR (Bug B1 non-helper partitioning works). DEFECT: a legacy
  WR_90773033_WeekEnding_041226_220404_Helper_Chris_Lopez_*.xlsx (no variant
  prefix) was ALSO emitted, coexisting with the _ReducedSub_Helper shadow. No
  _AEPBillable_Helper for that week is CORRECT (wk 041226 is pre-cutoff).

### 2. Bug C frozen-helper attribution after a real mid-week foreman swap
expected: After the data team deploys the `lookup_attribution(p_wr, p_week_ending, p_smartsheet_row_id)` PostgREST RPC in Supabase, a subcontractor WR that experiences a foreman swap produces helper files keyed on the FROZEN helper from `attribution_snapshot`, not the current Smartsheet `Foreman Helping?` value. Operator inspects two consecutive cron runs spanning the swap to confirm the prior-foreman's helper file is retained with their original line items.
result: blocked
blocked_by: prior-phase
reason: "lookup_attribution RPC is NOT deployed in Supabase. Live run 26138204743 confirmed: SUBCONTRACTOR_HELPER_CLAIM_ATTRIBUTION_ENABLED=1 (kill switch already ON), the pipeline DID call POST /rest/v1/rpc/lookup_attribution → HTTP 404 Not Found, and the fail-safe fired correctly (fallback reason=no_history → uses current Foreman Helping? value; circuit breaker opened after 3 failures, other billing_audit ops unaffected). The Python side is fully wired; only the data-team Supabase deliverable is missing."
unblock_steps: |
  The ONLY blocker is the data team creating the Supabase function + table.
  Python is deployed and already calling it. Required (contract in
  billing_audit/schema.sql L157-206):
  1. Ensure table billing_audit.attribution_snapshot exists with PK
     (wr, week_ending, smartsheet_row_id) and columns helper TEXT,
     helper_dept TEXT, source_run_id TEXT. (Populated by the existing
     freeze_attribution RPC — verify freeze_attribution is actually
     writing snapshots; the run showed freeze_attribution returning 200 OK.)
  2. Create RPC lookup_attribution(p_wr TEXT, p_week_ending DATE,
     p_smartsheet_row_id BIGINT) RETURNING a row with
     (helper TEXT, helper_dept TEXT, source_run_id TEXT), or NULL when
     no frozen attribution exists.
  3. Ensure the 'billing_audit' schema is in PostgREST exposed-schemas
     (Supabase Dashboard → Project Settings → API → Data API Settings →
     Exposed schemas) and reload the schema cache.
  4. No code change needed — SUBCONTRACTOR_HELPER_CLAIM_ATTRIBUTION_ENABLED
     is already pinned to '1' in the workflow. Once the RPC exists, the
     404s stop and frozen-helper attribution activates automatically.
  5. To then verify: observe two consecutive cron runs spanning a real
     mid-week foreman swap on a subcontractor WR and confirm the prior
     foreman's helper file retains their original line items.

### 3. PPP cleanup whitelist deletes pre-existing off-contract attachments
expected: If any stale primary-shape attachment exists on `SUBCONTRACTOR_PPP_SHEET_ID` from a pre-Bug-B1 deployment, the first cron run after merge logs `🗑️ Removed off-contract variant on sheet 8162920222379908: WR_<wr>_WeekEnding_<MMDDYY>_<ts>_<hash>.xlsx` at INFO level and the attachment disappears from PPP. `removed_off_contract` counter in the per-sheet summary line is non-zero on first run, zero on subsequent runs (steady state).
result: pass
evidence_run: 26138204743
evidence: "PPP cleanup whitelist pass RAN: '🧹 Variant pruning done: removed_variants=9, removed_off_contract=0'. The whitelist machinery is confirmed active (removed 9 stale variants). removed_off_contract=0 means no pre-existing off-contract orphans existed on PPP at this run — the correct 'nothing to clean' steady-state outcome, not a failure. Whitelist {'reduced_sub','reduced_sub_helper'} enforced."

### 4. Hash-history prune fires once, idempotent thereafter
expected: On first cron run after merge, the prune pass logs the affected-WR list and persists `_phase_prune_version=1` in `hash_history.json`. Subsequent runs no-op (no prune log).
result: pass
evidence_run: 26138204743
evidence: "First Phase-1.1 run logged EXACTLY ONE line: '🧹 Phase 1.1 hash-history prune (version 0 → 1): no orphans to drop.' and persisted _phase_prune_version=1 to the hash-history cache. 'No orphans to drop' is a valid first-run outcome (CI cache held no subcontractor primary orphans). Idempotency (silence on the next run) pending the in-progress 03:16 run 26139049246; sentinel was persisted so the version-gate will silently no-op next session."

## Summary

total: 4
passed: 2
issues: 0
pending: 1
skipped: 0
blocked: 1

## Gaps

- truth: "No legacy primary-no-suffix OR legacy _Helper_<name> Excel for any subcontractor WR appears on any target sheet — subcontractor helper rows partition to ONLY the _ReducedSub_Helper_<name> / _AEPBillable_Helper_<name> shadow variants."
  status: resolved
  resolved_by: "Plan 01.1-06 (commits 2805a63..59d2f8d) + WR-01 review follow-up (commit 6b2ad31). Code-side fix verified 9/9 must-haves in 01.1-VERIFICATION.md; live-cron re-confirmation tracked under Test 1 (result: pending)."
  reason: "User reported: Duplicate leak — a legacy _Helper_<name> file (no variant prefix) is emitted for subcontractor helper rows alongside the _ReducedSub_Helper_<name> shadow. Confirmed in live run 26138204743 for WR_90773033 wk 041226 (Chris_Lopez)."
  severity: major
  test: 1
  root_cause: "Bug B1 (Plan 01.1-02) partitioned ONLY the primary-emission path in group_source_rows (the `if not is_subcontractor_row and not valid_helper_row:` gate at generate_weekly_pdfs.py:4616-4629). The legacy helper-key emission at generate_weekly_pdfs.py:4636-4643 has NO is_subcontractor_row guard — it fires `keys_to_add.append(('helper', helper_key, helper_foreman))` for ALL valid helper rows including subcontractor ones. The design comment at 4728-4741 (D-09) explicitly documents the additive intent (legacy _HELPER_ + _REDUCEDSUB_HELPER_ + _AEPBILLABLE_HELPER_), so this is an intentional-but-incorrect asymmetry: non-helper sub rows partition, helper sub rows do not. Net effect: the legacy _Helper_<name> file uploads to TARGET_SHEET_ID and duplicates line items already present in the _ReducedSub_Helper_<name> shadow (which routes to TARGET + PPP)."
  artifacts:
    - path: "generate_weekly_pdfs.py"
      issue: "Legacy helper-key emission at L4636-4643 lacks the `not is_subcontractor_row` guard that Bug B1 added to the primary path at L4616-4629."
  missing:
    - "Add a partitioning guard so subcontractor helper rows do NOT emit the legacy ('helper', helper_key, ...) key — they should produce only the _REDUCEDSUB_HELPER_<name> (always) and _AEPBILLABLE_HELPER_<name> (post-cutoff) shadow keys. Mirror the Bug B1 primary-path partition: gate the legacy helper append on `not is_subcontractor_row` (keep a diagnostic debug log for the excluded case)."
    - "Verify the three-site identity-consistency invariant (per CLAUDE.md [2026-05-15 12:00] rule 1) still holds after the change: per-group identifier/file_identifier, valid_wr_weeks cleanup tuple, and current_keys hash-history prune key must all stay in lockstep for the now-removed legacy helper variant."
    - "Update Plan 01.1-02 / D-09 design note + add a regression test asserting a subcontractor helper row emits ONLY _REDUCEDSUB_HELPER_ / _AEPBILLABLE_HELPER_ keys and NOT the legacy _HELPER_ key (extend tests/test_subcontractor_helper_shadow_rescue.py::TestEndToEndPipeline)."
    - "Confirm no byte-identical-output regression for NON-subcontractor helper rows (ROADMAP success criterion #5) — the legacy _HELPER_ path must stay unchanged for primary/original-contract helper rows."
  debug_session: ""
