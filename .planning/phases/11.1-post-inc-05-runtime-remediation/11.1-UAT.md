---
status: resolved
phase: 11.1-post-inc-05-runtime-remediation
source: 11.1-01-SUMMARY.md, 11.1-02-SUMMARY.md, 11.1-03-SUMMARY.md, 11.1-04-SUMMARY.md, 11.1-VERIFICATION.md
started: 2026-09-02T05:40:35Z
updated: 2026-09-02T22:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. No local JSON cache, TTL, or GitHub Actions cache step was re-introduced; the skip index lives entirely inside the existing sheet_registry/get_sheet_watermarks INC-05 architecture (11.1-01 D6)
expected: No local JSON cache, TTL, or GitHub Actions cache step was re-introduced; the skip index lives entirely inside the existing sheet_registry/get_sheet_watermarks INC-05 architecture — why human: Architectural absence is not a positive test assertion — verified by code review (no new file, no new env var, no new cache step touched by this plan's 4 commits) rather than a runnable check.
result: pass
evidence: Confirmed on owner's request 2026-09-02: merge commit 675e3e2 touched no workflow, JSON, or config env; pipeline/discovery.py has zero json.dump/load, cache-path, or TTL code (the only hit is a comment describing the retired cache); the skip index reads get_sheet_watermarks (sheet_registry) at discovery.py:221 and client.Sheets.list_sheets at :241; the workflow's only actions/cache step caches pip (line 159); the retained use_discovery_cache input/env (2025-09-24, 004f1204) and the hash_history.json artifact-upload path pattern (2025-09-30, 7fae42f2) predate 11.1 and are not cache steps.

### 2. Every real (non-TEST_MODE) run logs the probed total_count and the seeded-with/seeded-empty counts per involved sheet (11.1-02 D7)
expected: Every real (non-TEST_MODE) run logs the probed total_count and the seeded-with/seeded-empty counts per involved sheet — why human: The log lines and span data attributes are implemented and code-reviewed against the plan's exact spec (per-sheet total_count INFO, ERROR on ceiling, per-sheet seeded-count INFO after the loop), but no test captures real production log output from a live Smartsheet call -- confirming the actual wording/values on a real run is a post-merge production observation, the same posture SC-1/D-11.1-04 already carries in 11.1-VALIDATION.md.
result: pass
evidence: Verified on owner's request against run 33570018457 (first run on the 11.1 code, 2026-09-02 01:20Z): target sheet probe total_count = 2501, pre-seeded 486 row(s) with attachments / 276 empty; PPP sheet probe total_count = 26, pre-seeded 5 with / 799 empty; both under the 25000 ceiling, no ERROR 'bulk attachment listing skipped' line.

### 3. No local JSON cache, Actions cache step, or retired two-phase attachment pre-fetch was re-introduced; no new *_MAX_MINUTES/*_TIMEOUT_SEC sub-budget constant was added (11.1-02 D8)
expected: No local JSON cache, Actions cache step, or retired two-phase attachment pre-fetch was re-introduced; no new *_MAX_MINUTES/*_TIMEOUT_SEC sub-budget constant was added — why human: Architectural absence is a code-review fact, not a positive runnable assertion -- verified by grep + diff inspection (no new file, no new env var beyond BULK_ATTACHMENT_LISTING_MAX_TOTAL which is explicitly not a time budget, no cache step touched).
result: pass
evidence: Verified on owner's request against merge commit 675e3e2: non-test/non-doc files touched are pipeline/discovery.py, pipeline/orchestrate.py, pipeline_memory/reader.py only (no workflow, no JSON); the only new os.getenv is BULK_ATTACHMENT_LISTING_MAX_TOTAL (a count ceiling, not a time budget); the sole added lines naming *_MAX_MINUTES/*_TIMEOUT_SEC are a comment and a log string that reference the retired names; no json.dump/json.load, cache path, actions/cache, ThreadPoolExecutor or as_completed lines were added (pre-seed is a single never-raising call).

### 4. Wall clock < ~75 min, no budget stop, all groups processed (roadmap SC-1)
expected: The first scheduled frequent production run on master with the 11.1 fix completes ALL groups (no TIME_BUDGET_MINUTES=165 graceful stop) with total wall clock under ~75 minutes.
result: pass
resolved: "2026-09-02 22:35Z after 11.1-04 / PR #384 (merged 13e8e76): canary run 33683979474 (schedule 21:14Z, build e2efdc0 containing 13e8e76) was a genuine registry-skip MISS (0/121 eligible, 121 fully validated) and still met SC-1 — Phase 1 37.7 s (was 3,214 / 4,999 s on the two prior miss runs), Python Duration 0:50:47 (50.8 min), 3,178 groups all processed, 6 generated, no TIME_BUDGET stop, group phase 0.52 s/group, INC-06 release line present, job clock 54.7 min with no exit hang. Evidence: 11.1-04-SUMMARY.md; re-verified independently in 11.1-VERIFICATION.md (2026-09-02T22:30Z, 20/20). Original issue and reconciliation kept below for the record."
reported: "Verified on owner's request: run 33570018457 (schedule, first on the 11.1 code) processed ALL groups (611 generated, 2413 skipped, 154 no-target-row) with no time-budget stop, but the Python duration was 2:15:50 and the core job 00:56Z to 03:19Z, over the ~75 min target. Group phase 6384.5 s; measured from the run's own request log: freeze_attribution 84.6 min (214,215 calls) + pipeline_run 13.0 min, because INC-05 D-12 also retired the frozen-row cache warm start. Discovery itself was 40.5 s. Part of the 611 files were a one-time backlog (owner hash reset + three zero-file runs)."
severity: major
reconciled: "2026-09-02 verify-work resume: group-phase component RESOLVED by 11.1-03 / PR #378 (2008fd9). Post-fix runs, all groups processed and no budget stop on each: 33634833356 (08:17 CDT) Duration 96.0 min, skip index 0/121, Phase 1 3,214 s; 33647771644 (10:19 CDT) Duration 129.7 min, 0/121, 4,999 s; 33659869696 (12:42 CDT, first build with #379) Duration 30.8 min, 118/121, 44.4 s. SC-1 is met when the registry-version skip engages and missed when it does not; the residual is the full-validation cost on a skip miss (G-11.1-4 diagnosis below). Test stays open."

### 5. Discovery phase drops to low single-digit minutes (roadmap SC-2)
expected: On that run the '⏭️ Discovery validation split:' line shows a non-zero skipped count and the Phase 1 elapsed time is low single-digit minutes (down from the ~67-min regression).
result: pass
evidence: Verified on owner's request against run 33570018457: '⏭️ Discovery validation split: 121 candidates, 117 skipped via sheet_registry, 4 fully validated (D-11.1-01)' and '⚡ Phase 1 complete: 121 sheets discovered in 40.5s' (vs 4162.5 s on the pre-11.1 run 33536584371 and 0.8 s pre-INC-05).

### 6. Group-processing cost returns to ≲0.5 s/group (roadmap SC-3)
expected: On that run both probed attachment total_count values stay well under the 25000 ceiling (no ERROR 'bulk attachment listing skipped' line) and the observed group-processing cost is roughly 0.28–0.5 s per group.
result: pass
evidence: Resolved by 11.1-03 / PR #378 (2008fd9), reconciled 2026-09-02. Post-fix group phase: run 33634833356 1,352 s / 3,176 groups = 0.43 s/group (freeze_attribution 117 calls, was 214,233); run 33647771644 1,265 s / 3,177 = 0.40 s/group (200 calls); run 33659869696 warm-start 18:07:18Z -> group_state flushed 18:13:13Z = 355 s / 3,178 = 0.11 s/group (19 calls, 3 generated). Attachment probes healthy on all three (run 33659869696: target total_count 3104, PPP 26, no 'bulk attachment listing skipped' ERROR).

### 7. Changed or new sheet still gets full validation on a live run
expected: A sheet edited or added since its last sheet_registry write appears on the fully-validated side of the split line (its Sheets.get_sheet call fires); it is never skipped.
result: pass
evidence: Verified on owner's request: both live runs fully validated exactly 4 sheets (matching the split line) and the set changed between runs — run 33570018457: Backup 100, Backup 104, Intake Promax 10, Backup 93; run 33579406295: Backup 75, Backup 11, Backup 3, Intake Promax 10. Backup 11 and Backup 3 were registry-skipped in the first run and fully validated in the second, i.e. a sheet edited after its last sheet_registry write took the full get_sheet path; the previously validated ones were skipped once the registry was refreshed.

### 8. Registry-matched candidate sheet is discovered end-to-end with zero client.Sheets.get_sheet calls, returning the identical {'id','name','column_mapping'} contract
expected: Registry-matched candidate sheet is discovered end-to-end with zero client.Sheets.get_sheet calls, returning the identical {'id','name','column_mapping'} contract
result: pass
source: automated
coverage_id: 11.1-01 D1
verification: tests/test_incremental_read.py::DiscoveryRegistrySkipTests::test_registry_matched_sheet_skips_get_sheet_call; tests/test_incremental_read.py::DiscoverySkipIndexTests::test_happy_path_returns_index_entry_on_version_match

### 9. All nine doubt-branch inputs (no registry row, version mismatch, empty/incomplete mapping, live version None, bulk-probe exception, watermarks unavailable, all-versions-None) fall back to full validation, never skip
expected: All nine doubt-branch inputs (no registry row, version mismatch, empty/incomplete mapping, live version None, bulk-probe exception, watermarks unavailable, all-versions-None) fall back to full validation, never skip
result: pass
source: automated
coverage_id: 11.1-01 D2
verification: tests/test_incremental_read.py::DiscoverySkipIndexTests (8 doubt-branch tests); tests/test_incremental_read.py::DiscoveryRegistrySkipTests (8 end-to-end doubt-branch tests)

### 10. INC-05 fail-closed guard (_failed_validation_sids -> RuntimeError) is unchanged: exactly one append site, and a full-validation exception still aborts the run naming the sid
expected: INC-05 fail-closed guard (_failed_validation_sids -> RuntimeError) is unchanged: exactly one append site, and a full-validation exception still aborts the run naming the sid
result: pass
source: automated
coverage_id: 11.1-01 D3
verification: tests/test_incremental_read.py::DiscoveryRegistrySkipTests::test_validation_exception_raises_runtime_error_naming_sid; tests/test_incremental_read.py::SkipGateLiveConfirmationWiringTests::test_discovery_fails_closed_on_validation_exception (unmodified pin)

### 11. Every discovery run logs a candidates/skipped/fully-validated split line citing D-11.1-01
expected: Every discovery run logs a candidates/skipped/fully-validated split line citing D-11.1-01
result: pass
source: automated
coverage_id: 11.1-01 D4
verification: tests/test_incremental_read.py::DiscoveryRegistrySkipTests::test_validation_split_log_line_reports_skipped_and_full_counts

### 12. get_sheet_watermarks exposes 'name' while keeping its fail-open contract (empty dict on any client/transport/response failure) unchanged
expected: get_sheet_watermarks exposes 'name' while keeping its fail-open contract (empty dict on any client/transport/response failure) unchanged
result: pass
source: automated
coverage_id: 11.1-01 D5
verification: tests/test_incremental_read.py::SheetWatermarksReadTests (4 tests, 3 fail-open siblings unmodified)

### 13. A skip-candidate row on a successfully-listed sheet reaches _has_existing_week_attachment with a pre-seeded listing and zero client.Attachments.list_row_attachments calls for that row
expected: A skip-candidate row on a successfully-listed sheet reaches _has_existing_week_attachment with a pre-seeded listing and zero client.Attachments.list_row_attachments calls for that row
result: pass
source: automated
coverage_id: 11.1-02 D1
verification: tests/test_incremental_read.py::AttachmentPrepopulationTests::test_seeded_row_never_triggers_per_row_call; tests/test_incremental_read.py::AttachmentPrepopulationTests::test_buckets_listing_by_parent_id_row_only

### 14. A row absent from a fully-successful bulk listing is seeded with an EMPTY list (resolves to regenerate, the safe direction) and still triggers zero per-row calls
expected: A row absent from a fully-successful bulk listing is seeded with an EMPTY list (resolves to regenerate, the safe direction) and still triggers zero per-row calls
result: pass
source: automated
coverage_id: 11.1-02 D2
verification: tests/test_incremental_read.py::AttachmentPrepopulationTests::test_row_without_attachment_seeded_empty_no_per_row_call

### 15. Any probe failure, listing failure, or over-ceiling total_count seeds nothing for that sheet, leaving the shipped lazy per-row path intact
expected: Any probe failure, listing failure, or over-ceiling total_count seeds nothing for that sheet, leaving the shipped lazy per-row path intact
result: pass
source: automated
coverage_id: 11.1-02 D3
verification: tests/test_incremental_read.py::AttachmentPrepopulationTests::test_probe_exception_seeds_nothing; tests/test_incremental_read.py::AttachmentPrepopulationTests::test_listing_exception_seeds_nothing; tests/test_incremental_read.py::AttachmentPrepopulationTests::test_over_ceiling_seeds_nothing_and_skips_bulk_call

### 16. _is_row_attachment accepts only ROW-parent attachments and rejects SHEET/COMMENT, pinned against the real installed SDK 4.3.0 enum
expected: _is_row_attachment accepts only ROW-parent attachments and rejects SHEET/COMMENT, pinned against the real installed SDK 4.3.0 enum
result: pass
source: automated
coverage_id: 11.1-02 D4
verification: tests/test_incremental_read.py::AttachmentParentTypeComparisonTests (4 tests: ROW/SHEET/COMMENT/missing-attribute)

### 17. _live_row_attachments's body and both _has_existing_week_attachment call sites are byte-for-byte unmodified from pre-phase text; all three pre-existing pinned tests pass unmodified
expected: _live_row_attachments's body and both _has_existing_week_attachment call sites are byte-for-byte unmodified from pre-phase text; all three pre-existing pinned tests pass unmodified
result: pass
source: automated
coverage_id: 11.1-02 D5
verification: tests/test_incremental_read.py::LiveRowAttachmentsTests (3 tests, unmodified); tests/test_incremental_read.py::SkipGateLiveConfirmationWiringTests::test_existence_checks_use_live_listing_never_stubs (unmodified); tests/test_subproject_e_hash_store.py::test_skip_gate_requires_ppp_attachment_for_reduced_sub (unmodified); git diff fbe7be8..HEAD -- pipeline/orchestrate.py tests/test_incremental_read.py contains zero '-' (removed) lines -- purely additive

### 18. main()'s pre-seed block is wired once per involved sheet ahead of the group loop, guarded on TEST_MODE/ATTACHMENT_REQUIRED_FOR_SKIP, skips an empty/falsy leg, and cannot abort a run on failure
expected: main()'s pre-seed block is wired once per involved sheet ahead of the group loop, guarded on TEST_MODE/ATTACHMENT_REQUIRED_FOR_SKIP, skips an empty/falsy leg, and cannot abort a run on failure
result: pass
source: automated
coverage_id: 11.1-02 D6
verification: tests/test_incremental_read.py::AttachmentPreseedWiringTests (5 source-pin tests)

### 19. Full pytest suite and all 6 gates green at plan close; Living Ledger records the phase's durable rules
expected: Full pytest suite and all 6 gates green at plan close; Living Ledger records the phase's durable rules
result: pass
source: automated
coverage_id: 11.1-02 D9
verification: python -m pytest tests/ -q -> 1886 passed, 1 skipped, 306 subtests; bash scripts/run_6_gates.sh -> ALL 6 GATES PASSED (Gate 4 mypy delta neutral, 72 -> 72); memory-bank/living-ledger.md [2026-09-01 13:50] entry, tail -40 confirmed to include the header and all required citations

## Summary

total: 19
passed: 19
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-11.1-4
  truth: "The first scheduled frequent production run on master with the 11.1 fix completes ALL groups (no TIME_BUDGET_MINUTES=165 graceful stop) with total wall clock under ~75 minutes."
  status: resolved
  resolved_by: 11.1-03-PLAN.md (component a, PR #378 2008fd9) + 11.1-04-PLAN.md (component b, PR #384 13e8e76)
  resolved_at: 2026-09-02
  resolution: "Component (b) proven in production on canary run 33683979474 (build e2efdc0 ⊇ 13e8e76, schedule 21:14Z): a genuine registry-skip MISS (0/121 eligible, 121 fully validated) with Phase 1 37.7 s, Python Duration 50.8 min, all 3,178 groups, no budget stop, 0.52 s/group. Bounded row_numbers=[1,2,3] validation read replaces the full-row download; the same response seeds the date-column samples (empty rows=[] seeded as [] per the PR-thread fix adc6639). Fix candidate (b) column-set-hash skip key stays deferred. Details: 11.1-04-SUMMARY.md; re-verified in 11.1-VERIFICATION.md 2026-09-02T22:30Z."
  reason: "User reported: Verified on owner's request: run 33570018457 (schedule, first on the 11.1 code) processed ALL groups (611 generated, 2413 skipped, 154 no-target-row) with no time-budget stop, but the Python duration was 2:15:50 and the core job 00:56Z to 03:19Z, over the ~75 min target. Group phase 6384.5 s; measured from the run's own request log: freeze_attribution 84.6 min (214,215 calls) + pipeline_run 13.0 min, because INC-05 D-12 also retired the frozen-row cache warm start. Discovery itself was 40.5 s. Part of the 611 files were a one-time backlog (owner hash reset + three zero-file runs)."
  severity: major
  test: 4
  partial_resolution: "Component (a), group phase: RESOLVED by 11.1-03-PLAN.md / PR #378 (2008fd9), reconciled 2026-09-02 -- per-row freeze_attribution RPC gone (117 / 200 / 19 calls on runs 33634833356 / 33647771644 / 33659869696, was 214,233); SC-1 met on 33659869696 (Duration 30.8 min). Component (b) below keeps this gap OPEN: do not auto-resolve it on 11.1-03's SUMMARY alone."
  root_cause: "Residual (b): when the D-11.1-01 registry-version skip misses (all 121 sheetVersions moved since the previous run's registry write -- 0/121 on two runs, 118/121 on the next, episodic not continuous), full validation downloads every row of every source sheet: _validate_single_sheet calls client.Sheets.get_sheet(sid, include='columns') (include does not restrict rows) yet reads only sheet.columns; 26-41 s wall per sheet at 8 workers = 3,214-4,999 s for 121 sheets (54-83 min) vs 44 s on a hit. Registry read/write failure, writer drift and rate limiting ruled out (all HTTP 200, identical upsert column sets, zero 429 lines). Diagnosed 2026-09-02."
  artifacts:
    - path: "pipeline/discovery.py"
      issue: "_validate_single_sheet (~line 468) fetches the full sheet via get_sheet(sid, include='columns') to read column metadata only; its sample fallback (~line 496) already issues a bounded get_sheet(sid, row_numbers=[1,2,3]) that returns columns, name and rows"
    - path: "pipeline/discovery.py"
      issue: "_build_discovery_skip_index keys the skip on sheetVersion, which moves for all 121 sheets whenever a shared upstream change touches them"
  missing:
    - "Bounded validation read: take columns (+ name) from a row-limited get_sheet (reuse the existing row_numbers=[1,2,3] sample call) or get_columns + the bulk list_sheets name, so a skip miss costs ~1-2 s/sheet instead of 26-41 s; identical {'id','name','column_mapping'} contract and title/type matching; fixtures/tests in the same change; no cache, TTL or Actions cache step (D-11.1-03)"
    - "Optional, second order: make the skip key tolerant of cross-sheet recalcs (e.g. a column-set hash) -- likely unnecessary once a miss is cheap"
  debug_session: ".planning/debug/11.1-discovery-full-validation-cost.md"
  fix_ref: "component (a) PR #378 perf/freeze-row-cache-warm-start (11.1-03, done); component (b) PR #384 perf/discovery-bounded-validation-read (11.1-04, merged 13e8e76, canary 33683979474 PASS)"
- gap_id: G-11.1-6
  truth: "On that run both probed attachment total_count values stay well under the 25000 ceiling (no ERROR 'bulk attachment listing skipped' line) and the observed group-processing cost is roughly 0.28–0.5 s per group."
  status: resolved
  resolved_by: 11.1-03-PLAN.md
  resolved_at: 2026-09-02
  reason: "User reported: Verified on owner's request: attachment probes healthy on run 33570018457 (target total_count 2501, PPP 26, no ceiling ERROR) but observed group-processing cost was 6384.5 s / 3178 groups = 2.0 s per group (median skip 1.73 s), and 6223.1 s / 3177 = 1.96 s per group on run 33579406295, against the criterion of roughly 0.28 to 0.5 s. Same D-12 frozen-row-cache regression as test 4."
  severity: blocker
  test: 6
  root_cause: "Same as G-11.1-4: no warm start for billing_audit_row_cache after INC-05 D-12, so each skipped group pays one freeze_attribution RPC per row plus a pipeline_run fingerprint pair (~1.7 s median per skip)."
  artifacts:
    - path: "pipeline/orchestrate.py"
      issue: "billing_audit_row_cache starts empty; _has_uncached_freeze_candidates true for every group"
  missing:
    - "Warm-start the cache from the lookup_attribution_bulk key set (PR #378, open at ba41451)"
  debug_session: "memory-bank/living-ledger.md [2026-09-01 22:40]"
  fix_ref: "PR #378 perf/freeze-row-cache-warm-start -- merged 2008fd9; canary 0.43 / 0.40 / 0.11 s per group on runs 33634833356 / 33647771644 / 33659869696"
