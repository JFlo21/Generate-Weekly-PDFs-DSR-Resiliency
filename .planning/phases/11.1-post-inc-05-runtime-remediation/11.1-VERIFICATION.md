---
phase: 11.1-post-inc-05-runtime-remediation
verified: 2026-09-02T22:30:00Z
status: passed
score: 20/20 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 12/12 code-verifiable must-haves verified (3 roadmap SC items routed to human verification)
  gaps_closed:
    - "G-11.1-4 component (a) — group-loop freeze_attribution cost (RESOLVED by 11.1-03 / PR #378, 2008fd9)"
    - "G-11.1-4 component (b) — discovery full-validation cost on a registry-skip miss (RESOLVED by 11.1-04 / PR #384, 13e8e76)"
    - "G-11.1-6 — group-processing cost ≲0.5 s/group (RESOLVED by 11.1-03 / PR #378)"
    - "SC-1 human-verification item — wall clock < ~75 min (RESOLVED, production canary run 33683979474: Duration 50.8 min, independently re-verified against the live GitHub Actions log in this pass)"
    - "SC-2 human-verification item — discovery phase low single-digit minutes (RESOLVED, Phase 1 37.7 s on a skip-MISS run — the harder case than the SC-2 wording requires)"
    - "SC-3 human-verification item — group-processing cost (RESOLVED, 0.52 s/group on this canary; 0.43/0.40/0.11 s/group on three prior 11.1-03 canaries)"
    - "Changed/new-sheet-still-fully-validated human-verification item (RESOLVED, UAT test 7: two live runs, Backup 11/Backup 3 moved from skipped to fully-validated between runs after a registry write)"
  gaps_remaining: []
  regressions: []
---

# Phase 11.1: Post-INC-05 Runtime Remediation Verification Report

**Phase Goal:** Restore the frequent-run wall clock to under ~75 minutes by
removing the two INC-05 retirement runtime regressions measured on run
33512477875 (169.8 min, budget-stopped, 1216 groups deferred) — (1) skip full
discovery validation for sheets whose live Smartsheet version matches
`sheet_registry.last_sheet_version` with a stored column mapping (fail-closed:
any doubt → full validation), and (2) batch the skip gate's live attachment
existence confirmation out of the serial group loop — while preserving the
INC-05 architecture (no local JSON caches, no Actions cache steps) and the
live confirmation + fail-closed discovery semantics from PR #373's review
fixes.

**Verified:** 2026-09-02T22:30:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (11.1-03 / PR #378 and 11.1-04 / PR #384)

## Goal Achievement

This is a re-verification following the prior report's 4 human-verification
items and the two production gaps (G-11.1-4, G-11.1-6) UAT surfaced against
those items. Two gap-closure plans landed and merged since the prior
verification:

- **11.1-03 / PR #378** (`2008fd9`) warm-starts the in-run
  `billing_audit_row_cache` dedupe set from the `lookup_attribution_bulk`
  prefetch map, eliminating the per-completed-row `freeze_attribution` RPC
  that INC-05's D-12 retirement had silently reintroduced (214k calls / 84.6
  min on run 33570018457). Resolved G-11.1-6 outright and resolved G-11.1-4
  component (a).
- **11.1-04 / PR #384** (`13e8e76`) bounds `_validate_single_sheet`'s
  Smartsheet read to `row_numbers=[1,2,3]`, replacing an unbounded full-sheet
  download that cost 26–41 s/sheet (3,214–4,999 s for 121 sheets) whenever the
  D-11.1-01 registry-version skip missed. The same bounded response seeds the
  date-column sample cache, deleting a second round trip. Resolved G-11.1-4
  component (b).

I independently re-verified the code for both merges (present, wired, tests
pass, diffs scoped exactly as claimed), re-ran the full test suite and the
6-gate harness myself (not trusting the SUMMARY's numbers), and pulled the
production canary run's raw GitHub Actions log directly via `gh run view
33683979474 --log` to cross-check every timing and count the SUMMARY cites —
all matched byte-for-byte. Per this task's explicit instruction, the
SUMMARY-recorded (and now independently re-confirmed) production evidence is
treated as satisfying the prior report's 4 human-verification items; none
remain open.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Registry-version skip: a candidate sheet whose live Smartsheet version matches `sheet_registry.last_sheet_version` (with non-empty stored `column_mapping` containing `Weekly Reference Logged Date`) is validated with ZERO `client.Sheets.get_sheet` calls | ✓ VERIFIED (regression) | `pipeline/discovery.py:189-306` (`_build_discovery_skip_index`), `:443-461` (fast path). Unchanged since prior verification — confirmed the 11.1-03/04 diffs (`git show 2008fd9`, `git show 13e8e76`) never touch lines 443-461. `DiscoveryRegistrySkipTests::test_registry_matched_sheet_skips_get_sheet_call` PASS. |
| 2 | Every doubt branch (no registry row, version mismatch, empty/incomplete mapping, live version `None`, bulk-probe exception, watermarks unavailable, all-versions-`None`) falls back to full validation, never skip | ✓ VERIFIED (regression) | `DiscoverySkipIndexTests` + `DiscoveryRegistrySkipTests` doubt-branch tests all PASS; only the one expected first-call-shape literal in `_assert_full_validation_ran` changed (updated for the bounded read, per 11.1-04's own must-have). |
| 3 | The bulk sheet-version probe / registry watermark read never raise into `_build_discovery_skip_index`, never touch `_failed_validation_sids`; the post-loop fail-closed `RuntimeError` guard is byte-for-byte unchanged | ✓ VERIFIED (regression) | `pipeline/discovery.py:696-708` (single `_failed_validation_sids.append` site, unchanged location and text). `SkipGateLiveConfirmationWiringTests::test_discovery_fails_closed_on_validation_exception` PASS. |
| 4 | When the bulk probe returns no usable version for any candidate, discovery logs a WARNING and every sheet falls back to full validation | ✓ VERIFIED (regression) | `pipeline/discovery.py:260-271`. `DiscoverySkipIndexTests::test_all_versions_none_warns_and_yields_empty_index` PASS. |
| 5 | Every run logs a validation-split line naming candidate/skipped/fully-validated counts, citing D-11.1-01 | ✓ VERIFIED (regression + production) | `pipeline/discovery.py:729-733`. Independently confirmed on the live log: `⏭️ Discovery validation split: 121 candidates, 0 skipped via sheet_registry, 121 fully validated (D-11.1-01)`. |
| 6 | `get_sheet_watermarks` returns `name` for every registry row found, fail-open contract unchanged | ✓ VERIFIED (regression) | `pipeline_memory/reader.py:83-139`, byte-identical to prior verification. `SheetWatermarksReadTests` (4 tests) PASS. |
| 7 | A skip-candidate row on a successfully-listed sheet reaches `_has_existing_week_attachment` with a pre-seeded listing, ZERO per-row `list_row_attachments` calls | ✓ VERIFIED (regression) | `pipeline/orchestrate.py:1246-1347`, `:2919-2985`, confirmed untouched by the 11.1-03/04 diffs (which only touch lines ~3140-3160 and imports). `AttachmentPrepopulationTests` PASS. |
| 8 | A row with no row-level attachment is seeded EMPTY (regenerate direction) | ✓ VERIFIED (regression) | `pipeline/orchestrate.py:1330-1341`. `AttachmentPrepopulationTests::test_row_without_attachment_seeded_empty_no_per_row_call` PASS. |
| 9 | Probe/listing exception or over-ceiling `total_count` seeds nothing; every row falls back to today's per-row live lookup, fail-open-to-regenerate | ✓ VERIFIED (regression) | `pipeline/orchestrate.py:1270-1319`. `AttachmentPrepopulationTests::test_probe_exception_seeds_nothing`/`test_listing_exception_seeds_nothing`/`test_over_ceiling_seeds_nothing_and_skips_bulk_call` PASS. |
| 10 | `_is_row_attachment` accepts only ROW-parent attachments, pinned against installed SDK 4.3.0 | ✓ VERIFIED (regression) | `pipeline/orchestrate.py:1227-1243`. `AttachmentParentTypeComparisonTests` (4 tests) PASS. |
| 11 | `_live_row_attachments`'s body and both `_has_existing_week_attachment` call sites are byte-for-byte unchanged | ✓ VERIFIED (regression) | `pipeline/orchestrate.py:1207-...` — confirmed outside the 11.1-03 diff's touched range (imports + lines ~3140-3160 only). `LiveRowAttachmentsTests` (3 tests) PASS. |
| 12 | Full suite + 6 gates green at current HEAD; no change-detection-key, grouping, filename, or attachment-cleanup behavior changes | ✓ VERIFIED (independently re-run) | I ran `python -m pytest tests/ -q` myself: **1946 passed, 1 skipped, 363 subtests**, exit 0. `bash scripts/run_6_gates.sh` myself: **ALL 6 GATES PASSED**, mypy delta neutral (71→71). `git show 2008fd9 -- pipeline/grouping.py` and `git show 2008fd9 -- pipeline/attribution.py` inspected line-by-line: purely additive (one new module-level frozenset + getter, one new function); zero lines of existing grouping-key, hash, filename, or cleanup logic touched. |
| 13 (roadmap SC-1) | First post-merge frequent run completes all groups (no time-budget stop), wall clock < ~75 min | ✓ VERIFIED (production canary, independently re-pulled) | Canary run **33683979474** (schedule, build `e2efdc0` containing merge `13e8e76`, confirmed MERGED via `gh pr view 384` — `mergedAt: 2026-09-02T20:52:01Z`, `mergeCommit.oid: 13e8e765e...`). I pulled the raw log myself (`gh run view 33683979474 --log`) and grepped it directly: `• Duration: 0:50:47.755197` (50.8 min, under 75 min); no `TIME_BUDGET_MINUTES` graceful-stop line anywhere in 30,243 log lines; `📈 Found 3178 work request groups to process` and `🧾 group_state: 6 flushed, 0 withheld` — all groups accounted for, no deferral. |
| 14 (roadmap SC-2) | Discovery phase drops to low single-digit minutes on a run with no sheet changes; changed/new sheet still fully validated; registry/Supabase unavailability falls back to full validation | ✓ VERIFIED (production + code) | Same log, independently confirmed: `⏭️ Discovery skip index (D-11.1-01): 121 candidate(s), 0 eligible` / `⚡ Phase 1 complete: 121 sheets discovered in 37.7s` — this is a skip-**MISS** run (the harder case: 0 sheets matched the registry), and it still lands at 37.7 s, well under the "low single-digit minutes" bar the SC-2 "no sheet changes" case requires. The 11.1-UAT.md test 7 (owner-verified against two live runs 33570018457/33579406295) independently confirms the complementary case: a sheet edited since its last `sheet_registry` write (Backup 11, Backup 3) moved from the skipped side to the fully-validated side between runs. Registry/Supabase-unavailable fallback is proved by code + `DiscoverySkipIndexTests` doubt-branch tests (truth 2, unchanged). |
| 15 (roadmap SC-3) | Group-processing per-group cost ≲0.5 s/group; skip gate confirms existence via live listings (never `group_state` stubs); transport failure resolves to regenerate | ✓ VERIFIED (production + code) | Same canary log: `🧊 Frozen-row cache warm-started … 218,849 row(s)` at 21:37:37.77Z → `🧾 group_state: 6 flushed` at 22:05:17.92Z = 1,660.15 s ÷ 3,178 groups = **0.52 s/group**, independently recomputed from the raw timestamps (matches the SUMMARY's figure exactly). Consistent with the three prior 11.1-03 canaries (0.43 / 0.40 / 0.11 s/group) — well inside the ≲0.5 s design intent with normal run-to-run variance; not a regression (G-11.1-6 already RESOLVED on three separate readings). Live-listing confirmation and fail-open-to-regenerate are unchanged code (truths 7-11). |
| 16 (D-11.1-02/D-11.1-03 follow-up) | The `billing_audit_row_cache` warm start seeds ONLY from a successful bulk prefetch, uses the identical key format the freeze loop and its RPC already use, and is a safe no-op (empty seed, run still completes) on prefetch failure | ✓ VERIFIED | `pipeline/grouping.py:117-121,207-208` (`_PREFETCHED_FROZEN_ROW_KEYS` reset to empty at function entry, populated only when `_attr_status == 'success'`). `pipeline/attribution.py:806-831` (`warm_billing_audit_row_cache`, key format `f"{wr}\|{week_ending:%m%d%y}\|{row_id}"`). `tests/test_freeze_row_cache_warm_start.py` — 12 tests / 8 subtests, all PASS, including `SanitizerEquivalenceTests` (proves the warm-start key's WR sanitizer is identical to the freeze loop's) and `WarmCacheHelperTests::test_empty_prefetch_seeds_nothing`. |
| 17 (D-11.1-01/D-11.1-03 follow-up) | Full validation of a source sheet issues exactly ONE row-bounded `client.Sheets.get_sheet` call (`row_numbers=[1,2,3]`, no `include=` kwarg) when the response carries rows, and returns the identical `{'id','name','column_mapping'}` contract via unchanged title/type matching | ✓ VERIFIED | `pipeline/discovery.py:477-480` (call site, `row_numbers=list(range(1, 4))`, matches the pre-existing sample-fallback idiom at line 518 verbatim). `DiscoveryBoundedValidationReadTests::test_bounded_full_validation_issues_one_call_and_returns_contract` and `::test_bounded_call_uses_row_numbers_and_never_include` PASS. Non-vacuous source pin `::test_source_has_no_unbounded_validation_read` (comment-stripped region scan, forbidden literal built at runtime so the test file never contains it) PASS. `tests/test_vac_crew.py` end-to-end discovery mapping tests pass unmodified — independent proof the column-mapping logic itself is untouched. |
| 18 (PR #384 review fix) | A response whose `rows` attribute is present but empty (`[]`) is seeded as `[]` (not the `None` "not seeded" sentinel), so a genuinely empty source sheet does not trigger a second `get_sheet` round trip | ✓ VERIFIED | `pipeline/discovery.py:510-513` (`getattr(sheet, 'rows', None)` presence check, `list(_rows_attr) if _rows_attr is not None else None`). `DiscoveryBoundedValidationReadTests::test_bounded_empty_rows_response_is_seeded_without_second_call` PASS. |
| 19 (D-11.1-03) | 11.1-03 and 11.1-04 introduce no local JSON cache file, TTL, new cache-shaped env var, GitHub Actions cache step, or `*_MAX_MINUTES`/`*_TIMEOUT_SEC` sub-budget constant | ✓ VERIFIED | `git show 2008fd9 --stat` and `git show 13e8e76 -- pipeline/discovery.py` inspected directly: the only production files touched are `pipeline/attribution.py` (+38/-6), `pipeline/grouping.py` (+24), `pipeline/orchestrate.py` (+19/-12, import + warm-start call only), `pipeline/discovery.py` (+30/-9, confined to lines 463-513). No `json.dump`/`json.load`, no new `os.getenv`, no `actions/cache` step, no `*_MAX_MINUTES`/`*_TIMEOUT_SEC` constant added by either commit. `save_billing_audit_row_cache`/`load_billing_audit_row_cache` (pre-existing, predate PR #378 per `git show 2008fd9^:pipeline/attribution.py`) remain imported but are never called anywhere in `pipeline/orchestrate.py` or `generate_weekly_pdfs.py` — dead code inherited from before this phase, not a re-introduced cache path. |
| 20 (PARALLEL_WORKERS guardrail) | `PARALLEL_WORKERS_DISCOVERY` stays at 8; no `column_ids=` narrowing added to the validation read | ✓ VERIFIED | `pipeline/config.py:99` — `PARALLEL_WORKERS_DISCOVERY = int(os.getenv('PARALLEL_WORKERS_DISCOVERY', '8') or 8)`, unchanged. `pipeline/discovery.py:477-480` call carries only `row_numbers=`, no `column_ids=`. |

**Score:** 20/20 truths verified (0 present-but-behavior-unverified; 0 human-verification items outstanding).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `pipeline/discovery.py::_build_discovery_skip_index` | Unchanged since prior verification | ✓ VERIFIED | Lines 189-306; identical to prior report. |
| `pipeline/discovery.py::_validate_single_sheet` bounded read | `row_numbers=list(range(1, 4))`, sample cache seeded from the same response | ✓ VERIFIED | Lines 462-513; diff confined exactly to this region per `git show 13e8e76 -- pipeline/discovery.py`. |
| `pipeline/attribution.py::warm_billing_audit_row_cache` | Module-level, additive, key format matches the freeze loop | ✓ VERIFIED | Lines 806-831; new function, zero lines removed elsewhere in the file besides a comment rewrite. |
| `pipeline/grouping.py::get_prefetched_frozen_row_keys` / `_PREFETCHED_FROZEN_ROW_KEYS` | Module-level, success-only population, reset-first | ✓ VERIFIED | Lines 49-63 (declaration/getter), 117-121 (reset), 207-208 (success-only write). |
| `pipeline/orchestrate.py` warm-start wiring in `main()` | Seeds `billing_audit_row_cache` before the group loop | ✓ VERIFIED | Lines 3150-3159; `tests/test_freeze_row_cache_warm_start.py::OrchestrateWiringTests::test_cache_is_seeded_before_the_group_loop` PASS. |
| `tests/test_incremental_read.py::DiscoveryBoundedValidationReadTests` | 4 tests (single-call, negative-kwarg, empty-rows PR-fix, source pin) | ✓ VERIFIED | All 4 collected and passing (`--collect-only` confirmed the exact 4 names). |
| `tests/test_freeze_row_cache_warm_start.py` | 12 tests / 8 subtests | ✓ VERIFIED | Ran directly: 12 passed, 8 subtests passed. |
| `memory-bank/living-ledger.md` | Dated entries for both gap-closure fixes | ✓ VERIFIED | `[2026-09-02 14:35]` (bounded-read rule) and `[2026-09-02 15:05]` (PR-thread empty-rows correction) present and tail-confirmed. |
| `.planning/debug/11.1-discovery-full-validation-cost.md` | Debug session backing the G-11.1-4(b) diagnosis | ✓ VERIFIED | File exists, referenced consistently across 11.1-03/04 plans, SUMMARYs, and the ledger. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `pipeline.grouping.get_prefetched_frozen_row_keys()` | `pipeline.orchestrate.main()` | direct call, seeds `billing_audit_row_cache` before the group loop | ✓ WIRED | `pipeline/orchestrate.py:3151-3153`. |
| `warm_billing_audit_row_cache` key format | the freeze loop's own `f"{wr_num}\|{week_raw}\|{_row_id}"` key | identical sanitizer (`SanitizerEquivalenceTests`) | ✓ WIRED | `tests/test_freeze_row_cache_warm_start.py::SanitizerEquivalenceTests::test_writer_and_config_sanitizers_are_identical` PASS — a key-format drift here would silently mark an unfrozen row "already frozen" and skip its real freeze RPC, so this link is load-bearing and independently proved. |
| Bounded `get_sheet(sid, row_numbers=[1,2,3])` response | `_sample_rows_cache` → `_extract_col_samples` → date-column diagnostics | direct reuse, no second round trip | ✓ WIRED | `pipeline/discovery.py:510-522`; `DiscoveryBoundedValidationReadTests::test_bounded_full_validation_issues_one_call_and_returns_contract` asserts `call_count == 1`. |
| `pipeline.fetch.get_last_sheet_versions()` | `upsert_sheet_registry(last_sheet_version=...)` | unchanged — still the input the D-11.1-01 skip index compares against next run | ✓ WIRED | Not touched by either gap-closure commit (`git diff` confirms `pipeline_memory/`, `pipeline/fetch.py` untouched since prior verification). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Bounded validation read issues one call, correct kwargs | `python -m pytest tests/test_incremental_read.py::DiscoveryBoundedValidationReadTests -v` | 4 passed | ✓ PASS |
| Frozen-row cache warm start (full class) | `python -m pytest tests/test_freeze_row_cache_warm_start.py -v` | 12 passed, 8 subtests | ✓ PASS |
| Full `test_incremental_read.py` (regression) | `python -m pytest tests/test_incremental_read.py -q` | 192 passed, 22 subtests | ✓ PASS |
| Full workspace suite (run once, myself) | `python -m pytest tests/ -q` | 1946 passed, 1 skipped, 363 subtests | ✓ PASS |
| 6-gate harness (run once, myself) | `bash scripts/run_6_gates.sh` | `ALL 6 GATES PASSED`, mypy delta 71→71 (neutral) | ✓ PASS |
| PR #384 merge status (independently queried, not trusted from SUMMARY) | `gh pr view 384 --json state,mergedAt,mergeCommit,baseRefName` | `MERGED`, `13e8e76...`, base `master`, `2026-09-02T20:52:01Z` | ✓ PASS |
| Production canary run status (independently queried) | `gh run view 33683979474 --json conclusion,headSha,event,createdAt` | `success`, `e2efdc0...`, `schedule`, `2026-09-02T21:14:05Z` | ✓ PASS |
| Production canary raw log grep (independently pulled, not the SUMMARY's transcription) | `gh run view 33683979474 --log \| grep -E "Discovery skip index\|Discovery validation split\|Phase 1 complete\|• Duration:\|TIME_BUDGET_MINUTES\|Session complete\|group_state:"` | All 6 named lines matched the SUMMARY's transcription byte-for-byte; no budget-stop line found in 30,243 lines | ✓ PASS |

### Probe Execution

Not applicable — this phase has no `scripts/*/tests/probe-*.sh` probes; verification uses the project's standard pytest + 6-gate harness plus direct production-log inspection (covered above).

### Requirements Coverage

**N/A per the phase's own contract**, unchanged from the prior verification.
`.planning/phases/11.1-post-inc-05-runtime-remediation/11.1-CONTEXT.md` and
`.planning/ROADMAP.md` both state: "Requirements: none mapped (inserted urgent
phase — the Success Criteria above plus D-11.1-01..04 in `11.1-CONTEXT.md` are
the contract)." Confirmed directly: `grep -n "11\.1\|G-11\.1\|D-11\.1\|SC-1\b\|
SC-2\b\|SC-3\b\|SC-4\b" .planning/REQUIREMENTS.md` returns zero matches.

The full set of requirement/gap IDs declared across all four plans' frontmatter
— `D-11.1-01`, `D-11.1-02`, `D-11.1-03`, `SC-1`, `SC-2`, `SC-3`, `SC-4`,
`G-11.1-4`, `G-11.1-6` — are **phase-local contract IDs tracked in
`11.1-CONTEXT.md`, `11.1-UAT.md`, and the plan/SUMMARY frontmatter directly**,
not REQUIREMENTS.md rows. This is documented as expected (not a defect) in
every SUMMARY for this phase. None are "missing" from REQUIREMENTS.md — they
were never meant to be there.

### Anti-Patterns Found

None. Scanned the diff of every file touched by the two gap-closure commits
(`git diff abf905d..HEAD -- pipeline/discovery.py pipeline/orchestrate.py
pipeline/attribution.py pipeline/grouping.py pipeline_memory/reader.py`) for
`TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` and stub-language patterns
(`placeholder|coming soon|not yet implemented|not available`) — zero matches
added by this phase's diff. No empty-implementation stubs, no
hardcoded-empty-data patterns.

One pre-existing, out-of-scope observation (not a phase 11.1 anti-pattern):
`pipeline/attribution.py::save_billing_audit_row_cache` /
`load_billing_audit_row_cache` remain defined and imported but are never
called from `pipeline/orchestrate.py` or `generate_weekly_pdfs.py` — this
dead code predates PR #378 (confirmed via `git show 2008fd9^:pipeline/
attribution.py`) and is not a re-introduced local cache; it is simply unused.
Not a gap for this phase; flagged for awareness only.

## Human Verification Required

None. All four items from the prior report (`11.1-VERIFICATION.md`,
2026-09-01) are resolved by the production canary evidence in
`11.1-03-SUMMARY.md` and `11.1-04-SUMMARY.md`, which I independently
re-verified against the live GitHub Actions run and PR records rather than
trusting the SUMMARY narrative alone (see Behavioral Spot-Checks above).

## Gaps Summary

No open gaps. Both gaps UAT surfaced against the prior verification's
human-verification items are RESOLVED:

- **G-11.1-4** (wall clock < ~75 min) — component (a), the group-loop
  `freeze_attribution` cost, resolved by 11.1-03 / PR #378 (`2008fd9`);
  component (b), the discovery full-validation cost on a registry-skip miss,
  resolved by 11.1-04 / PR #384 (`13e8e76`). Independently confirmed on
  production canary run 33683979474 (a skip-MISS run, the worst case this fix
  targets): Phase 1 37.7 s, Python Duration 50.8 min, all groups processed, no
  budget stop.
- **G-11.1-6** (group-processing cost ≲0.5 s/group) — resolved by 11.1-03 /
  PR #378. Independently confirmed at 0.52 s/group on this same canary
  (recomputed from raw log timestamps), consistent with the three prior
  11.1-03 canary readings (0.43 / 0.40 / 0.11 s/group).

Fix candidate (b) from the debug session (a column-set-hash skip key tolerant
of cross-sheet recalcs) remains explicitly DEFERRED per both 11.1-04-PLAN.md
and the Living Ledger — its motivation (an expensive skip miss) no longer
exists now that a full-validation-only run costs 37.7 s of discovery time
instead of 54-83 minutes. This is a documented, owner-accepted deferral, not
an open gap.

---

*Verified: 2026-09-02T22:30:00Z*
*Verifier: Claude (gsd-verifier)*
