---
phase: 11.1-post-inc-05-runtime-remediation
verified: 2026-09-01T19:23:01Z
status: human_needed
score: 12/12 code-verifiable must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Trigger the first post-merge production `frequent` scheduled run on `TARGET_SHEET_ID` after this phase's PR merges to master, with no forced full regen."
    expected: "Run completes all groups with no `TIME_BUDGET_MINUTES` stop; total wall clock < ~75 min (roadmap SC-1)."
    why_human: "Requires a real production Smartsheet run; not observable from static code. Compare against run 33512477875 (169.8 min, budget-stopped) and pre-merge baseline 33449808275 (76.8 min) per 11.1-VALIDATION.md."
  - test: "On that same run, read the `⏭️ Discovery validation split:` log line and the Phase 1 elapsed-time log line."
    expected: "Discovery phase (Phase 1) elapsed time drops to low single-digit minutes when no sheet's live version changed since the last run; skipped count > 0."
    why_human: "The registry-version skip mechanism and its doubt-branch fallbacks are proven correct by 19 passing unit/end-to-end tests (code-verifiable), but the actual wall-clock magnitude on live Smartsheet data — and whether `sheet_registry` is warm enough on the first post-merge run to produce a meaningful skip count — is a live production observation per 11.1-VALIDATION.md Manual-Only Verifications."
  - test: "On that same run, read the per-sheet `📎 Attachment total_count probe:` and `📎 Pre-seeded live attachment listings for sheet ...` log lines for TARGET_SHEET_ID and SUBCONTRACTOR_PPP_SHEET_ID, and compute observed group-processing seconds/group."
    expected: "Both sheets' probed `total_count` stay below the 25000 `BULK_ATTACHMENT_LISTING_MAX_TOTAL` ceiling (no ERROR line fires); group-processing cost returns to ≲0.5 s/group (roadmap SC-3)."
    why_human: "The bulk pre-seed mechanism, its ceiling fallback, and its never-raising contract are proven by 16 passing unit tests (code-verifiable), but the real per-sheet attachment counts and the resulting seconds/group are live production measurements per 11.1-VALIDATION.md Manual-Only Verifications and 11.1-02-SUMMARY.md coverage item D7."
  - test: "Confirm a changed or newly-created source sheet appears on the full-validation side of the split line on a live run (not the skip side)."
    expected: "A sheet whose live Smartsheet version differs from its stored `sheet_registry.last_sheet_version` is fully re-validated, never skipped."
    why_human: "The version-mismatch doubt branch is unit-tested against a mocked SDK and mocked Supabase (DiscoveryRegistrySkipTests::test_version_mismatch_performs_full_validation, passing); confirming it holds against a real Smartsheet sheet edit is a live observation, per 11.1-VALIDATION.md's 'Discovery skip vs full validation on changed sheet' row."
---

# Phase 11.1: Post-INC-05 Runtime Remediation Verification Report

**Phase Goal:** Restore the frequent-run wall clock to under ~75 minutes by
removing the two INC-05 retirement runtime regressions measured on run
33512477875 (169.8 min, budget-stopped, 1216 groups deferred) — (1) skip full
discovery validation for sheets whose live Smartsheet version matches
`sheet_registry.last_sheet_version` with a stored column mapping (fail-closed:
any doubt → full validation), and (2) batch the skip gate's live attachment
existence confirmation out of the serial group loop — while preserving the
INC-05 architecture (no local JSON caches, no Actions cache steps) and the
live confirmation + fail-closed discovery semantics from PR #373's review
fixes.

**Verified:** 2026-09-01T19:23:01Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

Every mechanism, guard, and prohibition the phase's own plans and the
ROADMAP contract require is present, wired, and covered by a passing test —
19 new discovery tests + 16 new attachment-pre-seed tests, all green, plus
every pre-existing pinned test (`SheetWatermarksReadTests`,
`SkipGateLiveConfirmationWiringTests`, `LiveRowAttachmentsTests`,
`test_skip_gate_requires_ppp_attachment_for_reduced_sub`,
`tests/test_skip_no_target_row.py`'s guard-text pin) passing with its body
byte-for-byte unmodified. The phase's numeric performance claims (< ~75 min
wall clock; low single-digit-minute discovery; ≲0.5 s/group), however, are
explicitly POST-MERGE production observations per `11.1-VALIDATION.md`'s own
Manual-Only Verifications table — no code-level check can produce them, so
this report routes them to human verification rather than marking them
verified or failed.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Registry-version skip: a candidate sheet whose live Smartsheet version matches `sheet_registry.last_sheet_version` (with non-empty stored `column_mapping` containing `Weekly Reference Logged Date`) is validated with ZERO `client.Sheets.get_sheet` calls, returning the identical `{'id','name','column_mapping'}` contract | ✓ VERIFIED | `pipeline/discovery.py:189-303` (`_build_discovery_skip_index`) + `:440-458` (fast path, first statement of `_validate_single_sheet`, above its `try:`). `DiscoveryRegistrySkipTests::test_registry_matched_sheet_skips_get_sheet_call` PASS; `DiscoverySkipIndexTests::test_happy_path_returns_index_entry_on_version_match` PASS. |
| 2 | Every doubt branch (no registry row, version mismatch, empty/incomplete mapping, live version `None`, bulk-probe exception, watermarks unavailable, all-versions-`None`) falls back to full validation, never skip | ✓ VERIFIED | 9 named tests each in `DiscoverySkipIndexTests` and `DiscoveryRegistrySkipTests` — all PASS. Code: `_build_discovery_skip_index`'s admission gate at `pipeline/discovery.py:270-286` requires ALL five conditions; any miss leaves the sid out of the index. |
| 3 | The bulk sheet-version probe and the registry watermark read never raise out of `_build_discovery_skip_index` and never touch `_failed_validation_sids`; the INC-05 post-loop `RuntimeError` fail-closed guard is byte-for-byte unchanged | ✓ VERIFIED | `pipeline/discovery.py:217-303`: outer `try`/`except Exception` returns `{}`; inner probe `try`/`except` at `:236-249` is deliberately separate from `_validate_single_sheet`'s own except block at `:657-672`. `_failed_validation_sids.append` count = 1 (grep, comments stripped). `SkipGateLiveConfirmationWiringTests::test_discovery_fails_closed_on_validation_exception` and `DiscoveryRegistrySkipTests::test_validation_exception_raises_runtime_error_naming_sid` both PASS with the pinned test's body unmodified. |
| 4 | When the bulk probe returns no usable version for any candidate, discovery logs a WARNING naming the degradation and every sheet falls back to full validation | ✓ VERIFIED | `pipeline/discovery.py:257-268`. `DiscoverySkipIndexTests::test_all_versions_none_warns_and_yields_empty_index` PASS. |
| 5 | Every run logs a validation-split line naming candidate/skipped/fully-validated counts, citing D-11.1-01 | ✓ VERIFIED | `pipeline/discovery.py:698-708`, additive — placed after the pre-existing `⚡ Discovery complete:` line, which is untouched. `DiscoveryRegistrySkipTests::test_validation_split_log_line_reports_skipped_and_full_counts` PASS. |
| 6 | `get_sheet_watermarks` returns `name` for every registry row found, and still returns `{}` on any client/transport/response failure (fail-open contract unchanged) | ✓ VERIFIED | `pipeline_memory/reader.py:83-139`: `name` added to the `.select(...)` string and docstring; the `client is None`, `with_retry(...) is None`, and `not rows` guards are unmodified. `SheetWatermarksReadTests` (4 tests, 3 fail-open siblings byte-identical) all PASS. |
| 7 | A skip-candidate row on a successfully-listed sheet reaches `_has_existing_week_attachment` with a pre-seeded listing and triggers ZERO `client.Attachments.list_row_attachments` calls | ✓ VERIFIED | `pipeline/orchestrate.py:1246-1347` (`_preseed_live_attachment_listings`) + `:2919-2985` (`main()` wiring block). `AttachmentPrepopulationTests::test_seeded_row_never_triggers_per_row_call` and `::test_buckets_listing_by_parent_id_row_only` PASS. |
| 8 | A row on a successfully-listed sheet with no row-level attachment is seeded EMPTY, resolving to "no attachment" (regenerate direction) | ✓ VERIFIED | `pipeline/orchestrate.py:1330-1341` (`setdefault`-seed loop). `AttachmentPrepopulationTests::test_row_without_attachment_seeded_empty_no_per_row_call` PASS. |
| 9 | If a sheet's bulk listing raises, its `total_count` probe raises, or `total_count` exceeds the 25000 ceiling (`BULK_ATTACHMENT_LISTING_MAX_TOTAL`), NOTHING is seeded for that sheet and every row uses today's per-row live lookup, fail-open-to-regenerate unchanged (D-11.1-05) | ✓ VERIFIED | `pipeline/orchestrate.py:1270-1319` (probe try/except, ceiling guard at ERROR level naming option (b) as the documented remedy, listing try/except). `AttachmentPrepopulationTests::test_probe_exception_seeds_nothing`, `::test_listing_exception_seeds_nothing`, `::test_over_ceiling_seeds_nothing_and_skips_bulk_call` all PASS. Option (b) (bounded parallel per-skip-candidate prefetch) confirmed NOT built — no new `ThreadPoolExecutor` in the diff (`git diff origin/master...HEAD -- pipeline/orchestrate.py \| grep ThreadPoolExecutor` empty). |
| 10 | `_is_row_attachment` accepts only ROW-parent attachments and rejects SHEET/COMMENT, pinned against the installed smartsheet-python-sdk 4.3.0 `AttachmentParentType` enum | ✓ VERIFIED | `pipeline/orchestrate.py:1227-1243`, imports the real SDK enum at `:43-44`. `AttachmentParentTypeComparisonTests` (4 tests: ROW accepted, SHEET rejected, COMMENT rejected, missing-attribute rejected) all PASS. |
| 11 | `_live_row_attachments`'s body and both `_has_existing_week_attachment` call sites are byte-for-byte unchanged from pre-phase (`origin/master`) text | ✓ VERIFIED | Direct `diff` of the function body (`pipeline/discovery.py`... wait, `pipeline/orchestrate.py:1182-1211` current vs `origin/master:1175-1204`) → 0 differences (31 lines each, identical). Both `_has_existing_week_attachment(` call-site 12-line windows (current `:3902-3913`/`:3943-3954` vs `origin/master:3693-3704`/`:3734-3745`) → 0 differences. `LiveRowAttachmentsTests` (3 tests) and `SkipGateLiveConfirmationWiringTests::test_existence_checks_use_live_listing_never_stubs` PASS unmodified. |
| 12 | Full suite + 6 gates green; no change-detection-key, grouping, filename, or attachment-cleanup behavior changes | ✓ VERIFIED | `python -m pytest tests/ -q` → 1886 passed, 1 skipped, 306 subtests, exit 0. `bash scripts/run_6_gates.sh` → `ALL 6 GATES PASSED`, exit 0. `git diff origin/master...HEAD --stat` touches only `pipeline/discovery.py`, `pipeline/orchestrate.py`, `pipeline_memory/reader.py` (production code) plus tests/docs/mypy-golden; zero lines removed from either production file except the 2-line `reader.py` docstring/select-list edit (Step A of Plan 01, `+name`). No `content_hash`/`wr, week_ending`/`group_key` composition touched. |
| 13 (roadmap SC-1) | First post-merge frequent run completes all groups (no time-budget stop) with wall clock < ~75 min | ⚠️ Not verifiable from code (human) | Explicitly a POST-MERGE production observation per `11.1-VALIDATION.md` Manual-Only Verifications and per this task's own instruction. No PR has merged yet as of this verification (`branch feat/11.1-runtime-remediation`, not yet opened per `11.1-02-SUMMARY.md`). |
| 14 (roadmap SC-2, magnitude) | Discovery phase drops from ~67 min to low single-digit minutes on a run with no sheet changes | ⚠️ Not verifiable from code (human) | Mechanism verified (truths 1-6); the numeric wall-clock magnitude requires a live run against real `sheet_registry` state per `11.1-VALIDATION.md`. |
| 15 (roadmap SC-3, magnitude) | Group-processing per-group cost returns to ≲0.5 s/group | ⚠️ Not verifiable from code (human) | Mechanism verified (truths 7-11); the numeric per-group cost requires a live run per `11.1-VALIDATION.md` and `11.1-02-SUMMARY.md` coverage item D7. |

**Score:** 12/12 code-verifiable must-haves verified (3 additional roadmap items are inherently POST-MERGE production observations, routed to human verification per phase design — not counted as failed or as verified).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `pipeline/discovery.py::_build_discovery_skip_index` | Module-level, never-raises, returns `dict[int, dict]` | ✓ VERIFIED | Lines 189-303; single outer `try`/`except Exception -> {}`; grep count of `def _build_discovery_skip_index` = 1. |
| `pipeline/discovery.py` registry fast path | First statement of `_validate_single_sheet`, above its `try:` | ✓ VERIFIED | Lines 440-458; confirmed placement precedes `try:` at line 459. |
| `pipeline_memory/reader.py::get_sheet_watermarks` | select list + docstring include `name` | ✓ VERIFIED | Lines 83-139; `grep -c 'last_full_read_at,column_mapping,name'` = 1. |
| `tests/test_incremental_read.py::DiscoverySkipIndexTests` | 9 tests | ✓ VERIFIED | All 9 collected and passing. |
| `tests/test_incremental_read.py::DiscoveryRegistrySkipTests` | 10 tests (incl. split-line + fail-closed re-proof) | ✓ VERIFIED | All 10 collected and passing. |
| `pipeline/orchestrate.py::_is_row_attachment` | Module-level pure predicate | ✓ VERIFIED | Lines 1227-1243; grep count = 1. |
| `pipeline/orchestrate.py::_preseed_live_attachment_listings` | Module-level, never raises, returns seeded counts | ✓ VERIFIED | Lines 1246-1347; grep count = 1. |
| `pipeline/orchestrate.py` attachment-listing ceiling constant | Read from env, default 25000 | ✓ VERIFIED | `_BULK_ATTACHMENT_LISTING_MAX_TOTAL` at lines 1223-1225, `os.getenv('BULK_ATTACHMENT_LISTING_MAX_TOTAL', '25000')`. |
| `pipeline/orchestrate.py` pre-seed block in `main()` | Immediately after `_live_attachment_listings` declaration | ✓ VERIFIED | Lines 2919-2985; source-pin test `AttachmentPreseedWiringTests::test_preseed_call_site_between_memo_declaration_and_first_existence_check` PASS. |
| `tests/test_incremental_read.py::AttachmentParentTypeComparisonTests` | 4 tests | ✓ VERIFIED | All 4 collected and passing. |
| `tests/test_incremental_read.py::AttachmentPrepopulationTests` | 7 tests | ✓ VERIFIED | All 7 collected and passing. |
| `tests/test_incremental_read.py::AttachmentPreseedWiringTests` | 5 tests | ✓ VERIFIED | All 5 collected and passing. |
| `memory-bank/living-ledger.md` | Dated Phase 11.1 entries | ✓ VERIFIED | `[2026-09-01 13:50]` (4 durable rules) and `[2026-09-01 14:05]` (Gate-4 re-baseline hygiene) entries present, name run `33512477875`, `D-11.1-01`, `D-11.1-02`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `pipeline_memory.reader.get_sheet_watermarks` | `pipeline.discovery._build_discovery_skip_index` | stored `name`+`column_mapping`+`last_sheet_version` | ✓ WIRED | `pipeline/discovery.py:47` imports it at module top; called at `:218`. |
| `client.Sheets.list_sheets(include=['sheetVersion'], include_all=True)` | `_build_discovery_skip_index` | live version per sheet_id | ✓ WIRED | Called once at `pipeline/discovery.py:237-242`, bucketed at `:251-254`. |
| `_build_discovery_skip_index` | `_validate_single_sheet` fast path -> `discover_source_sheets` return contract | closure capture of `_discovery_skip_index` | ✓ WIRED | Bound at `pipeline/discovery.py:435`, read at `:449`. |
| `client.Attachments.list_all_attachments(sheet_id, include_all=True)` | `_preseed_live_attachment_listings` | bulk listing -> bucket by `parent_id` | ✓ WIRED | `pipeline/orchestrate.py:1300-1305` call, `:1319-1328` bucket. |
| `_live_attachment_listings` memo | `_live_row_attachments` memo-hit fast path -> `_has_existing_week_attachment` (both call sites) | `setdefault` seed then unmodified memo-hit read | ✓ WIRED | Pre-seed writes the SAME dict object passed by reference (`pipeline/orchestrate.py:2966-2970`) that `_live_row_attachments` already reads (`:1182-1211`, unmodified). |
| `IndexResult.total_count` probe | ceiling guard -> seed-nothing fallback | `page_size=1` pre-flight | ✓ WIRED | `pipeline/orchestrate.py:1270-1296`. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Registry-matched sheet skips `get_sheet` call (single named test) | `python -m pytest tests/test_incremental_read.py::DiscoveryRegistrySkipTests::test_registry_matched_sheet_skips_get_sheet_call -q` | 1 passed | ✓ PASS |
| Pinned INC-05 fail-closed guard still fires (single named test) | `python -m pytest tests/test_incremental_read.py::SkipGateLiveConfirmationWiringTests::test_discovery_fails_closed_on_validation_exception -q` | 1 passed | ✓ PASS |
| `_live_row_attachments` unmodified (3 pinned tests) | `python -m pytest tests/test_incremental_read.py::LiveRowAttachmentsTests -q` | 3 passed | ✓ PASS |
| Circuit-breaker guard-text pin unmodified | `python -m pytest tests/test_skip_no_target_row.py -q` | 27 passed, 5 subtests | ✓ PASS |
| Full workspace suite (run once) | `python -m pytest tests/ -q` | 1886 passed, 1 skipped, 306 subtests | ✓ PASS |
| 6-gate harness (run once) | `bash scripts/run_6_gates.sh` | `ALL 6 GATES PASSED` | ✓ PASS |
| Syntax check | `python -m py_compile generate_weekly_pdfs.py pipeline/discovery.py pipeline/orchestrate.py pipeline_memory/reader.py` | exit 0 | ✓ PASS |

### Probe Execution

Not applicable — this phase has no `scripts/*/tests/probe-*.sh` probes; verification uses the project's standard pytest + 6-gate harness (covered above).

### Requirements Coverage

**N/A per the phase's own contract.** `11.1-CONTEXT.md` and ROADMAP.md both state: "Requirements: none mapped (inserted urgent phase — the Success Criteria above plus D-11.1-01..04 in `11.1-CONTEXT.md` are the contract)." Confirmed: `grep -n "11\.1\|D-11\.1" .planning/REQUIREMENTS.md` returns zero matches — REQUIREMENTS.md correctly has no Phase 11.1 entries. Both plans' frontmatter `requirements:` fields (D-11.1-01/02/03, SC-2/3/4) are decision-derived IDs tracked in `11.1-CONTEXT.md` and the plan/SUMMARY frontmatter directly, not in REQUIREMENTS.md — this is documented as expected (not a defect) in both SUMMARY files ("`requirements.mark-complete` reported `not_found`... Not a defect").

### Anti-Patterns Found

None. Scanned `pipeline/discovery.py`, `pipeline/orchestrate.py`, `pipeline_memory/reader.py` for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` and stub-language patterns (`placeholder|coming soon|not yet implemented|not available`) — zero matches inside this phase's actual diff (one unrelated pre-existing string "never a null placeholder" at `orchestrate.py:5003` predates this phase and is outside its diff). No empty-implementation stubs, no hardcoded-empty-data patterns introduced by this phase's new functions.

## Human Verification Required

### 1. Wall clock < ~75 min, no budget stop, all groups processed (roadmap SC-1)

**Test:** Trigger (or wait for) the first scheduled `frequent` production run after this phase's PR merges to `master`.
**Expected:** Run completes ALL groups (no `TIME_BUDGET_MINUTES=165` graceful stop) with total wall clock under ~75 minutes.
**Why human:** Requires live Smartsheet API traffic and real production data volume; the phase's own `11.1-VALIDATION.md` designates this Manual-Only. No PR has merged yet as of this verification.

### 2. Discovery phase drops to low single-digit minutes (roadmap SC-2 magnitude)

**Test:** On that first post-merge run, read the `⏭️ Discovery validation split:` log line and the Phase 1 elapsed-time figure.
**Expected:** Discovery elapsed time falls from the ~67-min pre-phase regression to low single digits when the run finds no sheet-version drift; the skipped count is non-zero.
**Why human:** The skip mechanism and every doubt branch are proven correct by 19 passing tests (code-level), but the actual wall-clock improvement depends on live `sheet_registry` state being warm and the bulk `list_sheets` probe's real latency — both live-only observations.

### 3. Group-processing cost returns to ≲0.5 s/group (roadmap SC-3 magnitude)

**Test:** On that same run, read the per-sheet `📎 Attachment total_count probe:` / `📎 Pre-seeded live attachment listings...` log lines for TARGET_SHEET_ID and SUBCONTRACTOR_PPP_SHEET_ID, and compute observed group-processing seconds/group from the run's own timing logs.
**Expected:** Both probed `total_count` values stay well under the 25000 ceiling (no ERROR "bulk attachment listing skipped" line fires); group-processing cost returns to roughly the pre-INC-05-regression 0.28-0.5 s/group range.
**Why human:** The pre-seed mechanism, ceiling fallback, and never-raising contract are proven by 16 passing tests; the real attachment counts on production sheets and the resulting seconds/group are live measurements, explicitly flagged as a post-merge observation in `11.1-02-SUMMARY.md`'s own coverage item D7.

### 4. Changed/new sheet still gets full validation on a live run

**Test:** Confirm, via a live run's log output, that a sheet edited (or newly added) since its last `sheet_registry` write appears on the full-validation side of the split line.
**Expected:** The changed sheet is fully re-validated (its `client.Sheets.get_sheet` call fires), never skipped.
**Why human:** Unit-proven against a mocked SDK/mocked Supabase (`DiscoveryRegistrySkipTests::test_version_mismatch_performs_full_validation`, passing); confirming the same behavior against a genuine Smartsheet version bump is a live observation per `11.1-VALIDATION.md`.

## Gaps Summary

No code-level gaps found. All 12 code-verifiable must-haves (drawn from the ROADMAP Success Criteria contract plus both plans' `must_haves` frontmatter — truths, artifacts, key links, and prohibitions, including the task's named checks: `_live_row_attachments` byte-for-byte parity, `pipeline.fetch.get_last_sheet_versions()` NOT used by the discovery skip path, the Wave 2 guard never containing the `SKIP_UPLOAD` literal pinned by `tests/test_skip_no_target_row.py:308`, and D-11.1-05's 25000-row ceiling with ERROR-level fallback and option (b) deliberately not built) are verified present, substantive, and wired, with passing automated evidence. The three items left for human verification are not gaps — they are the phase's own explicitly-documented POST-MERGE production observations (SC-1's wall clock, SC-2's discovery-time magnitude, SC-3's per-group-cost magnitude, and the live changed-sheet confirmation), which `11.1-VALIDATION.md`'s Manual-Only Verifications table and this task's own instructions both designate as not verifiable from static code. No PR for `feat/11.1-runtime-remediation` has merged yet, so no first post-merge run exists to observe.

---

*Verified: 2026-09-01T19:23:01Z*
*Verifier: Claude (gsd-verifier)*
