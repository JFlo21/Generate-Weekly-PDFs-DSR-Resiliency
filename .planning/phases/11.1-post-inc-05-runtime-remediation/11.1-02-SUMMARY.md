---
phase: 11.1-post-inc-05-runtime-remediation
plan: 02
subsystem: pipeline-orchestrate
tags: [smartsheet, attachments, orchestrate, performance, pipeline_memory, sentry]

# Dependency graph
requires:
  - phase: 11.1-post-inc-05-runtime-remediation
    provides: "Plan 01's discovery registry-version skip fast path (D-11.1-01) and this phase's dated Living Ledger / Gate-4 hygiene conventions"
  - phase: 11-incremental-read-affected-group-regeneration
    provides: "_live_row_attachments memo + both _has_existing_week_attachment live-confirmation call sites (PR #373 review fix), pre-seeded here without modification"
provides:
  - "pipeline/orchestrate.py::_is_row_attachment(att) -> bool -- pure ROW-parent-attachment predicate, pinned against installed smartsheet-python-sdk 4.3.0's EnumeratedValue wrapper"
  - "pipeline/orchestrate.py::_preseed_live_attachment_listings(client, sheet_id, row_ids, memo) -> tuple[int, int] -- never-raising bulk pre-seed helper with a total_count pre-flight and a size ceiling"
  - "main() pre-seed block wired immediately after the _live_attachment_listings memo declaration, guarded on TEST_MODE/ATTACHMENT_REQUIRED_FOR_SKIP, covering the target sheet and the PPP sheet"
  - "BULK_ATTACHMENT_LISTING_MAX_TOTAL env override (private module constant, default 25000)"
affects: ["11.1-VALIDATION.md (SC-1/D-11.1-04 post-merge wall-clock observation)", "future attachment-listing performance work"]

# Actuals (#2632) -- chars/4 over the realized diff (3 files, 510 insertions)
actuals:
  tokens: 6500
  tasks: 3
  commits: 5

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Bulk sheet-level listing pre-seeds an existing per-item memo instead of rewiring the per-item call sites that already read it"
    - "SDK EnumeratedValue comparison form verified live against the installed package (types.py::EnumeratedValue.__eq__) before writing the comparison, not assumed"
    - "Two independent containment layers (never-raising helper + an outer try/except around the caller) so a defect in new performance-only code can never abort a billing run"

key-files:
  created: []
  modified:
    - pipeline/orchestrate.py
    - tests/test_incremental_read.py
    - memory-bank/living-ledger.md

key-decisions:
  - "_is_row_attachment accepts both the real AttachmentParentType.ROW enum member and its plain 'ROW' string spelling -- confirmed by direct inspection of the installed SDK's EnumeratedValue.__eq__ that both forms compare correctly, resolving RESEARCH Pitfall 4 with evidence instead of a guess."
  - "_preseed_live_attachment_listings wraps itself in its own nested sentry_sdk.start_span (op=\"smartsheet.bulk_attachment_listing\", name=f\"Pre-seed live attachment listings: sheet {sheet_id}\"), nested inside main()'s aggregate span of the same op and the plan's literal name (\"Pre-seed live attachment listings\") -- gives per-sheet Sentry granularity without duplicating the outer span's exact name; Task 1's action text only specified 'same telemetry shape', Task 2 owns the literal outer op/name."
  - "RED state for both tdd=\"true\" tasks was verified by reverting the already-drafted pipeline/orchestrate.py edit (git checkout, patch saved to scratch) before each test commit, then reapplying it for GREEN -- guarantees a genuinely failing test run at RED even though production code and tests were authored in the same editing pass."

requirements-completed: [D-11.1-02, D-11.1-03, SC-3, SC-4]

coverage:
  - id: D1
    description: "A skip-candidate row on a successfully-listed sheet reaches _has_existing_week_attachment with a pre-seeded listing and zero client.Attachments.list_row_attachments calls for that row"
    requirement: "D-11.1-02"
    verification:
      - kind: unit
        ref: "tests/test_incremental_read.py::AttachmentPrepopulationTests::test_seeded_row_never_triggers_per_row_call"
        status: pass
      - kind: unit
        ref: "tests/test_incremental_read.py::AttachmentPrepopulationTests::test_buckets_listing_by_parent_id_row_only"
        status: pass
    human_judgment: false
  - id: D2
    description: "A row absent from a fully-successful bulk listing is seeded with an EMPTY list (resolves to regenerate, the safe direction) and still triggers zero per-row calls"
    requirement: "D-11.1-02"
    verification:
      - kind: unit
        ref: "tests/test_incremental_read.py::AttachmentPrepopulationTests::test_row_without_attachment_seeded_empty_no_per_row_call"
        status: pass
    human_judgment: false
  - id: D3
    description: "Any probe failure, listing failure, or over-ceiling total_count seeds nothing for that sheet, leaving the shipped lazy per-row path intact"
    requirement: "D-11.1-02"
    verification:
      - kind: unit
        ref: "tests/test_incremental_read.py::AttachmentPrepopulationTests::test_probe_exception_seeds_nothing"
        status: pass
      - kind: unit
        ref: "tests/test_incremental_read.py::AttachmentPrepopulationTests::test_listing_exception_seeds_nothing"
        status: pass
      - kind: unit
        ref: "tests/test_incremental_read.py::AttachmentPrepopulationTests::test_over_ceiling_seeds_nothing_and_skips_bulk_call"
        status: pass
    human_judgment: false
  - id: D4
    description: "_is_row_attachment accepts only ROW-parent attachments and rejects SHEET/COMMENT, pinned against the real installed SDK 4.3.0 enum"
    requirement: "D-11.1-02"
    verification:
      - kind: unit
        ref: "tests/test_incremental_read.py::AttachmentParentTypeComparisonTests (4 tests: ROW/SHEET/COMMENT/missing-attribute)"
        status: pass
    human_judgment: false
  - id: D5
    description: "_live_row_attachments's body and both _has_existing_week_attachment call sites are byte-for-byte unmodified from pre-phase text; all three pre-existing pinned tests pass unmodified"
    requirement: "D-11.1-02"
    verification:
      - kind: unit
        ref: "tests/test_incremental_read.py::LiveRowAttachmentsTests (3 tests, unmodified)"
        status: pass
      - kind: unit
        ref: "tests/test_incremental_read.py::SkipGateLiveConfirmationWiringTests::test_existence_checks_use_live_listing_never_stubs (unmodified)"
        status: pass
      - kind: unit
        ref: "tests/test_subproject_e_hash_store.py::test_skip_gate_requires_ppp_attachment_for_reduced_sub (unmodified)"
        status: pass
      - kind: other
        ref: "git diff fbe7be8..HEAD -- pipeline/orchestrate.py tests/test_incremental_read.py contains zero '-' (removed) lines -- purely additive"
        status: pass
    human_judgment: false
  - id: D6
    description: "main()'s pre-seed block is wired once per involved sheet ahead of the group loop, guarded on TEST_MODE/ATTACHMENT_REQUIRED_FOR_SKIP, skips an empty/falsy leg, and cannot abort a run on failure"
    requirement: "D-11.1-02"
    verification:
      - kind: unit
        ref: "tests/test_incremental_read.py::AttachmentPreseedWiringTests (5 source-pin tests)"
        status: pass
    human_judgment: false
  - id: D7
    description: "Every real (non-TEST_MODE) run logs the probed total_count and the seeded-with/seeded-empty counts per involved sheet"
    requirement: "SC-3"
    verification: []
    human_judgment: true
    rationale: "The log lines and span data attributes are implemented and code-reviewed against the plan's exact spec (per-sheet total_count INFO, ERROR on ceiling, per-sheet seeded-count INFO after the loop), but no test captures real production log output from a live Smartsheet call -- confirming the actual wording/values on a real run is a post-merge production observation, the same posture SC-1/D-11.1-04 already carries in 11.1-VALIDATION.md."
  - id: D8
    description: "No local JSON cache, Actions cache step, or retired two-phase attachment pre-fetch was re-introduced; no new *_MAX_MINUTES/*_TIMEOUT_SEC sub-budget constant was added"
    requirement: "D-11.1-03"
    verification:
      - kind: other
        ref: "grep -v '^[[:space:]]*#' pipeline/orchestrate.py | grep -c 'ATTACHMENT_PREFETCH' == 0"
        status: pass
    human_judgment: true
    rationale: "Architectural absence is a code-review fact, not a positive runnable assertion -- verified by grep + diff inspection (no new file, no new env var beyond BULK_ATTACHMENT_LISTING_MAX_TOTAL which is explicitly not a time budget, no cache step touched)."
  - id: D9
    description: "Full pytest suite and all 6 gates green at plan close; Living Ledger records the phase's durable rules"
    requirement: "SC-4"
    verification:
      - kind: other
        ref: "python -m pytest tests/ -q -> 1886 passed, 1 skipped, 306 subtests"
        status: pass
      - kind: other
        ref: "bash scripts/run_6_gates.sh -> ALL 6 GATES PASSED (Gate 4 mypy delta neutral, 72 -> 72)"
        status: pass
      - kind: other
        ref: "memory-bank/living-ledger.md [2026-09-01 13:50] entry, tail -40 confirmed to include the header and all required citations"
        status: pass
    human_judgment: false

# Metrics
duration: ~10min (first-to-last commit); ~25min including upfront reading/SDK verification
completed: 2026-09-01
status: complete
---

# Phase 11.1 Plan 02: Bulk Attachment Pre-seed (Fix 2) Summary

**Group-processing skip-gate confirmation now costs 2 bulk `Attachments.list_all_attachments` calls per run instead of one serial `list_row_attachments` call per skip-candidate row, with `_live_row_attachments` and both of its call sites left byte-for-byte unmodified.**

## Performance

- **Duration:** ~10 min commit-to-commit (13:44:19-13:53:51 CDT); ~25 min including upfront plan/context/research reading and live SDK verification
- **Started:** 2026-09-01T13:44:19-05:00 (first commit)
- **Completed:** 2026-09-01T13:53:51-05:00 (last commit)
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- `pipeline/orchestrate.py::_is_row_attachment(att)` -- pure predicate over `Attachment.parent_type`'s `EnumeratedValue` wrapper, verified against the real, installed smartsheet-python-sdk 4.3.0 (`types.py::EnumeratedValue.__eq__`): both `== AttachmentParentType.ROW` and `== 'ROW'` compare correctly; fails safe (`False`) on anything else, including a missing attribute.
- `pipeline/orchestrate.py::_preseed_live_attachment_listings(client, sheet_id, row_ids, memo)` -- `page_size=1` `total_count` pre-flight (logged INFO every run) -> `BULK_ATTACHMENT_LISTING_MAX_TOTAL` ceiling guard (default 25000, ERROR-logged + seeds nothing if exceeded) -> `include_all=True` bulk listing -> bucket ROW-parent entries by `parent_id` -> `setdefault`-seed every id in `row_ids` (empty list when absent). Never raises; every SDK call routes through `smartsheet_call_with_retry`.
- `main()` pre-seed block inserted immediately after the `_live_attachment_listings` memo declaration and before the pre-existing `group_state` attachment-identity block: guarded on `not TEST_MODE and ATTACHMENT_REQUIRED_FOR_SKIP` (textually distinct from the circuit-breaker guard pinned by `tests/test_skip_no_target_row.py:308`, which additionally requires `and not SKIP_UPLOAD`), iterates the target sheet (`target_map`) and PPP sheet (`target_map_ppp`) once each, skips a leg whose sheet id or row-id set is falsy/empty, and is wrapped in its own outer `try`/`except Exception` so a defect can never abort a billing run.
- `_live_row_attachments` and both `_has_existing_week_attachment` call sites are confirmed byte-for-byte unmodified: `git diff fbe7be8..HEAD` across both touched files contains zero removed lines, and all three pre-existing pinned tests (`LiveRowAttachmentsTests`, `SkipGateLiveConfirmationWiringTests::test_existence_checks_use_live_listing_never_stubs`, `test_skip_gate_requires_ppp_attachment_for_reduced_sub`) pass unmodified.
- 16 new tests added across 3 classes (`AttachmentParentTypeComparisonTests`, `AttachmentPrepopulationTests`, `AttachmentPreseedWiringTests`); full suite grew 1870 -> 1886 passed.
- Full suite green (1886 passed / 1 skipped / 306 subtests) and `bash scripts/run_6_gates.sh` -> `ALL 6 GATES PASSED` (Gate 4 mypy delta neutral, 72 -> 72, no re-baseline needed).
- Dated Living Ledger entry recording the phase's four durable rules (cache-retirement-does-not-retire-identity, per-item live confirmation belongs outside a serial loop, the pinned `EnumeratedValue` comparison form, empty-seed direction + size-ceiling fallback), naming run `33512477875`, `D-11.1-01`, and `D-11.1-02`.

## Task Commits

Each task was committed atomically (Tasks 1 and 2 followed RED/GREEN per their `tdd="true"` attribute):

1. **Task 1 (RED): failing tests for bulk attachment pre-seed** - `206449c` (test)
2. **Task 1 (GREEN): pre-seed live attachment memo from bulk sheet listings** - `77979d5` (feat)
3. **Task 2 (RED): failing wiring tests for pre-seed insertion in main()** - `ed7e8e6` (test)
4. **Task 2 (GREEN): wire the bulk attachment pre-seed into main()** - `4a5996e` (feat)
5. **Task 3: phase gate green, Living Ledger entry** - `554cd74` (docs)

**Plan metadata:** (this commit, immediately following)

## Files Created/Modified
- `pipeline/orchestrate.py` -- new `AttachmentParentType` import, `_BULK_ATTACHMENT_LISTING_MAX_TOTAL` constant, `_is_row_attachment`, `_preseed_live_attachment_listings`; new pre-seed block in `main()`
- `tests/test_incremental_read.py` -- `AttachmentParentTypeComparisonTests` (4 tests), `AttachmentPrepopulationTests` (7 tests), `AttachmentPreseedWiringTests` (5 tests)
- `memory-bank/living-ledger.md` -- dated `[2026-09-01 13:50]` Phase 11.1 entry (Fix 2's four durable rules)

## Decisions Made
- `_is_row_attachment` accepts both the real `AttachmentParentType.ROW` enum member and its plain `'ROW'` string spelling -- confirmed correct via direct inspection of the installed SDK's `EnumeratedValue.__eq__` rather than assumed, resolving RESEARCH Pitfall 4 with evidence.
- `_preseed_live_attachment_listings` carries its own nested `sentry_sdk.start_span` (same `op`, a per-sheet `name`) inside `main()`'s aggregate span of the same `op` and the plan's exact literal `name`. Task 1's action text only specified "same telemetry shape" for the helper; Task 2 owns the plan's literal outer op/name. This gives per-sheet Sentry granularity without duplicating the outer span's name.
- RED state for both `tdd="true"` tasks was verified by reverting the already-drafted `pipeline/orchestrate.py` edit (`git checkout`, patch saved to scratch) before each test commit, confirming a genuine `AttributeError`/`ValueError` failure, then reapplying the patch for GREEN -- since production code and tests were authored in the same editing pass, this guarantees the RED commit reflects an actually-failing state rather than an unverified claim.

## Deviations from Plan

None - plan executed exactly as written. `_is_row_attachment` and `_preseed_live_attachment_listings` match Task 1's Step B/C/D specification precisely (including the exact `BULK_ATTACHMENT_LISTING_MAX_TOTAL` override key and the private-constant placement); the `main()` wiring matches Task 2's guard order, per-leg iteration, skip conditions, and outer `try`/`except` exactly.

## Issues Encountered
- **`AttachmentPreseedWiringTests._preseed_block()` window too wide (self-caught during Task 2 GREEN).** The first draft bounded the block from the new guard to the first `_has_existing_week_attachment(` occurrence, which also swept in the unrelated circuit-breaker guard at line ~3149 (containing the pinned literal's `SKIP_UPLOAD` term) and made `test_gated_on_test_mode_first_then_attachment_required`'s `assertNotIn("SKIP_UPLOAD", block)` fail. Narrowed the end anchor to `_group_state_wrs = set(target_map or {})` (the very next pre-existing line after the new block) before committing GREEN -- fixed and re-verified in the same task, no separate commit needed since it was caught before the GREEN commit landed.
- **Living Ledger entry length vs. `tail -40` acceptance criterion.** The first draft (4 rules, ~74 inserted lines) pushed the entry's header line outside a `tail -40` window. Condensed to ~34 lines while keeping all four durable rules and the three required citations (`33512477875`, `D-11.1-01`, `D-11.1-02`); confirmed via `tail -40 | grep` before the Task 3 commit.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 11.1 is now feature-complete: both fixes (11.1-01 discovery skip, 11.1-02 this plan's bulk attachment pre-seed) are committed, gate-verified, and documented on `feat/11.1-runtime-remediation`.
- SC-1 / D-11.1-04 (frequent-run wall clock back under ~75 min) and this plan's SC-3 log-content confirmation remain phase-level, POST-MERGE production observations -- owned by `11.1-VALIDATION.md`'s Manual-Only Verifications table, requiring the first scheduled `production_frequent` run after this phase's PR merges.
- No blockers. Ready for phase close / PR review.

## Self-Check: PASSED

- Both modified production/test files and the ledger file confirmed present on disk (`pipeline/orchestrate.py`, `tests/test_incremental_read.py`, `memory-bank/living-ledger.md`).
- All 5 task commits confirmed in `git log` (`206449c`, `77979d5`, `ed7e8e6`, `4a5996e`, `554cd74`).
- `def _is_row_attachment` count = 1; `def _preseed_live_attachment_listings` count = 1; `_has_existing_week_attachment(` count = 2 (unchanged); `ATTACHMENT_PREFETCH` (code, comments stripped) count = 0.
- `git diff fbe7be8..HEAD` across both touched code files contains zero removed lines -- purely additive.
- All plan-level `<verification>` commands re-run clean: `python -m pytest tests/test_incremental_read.py -q` (193 passed), `python -m pytest tests/test_skip_no_target_row.py tests/test_subproject_e_hash_store.py -q` (276 passed, 21 subtests), `python -m pytest tests/ -q` (1886 passed / 1 skipped / 306 subtests), `bash scripts/run_6_gates.sh` (`ALL 6 GATES PASSED`, exit 0), `python -m py_compile generate_weekly_pdfs.py` (exit 0). `LiveRowAttachmentsTests`, `SkipGateLiveConfirmationWiringTests`, and `test_skip_gate_requires_ppp_attachment_for_reduced_sub` all pass with bodies unmodified.
- All plan `<success_criteria>` met (see Accomplishments); SC-3's log-content confirmation is documented as a post-merge observation under coverage D7, not silently skipped.

---
*Phase: 11.1-post-inc-05-runtime-remediation*
*Completed: 2026-09-01*
