---
phase: "11.1"
slug: "post-inc-05-runtime-remediation"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-01"
---

# Phase 11.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (existing suite, 1851 tests) |
| **Config file** | pyproject.toml / pytest defaults |
| **Quick run command** | `pytest tests/test_incremental_read.py -q` |
| **Full suite command** | `pytest tests/ -q` then `bash scripts/run_6_gates.sh` |
| **Estimated runtime** | ~35s quick · ~90s full · ~5 min gates |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/test_incremental_read.py -q`
- **After every plan wave:** Run `pytest tests/ -q`
- **Before `/gsd:verify-work`:** Full suite + `bash scripts/run_6_gates.sh` must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

*(Filled by the planner — every task gets a row; see PLAN.md `<verify>` blocks.)*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 11.1-01 T1 | 11.1-01 | 1 | D-11.1-01 | T-11.1-01 | Registry-version skip requires exact live-version equality plus a stored mapping containing `Weekly Reference Logged Date`; skip path performs no I/O and returns the frozen 3-key contract | unit + end-to-end (mocked SDK + mocked Supabase) | `python -m pytest tests/test_incremental_read.py -q` | ✅ (new classes added by this task) | ⬜ pending |
| 11.1-01 T2 | 11.1-01 | 1 | D-11.1-01 | T-11.1-02 | Nine doubt branches each fall back to full validation; probe/watermark failure never appends to `_failed_validation_sids`; INC-05 `RuntimeError` guard still fires | unit | `python -m pytest tests/test_incremental_read.py::SheetWatermarksReadTests tests/test_incremental_read.py::SkipGateLiveConfirmationWiringTests -q` | ✅ | ⬜ pending |
| 11.1-01 T3 | 11.1-01 | 1 | SC-2 | T-11.1-03 | Every run emits a candidates/skipped/fully-validated split line so a wrong skip is attributable | unit (assertLogs) + phase gate | `bash scripts/run_6_gates.sh` | ✅ | ⬜ pending |
| 11.1-02 T1 | 11.1-02 | 2 | D-11.1-02 | T-11.1-08 | `_is_row_attachment` accepts only ROW-parent entries against the installed SDK 4.3.0 enum; pre-seeded row triggers zero per-row calls; empty seed resolves to regenerate | unit (SDK enum pin + mocked client) | `python -m pytest tests/test_incremental_read.py -q` | ✅ (new classes added by this task) | ⬜ pending |
| 11.1-02 T2 | 11.1-02 | 2 | D-11.1-02 | T-11.1-06, T-11.1-10 | Pre-seed runs once per involved sheet before the loop, is skipped in TEST_MODE, and cannot abort the run; the three pre-existing pins stay unmodified | unit + regex source-pin | `python -m pytest tests/test_incremental_read.py tests/test_skip_no_target_row.py tests/test_subproject_e_hash_store.py -q` | ✅ | ⬜ pending |
| 11.1-02 T3 | 11.1-02 | 2 | SC-3, SC-4 | T-11.1-09, T-11.1-11 | `total_count` probe logged every run; ceiling guard degrades to today's lazy path; no retired pre-fetch constant reintroduced | phase gate | `python -m pytest tests/ -q` then `bash scripts/run_6_gates.sh` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] Live probe: attachment `total_count` on TARGET_SHEET_ID and
  SUBCONTRACTOR_PPP_SHEET_ID (RESEARCH.md Open Question 1 — sizes the
  fix-2 bulk listing; read-only Smartsheet call, no writes).
  **Satisfied in code, not as a one-off manual gate:** plan 11.1-02 Task 1
  Step D.2 makes a `page_size=1` `total_count` pre-flight part of
  `_preseed_live_attachment_listings` itself, logged at INFO on every real
  run, with a size ceiling (Step D.3) as the coded fallback. This is
  strictly stronger than a single manual measurement — it re-sizes the
  response every run instead of trusting a number taken once. The live
  value is observed on the first post-merge run (see Manual-Only below).
- [ ] Unit test pinning the `AttachmentParentType.ROW` / `parent_type`
  comparison form against the installed SDK 4.3.0 (RESEARCH.md Open
  Question 3 — never assume the enum comparison shape).
  **Owner:** plan 11.1-02 Task 1, `AttachmentParentTypeComparisonTests`
  (Test 1) — asserts ROW accepted and both SHEET and COMMENT rejected,
  imported from the installed `smartsheet` package, so a future SDK bump
  fails the test rather than silently mis-bucketing.
- [ ] RESEARCH.md Open Question 2 guard: if the bulk
  `list_sheets(include=['sheetVersion'])` probe returns no usable version,
  the run must degrade to full validation and say so.
  **Owner:** plan 11.1-01 Task 1 Step C.4 + Task 2 Test 8.

*Existing pytest infrastructure covers everything else.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Wall clock < ~75 min, no budget stop, all groups processed | Phase goal SC-1 | Needs a real production run | First post-merge frequent run: compare Phase 1 duration, s/group, and total vs run 33512477875 (169.8 min) and pre-merge baseline 33449808275 (76.8 min) |
| Discovery skip vs full validation on changed sheet | Phase goal SC-2 | Needs live sheet-version drift | Inspect run logs: read the `⚡ Discovery validation split:` line for the candidates/skipped/fully-validated counts; any changed or new sheet must appear on the full-validation side |
| Attachment `total_count` on both target sheets | RESEARCH Open Question 1 | Needs a live Smartsheet token | First post-merge run: read the per-sheet `total_count` INFO line emitted by the pre-seed pre-flight. If either sheet is at or above the 25000 ceiling, the ERROR line fires and the bounded per-skip-candidate prefetch (RESEARCH Fix 2 option b) becomes a follow-up |
| Live `.version` populated by `list_sheets(include=['sheetVersion'])` | RESEARCH Open Question 2 / Assumption A2 | Needs a live Smartsheet token | First post-merge run: confirm the skip-index INFO line shows real sample sheet-id/version pairs and a non-zero skipped count. A zero skipped count plus the no-usable-versions WARNING means the documented behavior did not hold live — safe (full validation) but the fix is inert |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
