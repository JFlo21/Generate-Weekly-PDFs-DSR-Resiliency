# Phase 11.1 Context — Post-INC-05 Runtime Remediation (INSERTED, urgent)

Inserted after Phase 11 on 2026-09-01. Owner (Juan) approved both fixes explicitly:
"yes implement both fixes 1 & 2 making sure to follow our gsd guidelines."

## Why this phase exists (measured evidence, first post-merge run)

PR #373 (`abf905d`, INC-05 retirement) merged 2026-09-01 02:54Z. First post-merge
frequent run **33512477875** (Tue 13:17Z) took **169.8 min** vs the 50–77 min norm,
hit `TIME_BUDGET_MINUTES=165` and stopped early with **1216 of 3171 groups
unprocessed**; the next scheduled run (33525432056) queued behind it and started
only when it finished (16:07Z). Cron interval is 2h — runs now outlast it, so the
schedule degrades and deferred groups can starve (billing-freshness risk).

Phase comparison vs last pre-merge run 33449808275 (76.8 min):

| Phase | Pre-merge | Post-merge 33512477875 |
|---|---|---|
| Phase 1 discovery/validation | 1.3s (discovery_cache.json Actions-cache hit) | **4008.6s (66.8 min)** — all 121 sheets fully re-validated, parallel w/8 |
| Phase 2 fetch | 1258s / 213,856 rows | 1218s / 213,987 rows (unchanged) |
| Attachment pre-fetch | 600s (bulk, parallel, 758/762 rows) | retired |
| Group processing | 872.8s ≈ 0.28 s/group (3167 groups; 11 gen / 3002 skip) | **4425.8s ≈ 2.26 s/group** (1955 of 3171; 0 gen / 1814 skip / 141 no-row; budget stop) |

Not causes (ruled out from logs): rate limiting (1 benign RPC retry total), group
count (3171 vs 3167), row volume (same), concurrent-run contention (runs serialize).

## Decisions (owner-approved direction)

- **D-11.1-01 (Fix 1 — discovery skip, ~67-min win):** Skip FULL validation for a
  candidate sheet when its live Smartsheet `version` is unchanged vs
  `pipeline_memory.sheet_registry.last_sheet_version` AND a stored, non-empty
  `column_mapping` exists for it — reuse the stored mapping. Any other case
  (new sheet, version changed, registry row missing, registry/Supabase
  unreachable, version check itself fails) → FULL validation exactly as today.
  **Fail-closed is non-negotiable**: degraded mode is "validate everything"
  (today's behavior), never "skip validation". The INC-05 fail-closed guard from
  PR #373 (`_failed_validation_sids` → RuntimeError) must be preserved.
- **D-11.1-02 (Fix 2 — group-loop live confirmation, ~60-min win):** Keep the live
  existence-confirmation semantics of the PR #373 review fix (stub ≠ proof of
  existence) but remove the serial per-row `list_row_attachments` calls from the
  group loop. Replace with EITHER (planner picks with evidence): (a) one paginated
  sheet-level attachment listing per target sheet (`GET /sheets/{id}/attachments`,
  `include_all=True`), bucketed by `parentId` into the same memo
  `_live_row_attachments` reads, OR (b) a bounded parallel pre-fetch (≤8 workers,
  Smartsheet 300 req/min) of row listings for skip-candidate rows only, before the
  group loop. Transport failure → the affected rows resolve to "no live listing"
  → skip gate falls back to regeneration (same fail-open-to-regenerate posture as
  the current `_live_row_attachments` None-return).
- **D-11.1-03:** No re-introduction of local JSON caches, Actions cache steps, or
  the retired bulk attachment pre-fetch phases. Both fixes live inside the INC-05
  architecture (`sheet_registry` + `group_state` + live confirmation).
- **D-11.1-04:** Target: frequent-run wall clock back under ~75 min. Verified via
  the first post-merge run of THIS phase's PR (same measurement method as above).

## Repository facts the planner should build on (verified 2026-09-01)

- `pipeline_memory/writer.py::upsert_sheet_registry` already persists
  `last_sheet_version` (refreshed unconditionally each run from
  `pipeline.fetch.get_last_sheet_versions()`) and `column_mapping` (selective
  payload inclusion via `column_mapping_sheets`, Phase 11 Plan 06 / D-03).
  Fix 1 therefore likely needs NO schema change — confirm the reader side
  (`pipeline_memory/reader.py`) exposes registry rows, and confirm what a cheap
  live version probe costs (smartsheet SDK `Sheets.get_sheet_version(sheet_id)`
  or equivalent — verify against current SDK 4.0.0 docs, Context7).
- Skip-gate live confirmation lives in `pipeline/orchestrate.py`:
  `_live_row_attachments(client, sheet_id, row_id, memo)` + module-level
  `_live_attachment_listings` memo, two call sites (target sheet + PPP), pinned by
  `LiveRowAttachmentsTests` and `SkipGateLiveConfirmationWiringTests` in
  `tests/test_incremental_read.py` (source-pin tests WILL need updating in the
  same plan that rewires the call sites).
- Discovery validation: `pipeline/discovery.py::_validate_single_sheet` under a
  ThreadPoolExecutor (`PARALLEL_WORKERS_DISCOVERY: 8`), INC-05 fail-closed guard
  appends to `_failed_validation_sids` and raises after the loop.
- Guardrails: change-detection key untouched; `PARALLEL_WORKERS ≤ 8`; no `@cell`;
  Gate-4 mypy baseline discipline (LF-only golden files — refreeze on Windows
  writes CRLF, strip `\r`); full suite + `scripts/run_6_gates.sh` before push;
  own PR off master (never push to master).

## Open questions for research (do NOT guess)

1. Does the smartsheet-python-sdk 4.0.0 expose a cheap per-sheet version probe
   (`Sheets.get_sheet_version`)? What does it cost per call, and is a bulk
   variant available (e.g. `Sheets.list_sheets` `modifiedAt`) that covers all
   121 candidates in ≤2 API calls?
2. Does `GET /sheets/{sheetId}/attachments` (SDK `Attachments.list_all_attachments`)
   return ROW-level attachments with `parentType`/`parentId`, and does it paginate
   past the default page size with `include_all=True`? (Determines D-11.1-02
   option a vs b.)
3. Where exactly does discovery decide "validate this sheet" — is there a single
   choke point where the registry-version skip can be inserted without touching
   the Phase-09-frozen `{'id','name','column_mapping'}` return contract?
