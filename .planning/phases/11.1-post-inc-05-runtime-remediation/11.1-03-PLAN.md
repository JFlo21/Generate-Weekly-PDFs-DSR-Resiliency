---
phase: 11.1-post-inc-05-runtime-remediation
plan: 03
gap_closure: true
gap_ids: [G-11.1-4, G-11.1-6]
type: gap-closure
subsystem: pipeline-orchestrate
tags: [billing_audit, freeze_attribution, performance, pipeline_memory]
status: fix-built-awaiting-merge
fix_ref: "PR #378 perf/freeze-row-cache-warm-start (ba41451)"
---

# 11.1-03 — Gap closure: warm-start the frozen-row cache (SC-1 / SC-3)

## Gaps addressed

| Gap | Test | Truth | Root cause (UAT diagnosis) |
|---|---|---|---|
| G-11.1-4 | 4 | First frequent run on the 11.1 code completes all groups, no budget stop, wall clock < ~75 min | INC-05 D-12 retired the frozen-row cache warm start; every completed row (~214k) re-sent to `freeze_attribution` each run (84.6 min) and the per-group fingerprint pair fired for every group (13 min) |
| G-11.1-6 | 6 | Group-processing cost ≲ 0.5 s/group | Same cause: ~1.7 s median per skipped group is one freeze RPC per row plus a `pipeline_run` select+upsert |

Diagnosis evidence: `memory-bank/living-ledger.md` `[2026-09-01 22:40]` (request counts by endpoint on run
33570018457 vs the pre-INC-05 run 33459309123).

## Fix (already built, owner-approved 2026-09-02 00:35, independent risk review SAFE)

PR #378 `perf/freeze-row-cache-warm-start`: `pipeline/grouping.py` publishes the
`lookup_attribution_bulk` key set it already fetches (`get_prefetched_frozen_row_keys`, success
only); `pipeline/attribution.py` `warm_billing_audit_row_cache` converts it to the loop's
`wr|MMDDYY|row_id` keys; `pipeline/orchestrate.py` seeds `billing_audit_row_cache` before the group
loop. Zero extra requests; change detection, grouping, hashing, filenames, attribution values and
Supabase writes unchanged. Tests: `tests/test_freeze_row_cache_warm_start.py` (12 / 8 subtests).
ALL 6 GATES PASSED; website build clean.

## Tasks

1. **Merge PR #378** (owner). No deploy step; the next scheduled run picks it up.
2. **Canary on the first scheduled run after merge** (13:00Z Sep 2 or later): the log shows
   `🧊 Frozen-row cache warm-started from the attribution prefetch: N row(s) already frozen` with a
   non-zero N; `run_summary.json` shows `snapshots_already_frozen` near 0 and `snapshots_written > 0`
   when new rows completed; `freeze_attribution` request count is back near the pre-INC-05 ~18k.
3. **Re-measure SC-1 / SC-3 on that run:** all groups processed, no budget stop, Python `Duration`
   under ~75 min; group phase seconds ÷ groups ≲ 0.5 s. Record the numbers in this plan's SUMMARY.
4. **Write `11.1-03-SUMMARY.md`** with the canary numbers so `/gsd-verify-work 11.1` reconciles
   G-11.1-4 and G-11.1-6 as resolved and the phase can seal.

## Must-haves

- No re-introduction of any local cache file or Actions cache step (D-11.1-03 still holds).
- The warm start is a no-op (empty seed) on prefetch failure; the run must still complete.

## Notes

- A related but separate defect found during UAT — INC-06, the interpreter-exit hang on abandoned
  shadow-parity workers (ledger `[2026-09-02 01:35]`) — is NOT part of this gap; it gets its own PR.
  It can make a run overrun the 180-min ceiling even after this fix lands, so the SC-1 canary must
  read the Python `Duration`, not just the job time.
