---
phase: 11.1-post-inc-05-runtime-remediation
plan: 01
subsystem: pipeline-discovery
tags: [smartsheet, discovery, sheet_registry, mypy, performance, pipeline_memory]

# Dependency graph
requires:
  - phase: 11-incremental-read-affected-group-regeneration
    provides: "pipeline_memory.sheet_registry (last_sheet_version, column_mapping, name) written every run by upsert_sheet_registry; get_sheet_watermarks reader"
provides:
  - "pipeline/discovery.py::_build_discovery_skip_index — never-raising, registry-version skip index builder"
  - "Registry fast path at the top of _validate_single_sheet, returning the identical {'id','name','column_mapping'} contract with zero client.Sheets.get_sheet calls on a version match"
  - "pipeline_memory/reader.py::get_sheet_watermarks now selects and returns 'name'"
  - "Operator-visible 'Discovery validation split' log line (candidates/skipped/fully-validated, D-11.1-01)"
affects: [11.1-02-attachment-confirmation-batching, future-discovery-perf-work]

# Actuals (#2632)
actuals:
  tokens: 9500
  tasks: 3
  commits: 4

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Registry-version skip fast path: pure, side-effect-free, placed above the existing try: so a miss can never touch the fail-closed exception collector"
    - "One-time bulk client.Sheets.list_sheets(include=['sheetVersion'], include_all=True) probe replaces N per-sheet get_sheet_version calls"
    - "Doubt -> full validation via dict-miss semantics (no special-case branching needed for most doubt inputs)"

key-files:
  created: []
  modified:
    - pipeline_memory/reader.py
    - pipeline/discovery.py
    - tests/test_incremental_read.py
    - tests/golden/mypy_baseline.txt
    - tests/golden/mypy_baseline_count.txt
    - memory-bank/living-ledger.md

key-decisions:
  - "Gate 4 (mypy) re-baselined 70 -> 72 lines, zero accepted findings — the delta is entirely [annotation-unchecked] NOTE line-number drift in pipeline/discovery.py from the new untyped _build_discovery_skip_index function; the real error set (28 errors in 7 files) is byte-identical before/after. Per the executor's hard_constraints, refroze the golden baseline rather than adding type annotations (which would diverge from this file's existing untyped-closure convention and risk unrelated new findings)."
  - "Skip-index admission requires exact last_sheet_version equality (not >=), a non-empty stored column_mapping, 'Weekly Reference Logged Date' present in that mapping, and a non-empty stored name — strict-mode parity with the full-validation path's own gate (T-11.1-01 mitigation)"
  - "The one-time bulk list_sheets probe and the watermark read each have their OWN try/except, deliberately separate from _validate_single_sheet's existing except block, so a probe/read failure can never touch _failed_validation_sids"

requirements-completed: [D-11.1-01, D-11.1-03, SC-2, SC-4]

coverage:
  - id: D1
    description: "Registry-matched candidate sheet is discovered end-to-end with zero client.Sheets.get_sheet calls, returning the identical {'id','name','column_mapping'} contract"
    requirement: "D-11.1-01"
    verification:
      - kind: unit
        ref: "tests/test_incremental_read.py::DiscoveryRegistrySkipTests::test_registry_matched_sheet_skips_get_sheet_call"
        status: pass
      - kind: unit
        ref: "tests/test_incremental_read.py::DiscoverySkipIndexTests::test_happy_path_returns_index_entry_on_version_match"
        status: pass
    human_judgment: false
  - id: D2
    description: "All nine doubt-branch inputs (no registry row, version mismatch, empty/incomplete mapping, live version None, bulk-probe exception, watermarks unavailable, all-versions-None) fall back to full validation, never skip"
    requirement: "D-11.1-01"
    verification:
      - kind: unit
        ref: "tests/test_incremental_read.py::DiscoverySkipIndexTests (8 doubt-branch tests)"
        status: pass
      - kind: unit
        ref: "tests/test_incremental_read.py::DiscoveryRegistrySkipTests (8 end-to-end doubt-branch tests)"
        status: pass
    human_judgment: false
  - id: D3
    description: "INC-05 fail-closed guard (_failed_validation_sids -> RuntimeError) is unchanged: exactly one append site, and a full-validation exception still aborts the run naming the sid"
    requirement: "D-11.1-01"
    verification:
      - kind: unit
        ref: "tests/test_incremental_read.py::DiscoveryRegistrySkipTests::test_validation_exception_raises_runtime_error_naming_sid"
        status: pass
      - kind: unit
        ref: "tests/test_incremental_read.py::SkipGateLiveConfirmationWiringTests::test_discovery_fails_closed_on_validation_exception (unmodified pin)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Every discovery run logs a candidates/skipped/fully-validated split line citing D-11.1-01"
    requirement: "SC-2"
    verification:
      - kind: unit
        ref: "tests/test_incremental_read.py::DiscoveryRegistrySkipTests::test_validation_split_log_line_reports_skipped_and_full_counts"
        status: pass
    human_judgment: false
  - id: D5
    description: "get_sheet_watermarks exposes 'name' while keeping its fail-open contract (empty dict on any client/transport/response failure) unchanged"
    requirement: "SC-4"
    verification:
      - kind: unit
        ref: "tests/test_incremental_read.py::SheetWatermarksReadTests (4 tests, 3 fail-open siblings unmodified)"
        status: pass
    human_judgment: false
  - id: D6
    description: "No local JSON cache, TTL, or GitHub Actions cache step was re-introduced; the skip index lives entirely inside the existing sheet_registry/get_sheet_watermarks INC-05 architecture"
    requirement: "D-11.1-03"
    verification: []
    human_judgment: true
    rationale: "Architectural absence is not a positive test assertion — verified by code review (no new file, no new env var, no new cache step touched by this plan's 4 commits) rather than a runnable check."

# Metrics
duration: ~30min
completed: 2026-09-01
status: complete
---

# Phase 11.1 Plan 01: Discovery Registry-Version Skip Summary

**Discovery now skips full per-sheet Smartsheet validation for any candidate whose live version still matches `pipeline_memory.sheet_registry`, cutting the ~67-minute Phase-1 discovery bottleneck to a single bulk `list_sheets` call plus per-sheet dict lookups, while every doubt input still falls back to today's full validation.**

## Performance

- **Duration:** ~30 min (approximate — no explicit start-timer was captured this session; measured from first RED commit `99ec1a9` to last commit `8dc375c`, 13:10:43–13:27:55, plus upfront reading/research time)
- **Started:** 2026-09-01T13:10:43-05:00 (first commit)
- **Completed:** 2026-09-01T13:27:55-05:00 (last commit)
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments
- `pipeline/discovery.py::_build_discovery_skip_index(client, sheet_ids)` — new, module-level, never-raising registry-version skip index, fed by one bulk `client.Sheets.list_sheets(include=['sheetVersion'], include_all=True)` probe per `discover_source_sheets()` call (not per sheet) and `pipeline_memory.reader.get_sheet_watermarks`
- Registry fast path inserted as the FIRST statement of `_validate_single_sheet`, above its existing `try:`, so a skip-index miss can never land inside the exception block that feeds `_failed_validation_sids`
- Full doubt-branch matrix (9 named behaviors: no registry row, version mismatch, empty mapping, mapping missing the key column, live version `None`, bulk-probe exception, watermarks unavailable, all-versions-`None`, and the pre-existing full-validation-exception fail-closed re-proof) proven by 16 new tests, all passing against the Task-1 implementation with zero production defects found
- Operator-visible `Discovery validation split: N candidates, N skipped via sheet_registry, N fully validated (D-11.1-01)` INFO line added immediately after the existing `Discovery complete:` line
- `pipeline_memory/reader.py::get_sheet_watermarks` now selects and returns `name`, keeping its documented fail-open contract byte-for-byte

## Task Commits

Each task was committed atomically (Task 1 followed RED/GREEN per its `tdd="true"` attribute):

1. **Task 1 (RED): failing tests for discovery skip** — `99ec1a9` (test)
2. **Task 1 (GREEN): registry-version discovery skip fast path** — `54642d5` (feat)
3. **Task 2: doubt-branch matrix tests** — `7debaca` (test)
4. **Task 3: operator-visible validation split + Gate-4 re-baseline** — `8dc375c` (feat)

**Plan metadata:** (this commit, immediately following)

## Files Created/Modified
- `pipeline_memory/reader.py` — `get_sheet_watermarks` select list and docstring gain `name`
- `pipeline/discovery.py` — new `_build_discovery_skip_index`; registry fast path in `_validate_single_sheet`; new validation-split log line
- `tests/test_incremental_read.py` — `DiscoverySkipIndexTests` (9 tests), `DiscoveryRegistrySkipTests` (10 tests), `SheetWatermarksReadTests.test_returns_dict_keyed_by_sheet_id` updated for `name`
- `tests/golden/mypy_baseline.txt` / `mypy_baseline_count.txt` — re-baselined 70 -> 72 lines (see Decisions)
- `memory-bank/living-ledger.md` — dated re-baseline hygiene entry per the Phase 09 rule

## Decisions Made
- **Gate-4 mypy re-baseline (70 -> 72), zero accepted findings.** `bash scripts/run_6_gates.sh` failed on Gate 4 after Task 3 with "mypy error lines increased (70 -> 72)". Diffed the frozen baseline against a fresh `python -m mypy generate_weekly_pdfs.py audit_billing_changes.py billing_audit pipeline` run programmatically: the 29 `error:`/summary lines are byte-identical (28 errors in 7 files, unchanged); the +2 delta is entirely `[annotation-unchecked]` NOTE lines in `pipeline/discovery.py` (3 removed at old line numbers, 5 added at new line numbers — net +2) caused by the new untyped `_build_discovery_skip_index` function and the registry fast path shifting every downstream line. Per this executor's hard_constraints ("If Gate 4 trips on line-number drift with the error set unchanged, refreeze"), refroze `tests/golden/mypy_baseline.txt` + `mypy_baseline_count.txt` (LF-only, verified via `test_golden_txt_baselines_contain_no_crlf`) and recorded a dated Living Ledger entry naming the cause and proof, mirroring the two prior same-phase precedents (65->68, 68->70). Did NOT add type annotations to silence the note — that would diverge from this file's existing untyped-closure convention (`_validate_single_sheet`, `_get_sample_rows`, `_extract_col_samples` are all untyped today) and risk surfacing unrelated new findings mid-plan.
- **Skip admission gate mirrors the full-validation gate exactly** (exact version equality, non-empty mapping, `Weekly Reference Logged Date` present, non-empty name) — this is the concrete mechanism satisfying T-11.1-01 (a stale registry row must never admit a sheet whose columns changed).
- **`requirements.mark-complete` reported `not_found` for all 4 requirement IDs** (D-11.1-01, D-11.1-03, SC-2, SC-4) — REQUIREMENTS.md does not track Phase 11.1's decision-derived IDs (the phase was inserted urgently, owner-approved directly in `11.1-CONTEXT.md`, bypassing the normal requirements-gathering flow). Not a defect; `requirements-completed` in this SUMMARY's frontmatter is the authoritative record per the plan's own frontmatter. D-11.1-03 and SC-4 additionally report `blocked` (shared with sibling plan `11.1-02`, not yet summarized) via the shared-ID gate.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking, per executor hard_constraints] Gate-4 mypy baseline re-frozen (70 -> 72 lines)**
- **Found during:** Task 3 (`bash scripts/run_6_gates.sh` after adding the split-line log statement)
- **Issue:** Gate 4 failed: "mypy error lines increased (70 -> 72)". New untyped `_build_discovery_skip_index` function and the discovery fast path shifted `pipeline/discovery.py`'s downstream line numbers, adding 2 net `[annotation-unchecked]` advisory NOTE lines vs the frozen baseline. The actual mypy `error:` set (28 errors in 7 files) is byte-identical before and after — proven programmatically (line-by-line diff, `error:`/summary lines untouched, only `note:` lines changed).
- **Fix:** Refroze `tests/golden/mypy_baseline.txt` and `tests/golden/mypy_baseline_count.txt` (72), regenerated via the gate's own invocation with LF-only bytes; added a dated Living Ledger entry naming the cause and proof, per the Phase 09 zero-accepted-findings re-baseline rule.
- **Files modified:** `tests/golden/mypy_baseline.txt`, `tests/golden/mypy_baseline_count.txt`, `memory-bank/living-ledger.md`
- **Verification:** `bash scripts/check_mypy_delta.sh` -> `PASS: mypy delta neutral or improved (72 -> 72)`; `bash scripts/run_6_gates.sh` -> `ALL 6 GATES PASSED`, exit 0; `test_golden_txt_baselines_contain_no_crlf` / `test_golden_txt_baselines_are_pinned_lf_via_gitattributes` both PASS.
- **Committed in:** `8dc375c` (Task 3 commit)

**2. [Documented plan-acceptance-criteria mismatch, not auto-fixed] `git diff --stat HEAD~1 -- tests/golden/` now reports changes**
- **Found during:** Task 3, re-reading the plan's own acceptance criteria after the Gate-4 refreeze above
- **Issue:** `11.1-01-PLAN.md` Task 3's acceptance criteria include `git diff --stat HEAD~1 -- tests/golden/ reports no changes (no baseline was refrozen by this plan)`. This plan's own author anticipated the refreeze scenario in the SAME task's `<action>` text ("If Gate 4 ... reports a delta ... fix the annotation rather than re-freezing") — but empirically the delta here is pure line-number drift with the error set unchanged, which is the specific case this executor's `<hard_constraints>` (from the orchestrator, a more specific and authoritative instruction than the plan's general guidance) explicitly directs to refreeze rather than annotate. The two instructions conflict on this one point; the hard_constraints govern.
- **Resolution:** Followed the executor's `<hard_constraints>` (refreeze), which matches this repo's own established precedent for this exact scenario (Living Ledger: 65->68 and 68->70 re-baselines during this same phase's predecessor work, both "zero accepted findings, line-number drift only"). The named acceptance-criterion line is therefore not literally true post-commit; documenting it here rather than silently ignoring it. All of Task 3's actual `<verify>` commands (full pytest suite, `run_6_gates.sh`, `py_compile`) pass, which is the plan's own overarching `<success_criteria>` ("Full suite plus all 6 gates green").
- **Files affected:** `tests/golden/mypy_baseline.txt`, `tests/golden/mypy_baseline_count.txt` (same as deviation 1)
- **Committed in:** `8dc375c` (Task 3 commit)

---

**Total deviations:** 2 (1 auto-fixed per Rule 3 / hard_constraints, 1 documented acceptance-criterion conflict with no code impact beyond deviation 1)
**Impact on plan:** Both are process/tooling housekeeping around the mypy gate, not production billing-pipeline behavior. Zero change to change-detection key, grouping, filename, attachment-cleanup, or any Smartsheet write path.

## Issues Encountered
None beyond the Gate-4 delta documented above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Fix 1 (this plan) is complete and independently gate-verified. `11.1-02-PLAN.md` (Fix 2 — attachment-confirmation batching) is next; its shared requirement IDs (D-11.1-03, SC-4) will complete once its own SUMMARY exists.
- SC-1 / D-11.1-04 (frequent-run wall clock back under ~75 min) remain phase-level, POST-MERGE production observations owned by `11.1-VALIDATION.md`'s Manual-Only Verifications table — not verifiable from this plan alone; requires the first scheduled `production_frequent` run after this phase's PR merges.
- No blockers for `11.1-02`.

## Self-Check: PASSED

- All 6 modified/created files confirmed present on disk (`pipeline_memory/reader.py`, `pipeline/discovery.py`, `tests/test_incremental_read.py`, `tests/golden/mypy_baseline.txt`, `tests/golden/mypy_baseline_count.txt`, `memory-bank/living-ledger.md`).
- All 4 task commits confirmed in `git log` (`99ec1a9`, `54642d5`, `7debaca`, `8dc375c`).
- `def _build_discovery_skip_index` count = 1; `_failed_validation_sids.append` count = 1 (single pre-existing site, unchanged).
- All plan-level `<verification>` commands re-run clean: `python -m pytest tests/test_incremental_read.py -q` (166 passed), `python -m pytest tests/ -q` (1870 passed / 1 skipped / 306 subtests), `bash scripts/run_6_gates.sh` (`ALL 6 GATES PASSED`, exit 0), `python -m py_compile generate_weekly_pdfs.py` (exit 0). The three pre-existing pinned classes (`SheetWatermarksReadTests` fail-open trio, `SkipGateLiveConfirmationWiringTests`) pass with bodies unmodified.
- All plan `<success_criteria>` met (see Accomplishments); the one acceptance-criterion line contradicted by the Gate-4 refreeze is documented under Deviations, not silently skipped.

---
*Phase: 11.1-post-inc-05-runtime-remediation*
*Completed: 2026-09-01*
