---
phase: 01-subcontractor-rate-logic-modification
plan: 06
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - tests/validate_production_safety.py
  - tests/test_subcontractor_pricing.py
  - website/docs/reference/environment.md
  - website/docs/operations/weekly-pipeline.md
autonomous: false
requirements: [SUB-01, SUB-02, SUB-03, SUB-04, SUB-05, SUB-06, SUB-07]
tags: [regression, production-safety, e2e, runbook, docusaurus]
user_setup: []
must_haves:
  truths:
    - "Per ROADMAP success criterion 5, a TEST_MODE end-to-end run produces byte-identical primary / helper / vac_crew Excel files relative to the pre-change baseline (verified via hash-history diff)"
    - "Per ROADMAP success criterion 5, a TEST_MODE end-to-end run produces NEW `_AEPBillable` and `_ReducedSub` files in `generated_docs/<week>/` for synthetic subcontractor data with the new variant scope"
    - "Per CLAUDE.md 2026-04-25 14:00 rule 3, `tests/validate_production_safety.py::validate_per_group_try_catches_all` window cap is bumped if-and-only-if the per-group billing_audit block grew beyond the existing 18 kB; otherwise unchanged. **Per round-3 checker Warning 8: Task 1's cap decision MUST be reconciled with the 24 kB window used by Plan 05 Task 3's `inspect.getsource` substring check — Plan 06 Task 1 is the authoritative source-of-truth on the cap; Plan 05's check is a substring-find-or-die on a superset window and is unaffected by either a hold-at-18000 or a bump-to-21000 decision.**"
    - "Per .claude/rules/documentation-maintenance.md, the Docusaurus runbook receives a synthesized changelog entry covering Phase 1 (what + why + how operator-facing impact); the env-var reference page documents the 3 new env vars"
    - "Per CLAUDE.md and ROADMAP, `pytest tests/ -v` exits 0 with new test classes added across `test_subcontractor_pricing.py`, `test_vac_crew.py`, `test_security_audit_followup.py`, `test_billing_audit_shadow.py` — and zero existing test regresses"
    - "Per ROADMAP success criterion 4, full test coverage matrix is present: CSV loader, hash-key extension, build_group_identity round-trip, target_map collision quarantine, freeze_row variant attribution"
  artifacts:
    - path: "tests/validate_production_safety.py"
      provides: "Window cap adjusted if billing_audit block grew; otherwise unchanged. Cap decision documented inline with reference to Plan 05's 24 kB substring window (Warning 8 reconciliation)."
    - path: "tests/test_subcontractor_pricing.py"
      provides: "TestPhase1IntegrationRegression class (hash-stability tests across all 7 variants) + TestPhase1FilenameRoundTripCoverage class (filename round-trip — split out from the byte-identical class per Warning 11)"
    - path: "website/docs/reference/environment.md"
      provides: "3 new env vars (`SUBCONTRACTOR_RATES_CSV`, `SUBCONTRACTOR_PPP_SHEET_ID`, `SUBCONTRACTOR_RATE_VARIANTS_ENABLED`) documented"
    - path: "website/docs/operations/weekly-pipeline.md"
      provides: "Synthesized changelog entry + operator-facing description of the 3 new file types and the missing-CU WARNING surface"
  key_links:
    - from: "TEST_MODE pipeline run"
      to: "hash-history baseline diff"
      via: "pre-change vs post-change comparison harness"
      pattern: "hash_history.json|TEST_MODE"
    - from: "operator (Docusaurus runbook)"
      to: "the 3 new env vars + new file variants"
      via: "website/docs reference + operations pages"
      pattern: "_AEPBillable|_ReducedSub|SUBCONTRACTOR_RATES_CSV"
---

<objective>
Lock the no-regression guarantee + ship the operator-facing runbook. End-to-end TEST_MODE
regression test asserts existing primary/helper/vac_crew outputs are byte-identical; the
production-safety validator window cap is bumped if needed; the Docusaurus runbook gains
a synthesized changelog entry plus environment-variable documentation.

Purpose: ROADMAP success criterion 5 is the hardest single guarantee in Phase 1 — that this
phase ships without breaking anything. The 4 other success criteria all reduce to "passes
tests + ships variants"; criterion 5 is the inverse — "shipping variants does NOT change
anything else". Plan 6 is the final gate that proves this empirically with a TEST_MODE
end-to-end run and pins the production-safety validator.

Additionally, the Docusaurus runbook must reflect the new operator surface so that the
on-call engineer reading at 2 AM sees the new env vars, the new file types, and the
missing-CU WARNING.

Output: TEST_MODE regression test that compares pre- vs post-change hashes on synthetic
data; production-safety validator window cap pinned; Docusaurus runbook updated; final
`pytest tests/` clean run; human-verification checkpoint for visual review of generated
Excel files.

**Round-3 checker resolutions absorbed:**
- Warning 8: Task 1 below explicitly reconciles its cap with Plan 05 Task 3's 24 kB substring window.
- Warning 10: Task 3 below switches the human-verification path from `TEST_MODE=true` to `SKIP_UPLOAD=true` against a real subcontractor sheet, because synthetic TEST_MODE rows do not carry CU codes present in `data/subcontractor_rates.csv` and therefore cannot exercise the CSV-driven price write end-to-end.
- Warning 11: Task 2 splits the prior `TestPhase1EndToEndByteIdenticalRegression` class into two — `TestPhase1IntegrationRegression` (hash-stability tests) and `TestPhase1FilenameRoundTripCoverage` (filename round-trip) — so the class name accurately describes its contents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-subcontractor-rate-logic-modification/01-CONTEXT.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-PATTERNS.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-01-SUMMARY.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-02-SUMMARY.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-03-SUMMARY.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-04-SUMMARY.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-05-SUMMARY.md
@.planning/intel/decisions.md
@CLAUDE.md
@.claude/rules/documentation-maintenance.md

<interfaces>
<!-- Key existing code Plan 6 must verify-not-modify (regression scope). -->

tests/validate_production_safety.py L124-145 — `validate_per_group_try_catches_all` (window cap = 18 kB at L140)
generate_weekly_pdfs.py per-group billing_audit block — search for `# ── Billing audit snapshot: freeze personnel` to find the header

TEST_MODE entry points in generate_weekly_pdfs.py:
- `TEST_MODE=true python generate_weekly_pdfs.py` — synthetic data, no Smartsheet API calls. **Note (per Warning 10): synthetic TEST_MODE rows use placeholder CU codes that are NOT present in `data/subcontractor_rates.csv`; the CSV-driven price write path therefore fall-throughs to SmartSheet pricing for every synthetic row, and the visual check in Task 3 cannot validate that the new prices land in the workbook cells. For end-to-end price-write validation use `SKIP_UPLOAD=true python generate_weekly_pdfs.py` against a real subcontractor sheet (no synthetic-data injection).**
- `TEST_MODE=true WR_FILTER=WR_12345,WR_67890 python generate_weekly_pdfs.py` — narrowed scope
- `SKIP_UPLOAD=true python generate_weekly_pdfs.py` — real fetch + Excel generation, no upload (preferred for Task 3 price-write verification per Warning 10)

generated_docs/ layout (output verification target):
- `generated_docs/<week_MMDDYY>/WR_*_WeekEnding_<MMDDYY>_<timestamp>{variant_suffix}_<hash>.xlsx`
- `generated_docs/hash_history.json` — change-detection state (capped at 1000 entries)
- `generated_docs/discovery_cache.json` — sheet discovery cache (TTL 7 days)

Docusaurus runbook (per `.claude/rules/documentation-maintenance.md`):
- Path: `website/docs/` (Docusaurus root)
- Required: synthesize the *what*, *why*, *how* into one coherent entry (NOT a commit dump)
- Required: explicit owner — "Python billing pipeline (generate_weekly_pdfs.py)" tier
- Required: validate via `cd website && npm run typecheck && npm run build`
- Existing env-var reference: `website/docs/reference/environment.md` (or equivalent — confirm via `ls website/docs/`)

Phase 1 deliverables to document:
1. New env vars: `SUBCONTRACTOR_RATES_CSV` (default `data/subcontractor_rates.csv`), `SUBCONTRACTOR_PPP_SHEET_ID` (default `8162920222379908`), `SUBCONTRACTOR_RATE_VARIANTS_ENABLED` (default `'1'`)
2. New file types in `generated_docs/<week>/`: `_AEPBillable`, `_ReducedSub`, `_AEPBillable_Helper_<name>`, `_ReducedSub_Helper_<name>`
3. New WARNING: "Subcontractor rates CSV missing N CU code(s) on sheet ..." — names the CSV file the operator must update
4. Schema change (operator one-time setup): apply `billing_audit/schema.sql` to Supabase to add the `variant TEXT` column

**Cross-plan window-size coordination (Warning 8):**
- Plan 05 Task 3's inline `inspect.getsource` substring check uses a **24 kB** window from the `# ── Billing audit snapshot: freeze personnel` header. That check looks for required substrings (the `variant=row.get(...)` kwargs, the `min(PARALLEL_WORKERS, ...)` cap, the `emit_run_fingerprint` call). A generous window is safe there because the assertions only LOOK for required content; they don't FORBID anything past the window.
- Plan 06 Task 1 (this plan) owns the authoritative `validate_production_safety.py` cap. The cap is bounded by the actual block size + small headroom (current default 18 kB; bump to 21 kB if measured block > 18 kB). Cap decision is documented inline.
- Reconciliation: the cap is on `validate_production_safety.py` enforcement; the 24 kB substring window in Plan 05 is on a separate find-or-die check. They serve different purposes and are intentionally different sizes. Plan 06 Task 1's inline comment will explicitly call out this relationship.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump validate_per_group_try_catches_all window cap if needed + full-suite regression (Warning 8 reconciliation)</name>
  <files>tests/validate_production_safety.py</files>
  <read_first>
    - tests/validate_production_safety.py L124-145 (full `validate_per_group_try_catches_all` — window cap site at L140)
    - generate_weekly_pdfs.py — find the per-group billing_audit block header (`# ── Billing audit snapshot: freeze personnel`) and measure character distance to the next `except Exception as _audit_err:`
    - **`.planning/phases/01-subcontractor-rate-logic-modification/01-05-PLAN.md` Task 3 — confirm Plan 05's inline `inspect.getsource` substring check uses a 24 kB window. The 24 kB window in Plan 05 is a SUPERSET of the (potentially-bumped-to-21 kB) cap here; the two are intentionally different sizes serving different purposes. Document the reconciliation in this task's inline comment per Warning 8.**
    - .planning/phases/01-subcontractor-rate-logic-modification/01-PATTERNS.md section 15 "window cap bump" (L731-756)
    - CLAUDE.md 2026-04-25 14:00 rule 3 (window cap bump policy)
  </read_first>
  <behavior>
    - Test 1: Measure the per-group billing_audit block size in `generate_weekly_pdfs.py` post-Plan 3-5; if ≤ 18000 chars, leave the cap at 18000; if > 18000, bump to the next 3 kB increment (e.g., 21000)
    - Test 2: `python tests/validate_production_safety.py` exits 0
    - Test 3: If the cap is bumped, an inline comment documents the new value and references Phase 1 + CLAUDE.md 2026-04-25 14:00 rule 3
    - Test 4 (Warning 8 reconciliation): The inline comment near L138-140 references the relationship to Plan 05 Task 3's 24 kB substring window, explaining why they intentionally differ.
  </behavior>
  <action>
    First, measure the actual block size post-Plan-3+5:

    ```bash
    python -c "
    from pathlib import Path
    src = Path('generate_weekly_pdfs.py').read_text(encoding='utf-8')
    idx = src.find('# ── Billing audit snapshot: freeze personnel')
    assert idx >= 0, 'block header not found'
    # Find the matching broad except clause
    end_idx = src.index('except Exception as _audit_err:', idx)
    block_size = end_idx - idx
    print(f'per-group billing_audit block size: {block_size} chars')
    print(f'current cap: 18000')
    print(f'bump required: {block_size > 18000}')
    "
    ```

    **Decision tree:**
    - If block size ≤ 18000: no change needed. Add a comment to `tests/validate_production_safety.py` near L138-140 noting "Verified post-Phase-1 (2026-05-14): block size ~{measured}. Cap remains at 18000."
    - If block size > 18000: bump the cap to the next 3 kB increment (round up: 18000 → 21000 if block ≤ 21000; → 24000 if block ≤ 24000; etc.). Update the inline comment:

    ```python
    # Search window capped at 21000 bytes (was 18000 pre-Phase-1).
    # Bumped 2026-05-14 because Plan 3 (variant tagging) + Plan 5
    # (freeze_row variant kwarg) grew the per-group billing_audit
    # block by ~2 kB. Per CLAUDE.md 2026-04-25 14:00 rule 3.
    #
    # Warning 8 reconciliation: Plan 05 Task 3's inline
    # `inspect.getsource` substring check uses a 24 kB window; that
    # is intentionally larger than this cap and is a separate
    # find-or-die check (it asserts required substrings ARE
    # present, not that the block is BOUNDED). The 21 kB cap here
    # is the authoritative source-of-truth on max allowed block
    # size; the 24 kB Plan 05 window is a superset that finds the
    # kwargs reliably across small reflows.
    window = src[idx:idx + 21000]
    ```

    If the cap stays at 18000, use a parallel comment:

    ```python
    # Search window remains capped at 18000 bytes post-Phase-1
    # (2026-05-14). Per measurement, the per-group billing_audit
    # block is ~{measured} chars after Plan 3/5 changes —
    # comfortably below the cap. Per CLAUDE.md 2026-04-25 14:00
    # rule 3.
    #
    # Warning 8 reconciliation: Plan 05 Task 3's inline
    # `inspect.getsource` substring check uses a 24 kB window; that
    # is intentionally larger than this cap and is a separate
    # find-or-die check (it asserts required substrings ARE
    # present, not that the block is BOUNDED). Both sizes are
    # valid; this 18 kB cap is the authoritative source-of-truth
    # on max allowed block size.
    ```

    Do NOT remove the cap entirely (it's a forward-compatibility guard against unrelated code displacing the broad-except clause). Do NOT extend the cap beyond what's actually needed (over-permissive caps reduce the validator's signal).

    Then run the full validator:

    ```bash
    python tests/validate_production_safety.py
    ```

    And the full test suite:

    ```bash
    pytest tests/ -v
    ```

    Both must exit 0.
  </action>
  <verify>
    <automated>
      python tests/validate_production_safety.py &&
      pytest tests/ -v
    </automated>
  </verify>
  <acceptance_criteria>
    - `python tests/validate_production_safety.py` exits 0
    - `pytest tests/ -v` exits 0 with at least the expected new test classes present (TestLoadSubcontractorRates, TestSubcontractorVariantGroupIdentityParsing, TestSubcontractorVariantHashAggregation, TestSubcontractorVariantGrouping, TestAEPBillableVariantGeneration, TestReducedSubVariantGeneration, TestSubcontractorVariantHelperShadowGeneration, TestSubcontractorMissingCUFallback, TestSubcontractorVariantOpenpyxlCompliance, TestResolveRowPriceCanonicalColumnNames, TestSubcontractorVariantKillSwitchAndScope, TestDualTargetMapIndependentQuarantine, TestDualTargetSheetRouting, TestFreezeRowVariantAttribution, TestPhase1IntegrationRegression, TestPhase1FilenameRoundTripCoverage)
    - If the window cap was bumped, the inline comment in `tests/validate_production_safety.py` mentions "Phase 1" and "2026-04-25 14:00 rule 3"
    - If the window cap was NOT bumped, a comment near L138-140 confirms the post-Phase-1 measurement
    - **Warning 8 reconciliation acceptance:** the inline comment near L138-140 mentions "Warning 8" or "Plan 05 Task 3" or "24 kB window" — making the inter-plan relationship explicit so a future engineer reading the validator knows why Plan 05 uses a different number.
  </acceptance_criteria>
  <done>Production-safety validator is current; full test suite passes; the window cap is right-sized for Phase 1's per-group billing_audit footprint; Warning 8 reconciliation is documented inline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Byte-identical-baseline regression test (split into TestPhase1IntegrationRegression + TestPhase1FilenameRoundTripCoverage per Warning 11)</name>
  <files>tests/test_subcontractor_pricing.py</files>
  <read_first>
    - generate_weekly_pdfs.py — `TEST_MODE` handler (search for `if TEST_MODE:` — confirm synthetic-data injection path)
    - .planning/phases/01-subcontractor-rate-logic-modification/01-PATTERNS.md section 12 "new variant generation regression class" L607-654 (hash-stability test patterns)
    - .planning/phases/01-subcontractor-rate-logic-modification/01-CONTEXT.md ROADMAP success criterion 5 (byte-identical guarantee)
    - tests/test_vac_crew.py L227-419 (`TestVacCrewHashAggregation` — analog for hash-stability assertions across variant changes)
  </read_first>
  <behavior>
    - **TestPhase1IntegrationRegression (hash-stability tests):**
      - Test 1: A synthetic group with `__variant='primary'`, identical row content, and unchanged `_RATES_FINGERPRINT` produces a stable hash across two `calculate_data_hash` calls regardless of `_SUBCONTRACTOR_RATES_FINGERPRINT` mutation
      - Test 2: A synthetic group with `__variant='helper'` produces a stable hash regardless of `_SUBCONTRACTOR_RATES_FINGERPRINT` mutation
      - Test 3: A synthetic group with `__variant='vac_crew'` produces a stable hash regardless of `_SUBCONTRACTOR_RATES_FINGERPRINT` mutation
      - Test 4: A synthetic group with `__variant='aep_billable'` produces a DIFFERENT hash when `_SUBCONTRACTOR_RATES_FINGERPRINT` changes (positive regression — sub fingerprint IS mixed in for new variants)
      - Test 6: `test_existing_variants_total_hash_count_unchanged` — Build a set of 10 synthetic groups across the 3 existing variants (primary, helper, vac_crew); compute hashes; assert the set has the same cardinality as a previously-recorded baseline (existence check — proves no variant got accidentally merged with another)
    - **TestPhase1FilenameRoundTripCoverage (filename round-trip):**
      - Test 5: For each variant in `['primary', 'helper', 'vac_crew', 'aep_billable', 'reduced_sub', 'aep_billable_helper', 'reduced_sub_helper']`: construct a filename matching the variant's expected suffix pattern; call `build_group_identity(filename)`; assert the returned `variant` matches the input variant
    - **Task 3 (separate human-verify task below):** manual checkpoint for end-to-end TEST_MODE / SKIP_UPLOAD verification
  </behavior>
  <action>
    **Class split (Warning 11):** the prior planning text grouped Tests 1-4 + 6 (hash-stability) with Test 5 (filename round-trip) under one class named `TestPhase1EndToEndByteIdenticalRegression`. The class name described the hash-stability content but not the filename round-trip — that was a name/content mismatch flagged by the checker. The fix is to split into two classes:

    1. `TestPhase1IntegrationRegression` — owns hash-stability tests (the "no-regression on existing variants" + "positive regression on new variants" invariants that lock ROADMAP success criterion 5). 5 test methods.
    2. `TestPhase1FilenameRoundTripCoverage` — owns the filename round-trip test across all 7 variants (proves the parser ↔ generator symmetry for cleanup, hash-history, target-row matching paths). 1 test method, but parametrized over 7 variants.

    Both classes live in `tests/test_subcontractor_pricing.py`. Both setUp/tearDown mirror `TestVacCrewHashAggregation` (L227-275) — pin `EXTENDED_CHANGE_DETECTION`, `RATE_CUTOFF_DATE`, `_RATES_FINGERPRINT`, AND the new `_SUBCONTRACTOR_RATES_FINGERPRINT` to test-isolated values. This is critical — the existing test classes have their own pinning; introducing a new test that mutates `_SUBCONTRACTOR_RATES_FINGERPRINT` without isolation could leak into other tests.

    **TestPhase1IntegrationRegression — 5 methods:**

    1. `test_primary_hash_byte_identical_across_sub_fingerprint_mutations`
       - Build a synthetic group with `__variant='primary'`, 3 rows
       - Set `_SUBCONTRACTOR_RATES_FINGERPRINT = 'A'`, compute `h1 = calculate_data_hash(rows)`
       - Set `_SUBCONTRACTOR_RATES_FINGERPRINT = 'B'`, compute `h2 = calculate_data_hash(rows)`
       - Assert `h1 == h2` (primary hash MUST NOT change)

    2. `test_helper_hash_byte_identical_across_sub_fingerprint_mutations`
       - Same as Test 1 but `__variant='helper'` with helper-foreman metadata

    3. `test_vac_crew_hash_byte_identical_across_sub_fingerprint_mutations`
       - Same as Test 1 but `__variant='vac_crew'` with vac-crew per-row metadata (see CLAUDE.md 2026-04-22 00:00 — per-row vac_crew fields must be in row_str)

    4. `test_aep_billable_hash_changes_on_sub_fingerprint_mutation`
       - Build a synthetic group with `__variant='aep_billable'`
       - Set `_SUBCONTRACTOR_RATES_FINGERPRINT = 'A'`, compute `h1`
       - Set `_SUBCONTRACTOR_RATES_FINGERPRINT = 'B'`, compute `h2`
       - Assert `h1 != h2` (positive — CSV edit forces regen)

    5. `test_existing_variants_total_hash_count_unchanged`
       - Build a set of 10 synthetic groups across the 3 existing variants (primary, helper, vac_crew)
       - Compute hashes; serialize the set
       - Assert the set has the same cardinality as a previously-recorded baseline (existence check — proves no variant got accidentally merged with another)

    Class docstring (mandatory per existing acceptance criterion): "Locks ROADMAP success criterion 5 — the byte-identical guarantee for primary / helper / vac_crew Excel files under the introduction of `_SUBCONTRACTOR_RATES_FINGERPRINT`. Any future change that accidentally widens the fingerprint mix-in to existing variants will fail Tests 1-3 here."

    **TestPhase1FilenameRoundTripCoverage — 1 method:**

    1. `test_filename_round_trip_for_all_seven_variants`
       - For each variant in `['primary', 'helper', 'vac_crew', 'aep_billable', 'reduced_sub', 'aep_billable_helper', 'reduced_sub_helper']`:
         - Construct a filename matching the variant's expected suffix pattern
         - Call `build_group_identity(filename)`
         - Assert the returned `variant` matches the input variant

    Class docstring: "Covers the filename round-trip for all 7 Phase 1 variants. Split out from `TestPhase1IntegrationRegression` per round-3 checker Warning 11 because this test exercises the parser/generator symmetry, not hash stability — different invariant, different class."

    No real CSV / Smartsheet API access required for any test in either class. All tests use `unittest.mock.patch` and manually construct row dicts / filenames.
  </action>
  <verify>
    <automated>
      pytest tests/test_subcontractor_pricing.py::TestPhase1IntegrationRegression tests/test_subcontractor_pricing.py::TestPhase1FilenameRoundTripCoverage -v
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^class TestPhase1IntegrationRegression" tests/test_subcontractor_pricing.py` returns exactly one match
    - `grep -nE "^class TestPhase1FilenameRoundTripCoverage" tests/test_subcontractor_pricing.py` returns exactly one match
    - `grep -nE "^class TestPhase1EndToEndByteIdenticalRegression" tests/test_subcontractor_pricing.py` returns 0 matches (the prior misnamed class must NOT exist — Warning 11 lock-in)
    - TestPhase1IntegrationRegression has 5 test methods (`test_primary_hash_byte_identical_across_sub_fingerprint_mutations`, `test_helper_hash_byte_identical_across_sub_fingerprint_mutations`, `test_vac_crew_hash_byte_identical_across_sub_fingerprint_mutations`, `test_aep_billable_hash_changes_on_sub_fingerprint_mutation`, `test_existing_variants_total_hash_count_unchanged`)
    - TestPhase1FilenameRoundTripCoverage has 1 test method (`test_filename_round_trip_for_all_seven_variants`)
    - The setUp/tearDown of TestPhase1IntegrationRegression pins `_SUBCONTRACTOR_RATES_FINGERPRINT` AND the existing constants (`EXTENDED_CHANGE_DETECTION`, `RATE_CUTOFF_DATE`, `_RATES_FINGERPRINT`)
    - All 6 tests pass
    - TestPhase1IntegrationRegression class docstring explicitly documents that it locks ROADMAP success criterion 5
    - TestPhase1FilenameRoundTripCoverage class docstring explicitly references Warning 11 (the split rationale)
  </acceptance_criteria>
  <done>ROADMAP success criterion 5 is locked by automated regression — a future change that accidentally widens the `_SUBCONTRACTOR_RATES_FINGERPRINT` mix-in to existing variants will fail loudly; filename round-trip coverage is in a separately-named class so the test taxonomy matches its content (Warning 11).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — end-to-end run produces expected file mix (price-write verification uses real subcontractor sheet via SKIP_UPLOAD per Warning 10)</name>
  <what-built>
    All Phase 1 code changes are in place and the automated test suite passes. Plan 6 Task 2 covered the byte-identical hash regression analytically. This checkpoint asks the operator to run the pipeline locally in two modes and visually verify both the file-mix coverage AND the CSV-driven price write to workbook cells.

    **Two-mode verification (per round-3 checker Warning 10):**

    1. **TEST_MODE (synthetic-data) run** — verifies that the new variant code paths emit the expected file mix (filenames, group keys, kill-switch behavior, helper-shadow ordering). Synthetic TEST_MODE rows do NOT carry CU codes that match `data/subcontractor_rates.csv`, so this mode cannot validate that CSV-driven prices land in workbook cells — every synthetic row will fall through to SmartSheet pricing (which is fine for file-mix coverage, but inadequate for the price-write check).

    2. **SKIP_UPLOAD against a real subcontractor sheet (no synthetic injection)** — verifies that for rows whose CU codes ARE in the CSV, the `Units Total Price` cell in the generated `_AEPBillable_*.xlsx` and `_ReducedSub_*.xlsx` workbooks equals `rate × qty` (CSV-driven), NOT the SmartSheet value. This is the price-write end-to-end gate. Files are generated on disk but NOT uploaded to Smartsheet.

    Specifically:
    - The 3 new env vars resolve to their D-03/D-12/D-13 defaults
    - The CSV at `data/subcontractor_rates.csv` loads cleanly (no parse errors in the startup banner)
    - For synthetic subcontractor WR groups (TEST_MODE), the expected mix of files appears: `_AEPBillable` (when snapshot date is post-cutoff) and `_ReducedSub` (always)
    - For real subcontractor data (SKIP_UPLOAD), the workbook cells reflect CSV-driven prices for rows with matching CUs
    - For synthetic helper-foreman events on subcontractor WRs, the shadow files `_AEPBillable_Helper_<name>` and `_ReducedSub_Helper_<name>` appear alongside the regular `_Helper_<name>` file
    - Existing primary / helper / vac_crew files continue to appear with unchanged content
    - Any missing CUs in real data surface as ONE WARNING per sheet with the first 10 CU codes listed
  </what-built>
  <how-to-verify>
    **Step A — TEST_MODE synthetic-data run (file-mix coverage; per Warning 10 this does NOT validate price writes):**

    Run the following commands from the repo root (PowerShell — note the `$env:` prefix for env vars):

    ```powershell
    # 1. Ensure dependencies are current
    pip install -r requirements.txt

    # 2. Clean generated_docs/ so the visual check starts fresh
    Remove-Item -Recurse -Force generated_docs -ErrorAction SilentlyContinue
    New-Item -ItemType Directory -Force -Path generated_docs | Out-Null

    # 3. Confirm the startup banner shows the new env vars + fingerprint
    $env:TEST_MODE = "true"
    $env:SKIP_UPLOAD = "true"
    python generate_weekly_pdfs.py 2>&1 | Select-String -Pattern "Subcontractor"

    # Expected lines (approximately):
    #   📊 Subcontractor rate variants ENABLED (SUBCONTRACTOR_RATES_CSV='data/subcontractor_rates.csv', SUBCONTRACTOR_PPP_SHEET_ID=8162920222379908)
    #   📊 Subcontractor rates loaded: <N> CUs, fingerprint=<16-hex-prefix>

    # 4. Run the full TEST_MODE pipeline (this generates synthetic data without Smartsheet API calls)
    python generate_weekly_pdfs.py

    # 5. Visually inspect generated_docs/<week>/ for the new file mix
    Get-ChildItem -Recurse generated_docs -Filter "*.xlsx" | Select-Object Name
    ```

    Expected outcomes for Step A (mark each):

    - [ ] Startup banner shows `SUBCONTRACTOR_RATE_VARIANTS_ENABLED=True`, the resolved CSV path, the resolved PPP sheet ID, the CU count loaded, and the fingerprint
    - [ ] `generated_docs/<week>/` contains at least one `WR_*_*_AEPBillable_*.xlsx` file (if synthetic data crosses the 2026-04-12 cutoff)
    - [ ] `generated_docs/<week>/` contains at least one `WR_*_*_ReducedSub_*.xlsx` file
    - [ ] Existing primary files (`WR_*_*_<hash>.xlsx` — no variant suffix) still appear
    - [ ] If helper-foreman events fire in the synthetic data: `WR_*_*_Helper_<name>_<hash>.xlsx` AND `WR_*_*_AEPBillable_Helper_<name>_<hash>.xlsx` AND `WR_*_*_ReducedSub_Helper_<name>_<hash>.xlsx` all appear
    - [ ] No CRITICAL / ERROR-level logs visible in the run output (any ERRORs indicate a regression)
    - [ ] No Sentry events fired during the run (search the log for `sentry_capture` invocations — should be zero for a clean run)

    **Step B — SKIP_UPLOAD real-data run (CSV-driven price-write verification; per Warning 10 this is the ONLY mode that exercises the price-write path end-to-end):**

    ```powershell
    # 1. Clean generated_docs/ for the second pass
    Remove-Item -Recurse -Force generated_docs -ErrorAction SilentlyContinue
    New-Item -ItemType Directory -Force -Path generated_docs | Out-Null

    # 2. Confirm a real SMARTSHEET_API_TOKEN is set (required for real-data mode)
    if (-not $env:SMARTSHEET_API_TOKEN) {
        Write-Host "ERROR: SMARTSHEET_API_TOKEN not set — cannot run real-data mode" -ForegroundColor Red
        Write-Host "Set it in .env or shell, then re-run." -ForegroundColor Red
        exit 1
    }

    # 3. Disable TEST_MODE, enable SKIP_UPLOAD; optionally narrow scope with WR_FILTER
    Remove-Item Env:TEST_MODE -ErrorAction SilentlyContinue
    $env:SKIP_UPLOAD = "true"
    # Optional: narrow scope to a few known subcontractor WRs
    # $env:WR_FILTER = "<known_subcontractor_WR_1>,<known_subcontractor_WR_2>"

    python generate_weekly_pdfs.py

    # 4. Inspect the new variant files for CSV-driven prices
    Get-ChildItem -Recurse generated_docs -Filter "*_AEPBillable_*.xlsx" | Select-Object FullName
    Get-ChildItem -Recurse generated_docs -Filter "*_ReducedSub_*.xlsx" | Select-Object FullName
    ```

    Expected outcomes for Step B (mark each):

    - [ ] At least one `_AEPBillable_*.xlsx` and one `_ReducedSub_*.xlsx` file produced from real subcontractor data
    - [ ] Logs show "Subcontractor rates CSV missing N CU code(s)" exactly ONCE per subcontractor sheet IF any CU codes are missing (zero such lines is also acceptable — depends on operator coverage of the CSV)

    Open one `_AEPBillable_*.xlsx` and one `_ReducedSub_*.xlsx` in Excel:
    - [ ] Workbook opens without "file is corrupted" or "repair needed" prompts
    - [ ] **CRITICAL (Warning 10 price-write gate):** `Units Total Price` column reflects CSV-driven values (NOT the SmartSheet pricing) for rows whose CU codes ARE present in `data/subcontractor_rates.csv`. Specifically:
      - Pick a row in the workbook
      - Note its CU code and Quantity
      - Look up the row's CU in `data/subcontractor_rates.csv`
      - For `_AEPBillable`: expected price = `new_<work_type>_price × Quantity`
      - For `_ReducedSub`: expected price = `reduced_<work_type>_price × Quantity`
      - Confirm the workbook cell matches (within rounding)
    - [ ] For rows whose CU is NOT in the CSV, the workbook cell equals the SmartSheet `Units Total Price` (fall-through path — D-16 invariant)
    - [ ] Headers, totals, and formatting match the existing primary file's style (consistent operator experience)

    If ALL the above pass (Step A + Step B), type `approved`. If any fail, describe the failure (which step, which file, what was expected vs observed).

    **If a real SMARTSHEET_API_TOKEN is unavailable in the operator's local env,** Step B can be deferred — the operator must mark `pending: SKIP_UPLOAD real-data verification` in the resume signal so the implementing team knows the price-write gate is still open. The CI pipeline / a follow-up production run is the fallback verification surface.
  </how-to-verify>
  <resume-signal>Type "approved" if BOTH Step A (TEST_MODE file-mix) AND Step B (SKIP_UPLOAD price-write) produce the expected outcomes, "approved-step-A-only; step B pending real-data run" if Step B is deferred for token reasons, or describe specific failures observed.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update Docusaurus runbook — env-var reference + synthesized changelog entry</name>
  <files>website/docs/reference/environment.md, website/docs/operations/weekly-pipeline.md</files>
  <read_first>
    - website/docs/reference/environment.md (current env-var reference — confirm exact path; if different, use the actual path)
    - website/docs/operations/weekly-pipeline.md (current weekly-pipeline runbook — confirm exact path; the operator-facing description page)
    - .claude/rules/documentation-maintenance.md (synthesized changelog rule — NOT a commit dump)
    - .planning/phases/01-subcontractor-rate-logic-modification/01-CONTEXT.md (full decision list to summarize)
    - .planning/phases/01-subcontractor-rate-logic-modification/01-01-SUMMARY.md..01-05-SUMMARY.md (final shapes of each plan's deliverables)
  </read_first>
  <behavior>
    - Test 1 (typecheck): `cd website && npm run typecheck` exits 0
    - Test 2 (build): `cd website && npm run build` exits 0 with no broken cross-links
    - Test 3 (content): the env-var reference page has new entries for `SUBCONTRACTOR_RATES_CSV`, `SUBCONTRACTOR_PPP_SHEET_ID`, `SUBCONTRACTOR_RATE_VARIANTS_ENABLED` — each entry includes default, valid values, kill-switch behavior, and rollback procedure
    - Test 4 (content): the weekly-pipeline operations page has a new section or callout describing the 3 new file types (`_AEPBillable`, `_ReducedSub`, and their `_Helper_<name>` shadow forms), the missing-CU WARNING, and the one-time `billing_audit/schema.sql` apply step
    - Test 5 (component ownership clarity per `.claude/rules/documentation-maintenance.md`): both new doc sections state "Python billing pipeline (`generate_weekly_pdfs.py`) owns this surface"
  </behavior>
  <action>
    First confirm the actual file paths:

    ```bash
    ls website/docs/reference/ website/docs/operations/ 2>/dev/null
    ```

    If `environment.md` / `weekly-pipeline.md` exist at those exact paths, modify them. Otherwise locate the closest equivalents (likely `website/docs/reference/configuration.md` or `website/docs/runbook/...`).

    **Change 1 — Env-var reference page.** Add 3 new entries in the same style as the existing `RATE_RECALC_*` documentation (search the file for `RATE_RECALC_WEEKLY_FALLBACK` to find the analog block). Each entry includes:

    ```markdown
    ### `SUBCONTRACTOR_RATES_CSV`

    **Default:** `data/subcontractor_rates.csv`
    **Component owner:** Python billing pipeline (`generate_weekly_pdfs.py`)
    **Purpose:** Path to the operator-managed subcontractor rate matrix CSV
    (17 columns, currency-formatted prices, keyed by CU code). Used by the
    `_AEPBillable` and `_ReducedSub` variants to compute `rate × qty` per
    work-type column.
    **Valid values:** Any filesystem path resolvable from the repo root.
    The loader tolerates UTF-8 BOM, strips `$` and thousands-comma from
    price cells, and skips rows whose all six priced columns are zero.
    **Rollback:** Setting to a path that does not exist causes the loader
    to log an ERROR and return an empty dict; new-variant generation
    silently falls through to SmartSheet pricing for every row, which is
    a safe no-op (per D-16 fail-safe contract).

    ### `SUBCONTRACTOR_PPP_SHEET_ID`

    **Default:** `8162920222379908`
    **Component owner:** Python billing pipeline (`generate_weekly_pdfs.py`)
    **Purpose:** Smartsheet sheet id for the secondary attachment target
    used by `_ReducedSub` and `_ReducedSub_Helper_<name>` files. Files
    of those variants attach to BOTH `TARGET_SHEET_ID` (5723337641643908)
    AND this sheet.
    **Valid values:** Integer Smartsheet sheet id, or empty/0 to disable
    PPP routing (degrades to single-sheet — see SUB-03 behavior).
    **Rollback:** Setting equal to `TARGET_SHEET_ID` is a no-op (the dual
    routing detects same-sheet config and skips the second upload). If the
    PPP sheet is unreachable, the pipeline logs an ERROR and degrades to
    single-sheet routing automatically.

    ### `SUBCONTRACTOR_RATE_VARIANTS_ENABLED`

    **Default:** `'1'` (truthy values: `1`, `true`, `yes`, `on`, case-insensitive)
    **Component owner:** Python billing pipeline (`generate_weekly_pdfs.py`)
    **Purpose:** Default-on kill switch (pattern matches
    `RATE_RECALC_SKIP_ORIGINAL_CONTRACT` and `RATE_RECALC_WEEKLY_FALLBACK`).
    When disabled, the entire new variant code path short-circuits — no
    `_AEPBillable` or `_ReducedSub` files generate, no dual-target
    routing fires, primary/helper/vac_crew flows are unchanged.
    **Rollback:** Set to `0` / `false` in the workflow env block to
    immediately disable new-variant generation without a code revert.
    State is logged in the startup banner — operators grepping the run
    header can confirm at a glance.
    ```

    **Change 2 — Weekly-pipeline operations page (synthesized changelog entry).** Add a new section near the top (after the existing "What This Runs" / "Schedule" sections, before "Troubleshooting") titled "**Subcontractor Rate Variants (added 2026-05-14, Phase 1)**":

    ```markdown
    ## Subcontractor Rate Variants

    *(Added 2026-05-14, Phase 1 — see also: PR #N, `.planning/phases/01-subcontractor-rate-logic-modification/`)*

    **What changed.** For every subcontractor-folder WR group, the
    Python billing pipeline (`generate_weekly_pdfs.py`) now produces
    two new Excel variants alongside the existing primary file:

    - `_AEPBillable_*.xlsx` — Priced via the 3%-increase columns of
      `data/subcontractor_rates.csv` (`new_install_price`,
      `new_remove_price`, `new_transfer_price`). What Linetec bills
      AEP under the contract awarded 2026-04-12. Generated ONLY when
      at least one row in the group has `Snapshot Date >= 2026-04-12`.
      Routes to `TARGET_SHEET_ID` (5723337641643908) only.

    - `_ReducedSub_*.xlsx` — Priced via the 13%-reduced columns
      (`reduced_*_price`). What Linetec pays its subcontractors.
      Generated for every subcontractor WR group regardless of
      snapshot date. Routes to BOTH `TARGET_SHEET_ID` AND
      `SUBCONTRACTOR_PPP_SHEET_ID` (8162920222379908) — so each
      `_ReducedSub` file appears twice in Smartsheet (once per sheet).

    When the existing helper-foreman detection rule fires on a
    subcontractor WR, shadow files `_AEPBillable_Helper_<name>` and
    `_ReducedSub_Helper_<name>` generate alongside the regular
    `_Helper_<name>` file — three helper files per shadow event.

    **Why.** AEP contract awarded a 3% rate increase effective
    2026-04-12; the operator team needs separate Excel files showing
    AEP-billable rates (`_AEPBillable`) and subcontractor-paid rates
    (`_ReducedSub`) so accounts payable and accounts receivable can
    reconcile against the new contract structure without manually
    re-pricing rows. The original-contract folder workflow remains
    Smartsheet-priced (see "Original-contract folder pricing" section).

    **How it impacts operators.**

    1. **One-time setup (operator action):** Apply the schema migration
       in `billing_audit/schema.sql` to the deployed Supabase project.
       Open Supabase Dashboard → SQL Editor → run the file's contents.
       The `ALTER TABLE ... ADD COLUMN IF NOT EXISTS variant TEXT;`
       statement is idempotent — safe to re-run. (See SUB-07.)

    2. **Three new env vars:** Documented in
       [`reference/environment.md`](../reference/environment.md):
       `SUBCONTRACTOR_RATES_CSV`, `SUBCONTRACTOR_PPP_SHEET_ID`,
       `SUBCONTRACTOR_RATE_VARIANTS_ENABLED`. All have safe defaults
       and a default-on kill switch for emergency rollback.

    3. **Missing-CU WARNING.** When the pipeline encounters a CU code
       in a Smartsheet row that's not present in
       `data/subcontractor_rates.csv`, it falls through to the
       SmartSheet `Units Total Price` value (never zero-out, never
       error) AND fires exactly ONE per-sheet WARNING listing the
       first 10 missing codes:

       ```
       Subcontractor rates CSV missing N CU code(s) on sheet <id>:
       <CU1>, <CU2>, ... Add to data/subcontractor_rates.csv to
       enable rate recalc for these rows. Sheet rows fell through to
       SmartSheet pricing.
       ```

       **Operator action:** Update `data/subcontractor_rates.csv` with
       the missing CU codes before the next run if the new-variant
       prices for those CUs matter for billing.

    4. **CSV file location.** The contract rate matrix lives at
       `data/subcontractor_rates.csv` (moved from the repo root in
       Phase 1, preserving git history via `git mv`). Operators update
       this file directly in the repo; the XLSX original remains
       gitignored.

    5. **Variant attribution in audit shadow.** `billing_audit.pipeline_run`
       gains a `variant TEXT` column populated with the 7 valid variant
       strings (`primary`, `helper`, `vac_crew`, `aep_billable`,
       `reduced_sub`, `aep_billable_helper`, `reduced_sub_helper`).
       Useful for forensic queries: "show me all reduced_sub rows for
       WR=X in week=Y".

    **Rollback procedure.** Set
    `SUBCONTRACTOR_RATE_VARIANTS_ENABLED=0` in the GitHub Actions
    workflow env block. The next run will skip all new-variant
    generation; primary / helper / vac_crew flows are unaffected. No
    code revert required.

    **Component owner:** Python billing pipeline
    (`generate_weekly_pdfs.py`) — NOT Notion sync, NOT the portal-v2
    Supabase tier. Route incident triage to the Python pipeline owner.
    ```

    **Change 3 — Validate Docusaurus build.** Per `.claude/rules/documentation-maintenance.md`:

    ```bash
    cd website
    npm install  # in case dependencies drifted
    npm run typecheck
    npm run build
    cd ..
    ```

    Both `typecheck` and `build` must exit 0 with no broken cross-links. Per the rule, if a renamed/deleted page is referenced from another page, fix the cross-link in the same commit.

    Do NOT introduce a separate ADR file for this — `.claude/rules/documentation-maintenance.md` rule 1 specifies that changelog entries synthesize the *what / why / how*; this section IS the synthesized entry. A separate ADR would duplicate content (the CLAUDE.md Living Ledger will receive its entry via the autonomous-cloud-memory-injection rule when the implementing PR is merged).
  </action>
  <verify>
    <automated>
      cd website && npm run typecheck && npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - The 3 new env vars appear as named sections in the env-var reference page
    - The weekly-pipeline operations page has a new "Subcontractor Rate Variants" section with all 5 numbered operator-impact items
    - Both pages reference `generate_weekly_pdfs.py` explicitly as the component owner (per `.claude/rules/documentation-maintenance.md` hybrid-ecosystem-clarity rule)
    - `cd website && npm run typecheck` exits 0
    - `cd website && npm run build` exits 0
    - The changelog entry IS a synthesized narrative (not a commit-message dump) — verifiable by re-reading: the section answers "what changed / why / how it impacts operators" coherently
  </acceptance_criteria>
  <done>The Docusaurus runbook reflects Phase 1 — on-call engineers reading at 2 AM see the new env vars, new file types, missing-CU WARNING, and the rollback procedure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production-safety validator → CI/PR gate | `validate_production_safety.py` is invoked from `.github/hooks/pre-push-tests.json` and CI checks; window-cap regression would fail loudly |
| TEST_MODE pipeline → operator visual review | Synthetic data outputs are operator-visible; visual verification depends on operator attention quality. Per Warning 10, synthetic data does NOT exercise the CSV-driven price write — SKIP_UPLOAD against real data is the only mode that does. |
| Docusaurus runbook → on-call engineer | Outdated or missing runbook content causes incident response failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering (production-safety validator bypass) | window cap precision | mitigate | Task 1 right-sizes the cap to the actual block size + small headroom. Over-permissive caps (e.g., infinity) are explicitly forbidden. Under-permissive caps cause false alarms. The bump policy is documented inline, including reconciliation with Plan 05 Task 3's 24 kB substring window (Warning 8). |
| T-06-02 | Repudiation (regression goes undetected) | byte-identical hash invariant | mitigate | Task 2's `TestPhase1IntegrationRegression` class pins the primary/helper/vac_crew hash invariants directly; a future change that accidentally widens `_SUBCONTRACTOR_RATES_FINGERPRINT` mix-in to existing variants fails Tests 1-3. Class split (Warning 11) keeps the test taxonomy aligned with content. |
| T-06-03 | Information Disclosure (runbook leaks tech details) | Docusaurus content | accept | The runbook is operator-facing and intentionally describes the rate logic, file names, and env vars — these are not secrets. Actual rate values (`$1,234.56`) are NOT in the runbook; they live in the gitignored XLSX and the committed CSV. |
| T-06-04 | Denial of Service (TEST_MODE produces wrong files) | human verification checkpoint | mitigate | Task 3's checkpoint explicitly enumerates the expected file mix (Step A) AND the expected CSV-driven price values (Step B against real data per Warning 10); the operator's `approved` response is the gate before Phase 1 ships. Failures route back to the planner for triage. Real-data fallback (`approved-step-A-only`) is permitted with explicit pending notation. |
| T-06-05 | Repudiation (operator misses missing-CU WARNING) | runbook discoverability | mitigate | Task 4's changelog entry has the missing-CU WARNING as a numbered operator-action item; the env-var reference page calls out the `SUBCONTRACTOR_RATES_CSV` rollback semantics. |
| T-06-06 | Tampering (price-write regression hidden by synthetic TEST_MODE) | end-to-end price-write gate | mitigate | Per Warning 10, Task 3 splits verification into Step A (TEST_MODE file-mix) and Step B (SKIP_UPLOAD real-data price-write). Step B is the ONLY mode that exercises the CSV-driven cell-write path end-to-end; without it, a regression in `_resolve_row_price` could ship undetected (the unit tests in Plan 03 catch the helper-level behavior, but the integration with the workbook cell writer is only covered by Step B). |
</threat_model>

<verification>
- `pytest tests/` (full suite) exits 0
- `python tests/validate_production_safety.py` exits 0
- `cd website && npm run typecheck && npm run build` exits 0
- Human verification checkpoint (Task 3) returns `approved` (or `approved-step-A-only; step B pending real-data run`) from the operator
- The 3 new env vars are documented in `website/docs/reference/environment.md` (or actual path)
- The synthesized changelog entry appears in `website/docs/operations/weekly-pipeline.md` (or actual path)
</verification>

<success_criteria>
Phase 1 ships cleanly:
- Full `pytest tests/` passes with the expected new test classes present (16+ new classes across Plans 1-6, including the split TestPhase1IntegrationRegression + TestPhase1FilenameRoundTripCoverage per Warning 11).
- `tests/validate_production_safety.py` passes with a right-sized window cap; inline comment reconciles with Plan 05's 24 kB substring window (Warning 8).
- ROADMAP success criterion 5 (byte-identical guarantee for existing variants) is locked by automated regression tests AND human visual verification on a TEST_MODE run (Step A) AND a SKIP_UPLOAD real-data run (Step B per Warning 10).
- The Docusaurus runbook is current — operators have the env-var reference, the file-type description, the missing-CU action item, and the rollback procedure all in one synthesized changelog entry.
- The component ownership of the new behavior is explicitly stated (Python billing pipeline) per `.claude/rules/documentation-maintenance.md` hybrid-ecosystem-clarity rule.
- All 22 CONTEXT.md decisions (D-01..D-22) are honored across the 6 plans; all 7 REQUIREMENTS.md items (SUB-01..SUB-07) are covered.
</success_criteria>

<output>
After completion, create `.planning/phases/01-subcontractor-rate-logic-modification/01-06-SUMMARY.md` describing:
- Final test counts (full pytest tests/ pass count + breakdown of new test classes by plan)
- Whether the production-safety validator window cap was bumped (if so, from N → M with rationale) — and the Warning 8 reconciliation note (cross-reference to Plan 05's 24 kB window)
- The exact Docusaurus pages updated and their final cross-link verification result
- A list of the 7 final variant strings + their routing (which sheet(s) each variant uploads to)
- The operator's human-verification checkpoint approval status + any items they flagged. Specifically note whether Step B (Warning 10 real-data price-write) was completed locally or deferred to a follow-up production run.
- Confirmation that `TestPhase1IntegrationRegression` and `TestPhase1FilenameRoundTripCoverage` are two separate classes (Warning 11 lock-in); the prior `TestPhase1EndToEndByteIdenticalRegression` name does NOT appear anywhere in the codebase.
- A final readiness statement: "Phase 1 is ready to ship via PR. CLAUDE.md Living Ledger entry will be appended autonomously when the PR is merged per the autonomous-cloud-memory-injection rule."
</output>
</output>
