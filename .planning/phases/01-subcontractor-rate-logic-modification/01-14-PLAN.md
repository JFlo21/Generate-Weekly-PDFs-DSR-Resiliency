---
phase: 01-subcontractor-rate-logic-modification
plan: 14
type: execute
wave: 12
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12, 13]
files_modified:
  - .github/workflows/weekly-excel-generation.yml
  - CLAUDE.md
  - tests/test_subcontractor_pricing.py
autonomous: true
requirements: [REVIEW-IN-04, REVIEW-LIVING-LEDGER]
tags: [gap-closure, code-review, workflow, env-pinning, living-ledger, autonomous-cloud-memory-injection]
user_setup: []
must_haves:
  truths:
    - "Per IN-04 + Living Ledger 2026-04-24 14:30 rule (workflow pinning makes the operator-facing feature state code-reviewable through git history), the three new Phase 1 env vars are pinned in the workflow `env:` block with explicit defaults: `SUBCONTRACTOR_RATES_CSV='data/subcontractor_rates.csv'`, `SUBCONTRACTOR_PPP_SHEET_ID='8162920222379908'`, `SUBCONTRACTOR_RATE_VARIANTS_ENABLED='1'`. Optional `AEP_BILLABLE_CUTOFF` is documented as unset-by-default with the rollback path documented in `website/docs/reference/environment.md`."
    - "Per IN-04 contract + the comment block convention from Living Ledger 2026-04-24 14:30, a LEGACY/ROLLBACK comment block above the three pinned vars documents the kill-switch rollback path: set `SUBCONTRACTOR_RATE_VARIANTS_ENABLED='0'` to disable Phase 1 variant generation entirely. The comment block names the responsible Living Ledger date so the operational rationale is one click away."
    - "Per CLAUDE.md AUTONOMOUS CLOUD MEMORY INJECTION (CRITICAL) rule + the standing pattern of timestamped entries, a new Living Ledger entry is appended to `CLAUDE.md` summarizing this gap-closure round (CR-01..CR-03 + WR-01..WR-06 + IN-01..IN-04). The entry follows the established `[YYYY-MM-DD HH:MM]` timestamp format and the multi-rule structure of prior major entries (2026-04-22 / 2026-04-23 round-9 / 2026-04-25 etc.). The entry's NEW rules are documented as numbered guardrails for future contributors."
    - "Per the test contract for ledger entries, a regression test exists that confirms the Living Ledger entry landed (validates the entry text is present in CLAUDE.md) so a future PR cannot silently revert the documentation."
    - "Phase 1 ROADMAP success criterion 5 (byte-identical primary / helper / vac_crew / ORIG-folder hashes) is preserved — this plan touches workflow config + documentation only; no `generate_weekly_pdfs.py` changes."
  artifacts:
    - path: ".github/workflows/weekly-excel-generation.yml"
      provides: "Three new Phase 1 env vars pinned in the workflow `env:` block with operator-rollback comment block referencing Living Ledger 2026-04-24 14:30 + 2026-05-14 (this entry)"
    - path: "CLAUDE.md"
      provides: "Living Ledger entry timestamped 2026-05-14 summarizing the 13-finding gap-closure round; documents 5+ new rules (3-site identity consistency rule, mirror-matcher invariant, explicit PII markers, defensive raise scoping, dual-target cleanup discipline, env-var override safe-parse pattern)"
    - path: "tests/test_subcontractor_pricing.py"
      provides: "TestPhase1GapClosureLedgerEntryPresent regression class — validates the ledger entry text is present in CLAUDE.md"
  key_links:
    - from: "GitHub Actions workflow env block"
      to: "Python pipeline env-var resolution"
      via: "explicitly-pinned defaults for Phase 1 Subcontractor env vars"
      pattern: "SUBCONTRACTOR_RATES_CSV:\\s*'data/subcontractor_rates.csv'|SUBCONTRACTOR_RATES_CSV:.*'data/subcontractor_rates.csv'"
    - from: "Phase 1 gap-closure round"
      to: "future contributor's design constraints"
      via: "Living Ledger entry with numbered rules"
      pattern: "\\[2026-05-14.*Phase 01 gap closure.*CR-01.*WR-04|Phase 01 gap closure"
---

<objective>
Final plan of the Phase 1 gap-closure round. Two operational-hygiene actions
plus the mandatory Living Ledger entry:

1. **IN-04** — Pin the three new Phase 1 env vars in `.github/workflows/weekly-excel-generation.yml`
   with explicit defaults. Currently they rely on Python-side defaults; per Living Ledger
   2026-04-24 14:30 (retired CSV-side rate recalc workflow pinning), explicit pinning at
   the workflow layer makes operator-facing feature state code-reviewable through git
   history and prevents a repo-Variable from silently overriding the default. Pair with
   a comment block documenting the kill-switch rollback path.

2. **CLAUDE.md Living Ledger entry** — Per the autonomous-cloud-memory-injection rule, a
   gap-closure round of this size MUST append a timestamped ledger entry summarizing the
   rules learned, so future contributors and future Claude instances have the operational
   context for why these patterns exist. The entry follows the established multi-rule
   structure: short context paragraph, then numbered rules. The most important new rule
   is the **3-site identity-consistency invariant** (from CR-01): "When adding a new
   variant whose filename embeds an identifier, the identity-tuple construction MUST be
   applied at ALL THREE main-loop sites in sync." This is a class of bug that bit
   Phase 1 once and will bite future variant additions if not documented.

3. **Regression test** — A small source-level test that asserts the Living Ledger entry
   landed (with the timestamp + characteristic substring), so a future PR cannot
   silently revert the documentation while keeping the code changes.

Bundle rationale: IN-04 is a workflow-layer change with no Python touch; the Living
Ledger update is a CLAUDE.md change; the regression test is a one-liner. None of them
touch `generate_weekly_pdfs.py`. Bundling avoids a final-plan churn without coupling
disparate code paths.

Output: workflow env block self-documents Phase 1 feature state; rollback path is
documented in two places (workflow comment + environment.md); future contributors
have a structured Living Ledger entry covering all 13 findings + the rules they
encode; regression test guards against silent ledger reversion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-subcontractor-rate-logic-modification/01-CONTEXT.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-REVIEW.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-07-PLAN.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-08-PLAN.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-09-PLAN.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-10-PLAN.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-11-PLAN.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-12-PLAN.md
@.planning/phases/01-subcontractor-rate-logic-modification/01-13-PLAN.md
@CLAUDE.md

<interfaces>
<!-- IN-04 — the workflow env block -->
.github/workflows/weekly-excel-generation.yml — locate the step-level `env:` block that
includes `SMARTSHEET_API_TOKEN`, `SUPABASE_URL`, `EXTENDED_CHANGE_DETECTION`, etc.
Around line ~216-280 per the earlier grep. The Phase 1 env vars are NOT currently
pinned there — they rely on Python module-level defaults.

The retired CSV-side rate recalc vars (`RATE_CUTOFF_DATE`, `NEW_RATES_CSV`,
`OLD_RATES_CSV`) ARE pinned per the 2026-04-24 14:30 ledger entry — they sit in the
same `env:` block as legacy placeholders. The new IN-04 block follows that template.

<!-- CLAUDE.md Living Ledger structure -->
CLAUDE.md — locate by the "## Living Ledger (Auto-Updated Context)" heading. Entries
are appended chronologically; the newest entry is the LAST bullet in the section.
The 2026-04-25 14:00 entry is the most recent (freeze_row parallelization). The new
2026-05-14 entry appends AFTER it.

Entry format (mirror the established pattern from 2026-04-22 / 2026-04-23 / 2026-04-25):
  - Opening: `- [YYYY-MM-DD HH:MM]` followed by a 1-2 sentence context paragraph.
  - Body: "**Root cause**" / "**Fix**" / "**New rules:**" multi-paragraph structure for
    significant entries; numbered rules for the takeaways future contributors must
    apply.
  - Regression-test reference at the bottom: which test classes were added.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IN-04 — Pin Phase 1 env vars in `weekly-excel-generation.yml`</name>
  <files>.github/workflows/weekly-excel-generation.yml</files>
  <read_first>
    - `.github/workflows/weekly-excel-generation.yml` — `grep -n "env:" .github/workflows/weekly-excel-generation.yml` to locate ALL env blocks. The STEP-level `env:` block (the larger one containing `SMARTSHEET_API_TOKEN`, `SUPABASE_URL`, etc.) is the target.
    - `.github/workflows/weekly-excel-generation.yml` — `grep -n "RATE_CUTOFF_DATE\\|NEW_RATES_CSV\\|OLD_RATES_CSV" .github/workflows/weekly-excel-generation.yml` to locate the existing LEGACY-pinned env vars. Read the comment block above them — IN-04's new comment block mirrors its style.
    - `.planning/phases/01-subcontractor-rate-logic-modification/01-REVIEW.md` IN-04 in full.
    - `CLAUDE.md` Living Ledger 2026-04-24 14:30 (the established workflow-pinning rationale).
  </read_first>
  <action>
Locate the step-level `env:` block in `.github/workflows/weekly-excel-generation.yml`
(the block containing `SMARTSHEET_API_TOKEN: ${{ secrets.SMARTSHEET_API_TOKEN }}`).
Find the section where the retired CSV-side rate-recalc vars (`RATE_CUTOFF_DATE`,
`NEW_RATES_CSV`, `OLD_RATES_CSV`) are pinned with the LEGACY comment block.

Add a new block IMMEDIATELY AFTER (or wherever is logically grouped with the retired
LEGACY block — the order is operator-readability, not functional). The new block:

```yaml
          # ─────────────────────────────────────────────────────
          # Phase 1: Subcontractor Rate Logic — workflow pinning
          # ─────────────────────────────────────────────────────
          # Pin Phase 1 feature defaults explicitly so the
          # operator-facing feature state is visible at the
          # workflow layer and a repo Variable cannot silently
          # override these without code review.
          #
          # Rollback path: set
          # ``SUBCONTRACTOR_RATE_VARIANTS_ENABLED: '0'`` to
          # disable ALL Phase 1 variant generation
          # (_AEPBillable, _ReducedSub, and their helper-shadow
          # twins). The existing primary / helper / vac_crew /
          # ORIG-folder pipelines are unaffected.
          #
          # Operator-actionable env vars and their meanings are
          # documented in ``website/docs/reference/environment.md``
          # — see the SUBCONTRACTOR_RATE_VARIANTS_ENABLED,
          # SUBCONTRACTOR_RATES_CSV, SUBCONTRACTOR_PPP_SHEET_ID,
          # and AEP_BILLABLE_CUTOFF sections. Workflow pinning
          # rationale: Living Ledger 2026-04-24 14:30 (the
          # retired CSV-side rate-recalc vars established this
          # pattern after a Variable-driven override caused a
          # silent regression).
          SUBCONTRACTOR_RATES_CSV: 'data/subcontractor_rates.csv'
          SUBCONTRACTOR_PPP_SHEET_ID: '8162920222379908'
          SUBCONTRACTOR_RATE_VARIANTS_ENABLED: '1'
          # AEP_BILLABLE_CUTOFF is intentionally UNSET in the
          # workflow — the Python default (datetime.date(2026,
          # 4, 12), the contract award date) is the source of
          # truth. Set this env var at the workflow level only
          # if a contract re-negotiation or retroactive billing
          # decision requires moving the cutoff. Format:
          # YYYY-MM-DD. Invalid values fail-safe to the default
          # with an error log. See Plan 01-11 / Living Ledger
          # 2026-05-14 for the safe-parse contract.
```

Notes:
- The indentation MUST match the surrounding env block (typically 10 spaces — read
  the existing env block first).
- DO NOT add `AEP_BILLABLE_CUTOFF` as a pinned default. Per IN-01 (plan 01-11),
  the contract is "unset = use Python default; set = override at workflow level."
  Pinning it would force every workflow edit to also touch the cutoff value,
  obscuring the operator-facing knob.
- DO NOT remove or modify any existing env-var pinnings (including the LEGACY block
  for retired CSV-side recalc vars).
- DO NOT use `@cell` (CLAUDE.md absolute ban, even though it's a YAML file).
- The new block goes BEFORE the line `# Basic Operation Controls` or wherever the
  natural grouping break occurs — read the surrounding env block to pick the spot.
  </action>
  <verify>
    <automated>grep -c "SUBCONTRACTOR_RATES_CSV:" .github/workflows/weekly-excel-generation.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SUBCONTRACTOR_RATES_CSV: 'data/subcontractor_rates.csv'" .github/workflows/weekly-excel-generation.yml` returns 1.
    - `grep -c "SUBCONTRACTOR_PPP_SHEET_ID: '8162920222379908'" .github/workflows/weekly-excel-generation.yml` returns 1.
    - `grep -c "SUBCONTRACTOR_RATE_VARIANTS_ENABLED: '1'" .github/workflows/weekly-excel-generation.yml` returns 1.
    - `grep -c "Rollback path:" .github/workflows/weekly-excel-generation.yml` returns 1 (comment block landed).
    - `grep -c "AEP_BILLABLE_CUTOFF" .github/workflows/weekly-excel-generation.yml` returns >= 1 (documented as intentionally unset).
    - `grep -c "Living Ledger 2026-04-24 14:30" .github/workflows/weekly-excel-generation.yml` returns 1 (cross-ref to the workflow-pinning rationale source).
    - **YAML validity check (best effort, if `python -c "import yaml"` is available):** `python -c "import yaml; yaml.safe_load(open('.github/workflows/weekly-excel-generation.yml'))"` exits 0. If PyYAML is not available in the execution environment, skip this check and document in the SUMMARY.
  </acceptance_criteria>
  <done>
    Three Phase 1 env vars pinned with explicit defaults; LEGACY comment block
    documents the rollback path; AEP_BILLABLE_CUTOFF intentionally unset with
    workflow comment naming the override pattern; existing env-var pinnings
    unchanged; YAML still parses.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Living Ledger entry to `CLAUDE.md`</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` — `grep -n "## Living Ledger (Auto-Updated Context)" CLAUDE.md` to locate the section. Read the LAST entry in the section to confirm format (the 2026-04-25 14:00 freeze_row parallelization entry).
    - `.planning/phases/01-subcontractor-rate-logic-modification/01-REVIEW.md` — full file (you'll summarize all 13 findings).
    - All `.planning/phases/01-subcontractor-rate-logic-modification/01-NN-PLAN.md` for NN in 07..13 (you'll cross-reference the plan numbers in the rules).
  </read_first>
  <action>
Append a new entry to the Living Ledger section in `CLAUDE.md`, AFTER the last
existing entry (the 2026-04-25 14:00 freeze_row parallelization entry). Use the
established format (timestamp + multi-paragraph body + numbered rules + regression
test reference).

```markdown
- [2026-05-14 23:55] Phase 01 (Subcontractor Rate Logic
  Modification) gap-closure round: post-merge code review
  (`/gsd-code-review 01`) surfaced **3 BLOCKER + 6 WARNING + 4
  INFO findings (13 total)** against the freshly-shipped Phase 1
  variant infrastructure. All 12 actionable findings (excluding
  the reference-only IN-03 hoist) closed by additive plans
  01-07 through 01-14. **Root causes** clustered around three
  classes of bug that the upstream unit-test suite did not
  catch because tests exercised helpers in isolation rather
  than the full main-loop attachment-identity / filter
  pipeline. **(1) Identity-tuple drift across the three
  main-loop sites** (CR-01): the per-group ``identifier`` /
  ``file_identifier`` construction, the ``valid_wr_weeks``
  cleanup-tuple builder, and the ``current_keys``
  hash-history-prune key all build the same identity tuple
  from row data and must stay in lockstep. Pre-fix the new
  ``aep_billable_helper`` / ``reduced_sub_helper`` variants
  fell through to the legacy ``User``-derived ``else`` branch
  at ALL THREE sites — masked by accident because two of the
  three wrongs (``identifier=''`` everywhere) cancelled out
  for hash-history, but the third (``file_identifier=''``
  versus parsed ``'Jane_Smith'``) silently broke
  ``_has_existing_week_attachment`` matching and
  ``delete_old_excel_attachments`` deletion for every
  helper-shadow attachment on every 2h cron run. Result:
  permanent regeneration churn + orphan accumulation on
  ``SUBCONTRACTOR_PPP_SHEET_ID`` (which has no end-of-run
  cleanup pre-WR-01). **(2) Filter matchers missed the
  four new variant suffix shapes** (CR-02, CR-03): the
  production ``EXCLUDE_WRS`` matcher (no TEST_MODE gate)
  and the TEST_MODE ``WR_FILTER`` matcher each carried 4
  hard-coded suffix patterns
  (``WR``, ``WR_HELPER_*``, ``WR_USER_*``, ``WR_VACCREW``)
  and missed the four new shapes
  (``WR_REDUCEDSUB``, ``WR_AEPBILLABLE``,
  ``WR_REDUCEDSUB_HELPER_*``, ``WR_AEPBILLABLE_HELPER_*``).
  Operator excluding a WR silently uploaded the new variants
  to BOTH target sheets; operator running the documented
  Step B diagnostic command saw zero new-variant output.
  **(3) PPP sheet had no symmetric end-of-run cleanup pass**
  (WR-01) AND **no attachment-prefetch participation**
  (WR-05): every ``_ReducedSub*`` upload to PPP paid an
  extra per-row ``list_row_attachments`` API call, and any
  identity-drift past the per-row delete path silently
  orphaned attachments permanently. Plus six smaller
  findings: env-var resolution asymmetry on
  ``SUBCONTRACTOR_PPP_SHEET_ID=''`` (WR-02), defensive raises
  on the new helper-shadow filename-suffix branches (WR-03),
  explicit PII markers for the new helper-shadow GROUP
  CREATED logs (WR-04), missing-CU attribution loop
  standardization on ``__source_sheet_id`` (WR-06),
  workflow env-var pinning (IN-04), env-overridable
  ``AEP_BILLABLE_CUTOFF`` (IN-01), and explicit
  ``Quantity`` coercion (IN-02). **Fix (additive,
  production-safe across 8 plans):** all changes are
  surgical — no existing test regresses, ROADMAP Phase 1
  success criterion 5 (byte-identical primary / helper /
  vac_crew / ORIG-folder hashes) preserved. **New rules:**
  (1) **Three-site identity-consistency invariant for new
  variants.** When adding a new variant whose filename
  embeds an identifier (helper foreman name, vac crew
  name, USER name, future variant), the identity-tuple
  construction MUST be applied at ALL THREE main-loop
  sites in sync: (a) the per-group
  ``identifier`` / ``file_identifier`` construction
  immediately before
  ``history_key = f"{wr_num}|{week_raw}|{variant}|{identifier}"``,
  (b) the ``valid_wr_weeks.add(...)`` cleanup-tuple
  builder that feeds
  ``cleanup_untracked_sheet_attachments``, AND (c) the
  ``current_keys`` set construction inside the
  ``if history_updates: ... if not _time_budget_exceeded:``
  hash-history-prune block. The three are siblings that
  ALL rebuild the same logical identity tuple; drift
  between any of them silently breaks attachment-identity
  matching or hash-history persistence. Adding a fourth
  variant in the future MUST apply the fix at all three
  sites simultaneously, and the existing source-level grep
  test in
  ``TestHelperShadowVariantFileIdentifier::test_production_valid_wr_weeks_and_current_keys_carry_shadow_variant_gate``
  is the regression guard. (2) **Mirror-matcher invariant
  for variant-aware filter functions.** Whenever a new
  variant emits a new group-key suffix shape from
  ``group_source_rows``, the ``_key_matches_wr`` AND
  ``_key_matches_excluded_wr`` matchers (nested inside
  ``group_source_rows``) MUST both be extended with the
  new suffix pattern. They are siblings; drift between
  them produces "operator excluded the WR but new
  variants still uploaded" (CR-02) or "operator filtered
  the WR but TEST_MODE produced no new-variant output"
  (CR-03). Existing tests:
  ``TestExcludeWrsMatchesAllVariants`` +
  ``TestWrFilterMatchesAllVariants``. (3) **Explicit PII
  markers for new INFO-level group-creation logs.** Per
  the 2026-04-20 12:00 rule, adding a new INFO log that
  embeds row content (foreman name / dept / job / CU /
  price) requires extending ``_PII_LOG_MARKERS`` in the
  SAME PR. The 2026-05-14 round caught a fragile-by-accident
  case where the new helper-shadow GROUP CREATED logs
  matched the substring ``"HELPER GROUP CREATED"`` only by
  string containment of the legacy marker. The new rule
  refines: when a new log shares a substring with an
  existing marker, add an EXPLICIT marker for the new
  text body — relying on accidental substring containment
  is fragile to future wording rewordings. (4) **Defensive
  raise scope discipline.** When adding a defensive
  ``raise ValueError`` to a NEW branch (e.g., a new variant
  filename-suffix builder), do NOT broaden the raise to
  pre-existing branches with the same code shape — even if
  the legacy branch carries the identical silent-fallthrough
  bug. Legacy branches have a longer test history and
  unknown downstream consumers; broadening the raise risks
  production regression. Add a TODO comment ABOVE the
  legacy branch instead, scoped as follow-up tech-debt
  cleanup. WR-03's regression test
  (``TestHelperShadowSuffixDefensiveRaise::test_legacy_helper_branch_does_not_raise_on_empty_foreman``)
  is the immutability guard. (5) **Dual-target cleanup
  invocation pattern.** When a phase adds a SECOND
  attachment target sheet (Phase 1's
  ``SUBCONTRACTOR_PPP_SHEET_ID``), every defense-in-depth
  layer that operates on TARGET_SHEET_ID MUST be replicated
  for the new sheet: (a) target_map / collision quarantine
  (Plan 04 already), (b) attachment prefetch (Plan 12 /
  WR-05), AND (c) end-of-run
  ``cleanup_untracked_sheet_attachments`` pass (Plan 13 /
  WR-01). Skipping any layer creates an orphan-accumulation
  trap because the per-row ``delete_old_excel_attachments``
  is correctness-critical but not exception-safe; the
  end-of-run cleanup is the belt-and-suspenders defense
  and is REQUIRED for every target sheet. (6) **Env-var
  override safe-parse pattern.** When exposing a previously
  hardcoded module constant via an env var (IN-01's
  ``AEP_BILLABLE_CUTOFF``), the resolution MUST: (a) accept
  empty-string as unset (use default), (b) wrap the
  ``strptime`` / ``int`` / ``parse`` call in
  ``try / except (ValueError, TypeError):`` with
  fallback-to-default + error log, (c) name the resolved
  value in the startup banner, AND (d) document the format
  + invalid-value-fallback contract in
  ``website/docs/reference/environment.md``. Operators
  should NEVER be able to crash the loader's module-import
  with a malformed env-var value. (7) **Workflow pinning
  for new feature env vars.** Per 2026-04-24 14:30 (retired
  CSV-side recalc vars), every new operator-facing kill
  switch or feature-default env var MUST be pinned in
  ``.github/workflows/weekly-excel-generation.yml`` with
  an explicit default. Workflow pinning makes the active
  feature state code-reviewable through git history; a
  repo-Variable that re-introduces a value can no longer
  silently override the default. Phase 1 added three pinned
  vars (``SUBCONTRACTOR_RATES_CSV``,
  ``SUBCONTRACTOR_PPP_SHEET_ID``,
  ``SUBCONTRACTOR_RATE_VARIANTS_ENABLED``); the optional
  ``AEP_BILLABLE_CUTOFF`` is documented as intentionally
  unset with the override pattern. Regression tests: 8 new
  classes across ``tests/test_subcontractor_pricing.py``
  (TestHelperShadowVariantFileIdentifier,
  TestSubcontractorPppSheetIdEmptyStringDisable,
  TestHelperShadowSuffixDefensiveRaise,
  TestAepBillableCutoffEnvVarOverride,
  TestResolveRowPriceQuantityCoercion,
  TestPhase1GapClosureLedgerEntryPresent) plus
  ``tests/test_security_audit_followup.py``
  (TestExcludeWrsMatchesAllVariants,
  TestWrFilterMatchesAllVariants,
  TestPiiLogMarkersIncludeSubcontractorVariants
  extended +2 methods, TestSourceSheetIdFieldConsistency,
  TestPppCleanupUntrackedAttachments) and
  ``tests/test_performance_optimizations.py``
  (TestPppAttachmentPrefetchBudget). Total: ~55-65 new
  tests; ``pytest tests/`` continues to exit 0 at every
  plan-completion checkpoint.
```

Notes:
- The text above is the FULL ENTRY — paste it character-for-character at the end of
  the Living Ledger section. Adjust the timestamp suffix (`23:55`) if you want a more
  precise commit-time stamp, but per CLAUDE.md the format is just
  `[YYYY-MM-DD HH:MM]`.
- The entry references Plan-NN tags + Living Ledger date stamps for cross-reference
  density. Do NOT replace any of those references with line numbers — line numbers
  drift per the 2026-04-22 rule.
- DO NOT use `@cell` (CLAUDE.md absolute ban).
- DO NOT modify any existing entry in the Living Ledger. The append is purely
  additive (mirrors every prior entry's discipline).
- Markdown formatting: the new entry is a single top-level `- ` bullet at the same
  indentation as the existing entries. The body's narrative paragraphs are run-on
  within the bullet (not separated by blank lines) to match the visual style of
  prior major entries (e.g., the 2026-04-23 round-9 entry).
  </action>
  <verify>
    <automated>grep -c "\\[2026-05-14" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\\[2026-05-14" CLAUDE.md` returns >= 1 (the new entry timestamp present).
    - `grep -c "Phase 01 (Subcontractor Rate Logic" CLAUDE.md` returns >= 1.
    - `grep -c "Three-site identity-consistency invariant" CLAUDE.md` returns 1 (rule 1 landed).
    - `grep -c "Mirror-matcher invariant" CLAUDE.md` returns 1 (rule 2 landed).
    - `grep -c "Explicit PII markers" CLAUDE.md` returns 1 (rule 3 landed).
    - `grep -c "Defensive raise scope discipline" CLAUDE.md` returns 1 (rule 4).
    - `grep -c "Dual-target cleanup invocation pattern" CLAUDE.md` returns 1 (rule 5).
    - `grep -c "Env-var override safe-parse pattern" CLAUDE.md` returns 1 (rule 6).
    - `grep -c "Workflow pinning for new feature env vars" CLAUDE.md` returns 1 (rule 7).
    - The new entry is the LAST bullet in the Living Ledger section (no other entry appears after it).
  </acceptance_criteria>
  <done>
    Living Ledger entry appended with timestamp, multi-paragraph context, 7 numbered
    new rules, and regression-test reference. Future contributors can read this
    single entry and apply the Phase 1 gap-closure round's rules to subsequent
    variant additions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression test — `TestPhase1GapClosureLedgerEntryPresent`</name>
  <files>tests/test_subcontractor_pricing.py</files>
  <read_first>
    - `tests/test_subcontractor_pricing.py` — append a new class at the very end (after all classes added by Plans 07-13). Locate placement by `grep -n "^class TestPhase1FilenameRoundTripCoverage" tests/test_subcontractor_pricing.py`.
  </read_first>
  <behavior>
    - Test 1: `CLAUDE.md` contains the new entry's timestamp prefix `[2026-05-14`.
    - Test 2: `CLAUDE.md` contains the characteristic phrase identifying this gap-closure round.
    - Test 3: `CLAUDE.md` contains all 7 new-rule names (one assertion per rule).
    - Test 4: The entry appears AFTER the last pre-existing entry (the 2026-04-25 14:00 freeze_row entry).
  </behavior>
  <action>
Append a new test class to `tests/test_subcontractor_pricing.py` at the END of the file
(after `TestPhase1FilenameRoundTripCoverage` and any classes added by Plans 11 / 13).

```python
class TestPhase1GapClosureLedgerEntryPresent(unittest.TestCase):
    """Phase 01 gap closure (Living Ledger / autonomous-cloud-memory):
    a CLAUDE.md Living Ledger entry timestamped 2026-05-14 MUST
    document the 13-finding gap-closure round and the 7 new rules
    encoded therein. Per CLAUDE.md's "AUTONOMOUS CLOUD MEMORY
    INJECTION (CRITICAL)" rule, any architectural standard /
    recurring fix / new operational rule introduced by a feature
    PR must be appended to the Living Ledger in the same PR.

    These source-level guards catch silent reversion of the
    documentation by a future PR.
    """

    @staticmethod
    def _read_ledger() -> str:
        # CLAUDE.md is at the repo root, sibling to
        # generate_weekly_pdfs.py.
        import pathlib
        repo_root = pathlib.Path(
            generate_weekly_pdfs.__file__,
        ).parent
        return (repo_root / 'CLAUDE.md').read_text(encoding='utf-8')

    def test_timestamp_present(self):
        ledger = self._read_ledger()
        self.assertIn('[2026-05-14', ledger)

    def test_round_summary_phrase_present(self):
        ledger = self._read_ledger()
        self.assertIn(
            'Phase 01 (Subcontractor Rate Logic Modification) gap-closure',
            ledger,
        )

    def test_all_seven_new_rules_named(self):
        ledger = self._read_ledger()
        expected_rules = (
            'Three-site identity-consistency invariant',
            'Mirror-matcher invariant',
            'Explicit PII markers',
            'Defensive raise scope discipline',
            'Dual-target cleanup invocation pattern',
            'Env-var override safe-parse pattern',
            'Workflow pinning for new feature env vars',
        )
        for rule in expected_rules:
            with self.subTest(rule=rule):
                self.assertIn(
                    rule, ledger,
                    f"Living Ledger 2026-05-14 entry must name "
                    f"the rule {rule!r}. See plan 01-14.",
                )

    def test_entry_appears_after_2026_04_25_freeze_row_entry(self):
        ledger = self._read_ledger()
        prev_idx = ledger.find('[2026-04-25 14:00]')
        new_idx = ledger.find('[2026-05-14')
        self.assertGreater(prev_idx, 0, "Prior 2026-04-25 14:00 entry not found.")
        self.assertGreater(new_idx, 0, "New 2026-05-14 entry not found.")
        self.assertLess(
            prev_idx, new_idx,
            "New 2026-05-14 entry must appear AFTER the 2026-04-25 "
            "14:00 freeze_row entry (Living Ledger entries are "
            "chronologically appended).",
        )

    def test_entry_references_regression_test_classes(self):
        # The ledger entry should name the regression-test classes
        # added by this round so future maintainers can trace
        # rule → test directly.
        ledger = self._read_ledger()
        for cls_name in (
            'TestHelperShadowVariantFileIdentifier',
            'TestExcludeWrsMatchesAllVariants',
            'TestWrFilterMatchesAllVariants',
            'TestPppAttachmentPrefetchBudget',
            'TestPppCleanupUntrackedAttachments',
        ):
            with self.subTest(cls_name=cls_name):
                self.assertIn(
                    cls_name, ledger,
                    f"Living Ledger 2026-05-14 entry must reference "
                    f"{cls_name} so future readers can trace rule → test."
                )
```

Notes:
- This test class also serves as a HARD INDEX of which test classes the 2026-05-14
  ledger entry mentions. If a future PR adds or removes a regression class from this
  round retroactively, both Task 2's ledger body and this test class's
  `cls_name` tuple must update in lockstep.
- The test class is in `test_subcontractor_pricing.py` rather than a new file because
  every test classes added by 01-07 through 01-13 are scattered across three test
  files; placing the index in this file keeps it discoverable next to the
  `TestPhase1IntegrationRegression` and similar Phase-1-themed classes.
  </action>
  <verify>
    <automated>pytest tests/test_subcontractor_pricing.py::TestPhase1GapClosureLedgerEntryPresent -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_subcontractor_pricing.py::TestPhase1GapClosureLedgerEntryPresent -v` exits 0 with at least 5 tests passing.
    - `grep -c "class TestPhase1GapClosureLedgerEntryPresent" tests/test_subcontractor_pricing.py` returns 1.
    - `pytest tests/ -v` exits 0 (full suite green).
    - `python -m py_compile tests/test_subcontractor_pricing.py` exits 0.
  </acceptance_criteria>
  <done>
    Regression test class proves the Living Ledger entry landed with: correct
    timestamp, characteristic phrase, all 7 named rules, chronological-append
    discipline, and named regression-test classes.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `pytest tests/ -v` exits 0 — full suite green plus ~5 new tests in `TestPhase1GapClosureLedgerEntryPresent`.
2. `python -m py_compile tests/test_subcontractor_pricing.py` exits 0.
3. `grep -c "SUBCONTRACTOR_RATES_CSV: 'data/subcontractor_rates.csv'" .github/workflows/weekly-excel-generation.yml` returns 1.
4. `grep -c "\\[2026-05-14" CLAUDE.md` returns >= 1.
5. `grep -c "Three-site identity-consistency invariant" CLAUDE.md` returns 1.
6. `grep -c "@cell" generate_weekly_pdfs.py" returns 0` (no Python change in this plan; sanity).
</verification>

<success_criteria>
- REVIEW-IN-04 closed: Phase 1 env vars pinned in workflow with rollback path documented;
  operator-facing feature state is code-reviewable through git history.
- Living Ledger contract honored per CLAUDE.md AUTONOMOUS CLOUD MEMORY INJECTION
  (CRITICAL) rule: this gap-closure round's 7 new rules are documented in a
  timestamped entry future contributors can apply.
- Regression test guards against silent ledger reversion by future PRs.
- Phase 1 ROADMAP success criteria preserved — no `generate_weekly_pdfs.py` touch.
</success_criteria>

<output>
After completion, create `.planning/phases/01-subcontractor-rate-logic-modification/01-14-SUMMARY.md`
documenting: workflow env-var diff (3 vars pinned); Living Ledger entry length (lines
appended); 7 new rules summary table; test counts; final wave-12 completion confirmation
that closes Phase 1's gap-closure round and unblocks the merge.
</output>
