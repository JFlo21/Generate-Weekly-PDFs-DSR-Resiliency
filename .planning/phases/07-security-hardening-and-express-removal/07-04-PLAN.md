---
phase: 07-security-hardening-and-express-removal
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - portal-v2/package.json
  - portal-v2/package-lock.json
  - .planning/phases/07-security-hardening-and-express-removal/07-SECURITY.md
autonomous: false
requirements: [SEC-04]
user_setup:
  - service: vercel
    why: "The header-presence curl check + the documented one-time SEC-04 sign-off run against the live Vercel deploy URL"
    dashboard_config:
      - task: "Provide the live production Vercel URL for the SEC-02 header curl check and the SEC-04 audit sign-off"
        location: "Vercel Dashboard → Project → Deployments → Production domain"

must_haves:
  truths:
    - "A /security-review pass + gsd-secure-phase 07 run finds no unresolved HIGH/critical RLS, signed-URL, or secret-handling findings"
    - "Every Phase 07 plan threat is dispositioned (mitigate/accept/transfer) with evidence in 07-SECURITY.md"
    - "The Phase 06 D-04 Realtime mitigations (count-only payload, role-gated subscription, RLS-per-subscriber) are re-verified present in code"
    - "The 2 critical + 5 high Dependabot CVEs on the deployed portal-v2/website surface are remediated; the 15 moderate are logged as deferred follow-ups"
    - "All 5 SEC-02 headers are confirmed present on the live Vercel response via curl"
  artifacts:
    - path: ".planning/phases/07-security-hardening-and-express-removal/07-SECURITY.md"
      provides: "Phase 07 threat register, SEC-03 verification, Phase 06 D-04 re-verification, probe results, header verification, accepted-risks log, audit trail"
      contains: "## Threat Register"
    - path: "portal-v2/package.json"
      provides: "Remediated dependency versions for the critical/high CVEs (e.g. vitest major bump)"
  key_links:
    - from: "07-SECURITY.md threat register"
      to: "implemented mitigations across 07-01/02/03 + Phase 06 useRealtimeArtifacts.ts"
      via: "gsd-security-auditor evidence with file:line cites"
      pattern: "T-07-"
    - from: "portal-v2/package.json"
      to: "npm audit (no remaining critical/high on the deployed surface)"
      via: "targeted version bumps"
      pattern: "vitest"
---

<objective>
Close the phase with the SEC-04 security audit: run `/security-review` AND
`gsd-secure-phase 07`, resolve every HIGH/critical RLS / signed-URL / secret-handling
finding, re-verify the Phase 06 D-04 Realtime mitigations exist in code, remediate
the critical/high Dependabot CVEs on the deployed surface (defer the 15 moderate),
and document everything in a new `07-SECURITY.md`.

Purpose: This is the milestone-gating audit. It consolidates the evidence produced
by 07-01 (headers/CSP), 07-02 (RLS/signed-URL probe + SEC-05 audit), and 07-03
(Express removal + SEC-03 gate) into a single threat register with dispositions, and
proves the deployed dependency surface carries no unaddressed critical/high CVE.

Output: A remediated `portal-v2/package.json` (+ lockfile) with the critical/high
CVEs fixed and the 107-test suite still green; a complete `07-SECURITY.md` with
`threats_open: 0` and a documented one-time live sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-security-hardening-and-express-removal/07-CONTEXT.md
@.planning/phases/07-security-hardening-and-express-removal/07-RESEARCH.md
@.planning/phases/07-security-hardening-and-express-removal/07-PATTERNS.md
@.planning/phases/03-supabase-data-layer-foundation/03-SECURITY.md
@portal-v2/src/hooks/useRealtimeArtifacts.ts

<interfaces>
<!-- 07-SECURITY.md analog: .planning/phases/03-supabase-data-layer-foundation/03-SECURITY.md.
     gsd-secure-phase 07 reads each Phase 07 PLAN.md <threat_model> block + SUMMARY threat flags,
     spawns gsd-security-auditor to verify each mitigation in code, and writes 07-SECURITY.md. -->

Phase 06 D-04 Realtime mitigations to re-verify (RESEARCH §Question 6, verified 2026-06-02):
- portal-v2/src/hooks/useRealtimeArtifacts.ts:
  - Layer 1 (role gate):   `if (loading || (!isBilling && !isAdmin)) return;`
  - Layer 3 (count-only):   `(_payload) => { setPendingCount((n) => n + 1); }`  // _payload NEVER enters state
  - Layer 2 (cleanup):      `void channel.unsubscribe()` in useEffect cleanup
- Supabase Realtime RLS-per-subscriber: postgres_changes only delivers rows the subscriber can read.

Dependabot (use `npm audit` as ground truth at execution time — counts drift; RESEARCH §Question 7):
- vitest CRITICAL (dev-only, not shipped): `npm install vitest@^4` (semver-major) → re-run npm test.
- Moderate (postcss, ws transitive): `npm audit fix` separately (no breaking change).
- portal/ CVEs auto-resolved by its deletion in 07-03. website/ : run `npm audit` and triage critical/high.

07-SECURITY.md required sections (03-SECURITY.md analog + D-05 additions):
  frontmatter (phase:7, status, threats_open, asvs_level:1), ## Trust Boundaries, ## Threat Register,
  ## Accepted Risks Log, ## Security Audit Trail, ## Sign-Off,
  PLUS Phase-07 additions: ## SEC-03 Verification, ## Phase 06 D-04 Realtime Mitigations,
  ## Probe Results (D-07/D-08), ## Header Verification (SEC-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Triage + remediate critical/high Dependabot CVEs on the deployed surface (defer the 15 moderate)</name>
  <read_first>
    - .planning/phases/07-security-hardening-and-express-removal/07-RESEARCH.md §"Question 7" (Dependabot triage — vitest major bump, npm audit as ground truth) + Pitfall 6 (vitest major bump breaking config)
    - .planning/phases/07-security-hardening-and-express-removal/07-CONTEXT.md §D-06 (fix critical/high; defer 15 moderate)
    - portal-v2/package.json (the dependency versions to bump)
  </read_first>
  <action>
    Use `npm audit` output as ground truth (RESEARCH §Question 7 notes the CONTEXT
    "2 critical + 5 high" count may have drifted; local audit on 2026-06-02 showed 1
    critical + 6 moderate). Triage and remediate ONLY the critical/high advisories on
    the DEPLOYED surface (portal-v2 + website). DEFER the 15 moderate as a tracked
    follow-up — do NOT run a blanket `npm audit fix --force`.

    1. `cd portal-v2 && npm audit --json` — enumerate current critical/high advisories.
    2. For the vitest CRITICAL (dev-only, NOT shipped to Vercel — a major bump per
       RESEARCH): `cd portal-v2 && npm install vitest@^4`. Then immediately
       `npm test` to confirm the 107 tests still pass; review vite.config.ts /
       vitest config for any deprecated 2.x→4.x options (RESEARCH Pitfall 6:
       globals, coverage provider, jsdom setup; check @testing-library/jest-dom +
       jest-axe peer deps). Fix any config breakage so the suite is green.
    3. For any OTHER critical/high (non-vitest) advisory: apply the targeted
       `npm install <pkg>@<fixed-version>` per advisory (NOT --force). Re-run
       `npm test` + `npm run build` after.
    4. `cd website && npm audit` — triage website/ critical/high the same way
       (targeted bumps); run `cd website && npm run build` to confirm Docusaurus
       still builds. (RESEARCH Open Question 3 — website audit not yet run.)
    5. Record the moderate advisories (count + package names) as a deferred follow-up
       to capture in 07-SECURITY.md's Accepted Risks Log (per D-06 / CONTEXT Deferred).

    Do NOT touch portal/ — it was deleted in 07-03, so its CVEs are already resolved.
  </action>
  <verify>
    <automated>cd portal-v2 && npm audit --audit-level=high --omit=dev ; echo "RUNTIME_AUDIT_EXIT:$?" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd portal-v2 && npm audit --audit-level=high --omit=dev` reports 0 critical/high vulnerabilities on the production (non-dev) dependency surface (exit 0).
    - `cd portal-v2 && npm test` passes the full suite (vitest major bump did not regress the 107 tests).
    - `cd portal-v2 && npm run build` exits 0.
    - `cd website && npm run build` exits 0 after any website/ critical/high bumps.
    - `grep -c '"vitest"' portal-v2/package.json` shows the bumped major (`^4` or later), OR the SUMMARY documents why a different remediation was applied per live npm audit.
    - The deferred moderate advisories (count + packages) are written down for 07-SECURITY.md's Accepted Risks Log.
  </acceptance_criteria>
  <done>The deployed portal-v2 + website surfaces carry no remaining critical/high CVE, the test suite + builds are green, and the deferred moderates are recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run /security-review + gsd-secure-phase 07; author 07-SECURITY.md (consolidate all phase evidence)</name>
  <read_first>
    - .planning/phases/03-supabase-data-layer-foundation/03-SECURITY.md (the 07-SECURITY.md analog — frontmatter, Trust Boundaries, Threat Register, Accepted Risks, Audit Trail, Sign-Off)
    - .planning/phases/07-security-hardening-and-express-removal/07-PATTERNS.md §"07-SECURITY.md" (frontmatter + section patterns + the Phase-07-specific additions)
    - .planning/phases/07-security-hardening-and-express-removal/07-RESEARCH.md §"Question 6" (gsd-secure-phase mechanics + Phase 06 D-04 code-verified evidence) + §"Security Domain" (ASVS categories + threat patterns)
    - .planning/phases/07-security-hardening-and-express-removal/07-CONTEXT.md §D-05
    - portal-v2/src/hooks/useRealtimeArtifacts.ts (re-verify the 3 D-04 layers in code)
    - The 07-01-SUMMARY.md, 07-02-SUMMARY.md, 07-03-SUMMARY.md (evidence to consolidate: header walkthrough, probe results, grep-gate output, smoke test)
  </read_first>
  <action>
    Run the SEC-04 audit and author the phase security doc. Per D-05, invoke BOTH:
    - `/security-review` skill — audits portal-v2 code + the live RLS/Storage/headers/
      secrets posture, adversarial stance ("assume each mitigation absent until grep
      proves it").
    - `gsd-secure-phase 07` workflow — reads each Phase 07 PLAN.md `<threat_model>`
      block + SUMMARY threat flags, spawns gsd-security-auditor to verify each
      declared mitigation in implemented code, and produces/updates 07-SECURITY.md.
      It BLOCKS advancement if threats_open > 0.

    Create `.planning/phases/07-security-hardening-and-express-removal/07-SECURITY.md`
    using the 03-SECURITY.md structure (07-PATTERNS.md gives the exact patterns):

    - Frontmatter: `phase: 7`, `slug: security-hardening-and-express-removal`,
      `status: open` (→ `verified` once threats_open=0), `threats_open: N`,
      `asvs_level: 1`, `created: 2026-06-XX`.
    - `## Trust Boundaries` — anon→PostgREST, pending→artifacts/storage,
      browser→Vercel Edge (headers/CSP), GitHub Actions→Supabase (service_role).
    - `## Threat Register` — consolidate ALL T-07-* threats from the four plans'
      `<threat_model>` blocks (07-01 headers/CSP, 07-02 probe/RLS, 07-03 Express
      removal/SEC-03, plus this plan's CVE threats). Each row: Threat ID, Category,
      Component, Disposition, Mitigation (with file:line evidence), Status. Every
      HIGH/critical must be `closed`.
    - `## SEC-03 Verification` — paste the 07-03 grep-gate output proving SERVICE_ROLE
      and VITE_API_BASE_URL absence from portal-v2/src + the operator's Vercel-env
      removal confirmation.
    - `## Phase 06 D-04 Realtime Mitigations` — re-verify in code and cite:
      Layer 1 role gate `if (loading || (!isBilling && !isAdmin)) return;`, Layer 3
      count-only `(_payload) => setPendingCount(n => n+1)`, Layer 2
      `void channel.unsubscribe()`, plus RLS-per-subscriber for postgres_changes.
    - `## Probe Results (D-07/D-08)` — paste the 07-02 live probe assertion output
      (SEC-01a/b/c + SEC-05/SEC-01d all PASS) + the SEC-05 TTL=300 audit note.
    - `## Header Verification (SEC-02)` — paste `curl -sI <VERCEL_URL> | grep -iE
      "x-frame-options|x-content-type-options|referrer-policy|strict-transport|content-security-policy"`
      output confirming all 5 headers present (enforcing CSP) on the live response.
    - `## Accepted Risks Log` — the deferred 15 moderate Dependabot CVEs (D-06), the
      Sentry-region assumption (if unresolved), any orphaned-service note, each with
      rationale + Accepted By + Date.
    - `## Security Audit Trail` — audit date, threats total/closed/open, run by.
    - `## Sign-Off` — checklist; set `threats_open: 0` + `status: verified` only when
      no HIGH/critical remains open.

    Resolve every HIGH/critical finding before setting status: verified. If the
    auditor surfaces a new HIGH/critical, fix it (or, for genuinely non-applicable
    items, document as an accepted risk with rationale) before closing.
  </action>
  <verify>
    <automated>cd "C:\Users\juflores\dev\Generate-Weekly-PDFs-DSR-Resiliency" && bash -c 'f=.planning/phases/07-security-hardening-and-express-removal/07-SECURITY.md; set -e; test -f "$f"; for s in "## Trust Boundaries" "## Threat Register" "## SEC-03 Verification" "## Phase 06 D-04 Realtime Mitigations" "## Probe Results" "## Header Verification" "## Accepted Risks Log" "## Security Audit Trail"; do grep -q "$s" "$f" || { echo "MISSING SECTION: $s"; exit 1; }; done; grep -q "T-07-" "$f" || { echo "MISSING threat IDs"; exit 1; }; grep -q "useRealtimeArtifacts" "$f" || { echo "MISSING D-04 code cite"; exit 1; }; echo "PASS: 07-SECURITY.md has all required sections + threat IDs + D-04 cite"'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-security-hardening-and-express-removal/07-SECURITY.md` exists and contains the sections: `## Trust Boundaries`, `## Threat Register`, `## SEC-03 Verification`, `## Phase 06 D-04 Realtime Mitigations`, `## Probe Results`, `## Header Verification`, `## Accepted Risks Log`, `## Security Audit Trail`.
    - The Threat Register contains `T-07-` threat IDs from all four plans, each with a disposition; no HIGH/critical row has status `open`.
    - The Phase 06 D-04 section cites `useRealtimeArtifacts.ts` with the role gate, count-only payload, and unsubscribe cleanup evidence.
    - The Probe Results section contains the four PASS assertions from 07-02's live run; the Header Verification section contains the curl output showing all 5 headers on the live deploy.
    - Frontmatter `threats_open: 0` and `status: verified` are set (only after all HIGH/critical are closed); the deferred 15 moderate CVEs appear in the Accepted Risks Log.
  </acceptance_criteria>
  <done>The /security-review + gsd-secure-phase 07 audit is complete with no open HIGH/critical findings, and 07-SECURITY.md consolidates the full threat register, SEC-03 verification, D-04 re-verification, probe results, header verification, and accepted-risks log with threats_open: 0.</done>
</task>

</tasks>

<threat_model>
ASVS Level: L1. Block on: HIGH severity. Primary ASVS focus: V14 Configuration, V4 Access Control, V6 Cryptography (audit).

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| build tooling → deployed bundle | A critical/high CVE in a shipped dependency is a live attack surface; dev-only CVEs (vitest) are lower urgency but still bumped. | npm dependency tree |
| auditor → implemented code | Every declared mitigation must be provable in code (adversarial stance), not assumed. | Source + policy evidence |
| browser → Supabase Realtime | The D-04 layers must still be present post-Phase-06; the audit re-verifies, not re-decides. | Realtime change events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-cve-runtime | Tampering / EoP | shipped npm dependencies | mitigate | Targeted bumps for all critical/high CVEs on portal-v2 + website; `npm audit --audit-level=high --omit=dev` → 0 (Task 1). |
| T-07-05-cve-devtool | Tampering | vitest (dev-only) | mitigate | `npm install vitest@^4` resolves the critical CVE; not shipped to Vercel so exploit risk is LOW, but bumped for hygiene; 107 tests re-verified green. |
| T-07-05-cve-moderate-defer | Information Disclosure (low) | 15 moderate advisories | accept | Deferred per D-06 to a dedicated dependency-maintenance follow-up; logged in 07-SECURITY.md Accepted Risks Log to avoid a regression-prone blanket bump. |
| T-07-05-realtime-d04 | Information Disclosure | useRealtimeArtifacts.ts | mitigate | Re-verify the 3 D-04 layers (role gate, count-only payload, unsubscribe) + RLS-per-subscriber in code; cite in 07-SECURITY.md (Task 2). |
| T-07-05-unverified-mitigation | (meta) audit completeness | all Phase 07 mitigations | mitigate | gsd-secure-phase 07 spawns gsd-security-auditor to prove each T-07-* mitigation in code; blocks if threats_open > 0. |
| T-07-05-header-regression | Tampering | live SEC-02 headers | mitigate | `curl -sI` on the live deploy confirms all 5 headers (enforcing CSP) actually present on the response — not just in vercel.json source (Task 2 Header Verification). |
</threat_model>

<verification>
- Task 1: `npm audit --audit-level=high --omit=dev` → 0 critical/high on the runtime surface; `npm test` + `npm run build` (portal-v2) + `npm run build` (website) all pass.
- Task 2: bash gate confirms 07-SECURITY.md has all 8 required sections + T-07- threat IDs + the useRealtimeArtifacts D-04 cite.
- Audit gate: `gsd-secure-phase 07` reports threats_open: 0; no HIGH/critical finding remains open.
- Header curl: all 5 SEC-02 headers present on the live Vercel response (recorded in 07-SECURITY.md).
</verification>

<success_criteria>
- /security-review + gsd-secure-phase 07 complete with no open HIGH/critical RLS, signed-URL, or secret-handling findings.
- Critical/high Dependabot CVEs on portal-v2 + website remediated; the 107-test suite + both builds green; 15 moderate deferred + logged.
- Phase 06 D-04 Realtime mitigations re-verified in code.
- 07-SECURITY.md is complete with the full threat register, SEC-03 verification, D-04 re-verification, probe results, header verification, accepted-risks log, and `threats_open: 0` / `status: verified`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-security-hardening-and-express-removal/07-04-SUMMARY.md` recording: the CVE remediation (versions bumped + audit result), the audit outcome (threats total/closed/open), and confirmation that 07-SECURITY.md is verified with threats_open: 0.
</output>
