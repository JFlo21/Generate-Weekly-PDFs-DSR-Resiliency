---
context: phase
phase: 07-security-hardening-and-express-removal
plan: 04
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-06-03T02:58:46.221Z
---

# Resume: Phase 07 — Security Hardening & Express Removal (plan 07-04, the milestone-gating audit)

> Plans 07-01, 07-02, 07-03 are **COMPLETE**. Only **07-04** remains. Finishing it
> closes Phase 07 **and** the v1.1 milestone (27/28 plans done, 96%).

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| `--omit=dev` audit hides the vitest CVE | Task 1's verify gate runs `npm audit --audit-level=high --omit=dev`, which suppresses the **dev-only** vitest advisory. Reading a clean exit code as "vitest is fixed" is wrong. | advisory | The authoritative confirmation is the **package.json version string**: `devDependencies.vitest` must be `^4`+ (the plan's `node -e` check asserts this independently of the runtime audit). |
| Alarm at `useToastContext` test logs | After the vitest 4 bump the suite prints noisy `useToastContext`/provider error logs, but the **suite is GREEN** (107 passing). | advisory | Treat the logs as benign; trust the pass/fail summary, not stderr noise. |
| Blanket `npm audit fix --force` | D-06 forbids a blanket multi-package bump (regression-prone). Only critical/high get fixed this phase. | advisory | Targeted `npm install <pkg>@<fixed>` per advisory; moderates → Accepted Risks Log. |
| Sweeping unrelated files into the commit | 9 pre-existing uncommitted files (`.github/prompts/*`, stray `.planning` artifacts) are **not** part of 07-04. | advisory | Stage only 07-04 deliverables (package.json/lock, 07-SECURITY.md, 07-04-SUMMARY.md) per task. |

_No `blocking`-severity constraints — nothing requires an understanding-check gate before resuming._

<current_state>
Executing **plan 07-04 (4 of 4)**. Branch `master`, HEAD `6dd1ed8`.

- **Task 1 (CVE remediation)** — IN PROGRESS.
  - **portal-v2: DONE & committed** (`6dd1ed8`): `vitest` bumped to `^4` (resolves the
    critical dev-only CVE), moderates deferred, 107-test suite re-verified green.
  - **website/: NOT finalized.** Audit was investigated this session — Docusaurus is
    pinned at `3.7.0` and transitive vulnerabilities surfaced in the `3.10.0` dep tree
    (claude-mem obs 1684/1686/1688) — but **no remediate-vs-defer decision was committed**
    and no `website/package.json` change is staged.
- **Task 2 (SEC-04 audit + 07-SECURITY.md)** — NOT STARTED. `07-SECURITY.md` is **absent**.
  `security-reviewer` skill and `gsd-secure-phase 07` have not been run.
</current_state>

<completed_work>

Completed this phase (prior plans + this session):
- Plan 07-01: SEC-02 security headers + Report-Only CSP shipped, zero violations (DONE).
- Plan 07-02: SEC-01/SEC-05 live RLS + signed-URL probe PASS; `scripts/security-probe.ts` regression harness (DONE).
- Plan 07-03: `portal/` Express backend deleted (29 files), all coupling severed, CSP
  flipped to **enforcing**, 6-step live smoke test PASS, SEC-03 grep-gate PASS (DONE).
- Plan 07-04 Task 1 (portal-v2 portion): `vitest@^4`, moderates deferred, suite green,
  committed `6dd1ed8` (DONE).
</completed_work>

<remaining_work>

- **Task 1 — website portion:** `cd website && npm audit --audit-level=high`. Remediate
  any critical/high via *targeted* bumps; `cd website && npm run build` must stay green.
  If a critical/high can't be cleanly fixed (Docusaurus transitive), document the residual
  + rationale. Record deferred moderates (portal-v2 + website) for the Accepted Risks Log.
- **Task 2 — SEC-04 audit + doc:**
  1. Invoke the **`security-reviewer`** skill by name (NOT a `/security-review` command —
     none exists here). Resolve every Critical/High, or accept-with-rationale.
  2. Run **`gsd-secure-phase 07`** (spawns gsd-security-auditor; BLOCKS if threats_open > 0).
  3. Author `07-SECURITY.md` mirroring `03-SECURITY.md`, with the Phase-07 sections:
     `## Trust Boundaries`, `## Threat Register` (all `T-07-*`), `## SEC-03 Verification`,
     `## Phase 06 D-04 Realtime Mitigations` (cite `useRealtimeArtifacts.ts` 3 layers),
     `## Probe Results (D-07/D-08)`, `## Header Verification (SEC-02)` (live `curl -sI`),
     `## Accepted Risks Log`, `## Security Audit Trail`, `## Sign-Off`.
     Set `threats_open: 0` + `status: verified` only when no HIGH/critical remains open.
  4. Write `07-04-SUMMARY.md`; mark 07-04 complete in STATE.md + ROADMAP; this closes the milestone.
</remaining_work>

<decisions_made>

- vitest bumped to `^4` (semver-major) — the critical CVE is only fixed in 4.x; dev-only
  (not shipped to Vercel) so exploit risk is LOW, but bumped for hygiene; config reviewed
  for 2.x→4.x breakage; 107 tests confirmed green.
- Moderate advisories **deferred**, not blanket-fixed (D-06) — they go to the Accepted Risks Log.
</decisions_made>

<blockers>
- **Human action (blocking Task 2):** the live production **Vercel URL** is needed for the
  SEC-02 header `curl -sI` check and the one-time SEC-04 sign-off. Source: Vercel Dashboard →
  Project → Deployments → Production domain.
- **Technical (Task 1 website):** Docusaurus pinned `3.7.0` vs transitive vulns in the `3.10.0`
  tree — remediate-vs-defer decision pending; re-run `npm audit` as ground truth.
</blockers>

## Required Reading (in order)
1. `.planning/phases/07-security-hardening-and-express-removal/07-04-PLAN.md` — the active plan (2 tasks, verify gates, threat model).
2. `.planning/phases/07-security-hardening-and-express-removal/07-CONTEXT.md` — D-05 (audit tooling) + D-06 (CVE triage scope).
3. `.planning/phases/03-supabase-data-layer-foundation/03-SECURITY.md` — the structural analog for 07-SECURITY.md.
4. `.planning/phases/07-security-hardening-and-express-removal/07-PATTERNS.md` — exact 07-SECURITY.md frontmatter + section patterns (currently untracked).
5. `07-01-SUMMARY.md`, `07-02-SUMMARY.md`, `07-03-SUMMARY.md` — evidence to consolidate (headers walkthrough, probe assertions, grep-gate output, smoke test).
6. `portal-v2/src/hooks/useRealtimeArtifacts.ts` — re-verify the 3 D-04 layers (role gate, count-only payload, unsubscribe).

## Infrastructure State
- **CSP is ENFORCING** in production (flipped in 07-03) — any header/curl check runs against the enforcing policy.
- **LIVE Supabase project = `poeyztlmsawfoqlanucc`** (the only one with both `profiles` + `artifacts`; 2,383+ artifact rows). Portal login `juflores@ltspower.com` (role=admin).
- **`portal/` no longer exists** (deleted in 07-03) — its CVEs are auto-resolved; do not touch it.
- No background servers/watchers running.

<context>
This is the final, milestone-gating plan. The hard part (Express removal, CSP enforce-flip,
RLS probe) is already done and verified. 07-04 is consolidation + a clean security sign-off:
fix the last CVEs on the shipped surface, prove every Phase-07 mitigation exists in code via
two independent auditors, and write the threat register. The single real unknown is the
website/ Docusaurus transitive-CVE call — settle that with a live `npm audit` first, then the
audit + doc work is mechanical. Don't forget the 9 unrelated uncommitted files are NOT part of
this work — stage only 07-04 deliverables.
</context>

<next_action>
Start with: `cd website && npm audit --audit-level=high --json` to get ground-truth on the
website critical/high CVEs, then decide remediate (targeted bump + `npm run build`) vs
document-as-residual. After Task 1 is closed, proceed to Task 2 (security-reviewer skill →
gsd-secure-phase 07 → author 07-SECURITY.md → 07-04-SUMMARY.md). Get the live Vercel URL from
the operator for the header curl + SEC-04 sign-off.
</next_action>
