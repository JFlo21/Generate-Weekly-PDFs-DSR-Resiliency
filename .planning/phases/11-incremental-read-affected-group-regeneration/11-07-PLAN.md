---
phase: 11-incremental-read-affected-group-regeneration
plan: 07
type: execute
wave: 7
depends_on: ["11-06"]
files_modified:
  - pipeline_memory/reader.py
  - pipeline/config.py
  - pipeline/orchestrate.py
  - pipeline/discovery.py
  - pipeline/cleanup.py
  - CLAUDE.md
  - .github/workflows/weekly-excel-generation.yml
  - .github/prompts/configuration-environment.md
  - tests/test_incremental_read.py
  - docs/run-memory-write-flip-checklist.md
autonomous: false
requirements: [INC-05, INC-04]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "get_parity_streak scans the newest run_ledger rows whose notes.execution_type is production_frequent backward: a pass counts, a fail resets to zero, and a skipped is excluded from the sequence — it neither counts nor resets (D-09)."
    - "There is no dedicated streak counter column; the streak is derived from run_ledger on demand, so it cannot drift from the evidence (D-09)."
    - "The retirement removes generated_docs/hash_history.json, generated_docs/discovery_cache.json, generated_docs/billing_audit_frozen_rows.json, the two attachment pre-fetch phases with their sub-budget constants, and the three GitHub Actions cache restore/save step pairs (D-12, INC-05)."
    - "group_state holds the attachment ids the retired attachment pre-fetch used to supply, proven on the flip PR's first real upload (INC-05)."
    - "The frequent-run wall clock is recorded before and after the retirement against the 94-minute baseline from run 32743959053 (ROADMAP success criterion 4)."
    - statement: "The retirement removals are committed only after a run_ledger scan records five consecutive parity-evaluated production_frequent runs at verdict pass, with skipped runs excluded from the sequence and any fail resetting the count."
      verification: backstop
  artifacts:
    - path: "pipeline_memory/reader.py"
      provides: "get_parity_streak — the D-09 streak scan over run_ledger"
      contains: "def get_parity_streak"
    - path: ".github/workflows/weekly-excel-generation.yml"
      provides: "Cache restore/save step pairs removed for the three retired local JSON caches"
    - path: "pipeline/orchestrate.py"
      provides: "Attachment pre-fetch phases and hash-history maintenance removed; group_state is the sole source of attachment ids and content hashes"
    - path: "docs/run-memory-write-flip-checklist.md"
      provides: "Streak evidence and the before/after wall-clock record"
      contains: "32743959053"
  key_links:
    - from: "pipeline/orchestrate.py"
      to: "pipeline_memory/reader.py"
      via: "the streak scan reads run_ledger notes.parity_verdict and notes.execution_type"
      pattern: "get_parity_streak"
    - from: "pipeline/orchestrate.py"
      to: "pipeline_memory/writer.py"
      via: "group_state.content_hash becomes the sole skip gate once hash_history.json is retired"
      pattern: "group_state"
  prohibitions:
    - statement: "Must NOT remove a local-cache or attachment-pre-fetch rollback path before the five-run parity streak is recorded in run_ledger."
      status: unverified
      verification: flagged
    - statement: "Must NOT count a skipped parity run toward the five consecutive-pass streak, and must NOT let a skipped run reset it."
      status: unverified
      verification: flagged
    - statement: "Must NOT bundle the retirement with any other change — it is its own PR, cut strictly after the streak."
      status: unverified
      verification: flagged
---

<objective>
Ship INC-05: retire the local JSON caches and the two attachment pre-fetch phases, strictly
after the D-09 parity streak proves the incremental path. Also lands D-09's streak scan,
which is the evidence gate for this plan and for INC-04's own gate.

Purpose: `hash_history.json`, `discovery_cache.json`, `billing_audit_frozen_rows.json` and
the two attachment pre-fetch phases are the live rollback path through burn-in. The three
`actions/cache/save@v4` steps carry `if: always()` precisely so a failed run keeps its
cache state. Removing them is the last irreversible-feeling step of the phase, and D-12
rates its undo cost as costly: reverting means restoring an operational rollback path in a
workflow and code PR.

Output: `get_parity_streak` in the reader, the retirement removals, the six GitHub Actions
cache steps removed, and the before/after wall-clock record against the 94-minute baseline
(run `32743959053`).

**This plan is cut as its own PR, strictly after the streak, and never bundled** (D-12). It
must not execute in the same run as plans 01-06. It edits
`.github/workflows/weekly-excel-generation.yml`, a protected area, so it opens with a
blocking decision checkpoint and is not autonomous.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-incremental-read-affected-group-regeneration/11-CONTEXT.md
@.planning/phases/11-incremental-read-affected-group-regeneration/11-RESEARCH.md
@.planning/phases/11-incremental-read-affected-group-regeneration/11-05-SUMMARY.md
@.planning/phases/11-incremental-read-affected-group-regeneration/11-06-SUMMARY.md
@docs/run-memory-write-flip-checklist.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_parity_streak — the D-09 evidence scan over run_ledger</name>
  <precondition>`pipeline_memory.run_ledger` holds at least one row whose `notes` carries a `parity_verdict`, written by a real run after plan 05 merged.</precondition>
  <files>pipeline_memory/reader.py, tests/test_incremental_read.py</files>
  <read_first>
    - `.planning/phases/11-incremental-read-affected-group-regeneration/11-CONTEXT.md` — D-09 in full: pass counts, fail resets, skipped is excluded from the sequence; no dedicated counter column.
    - `pipeline_memory/schema.sql` lines 210-245 — `run_ledger` columns `run_id`, `started_at`, `finished_at`, `status`, `mode`, `notes`.
    - `pipeline_memory/writer.py` lines 195-285 — `run_ledger_finish` and how `notes.execution_type` and the plan-05 `parity_verdict` / `parity_details` keys land in `notes`.
    - `pipeline_memory/reader.py` — the module conventions, per-query `op=` naming and fail-open return contract from plans 02, 04 and 06.
    - `pipeline/parity.py` — the verdict vocabulary (`pass`, `fail`, `skipped`) the scan reads.
  </read_first>
  <behavior>
    - Scanning newest-first over `run_ledger` rows whose `notes.execution_type` is `production_frequent`: five consecutive `pass` verdicts returns a streak of five.
    - A `fail` encountered before five passes resets the count to zero and stops the scan.
    - A `skipped` verdict is excluded from the sequence entirely: it neither increments the count nor resets it, and the scan continues past it.
    - A row with no `parity_verdict` in `notes` is treated the same as `skipped` — excluded, never counted.
    - Rows whose `notes.execution_type` is not `production_frequent` are ignored by the scan.
    - A Supabase failure returns None, which the caller reads as "cannot confirm" — never as a satisfied streak.
    - The scan reads a bounded window of recent rows rather than the whole table, and reports how many rows it examined so a streak of five is auditable.
  </behavior>
  <action>
    Implement CONTEXT.md D-09. Add `get_parity_streak(limit)` to `pipeline_memory/reader.py`: select `run_id`, `started_at`, `status`, `notes` from schema `pipeline_memory`, table `run_ledger`, ordered by `started_at` descending, limited to a bounded recent window. Give it `op="run_ledger_parity_streak"`.

    Walk the returned rows newest-first. Skip any row whose `notes.execution_type` is not `production_frequent`. For the remaining rows read `notes.parity_verdict`: a `pass` increments the count, a `fail` resets the count to zero and stops the scan, and both a `skipped` verdict and an absent verdict are excluded — the scan moves on without touching the count. Stop once the count reaches the target.

    Return a small dict carrying the streak count, the number of rows examined, the run ids that contributed, and the run id and verdict that stopped the scan if one did — so a claim of five is auditable rather than a bare integer. Return None on any failure, and document that None means "cannot confirm", never "streak satisfied".

    Do not add a counter column to the schema and do not cache the count anywhere. The whole point of deriving it on demand is that it cannot drift from the evidence.

    Add a `ParityStreakTests` class to `tests/test_incremental_read.py` covering all seven behaviors, driving the reader with a mocked Supabase response holding a crafted sequence of verdicts. The load-bearing tests are: a `skipped` sandwiched between passes does not break a streak, and a `fail` resets it. Write the tests first and watch them fail.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_incremental_read.py::ParityStreakTests -q` exits 0 and collects at least seven tests.
    - `pipeline_memory/reader.py` defines `get_parity_streak`.
    - A test asserts a `skipped` row between two `pass` rows yields a streak of two, not one and not a reset.
    - A test asserts a `fail` row yields a streak of zero.
    - A test asserts a Supabase failure returns None.
    - `git diff --exit-code -- pipeline_memory/schema.sql` exits 0 — no counter column added.
    - `python -m py_compile generate_weekly_pdfs.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_incremental_read.py -q</automated>
    <automated>git diff --exit-code -- pipeline_memory/schema.sql</automated>
    <automated>python -m py_compile generate_weekly_pdfs.py</automated>
  </verify>
  <done>The streak is derived from `run_ledger` on demand with pass-counts, fail-resets and skipped-excluded semantics, returns auditable detail rather than a bare number, and reports None rather than a false satisfaction when it cannot confirm.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: GATE — approve the INC-05 retirement and the protected workflow edit</name>
  <decision>Approve the INC-05 retirement PR — remove the local JSON caches, the two attachment pre-fetch phases, and the three GitHub Actions cache step pairs</decision>
  <context>
    Task 1 produced an auditable streak reading from `run_ledger`. This checkpoint is where
    Juan reads that evidence and decides whether the rollback path comes out.

    What is being removed is the live rollback path used through burn-in:
    `generated_docs/hash_history.json`, `generated_docs/discovery_cache.json`,
    `generated_docs/billing_audit_frozen_rows.json`, the target-row and PPP attachment
    pre-fetch phases with their `ATTACHMENT_PREFETCH_MAX_MINUTES` and
    `ATTACHMENT_PREFETCH_FUTURE_TIMEOUT_SEC` sub-budgets, and the three
    `actions/cache/restore@v4` plus `actions/cache/save@v4 if: always()` step pairs in
    `.github/workflows/weekly-excel-generation.yml`. Those `if: always()` saves exist
    precisely so a failed run keeps its cache state.

    `.github/workflows/weekly-excel-generation.yml` is a protected area: no workflow,
    schedule, cache or deployment change ships without Juan's explicit approval.

    D-12 rates the undo cost as **costly**: reverting means restoring an operational
    rollback path across both a workflow and a code change. It is not irreversible, but it
    is not a one-line revert either, and it must not be bundled with anything else.

    Before approving, confirm: the streak reading from task 1 shows five consecutive
    `pass` verdicts on `production_frequent` runs with no intervening `fail`; the flip PR's
    `group_state` attachment-id proof passed, so `group_state` genuinely holds the
    attachment ids the retired pre-fetch used to supply; and the current frequent-run wall
    clock has been recorded for the before/after comparison against the 94-minute baseline
    from run `32743959053`.
  </context>
  <options>
    <option id="retire-now">
      <name>Approve the full retirement</name>
      <pros>Removes the last of the local-cache machinery, the two pre-fetch phases and six workflow steps; delivers the wall-clock reduction ROADMAP success criterion 4 measures; `group_state` becomes the single source of truth for content hashes and attachment ids.</pros>
      <cons>Undo is a workflow-plus-code revert, not a one-line flag flip; the rollback path is gone for any defect discovered after this merge.</cons>
    </option>
    <option id="retire-code-keep-cache-steps">
      <name>Retire the pre-fetch phases and the code paths, keep the three workflow cache step pairs one more cycle</name>
      <pros>Captures most of the wall-clock saving while leaving the cache artifacts being written and restored, so a revert of the code change alone restores the old behaviour.</pros>
      <cons>Leaves six dead workflow steps saving artifacts nothing reads; the retirement has to be finished in a second PR anyway.</cons>
    </option>
    <option id="defer">
      <name>Defer the retirement to a later phase</name>
      <pros>Keeps the entire rollback path intact while the incremental flag beds in over more real runs.</pros>
      <cons>INC-05 stays open and Phase 11 cannot close on ROADMAP success criterion 4; the caches keep costing restore/save time on every run.</cons>
    </option>
  </options>
  <resume-signal>Select: retire-now, retire-code-keep-cache-steps, or defer. Paste the streak reading from task 1 with the decision.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retire the two attachment pre-fetch phases and their sub-budgets</name>
  <precondition>The decision checkpoint returned `retire-now` or `retire-code-keep-cache-steps`, and the streak reading from task 1 showed five consecutive `pass` verdicts.</precondition>
  <reversibility rating="costly">Undoing this removes and restores an operational rollback path across `orchestrate.py` and `config.py`; it touches every consumer of the attachment cache rather than one call site (CONTEXT.md D-12).</reversibility>
  <files>pipeline/orchestrate.py, pipeline/config.py, pipeline/cleanup.py, .github/prompts/configuration-environment.md, CLAUDE.md, docs/run-memory-write-flip-checklist.md</files>
  <read_first>
    - `pipeline/orchestrate.py` lines 1115-1200 — the target-row attachment pre-fetch block, its pre-flight guard and its per-future timeout.
    - `pipeline/orchestrate.py` lines 1290-1450 — the PPP attachment pre-fetch block.
    - `pipeline/config.py` lines 112-128 — `ATTACHMENT_PREFETCH_MAX_MINUTES` and `ATTACHMENT_PREFETCH_FUTURE_TIMEOUT_SEC`.
    - `pipeline/cleanup.py` — `_has_existing_week_attachment`, `delete_old_excel_attachments` and `cleanup_untracked_sheet_attachments`, the three consumers that today accept a missing cache entry and fall back to a per-row on-demand lookup. That fallback is what makes removal safe.
    - `pipeline_memory/writer.py` — `upsert_group_state` and the attachment-id columns that replace the pre-fetched cache.
    - `.github/prompts/configuration-environment.md` — the `ATTACHMENT_PREFETCH_*` entries to remove.
    - `CLAUDE.md` — the Data Pipeline Architecture section describing the pre-fetch phase, which must be updated in the same change so the documentation does not describe a phase that no longer exists.
  </read_first>
  <behavior>
    - Neither attachment pre-fetch phase runs; the pipeline issues no bulk pre-fetch calls.
    - The three cache consumers resolve attachment identity from `group_state` where it is available, and fall back to their existing per-row on-demand lookup otherwise — the fallback path they already have is not removed.
    - `ATTACHMENT_PREFETCH_MAX_MINUTES` and `ATTACHMENT_PREFETCH_FUTURE_TIMEOUT_SEC` are gone from `pipeline/config.py` and from the env reference; setting them has no effect and nothing imports them.
    - `TIME_BUDGET_MINUTES` and the runner's `timeout-minutes` are unchanged — this task removes work, it does not re-budget the session.
    - The full existing test suite passes; any test that asserted pre-fetch behaviour is removed or rewritten against the on-demand path, not deleted silently.
    - `CLAUDE.md`'s pipeline description no longer names a pre-fetch phase.
  </behavior>
  <action>
    Remove the two attachment pre-fetch phases from `pipeline/orchestrate.py` — the target-row block and the PPP block — together with their pre-flight guards, per-future timeouts and the in-memory cache they populate. Remove `ATTACHMENT_PREFETCH_MAX_MINUTES` and `ATTACHMENT_PREFETCH_FUTURE_TIMEOUT_SEC` from `pipeline/config.py` and their entries from `.github/prompts/configuration-environment.md`.

    Do not remove the on-demand fallback paths in `pipeline/cleanup.py`. `_has_existing_week_attachment`, `delete_old_excel_attachments` and `cleanup_untracked_sheet_attachments` already accept a missing cache entry and fall back to a per-row lookup; that fallback becomes the primary path and must stay. Where `group_state` already carries the attachment id for a group, prefer it over an on-demand call.

    Leave `TIME_BUDGET_MINUTES` at 165 and the runner's `timeout-minutes` at 180. This task removes work from the run; it does not re-budget the session, and 10-CONTEXT D-07 locks the schedule and budget family.

    Update `CLAUDE.md`'s Data Pipeline Architecture section so it no longer describes a pre-fetch phase or its budgets, and note in the same edit that `group_state` now supplies attachment identity. A description of a phase that no longer exists is worse than no description.

    Rewrite, do not silently delete, any existing test that asserted pre-fetch behaviour: each becomes an assertion about the on-demand path or about `group_state` supplying the attachment id. Record in the plan SUMMARY how many tests were rewritten and how many removed as genuinely obsolete.

    Measure and record the frequent-run wall clock immediately before this change lands, into `docs/run-memory-write-flip-checklist.md`, against the 94-minute baseline from run `32743959053`.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/ -q` exits 0.
    - `pipeline/config.py` no longer defines the two attachment pre-fetch constants, and no module imports them.
    - A test asserts the cleanup consumers still resolve attachment identity when no cache entry exists.
    - `git diff --exit-code -- .github/workflows/` exits 0 — this task changes no workflow step; task 4 owns that.
    - `docs/run-memory-write-flip-checklist.md` contains the baseline run id `32743959053` and the freshly measured before figure.
    - `python -m py_compile generate_weekly_pdfs.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <automated>python -c "import ast,sys; src=open('pipeline/config.py',encoding='utf-8').read(); names=[t.id for n in ast.walk(ast.parse(src)) if isinstance(n,ast.Assign) for t in n.targets if isinstance(t,ast.Name)]; left=[n for n in names if n.startswith('ATTACHMENT_PREFETCH_')]; print('PREFETCH_CONSTANTS_GONE' if not left else 'STILL_DEFINED:'+str(left)); sys.exit(1 if left else 0)"</automated>
    <automated>git diff --exit-code -- .github/workflows/</automated>
    <automated>python -m py_compile generate_weekly_pdfs.py</automated>
  </verify>
  <done>Both attachment pre-fetch phases and their sub-budget constants are gone; the on-demand fallback and `group_state` supply attachment identity; the time-budget family and the workflow are untouched; `CLAUDE.md` no longer describes a phase that does not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Retire the three local JSON caches and their workflow cache steps</name>
  <precondition>The decision checkpoint returned `retire-now` (option `retire-code-keep-cache-steps` skips this task's workflow edits and stops after the code removals).</precondition>
  <reversibility rating="costly">Undoing this restores an operational rollback path across a protected workflow file and three code paths; the `if: always()` cache saves exist so a failed run keeps its state, and that safety net is what is being removed (CONTEXT.md D-12).</reversibility>
  <files>.github/workflows/weekly-excel-generation.yml, pipeline/orchestrate.py, pipeline/discovery.py, pipeline/config.py, .github/prompts/configuration-environment.md, CLAUDE.md, docs/run-memory-write-flip-checklist.md, tests/test_incremental_read.py</files>
  <read_first>
    - `.github/workflows/weekly-excel-generation.yml` lines 155-195 — the three `actions/cache/restore@v4` steps for `hash_history.json`, `discovery_cache.json` and `billing_audit_frozen_rows.json`.
    - `.github/workflows/weekly-excel-generation.yml` lines 765-800 — the three matching `actions/cache/save@v4` steps carrying `if: always()`.
    - `.github/workflows/weekly-excel-generation.yml` lines 190-260 — the execution-type step and the env block, both of which must stay exactly as they are.
    - `pipeline/discovery.py` lines 185-300 and around line 673 — `discover_source_sheets` and the `discovery_cache.json` TTL logic.
    - `pipeline/orchestrate.py` — the hash-history load, update and persist paths, and the `group_state.content_hash` write that replaces them.
    - `pipeline/config.py` — `DISCOVERY_CACHE_TTL_MIN`, `USE_DISCOVERY_CACHE`, `RESET_HASH_HISTORY` and the frozen-rows path constants.
    - `billing_audit/` — the `billing_audit_frozen_rows.json` producer and consumer.
    - `CLAUDE.md` — the Critical Pitfalls entry stating hash history is ephemeral in CI, and the Configuration section listing the cache TTL variables.
  </read_first>
  <behavior>
    - `group_state.content_hash` is the sole change-detection skip gate; no run reads or writes `generated_docs/hash_history.json`.
    - Sheet discovery reads `sheet_registry` rather than `generated_docs/discovery_cache.json`; the TTL constants are removed.
    - The frozen-rows path no longer reads or writes `generated_docs/billing_audit_frozen_rows.json`.
    - The three `actions/cache/restore@v4` and three `actions/cache/save@v4 if: always()` step pairs are removed from the workflow; the execution-type step, the env block, the cron schedule, `timeout-minutes` and `TIME_BUDGET_MINUTES` are byte-identical to before.
    - The operator flags that force full regeneration still work: `RESET_HASH_HISTORY`, `REGEN_WEEKS`, `RESET_WR_LIST` and `FORCE_GENERATION` continue to escalate a run to full mode via D-02 trigger 5, now operating against `group_state` rather than the JSON file.
    - The `advanced_options` key-value parser in the workflow is untouched.
    - The frequent-run wall clock is measured after the change and recorded next to the before figure and the 94-minute baseline.
  </behavior>
  <action>
    Remove the three local JSON caches and the six workflow steps that carry them, completing CONTEXT.md D-12.

    In code: make `group_state.content_hash` the sole skip gate, removing the `generated_docs/hash_history.json` load, update and persist paths from `pipeline/orchestrate.py`. Point sheet discovery at `sheet_registry` instead of `generated_docs/discovery_cache.json` and remove the TTL constants from `pipeline/config.py` and their entries from `.github/prompts/configuration-environment.md`. Remove the `generated_docs/billing_audit_frozen_rows.json` read and write path.

    Keep every operator escalation flag working. `RESET_HASH_HISTORY`, `REGEN_WEEKS`, `RESET_WR_LIST` and `FORCE_GENERATION` are documented in operator runbooks and in the `force-week-regeneration` project skill; they must continue to force a full regeneration, now by escalating the run to full mode through D-02 trigger 5 and by ignoring `group_state.content_hash` for the flagged scope. Add a test for each flag.

    In the workflow — protected area, approved by the decision checkpoint above — remove exactly the three `actions/cache/restore@v4` steps and the three matching `actions/cache/save@v4 if: always()` steps, and nothing else. The execution-type step, the env block, the cron schedule entries, `timeout-minutes: 180`, `TIME_BUDGET_MINUTES: '165'` and the `advanced_options` key-value parser all stay exactly as they are. Show the resulting diff in the plan SUMMARY so the change is reviewable line by line.

    Update `CLAUDE.md`: the Critical Pitfalls entry about hash history being ephemeral in CI, the Configuration section entries for the discovery-cache TTL variables, and the Data Pipeline Architecture description of the change-detection step, so all three describe `group_state` rather than the retired files.

    Measure the frequent-run wall clock after this change lands and record it in `docs/run-memory-write-flip-checklist.md` next to the before figure and the 94-minute baseline from run `32743959053`, satisfying ROADMAP success criterion 4.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/ -q` exits 0.
    - No module reads or writes `generated_docs/hash_history.json`, `generated_docs/discovery_cache.json` or `generated_docs/billing_audit_frozen_rows.json`.
    - The workflow file contains zero `actions/cache/restore@v4` and zero `actions/cache/save@v4` steps.
    - The workflow still contains `timeout-minutes: 180`, `TIME_BUDGET_MINUTES` set to `'165'`, the four cron schedule entries, and the `advanced_options` parser.
    - A test asserts each of `RESET_HASH_HISTORY`, `REGEN_WEEKS`, `RESET_WR_LIST` and `FORCE_GENERATION` still forces a full regeneration.
    - `docs/run-memory-write-flip-checklist.md` carries the baseline, the before figure and the after figure.
    - `bash scripts/run_6_gates.sh` reports all six gates passed.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <automated>python -c "import sys,pathlib; t=pathlib.Path('.github/workflows/weekly-excel-generation.yml').read_text(encoding='utf-8'); caches=t.count('actions/cache/'); keep=[k for k in ('timeout-minutes: 180','TIME_BUDGET_MINUTES','advanced_options') if k not in t]; print('WORKFLOW_OK' if caches==0 and not keep else 'caches='+str(caches)+' missing='+str(keep)); sys.exit(0 if caches==0 and not keep else 1)"</automated>
    <automated>python -c "import sys,subprocess; out=subprocess.run(['git','grep','-l','-e','hash_history.json','-e','discovery_cache.json','-e','billing_audit_frozen_rows.json','--','pipeline/','billing_audit/','generate_weekly_pdfs.py'],capture_output=True,text=True).stdout.strip(); print('CACHE_REFS:',out or '(none)'); sys.exit(0 if not out else 1)"</automated>
    <automated>bash scripts/run_6_gates.sh</automated>
  </verify>
  <done>The three local JSON caches and their six workflow cache steps are gone; `group_state` is the sole skip gate and discovery source; every operator escalation flag still forces a full regeneration; the schedule, budgets and `advanced_options` parser are untouched; the before and after wall-clock figures are recorded against the baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parity streak evidence → an irreversible-feeling production change | A wrong streak reading authorises removing the rollback path. |
| Local cache artifacts → operational rollback | The three `if: always()` cache saves are the mechanism by which a failed run keeps its state; removing them removes the recovery path. |
| Editor → protected workflow file | `.github/workflows/weekly-excel-generation.yml` carries the schedule, the runner timeout and the session budget alongside the cache steps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-36 | Repudiation | A falsely satisfied parity streak | critical | mitigate | Task 1: the streak is derived on demand from `run_ledger` with no cached counter, returns the contributing run ids for audit, and returns None rather than a number when it cannot confirm. The blocking decision checkpoint requires the reading to be pasted with the decision. |
| T-11-37 | Denial of service | Rollback path removed while a latent defect remains | high | mitigate | The blocking-human decision checkpoint with a keep-the-cache-steps middle option; D-12's ordering (strictly after the streak, never bundled) is a `must_haves` prohibition. |
| T-11-38 | Tampering | Collateral edit to the protected workflow | critical | mitigate | Task 4 removes exactly six cache steps and nothing else; a verify command asserts the schedule, `timeout-minutes: 180`, `TIME_BUDGET_MINUTES` and the `advanced_options` parser all survive, and the diff is shown in the SUMMARY for line-by-line review. |
| T-11-39 | Denial of service | Session hard-killed by a budget/timeout mismatch | high | mitigate | Task 3 and task 4 both leave `TIME_BUDGET_MINUTES` at 165 and `timeout-minutes` at 180; the 15-minute gap reserved for cache-save and artifact upload is not consumed by this change. |
| T-11-40 | Tampering | Operator escalation flags silently broken by the hash-history removal | high | mitigate | Task 4 adds a test per flag asserting `RESET_HASH_HISTORY`, `REGEN_WEEKS`, `RESET_WR_LIST` and `FORCE_GENERATION` still force a full regeneration against `group_state`. |
| T-11-41 | Tampering | Attachment identity lost with the pre-fetch cache | high | mitigate | Task 3 keeps the existing per-row on-demand fallback in `pipeline/cleanup.py` and prefers `group_state`'s stored attachment ids, whose correctness the flip PR's IN-01 item proved on a real upload. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` — full suite green.
- `python -m py_compile generate_weekly_pdfs.py` — syntax gate.
- `bash scripts/run_6_gates.sh` — all six gates.
- Workflow guard: zero `actions/cache/` steps remain while the schedule, `timeout-minutes`, `TIME_BUDGET_MINUTES` and the `advanced_options` parser survive.
- `git diff --exit-code -- pipeline_memory/schema.sql` — zero schema change across the whole phase.

Manual (recorded in `11-VALIDATION.md` Manual-Only Verifications): the frequent-run wall
clock before and after, compared against the 94-minute baseline from run `32743959053`
(ROADMAP success criterion 4), and the streak evidence pasted at the decision checkpoint.
</verification>

<success_criteria>
1. `get_parity_streak` derives the streak from `run_ledger` with pass-counts, fail-resets and skipped-excluded semantics, and reports None rather than a false satisfaction.
2. The retirement is approved at a blocking-human decision checkpoint with the streak reading attached.
3. Both attachment pre-fetch phases and their sub-budget constants are removed; attachment identity comes from `group_state` with the existing on-demand fallback intact.
4. The three local JSON caches and their six workflow cache steps are removed; `group_state.content_hash` is the sole skip gate.
5. The schedule, `timeout-minutes`, `TIME_BUDGET_MINUTES` and the `advanced_options` parser are untouched.
6. The before and after frequent-run wall clock is recorded against the 94-minute baseline.
</success_criteria>

## Artifacts this phase produces

Created by this plan:
- Function `pipeline_memory.reader.get_parity_streak`
- Test class `ParityStreakTests` in `tests/test_incremental_read.py`
- The streak-evidence and wall-clock sections of `docs/run-memory-write-flip-checklist.md`

Removed by this plan (not new symbols, listed so drift verification does not read the absences as regressions): the two attachment pre-fetch phases in `pipeline.orchestrate`; config constants `ATTACHMENT_PREFETCH_MAX_MINUTES`, `ATTACHMENT_PREFETCH_FUTURE_TIMEOUT_SEC`, `DISCOVERY_CACHE_TTL_MIN`, `USE_DISCOVERY_CACHE`; the `generated_docs/hash_history.json`, `generated_docs/discovery_cache.json` and `generated_docs/billing_audit_frozen_rows.json` read/write paths; six `actions/cache/` workflow steps.

Created by other Phase 11 plans (excluded from drift verification): `pipeline_memory/reader.py` itself plus `get_sheet_watermarks`, `get_last_run_ledger_status` (plan 02), `map_affected_to_sheets` (plan 04), `get_row_state_row_ids` (plan 06); `pipeline.fetch._is_abbreviated_response`, `pipeline.fetch.fetch_sheet_delta`, `pipeline.orchestrate.resolve_run_mode`, `RUN_MEMORY_INCREMENTAL_ENABLED`, `SAFETY_WINDOW_MINUTES`, `run_ledger.notes` key `fallback_reason`, `tests/test_incremental_read.py`, `tests/fixtures/incremental/abbreviated_sheet_response.json` (plan 02); `keep_historical` on `pipeline.cleanup.cleanup_untracked_sheet_attachments` (plan 03); the PHASE 2a / PHASE 2b branch (plan 04); `pipeline/parity.py` with `compare_shadow_parity` and `run_shadow_delta_reads`, the `RUN_MEMORY_SHADOW_*` constants, `run_ledger.notes` keys `parity_verdict` and `parity_details`, `tests/test_parity_shadow.py` (plan 05); `pipeline_memory.writer.mark_rows_deleted`, `tests/test_deep_run_reconciliation.py`, `tests/fixtures/incremental/deleted_row.json` and `formula_only_change.json` (plan 06); `__mem_quantity`, `__mem_units_total_price`, `docs/run-memory-write-flip-checklist.md` (plan 01).

<output>
Create `.planning/phases/11-incremental-read-affected-group-regeneration/11-07-SUMMARY.md` when done.
</output>
