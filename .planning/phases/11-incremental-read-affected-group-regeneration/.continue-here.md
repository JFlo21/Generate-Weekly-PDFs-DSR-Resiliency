---
context: phase
phase: 11-incremental-read-affected-group-regeneration
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-08-26T05:38:08Z
---

# Phase 11 — paused mid `/gsd-discuss-phase 11` (advisor mode), nothing locked yet

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| `uat-passed` treats `skipped` as a blocker | GSD `phase uat-passed --require-verification` counts ANY `result: skipped` UAT test as a blocker, including the workflow's own "Deferred follow-up" skip — Phase 10 could not transition until the decided item was re-recorded | advisory | Record a decided decision-test as `result: pass` + `note:` + a `## Deferred Follow-Ups` entry; never `skipped` |
| `phase.complete` mis-reports on this ROADMAP layout | `roadmap.analyze` cannot parse this repo's ROADMAP.md; `phase.complete` updated STATE.md only and returned `is_last_phase: true` although Phases 11–13 exist | advisory | Hand-evolve ROADMAP/PROJECT/STATE after `phase.complete`; verify the next phase exists in ROADMAP.md before trusting `is_last_phase` |
| `api-coverage` cell-length gate | `verify:pre` gate blocks on `COVERAGE.md` capability cells > 80 chars / reason cells > 200 chars; plan-time authoring does not check | advisory | Keep COVERAGE.md cells terse when the planner writes it; trim without changing INTEGRATE/OPT-OUT |
| Bot-branch CI noise | `code/snyk` ERRORs and the Azure DevOps mirror check FAILs at ~15 s on Dependabot branches (no secrets); the Azure check also failed pre-test on merged #350 | advisory | Judge PRs on "Compile and test" / lint / coverage; the Azure mirror check itself needs a look |
| Git-Bash heredoc quoting | A `python - <<'EOF'` block containing `'''` strings failed to parse under the Bash tool | advisory | Write scripts to the scratchpad with the Write tool and run the file |

<current_state>
Branch `feat/phase-11-incremental-read` (cut from master `fb11109` after PR #350 merged as `99dc25d`;
ledger commits `7982b0f`, `b96afff`). Phase 10 is CLOSED and on master. Open-PR triage resolved
(Seer ×4 closed, Dependabot #344/#345 merged, Cursor docs ×3 closed); a 44-PR backlog remains
(see `.claude/project-state.md`).

`/gsd-plan-phase 11` was started, found no CONTEXT.md, and Juan chose "Run discuss-phase first".
`/gsd-discuss-phase 11` is IN PROGRESS in advisor mode (USER-PROFILE.md exists; calibration tier
`full_maturity` from vendor philosophy = conservative; NON_TECHNICAL_OWNER = false; advisor model
sonnet). Completed so far: phase dir created; prior context loaded (10-CONTEXT D-01..D-09, spec
§4/§8/§9, MEM-04 PASS / D-09 gate OPEN); todos cross-referenced — Juan FOLDED
`2026-08-25-run-memory-review-followups.md` (WR-01/WR-04/IN-01) and left out
`2026-08-25-fix-snapshot-store-int-arg-type.md`; codebase scouted (grep fallback, no
`.planning/codebase/` maps); four gray areas presented and ALL FOUR selected by Juan:
1. Read watermark & safety window
2. Affected-group regen & row source
3. Parity proof harness (INC-04)
4. Rollout, kill switch & retirement order
Four `gsd-advisor-researcher` subagents ran in parallel (one per area) and ALL FOUR returned before
the pause; their comparison tables + rationale are persisted verbatim-in-substance as
`11-ADVISOR-read-watermark-safety-window.md`, `11-ADVISOR-affected-group-regen-row-source.md`,
`11-ADVISOR-parity-proof-harness.md`, `11-ADVISOR-rollout-kill-switch-retirement.md` (research
inputs, NOT decisions). Do NOT re-dispatch them on resume. NO area has been discussed with Juan;
NO decision is locked; no CONTEXT.md exists. `11-DISCUSS-CHECKPOINT.json` records the selection
state (areas_remaining = all four) and points at the four advisor files.

Advisor recommendations at a glance (Juan has not accepted any): watermark → fixed 15-min
SAFETY_WINDOW with capture-time `last_read_at` + seven FULL-read escalation triggers; regen → Option
C hybrid (affected set decides scope, scoped full-sheet re-fetch, unmodified grouping/excel path;
row_state-exclusive deferred to a later slice); parity → shadow-incremental in-process comparison
persisted in `run_ledger.notes`, alternating runs ruled out; rollout → separate operator-gated
write-flag-flip PR first (A) + `production_frequent`-only scope, `run_ledger.mode` for fallback
visibility, INC-05 retirement as its own PR after parity (D).
</current_state>

<completed_work>

Completed this session (2026-08-25 → 26):
- Phase 10 UAT 2/2 decided, verification `passed`, transition run, ROADMAP/STATE/PROJECT evolved
- PR #350 Greptile findings ×3 fixed test-first (`6965f95`), PR merged `99dc25d`, local sync
- PR triage: Seer #343/#346/#347/#348 closed; Dependabot #344/#345 merged; Cursor #328/#331/#338 closed
- Ledgers + vault written back; branch `feat/phase-11-incremental-read` created and pushed
- `/gsd-discuss-phase 11`: init, prior context, todo fold, scout, gray-area selection (above)
</completed_work>

<remaining_work>

- Resume `/gsd-discuss-phase 11` → it will detect `11-DISCUSS-CHECKPOINT.json` and offer Resume;
  re-run the advisor research for the four areas, present each table, capture Juan's picks,
  write `11-CONTEXT.md` + `11-DISCUSSION-LOG.md`, commit
- Then `/gsd-plan-phase 11` (research → plan → check), execute, verify
- Backlog: 44 open PRs need their own triage pass (not part of Phase 11)
</remaining_work>

<decisions_made>

- Phase 10 SC4 "byte-identical" satisfied by the canonicalized-content standard (Juan, UAT test 1)
- `group_state` attachment-id proof deferred to the flag-flip PR (Juan, UAT test 2)
- Post-merge ledger commits go on a feature branch (`feat/phase-11-incremental-read`), never to master
- Discuss before plan for Phase 11 (Juan) — design-heavy phase
- Fold the run-memory REVIEW follow-ups todo into Phase 11 context (Juan); snapshot_store mypy todo stays out
- Carry-forward locks NOT to re-ask: D-07 weekly deep run = full reconciliation, no cron change;
  D-09 gate OPEN (MEM-04 PASS); schema D-01..D-05; Supabase fail-open contract; SDK pin 4.3.0
  (`if_version_after`/`rows_modified_since` verified); `RUN_MEMORY_WRITE_ENABLED` OFF in prod —
  the write-flag flip (preconditions WR-01/WR-04/IN-01 + low-activity comparator rerun) precedes
  any incremental read
</decisions_made>

<blockers>
- None technical. Human input needed: Juan's picks for the four gray areas (advisor tables first).
</blockers>

## Required Reading (in order)
1. `.claude/project-state.md` — where the repo stands (Phase 10 closed, triage, backlog)
2. `.planning/phases/11-incremental-read-affected-group-regeneration/11-DISCUSS-CHECKPOINT.json` — selection state to resume
3. `.planning/phases/10-run-memory-foundation-shadow-writes/10-CONTEXT.md` — locked D-01..D-09
4. `docs/superpowers/specs/2026-08-24-supabase-run-memory-design.md` §4 run algorithm, §8 decisions, §9 expected effect (untracked file — do not delete)
5. `.planning/phases/10-run-memory-foundation-shadow-writes/10-05-SUMMARY.md` — MEM-04 PASS verdict, SAFETY_WINDOW probes
6. `.planning/todos/pending/2026-08-25-run-memory-review-followups.md` — folded follow-ups (WR-01/WR-04/IN-01 open; WR-02/WR-03 closed)
7. `memory-bank/living-ledger.md` entries `[2026-08-25 21:50]`, `[23:25]`, `[23:55]`, `[2026-08-26 00:25]`

## Critical Anti-Patterns (do NOT repeat these)
- See the table above — all advisory; none gates resumption.

## Infrastructure State
- Supabase `poeyztlmsawfoqlanucc`: `pipeline_memory` schema LIVE (service_role-only, no DELETE); production write path OFF (`RUN_MEMORY_WRITE_ENABLED` unset in `weekly-excel-generation.yml`); two diagnostic `run_ledger` rows remain (operator delete only)
- Smartsheet `Sandbox` rig sheets `6295051624730500` / `4909062725521284` — disposable
- GitHub: master `fb11109`; branch `feat/phase-11-incremental-read` = `b96afff` pushed; no open PR from it
- Local: `generated_docs/hash_history.json` carries a pre-existing prune-marker diff (leave or discard); scratchpad-only helper scripts are outside the repo
- No background processes, no async jobs

<context>
Phase 11 is design-heavy: how incremental reads (`if_version_after` → `rows_modified_since` −
SAFETY_WINDOW) compose with the production grouping/upload code; affected (WR, week) set is
already returned by `upsert_rows_bulk` but was locked observability-only in Phase 10; parity
(INC-04) must be proven in-process within one run because two separate runs can never be byte-
identical on live data (Phase 10 lesson); rollout must sequence the write-flag flip before the
incremental flag; INC-05 retirement of hash_history/discovery_cache/frozen_rows/pre-fetch only
after parity. The advisor tables were meant to give Juan 3–5 stability-first options per area.
</context>

<next_action>
Start with: `/gsd-discuss-phase 11` → choose "Resume" at the checkpoint prompt → skip advisor
research (all four tables are on disk, see checkpoint `advisor_research_persisted`) → present each
table + rationale one area at a time, AskUserQuestion with the table's Option column, record Juan's
pick, ≤2 follow-ups per area → "Ready to create context?" → write `11-CONTEXT.md` +
`11-DISCUSSION-LOG.md`, commit → `/gsd-plan-phase 11`.
</next_action>
