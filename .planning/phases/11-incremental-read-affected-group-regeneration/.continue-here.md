---
context: phase
phase: 11-incremental-read-affected-group-regeneration
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-08-30T07:42:22.683Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Never `git add -A` (or `git add <dir>`) in this public repo — stage named paths only. `.planning/` carries untracked working files by design (session lock, research cache, pattern maps, debug notes with real crew names / WRs); an `add -A .planning` on #370 pushed a PII-bearing debug note in a superseded commit. Mitigation now in place: `.gitignore` covers `.planning/milestone.lock`, `.planning/research/.cache/`, `.planning/debug/`; the rule is in the ledger `[2026-08-29 16:45]`.
- [ ] CONSTRAINT: A streak reading of `0` is NOT "no passes" — `get_parity_streak()` zeroes on any `fail` it meets before reaching the target. Read `run_ledger.notes` (`execution_type`, `streak_eligible`, `parity_verdict`) and `status` to see the banked passes; never decide 11-08 from the bare integer.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Directory-wide staging in a public repo | `git add -A .planning` swept four untracked files (session lock, research cache, pattern map, PII debug note) into a pushed commit on #370; caught by review bots, branch rewritten within minutes, never merged. | blocking | Stage named paths; `.gitignore` now covers the lock, cache and `.planning/debug/`; verify `git diff --cached --stat` before every commit. |
| Reading the streak integer as the evidence | `get_parity_streak()` returns `0` while four counted passes exist, because the older fail resets and stops the walk. | blocking | Read the `run_ledger` rows; the `contributing_run_ids` list is emptied on a reset. |
| Probing a gsd-tools `state` subcommand with no args | `state advance-plan` (no args) mutated STATE.md; `state milestone-switch --milestone` resets Current Position to "defining requirements" — wrong for an executing milestone. | advisory | Back up STATE.md before any `state` call; for a pointer change edit the frontmatter surgically then `state update-progress`. |
| Long heredocs in the Bash tool | Multi-hundred-line `<<'EOF'` scripts fail to parse and nothing runs. | advisory | Write scripts to the scratchpad with the Write tool, then execute them. |
| Deriving a CDT timestamp by feel | A ledger entry was stamped 14:45 CDT for work done at 21:45Z (16:45 CDT); caught by Codex. | advisory | Compute Central time from the UTC clock (`date -u` −5h CDT / −6h CST). |
| Stacked PR orphaned by squash-merge | #352 was stacked on a feature branch; squash-merging #351 deleted that branch and GitHub auto-closed #352. | advisory | Cut follow-up PRs from `master`, or stack only on a branch whose PR is still open (#372 on #371 — merge #371 first). |
| `git rebase` refused by an unstaged runtime artifact | `generated_docs/hash_history.json` was always dirty; rebase aborted silently inside a `set -e` chain. | advisory | Those files are untracked since #366; still check `git status` before rebase. |
| Operator SQL written from memory | Runbook referenced `run_ledger.sheets_errored`, which does not exist (`notes->>'mem_sheets_errored'`). | advisory | Verify column names against `information_schema` before writing SQL into docs. |
| Executor spawned for a `blocking-human` first task | 11-08's Task 1 is a gate; an executor can only bounce it. | advisory | Present the gate from the plan text in the main session; spawn only after the decision is recorded. |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 11, plan 11-08 (INC-05 retirement), Task 1 = the `blocking-human` gate. 11-07 recorded `defer`. D-09 was amended by the owner on 2026-08-29 (PR #372, open, stacked on docs PR #371): the streak counts `production_frequent`, `weekend_maintenance`, and `manual` runs whose `notes.streak_eligible` is true; a `pass` counts only on a `status = success` row; only the weekly deep run stays out. Counted passes since the 2026-08-28 03:49Z fail: Fri 17:04Z, Fri 23:12Z, Sat 19:11Z = **3 of 5**. The 2026-08-29 21:58Z owner-directed manual run passed parity (group 4/4, read 121 sheets / 0 mismatches, 154 withheld, `group_state` 192/192 with `attachment_id`) but predates the marker and is not counted (not back-filled by hand). Master is `c2859e2`; working tree left on `feat/parity-streak-count-all-run-types` so this handoff commits with #372.
</current_state>

<completed_work>

Completed Tasks:
- 11-01 … 11-07: Done (see HANDOFF.json for commits); write flip live since #353.
- Post-phase PR chain through 2026-08-29: #365 no-target-row skip (verified: 154 groups withheld, ~13–15 min/run saved), #366 identifier scrub, #367/#368 docs, #369 Notion counter (type-gated), #370 GSD hygiene (STATE → v1.4; stale v1.2 checkpoint + ingest report archived) — all merged.
- #371 (docs; classifier facts, PII locator removed, `.gitignore` guards) and #372 (D-09 amendment + review guards; 1851 tests) — open, review-clean.
- 11-08 Task 1: In progress — gate not yet satisfied (3 of 5).
</completed_work>

<remaining_work>

- Merge #371 then #372; pull master.
- Two more counted passes (Sat 23Z, Sun 15Z at the earliest; weekend runs count after #372; a default-input dispatch counts once the writer with the marker is on master).
- Re-open 11-07 Task 2: record `retire-now` or `retire-code-keep-cache-steps` in 11-07-SUMMARY.md with the real `get_parity_streak()` output pasted.
- `/gsd-execute-phase 11` → 11-08 as its own PR (never bundled); ledger/docs write-back rides on it (owner rule: no standalone docs PRs).
- Then: WINDOWS.md #2 (deep-run deleted-last-row deviation); the three `.planning/todos/pending`; Phase 01.1 W007; optional 2-runs/day weekend cron decision.
</remaining_work>

<decisions_made>

- D-09 amended (owner): streak counts weekend + manual runs — production is logged through the weekend, and a dispatch runs the same code path on the same sheets.
- Review guards (PR #372 round 2): `run_ledger_finish` writes `notes.streak_eligible` (False under `MAX_GROUPS`, `WR_FILTER`, `EXCLUDE_WRS`, `REGEN_WEEKS`, `RESET_WR_LIST`, `FORCE_GENERATION`, `RESET_HASH_HISTORY`, `TEST_MODE`, `SKIP_UPLOAD`, non-`both` `RES_GROUPING_MODE`); manual rows count only when True; a failed job's pass is excluded (not a reset); a `fail` resets regardless of status.
- No GitHub Support purge of the superseded commit (owner); its locator lives only in the private session incident record.
- No standalone docs PRs (owner); Stop-hook write-back is satisfied by a vault edit.
- Weekend cron left at 3 runs/day; no workflow edit without explicit approval.
</decisions_made>

<blockers>
- 11-08 gate: 3 of 5 counted passes; external (scheduled runs) — wait, or owner override of D-12.
- Human: merge #371 → #372; add Notion Number property `Groups No Target Row`.
</blockers>

## Required Reading (in order)
1. `.claude/project-state.md` — current repo status, PR chain, exact next steps (note: the #371/#372 branch copy is newer than master's until they merge).
2. `memory-bank/living-ledger.md` `[2026-08-29 17:55]` (D-09 amendment + guards), `[2026-08-29 16:45]` (classifier facts, staging rule), `[2026-08-28 20:15]` (parity read-back, Notion export).
3. `.planning/phases/11-incremental-read-affected-group-regeneration/11-07-SUMMARY.md` — the recorded `defer` decision and the amended re-authorisation path.
4. `.planning/phases/11-incremental-read-affected-group-regeneration/11-08-PLAN.md` — Task 1 gate as amended (counted execution types).
5. `.planning/phases/11-incremental-read-affected-group-regeneration/11-CONTEXT.md` D-09 — amendment note above the original text.
6. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses.

## Critical Anti-Patterns (do NOT repeat these)
- See the table above; the first two rows are `blocking`.
- The former root `.planning/.continue-here.md` (Phase 08 / v1.2, 2026-06-08) is archived as `.planning/milestones/v1.2-continue-here-2026-06-08.md` — not Phase 11.

## Infrastructure State
- Production workflow `weekly-excel-generation.yml` on `master`: `RUN_MEMORY_WRITE_ENABLED: '1'`; `RUN_MEMORY_INCREMENTAL_ENABLED` unset. Cron: weekdays UTC 13/15/17/19/21/23/01 (7/day), Sat/Sun 15/19/23Z (3/day), Monday 05Z deep run (`weekly_comprehensive` by cron identity). Classification is by Central-time weekday at job start; `workflow_dispatch` → `manual`.
- Supabase `poeyztlmsawfoqlanucc` `pipeline_memory.run_ledger` tail (newest first): 33277374958.1 manual pass · 33270215341.1 weekend pass · 33259633187.1 weekend skipped · 33219619070.1 frequent pass · 33214308280.1 frequent skipped · 33192967064.1 frequent pass · 33139987509.1 frequent FAIL. `group_state` 192 rows, all with `attachment_id`.
- Untracked by design: `11-PATTERNS.md` (lands with the 11-08 PR); `.planning/debug/unknown-foreman-helper-shadow-2026-08-24.md` (PII — alias before ever tracking).
- No background processes; the run monitor for 33277374958 has exited.

## Pre-Execution Critique Required
- Not applicable (no design pending execution; 11-08 executes from its existing plan once the gate opens).

<context>
The remaining phase is evidence-gated and the evidence is accumulating on its own: every parity-evaluated run since #361 has passed (four passes, two skipped, zero fails across three execution types). #372 makes the gate count what production actually does (weekends, dispatches) while refusing partial or failed observations. Nothing in 11-08 should be touched until 11-07 Task 2 is re-recorded with a real streak reading of five.
</context>

<next_action>
Start with: `gh pr view 371 --json state,mergedAt` and `gh pr view 372 --json state,mergedAt`; once both are merged, `git checkout master && git pull --ff-only`, then run `get_parity_streak()` (see HANDOFF.json `next_action` for the exact command). If it reports `streak >= 5`, re-open 11-07 Task 2 and run `/gsd-execute-phase 11` for 11-08 as its own PR; otherwise list the counted rows from `run_ledger.notes` and wait for the next scheduled run.
</next_action>
