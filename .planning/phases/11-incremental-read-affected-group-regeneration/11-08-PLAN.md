---
phase: 11-incremental-read-affected-group-regeneration
plan: 08
type: execute
wave: 8
depends_on: ["11-07"]
files_modified:
  - pipeline/orchestrate.py
  - pipeline/config.py
  - pipeline/discovery.py
  - pipeline/cleanup.py
  - .github/workflows/weekly-excel-generation.yml
  - .github/prompts/configuration-environment.md
  - CLAUDE.md
  - memory-bank/living-ledger.md
  - docs/run-memory-write-flip-checklist.md
  - tests/test_incremental_read.py
autonomous: false
requirements: [INC-05]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The retirement removes generated_docs/hash_history.json, generated_docs/discovery_cache.json, generated_docs/billing_audit_frozen_rows.json, the two attachment pre-fetch phases with their sub-budget constants, and the three GitHub Actions cache restore/save step pairs (D-12, INC-05)."
    - "group_state holds the attachment ids the retired attachment pre-fetch used to supply, proven on the flip PR's first real upload (INC-05)."
    - "group_state.content_hash is the sole change-detection skip gate; no run reads or writes generated_docs/hash_history.json (INC-05, D-12)."
    - "Sheet discovery reads sheet_registry rather than generated_docs/discovery_cache.json (INC-05, D-12)."
    - "The operator escalation flags RESET_HASH_HISTORY, REGEN_WEEKS, RESET_WR_LIST and FORCE_GENERATION still force a full regeneration, now by escalating the run to full mode through D-02 trigger 5 against group_state."
    - "The frequent-run wall clock is recorded before and after the retirement against the 94-minute baseline from run 32743959053 (ROADMAP success criterion 4)."
    - "memory-bank/living-ledger.md carries a dated [YYYY-MM-DD HH:MM] Phase 11 entry recording the D-02 escalation triggers, the shadow-parity comparator, the D-09 streak semantics and the INC-05 retirement (CLAUDE.md Autonomous Cloud Memory Injection)."
    - statement: "The retirement removals are committed only after plan 11-07's run_ledger scan recorded five consecutive parity-evaluated runs of the counted execution types (production_frequent, weekend_maintenance, manual -- D-09 as amended by the owner 2026-08-29) at verdict pass and its blocking-human checkpoint returned an approving option."
      verification: backstop
  artifacts:
    - path: ".github/workflows/weekly-excel-generation.yml"
      provides: "Cache restore/save step pairs removed for the three retired local JSON caches"
    - path: "pipeline/orchestrate.py"
      provides: "Attachment pre-fetch phases and hash-history maintenance removed; group_state is the sole source of attachment ids and content hashes"
    - path: "docs/run-memory-write-flip-checklist.md"
      provides: "Streak evidence and the before/after wall-clock record"
      contains: "32743959053"
    - path: "memory-bank/living-ledger.md"
      provides: "Dated Phase 11 closing entry — the operational rules this phase establishes"
      contains: "Phase 11"
  key_links:
    - from: "pipeline/orchestrate.py"
      to: "pipeline_memory/writer.py"
      via: "group_state.content_hash becomes the sole skip gate once hash_history.json is retired"
      pattern: "group_state"
    - from: "pipeline/discovery.py"
      to: "pipeline_memory/reader.py"
      via: "sheet discovery reads sheet_registry instead of the retired discovery_cache.json"
      pattern: "sheet_registry"
    - from: "pipeline/cleanup.py"
      to: "pipeline_memory/writer.py"
      via: "the cleanup consumers resolve attachment identity from group_state, falling back to the existing per-row on-demand lookup"
      pattern: "group_state"
  prohibitions:
    - statement: "Must NOT execute any task in this plan before plan 11-07's decision checkpoint returned retire-now or retire-code-keep-cache-steps with the streak reading attached."
      status: unverified
      verification: flagged
    - statement: "Must NOT change the cron schedule, timeout-minutes, TIME_BUDGET_MINUTES or the advanced_options key-value parser while editing the workflow — only the six cache steps come out."
      status: unverified
      verification: flagged
    - statement: "Must NOT remove the per-row on-demand attachment fallback in pipeline/cleanup.py — that fallback becomes the primary path and is what makes the pre-fetch removal safe."
      status: unverified
      verification: flagged
    - statement: "Must NOT bundle this plan with plans 01-07 — D-12 requires the retirement to be its own PR, cut strictly after the streak."
      status: unverified
      verification: flagged
---

<objective>
Ship INC-05: retire the local JSON caches and the two attachment pre-fetch phases, and
close Phase 11 with its Living Ledger entry. Executed strictly after plan 11-07's streak
reading and its blocking-human decision.

Purpose: `hash_history.json`, `discovery_cache.json`, `billing_audit_frozen_rows.json` and
the two attachment pre-fetch phases are the live rollback path through burn-in. The three
`actions/cache/save@v4` steps carry `if: always()` precisely so a failed run keeps its
cache state. Removing them is the last irreversible-feeling step of the phase, and D-12
rates its undo cost as costly: reverting means restoring an operational rollback path in a
workflow and code PR.

Output: the retirement removals, the six GitHub Actions cache steps removed, the
before/after wall-clock record against the 94-minute baseline (run `32743959053`), and the
dated Phase 11 entry in `memory-bank/living-ledger.md`.

**This plan is cut as its own PR and never bundled** (D-12). It must not execute in the
same run as plans 01-07. It edits `.github/workflows/weekly-excel-generation.yml`, a
protected area, so it opens with a blocking gate that reads back plan 11-07's recorded
decision and re-confirms the streak still holds at execution time — the two plans land in
separate PRs, possibly days apart, and an intervening `fail` verdict invalidates the
authorisation. It is not autonomous.

Task shape note: of the four tasks below, task 1 is a blocking gate and task 4 is a
documentation append. The two heavy tasks are the pre-fetch retirement and the cache /
workflow retirement.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-incremental-read-affected-group-regeneration/11-CONTEXT.md
@.planning/phases/11-incremental-read-affected-group-regeneration/11-RESEARCH.md
@.planning/phases/11-incremental-read-affected-group-regeneration/11-07-SUMMARY.md
@docs/run-memory-write-flip-checklist.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: GATE — read back the 11-07 decision and re-confirm the streak still holds</name>
  <what-built>
    Plans 01-06 landed the incremental read path, the D-06 preservation gates, the scoped
    regeneration branch, the shadow-parity comparator and the deep-run reconciliation.
    Plan 07 built `get_parity_streak` and recorded the INC-05 retirement decision plus the
    `run_ledger` streak reading in `11-07-SUMMARY.md`.

    This plan is a separate PR cut after that decision (D-12: the retirement is never
    bundled), so the authorisation has to be re-confirmed at the moment the removals
    actually happen. Real scheduled runs continue between the two PRs, and a single `fail`
    verdict in that window withdraws the approval that plan 07 obtained.
  </what-built>
  <how-to-verify>
    1. Open `11-07-SUMMARY.md` and read the recorded option id. It must be `retire-now`
       or `retire-code-keep-cache-steps`. On `retire-code-keep-cache-steps`, task 3's
       workflow edits are skipped and only its code removals apply. On `defer`, stop —
       this plan does not execute this phase.
    2. Re-run the streak scan now: `get_parity_streak` must still report five consecutive
       `pass` verdicts across the counted execution types it implements (`production_frequent`,
       `weekend_maintenance`, and `manual` only when `notes.streak_eligible` is true; passes only
       on `status = success` rows -- D-09 as amended 2026-08-29) with no intervening `fail`. A `fail`
       since the decision withdraws the authorisation. A `None` return means "cannot
       confirm" and is also a stop.
    3. Read the current frequent-run wall clock from the Actions log for the "before"
       figure, against the 94-minute baseline from run `32743959053`.
    4. Confirm this PR contains no plan 01-07 work — D-12 requires the retirement to be
       its own PR.

    Paste the option id, the fresh streak reading (count, contributing run ids, stopping
    row) and the "before" wall-clock figure with its run id.
  </how-to-verify>
  <resume-signal>Reply with the option id, the fresh streak reading, and the before wall-clock figure. Reply `halt` if the streak no longer holds or the recorded option was `defer`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire the two attachment pre-fetch phases and their sub-budgets</name>
  <precondition>Task 1's gate returned `retire-now` or `retire-code-keep-cache-steps`, and the re-run streak reading showed five consecutive `pass` verdicts.</precondition>
  <reversibility rating="costly">Undoing this removes and restores an operational rollback path across `orchestrate.py` and `config.py`; it touches every consumer of the attachment cache rather than one call site (CONTEXT.md D-12).</reversibility>
  <files>pipeline/orchestrate.py, pipeline/config.py, pipeline/cleanup.py, .github/prompts/configuration-environment.md, CLAUDE.md, docs/run-memory-write-flip-checklist.md</files>
  <read_first>
    - `pipeline/orchestrate.py` lines 1115-1200 — the target-row attachment pre-fetch block, its pre-flight guard and its per-future timeout.
    - `pipeline/orchestrate.py` lines 1290-1450 — the PPP attachment pre-fetch block.
    - `pipeline/config.py` lines 112-128 — `ATTACHMENT_PREFETCH_MAX_MINUTES` and `ATTACHMENT_PREFETCH_FUTURE_TIMEOUT_SEC`.
    - `pipeline/cleanup.py` — `_has_existing_week_attachment`, `delete_old_excel_attachments` and `cleanup_untracked_sheet_attachments`, the three consumers that today accept a missing cache entry and fall back to a per-row on-demand lookup. That fallback is what makes removal safe.
    - `pipeline_memory/writer.py` — `upsert_group_state` and the attachment-id columns that replace the pre-fetched cache.
    - `.github/prompts/configuration-environment.md` — the `ATTACHMENT_PREFETCH_*` entries to remove.
    - `CLAUDE.md` — the Data Pipeline Architecture section describing the pre-fetch phase, which must be updated in the same change so the documentation does not describe a phase that no longer exists.
  </read_first>
  <behavior>
    - Neither attachment pre-fetch phase runs; the pipeline issues no bulk pre-fetch calls.
    - The three cache consumers resolve attachment identity from `group_state` where it is available, and fall back to their existing per-row on-demand lookup otherwise — the fallback path they already have is not removed.
    - `ATTACHMENT_PREFETCH_MAX_MINUTES` and `ATTACHMENT_PREFETCH_FUTURE_TIMEOUT_SEC` are gone from `pipeline/config.py` and from the env reference; setting them has no effect and nothing imports them.
    - `TIME_BUDGET_MINUTES` and the runner's `timeout-minutes` are unchanged — this task removes work, it does not re-budget the session.
    - The full existing test suite passes; any test that asserted pre-fetch behaviour is removed or rewritten against the on-demand path, not deleted silently.
    - `CLAUDE.md`'s pipeline description no longer names a pre-fetch phase.
  </behavior>
  <action>
    Remove the two attachment pre-fetch phases from `pipeline/orchestrate.py` — the target-row block and the PPP block — together with their pre-flight guards, per-future timeouts and the in-memory cache they populate. Remove `ATTACHMENT_PREFETCH_MAX_MINUTES` and `ATTACHMENT_PREFETCH_FUTURE_TIMEOUT_SEC` from `pipeline/config.py` and their entries from `.github/prompts/configuration-environment.md`.

    Do not remove the on-demand fallback paths in `pipeline/cleanup.py`. `_has_existing_week_attachment`, `delete_old_excel_attachments` and `cleanup_untracked_sheet_attachments` already accept a missing cache entry and fall back to a per-row lookup; that fallback becomes the primary path and must stay. Where `group_state` already carries the attachment id for a group, prefer it over an on-demand call.

    Leave `TIME_BUDGET_MINUTES` at 165 and the runner's `timeout-minutes` at 180. This task removes work from the run; it does not re-budget the session, and 10-CONTEXT D-07 locks the schedule and budget family.

    Update `CLAUDE.md`'s Data Pipeline Architecture section so it no longer describes a pre-fetch phase or its budgets, and note in the same edit that `group_state` now supplies attachment identity. A description of a phase that no longer exists is worse than no description.

    Rewrite, do not silently delete, any existing test that asserted pre-fetch behaviour: each becomes an assertion about the on-demand path or about `group_state` supplying the attachment id. Record in the plan SUMMARY how many tests were rewritten and how many removed as genuinely obsolete.

    Record the "before" frequent-run wall clock read at task 1's gate into `docs/run-memory-write-flip-checklist.md`, against the 94-minute baseline from run `32743959053`.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/ -q` exits 0.
    - `pipeline/config.py` no longer defines the two attachment pre-fetch constants, and no module imports them.
    - A test asserts the cleanup consumers still resolve attachment identity when no cache entry exists.
    - `git diff --exit-code -- .github/workflows/` exits 0 — this task changes no workflow step; task 3 owns that.
    - `docs/run-memory-write-flip-checklist.md` contains the baseline run id `32743959053` and the freshly measured before figure.
    - `python -m py_compile generate_weekly_pdfs.py` exits 0.
    - The plan-level `bash scripts/run_6_gates.sh` gate is green before this plan is declared complete (run once per plan, not per task — it exceeds the 30-second sampling latency in `11-VALIDATION.md`).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_incremental_read.py -q</automated>
    <automated>python -m pytest tests/ -q</automated>
    <automated>python -c "import ast,sys; src=open('pipeline/config.py',encoding='utf-8').read(); names=[t.id for n in ast.walk(ast.parse(src)) if isinstance(n,ast.Assign) for t in n.targets if isinstance(t,ast.Name)]; left=[n for n in names if n.startswith('ATTACHMENT_PREFETCH_')]; print('PREFETCH_CONSTANTS_GONE' if not left else 'STILL_DEFINED:'+str(left)); sys.exit(1 if left else 0)"</automated>
    <automated>git diff --exit-code -- .github/workflows/</automated>
    <automated>python -m py_compile generate_weekly_pdfs.py</automated>
  </verify>
  <done>Both attachment pre-fetch phases and their sub-budget constants are gone; the on-demand fallback and `group_state` supply attachment identity; the time-budget family and the workflow are untouched; `CLAUDE.md` no longer describes a phase that does not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retire the three local JSON caches and their workflow cache steps</name>
  <precondition>Task 1's gate returned `retire-now` (option `retire-code-keep-cache-steps` skips this task's workflow edits and stops after the code removals).</precondition>
  <reversibility rating="costly">Undoing this restores an operational rollback path across a protected workflow file and three code paths; the `if: always()` cache saves exist so a failed run keeps its state, and that safety net is what is being removed (CONTEXT.md D-12).</reversibility>
  <files>.github/workflows/weekly-excel-generation.yml, pipeline/orchestrate.py, pipeline/discovery.py, pipeline/config.py, .github/prompts/configuration-environment.md, CLAUDE.md, docs/run-memory-write-flip-checklist.md, tests/test_incremental_read.py</files>
  <read_first>
    - `.github/workflows/weekly-excel-generation.yml` lines 155-195 — the three `actions/cache/restore@v4` steps for `hash_history.json`, `discovery_cache.json` and `billing_audit_frozen_rows.json`.
    - `.github/workflows/weekly-excel-generation.yml` lines 765-800 — the three matching `actions/cache/save@v4` steps carrying `if: always()`.
    - `.github/workflows/weekly-excel-generation.yml` lines 190-260 — the execution-type step and the env block, both of which must stay exactly as they are.
    - `pipeline/discovery.py` lines 185-300 and around line 673 — `discover_source_sheets` and the `discovery_cache.json` TTL logic.
    - `pipeline/orchestrate.py` — the hash-history load, update and persist paths, and the `group_state.content_hash` write that replaces them.
    - `pipeline/config.py` — `DISCOVERY_CACHE_TTL_MIN`, `USE_DISCOVERY_CACHE`, `RESET_HASH_HISTORY` and the frozen-rows path constants.
    - `billing_audit/` — the `billing_audit_frozen_rows.json` producer and consumer.
    - `CLAUDE.md` — the Critical Pitfalls entry stating hash history is ephemeral in CI, and the Configuration section listing the cache TTL variables.
  </read_first>
  <behavior>
    - `group_state.content_hash` is the sole change-detection skip gate; no run reads or writes `generated_docs/hash_history.json`.
    - Sheet discovery reads `sheet_registry` rather than `generated_docs/discovery_cache.json`; the TTL constants are removed.
    - The frozen-rows path no longer reads or writes `generated_docs/billing_audit_frozen_rows.json`.
    - The three `actions/cache/restore@v4` and three `actions/cache/save@v4 if: always()` step pairs are removed from the workflow; the execution-type step, the env block, the cron schedule, `timeout-minutes` and `TIME_BUDGET_MINUTES` are byte-identical to before.
    - The operator flags that force full regeneration still work: `RESET_HASH_HISTORY`, `REGEN_WEEKS`, `RESET_WR_LIST` and `FORCE_GENERATION` continue to escalate a run to full mode via D-02 trigger 5, now operating against `group_state` rather than the JSON file.
    - The `advanced_options` key-value parser in the workflow is untouched.
    - The frequent-run wall clock is measured after the change and recorded next to the before figure and the 94-minute baseline.
  </behavior>
  <action>
    Remove the three local JSON caches and the six workflow steps that carry them, completing CONTEXT.md D-12.

    In code: make `group_state.content_hash` the sole skip gate, removing the `generated_docs/hash_history.json` load, update and persist paths from `pipeline/orchestrate.py`. Point sheet discovery at `sheet_registry` instead of `generated_docs/discovery_cache.json` and remove the TTL constants from `pipeline/config.py` and their entries from `.github/prompts/configuration-environment.md`. Remove the `generated_docs/billing_audit_frozen_rows.json` read and write path.

    Keep every operator escalation flag working. `RESET_HASH_HISTORY`, `REGEN_WEEKS`, `RESET_WR_LIST` and `FORCE_GENERATION` are documented in operator runbooks and in the `force-week-regeneration` project skill; they must continue to force a full regeneration, now by escalating the run to full mode through D-02 trigger 5 and by ignoring `group_state.content_hash` for the flagged scope. Add a test for each flag.

    In the workflow — protected area, approved at plan 11-07's decision checkpoint and re-confirmed at task 1 — remove exactly the three `actions/cache/restore@v4` steps and the three matching `actions/cache/save@v4 if: always()` steps, and nothing else. The execution-type step, the env block, the cron schedule entries, `timeout-minutes: 180`, `TIME_BUDGET_MINUTES: '165'` and the `advanced_options` key-value parser all stay exactly as they are. Show the resulting diff in the plan SUMMARY so the change is reviewable line by line.

    Update `CLAUDE.md`: the Critical Pitfalls entry about hash history being ephemeral in CI, the Configuration section entries for the discovery-cache TTL variables, and the Data Pipeline Architecture description of the change-detection step, so all three describe `group_state` rather than the retired files.

    Measure the frequent-run wall clock after this change lands and record it in `docs/run-memory-write-flip-checklist.md` next to the before figure and the 94-minute baseline from run `32743959053`, satisfying ROADMAP success criterion 4.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/ -q` exits 0.
    - No module reads or writes `generated_docs/hash_history.json`, `generated_docs/discovery_cache.json` or `generated_docs/billing_audit_frozen_rows.json`.
    - The workflow file contains zero `actions/cache/restore@v4` and zero `actions/cache/save@v4` steps.
    - The workflow still contains `timeout-minutes: 180`, `TIME_BUDGET_MINUTES` set to `'165'`, the four cron schedule entries, and the `advanced_options` parser.
    - A test asserts each of `RESET_HASH_HISTORY`, `REGEN_WEEKS`, `RESET_WR_LIST` and `FORCE_GENERATION` still forces a full regeneration.
    - `docs/run-memory-write-flip-checklist.md` carries the baseline, the before figure and the after figure.
    - The plan-level `bash scripts/run_6_gates.sh` gate is green before this plan is declared complete (run once per plan, not per task — it exceeds the 30-second sampling latency in `11-VALIDATION.md`).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_incremental_read.py -q</automated>
    <automated>python -m pytest tests/ -q</automated>
    <automated>python -c "import sys,pathlib; t=pathlib.Path('.github/workflows/weekly-excel-generation.yml').read_text(encoding='utf-8'); caches=t.count('actions/cache/'); keep=[k for k in ('timeout-minutes: 180','TIME_BUDGET_MINUTES','advanced_options') if k not in t]; print('WORKFLOW_OK' if caches==0 and not keep else 'caches='+str(caches)+' missing='+str(keep)); sys.exit(0 if caches==0 and not keep else 1)"</automated>
    <automated>python -c "import sys,subprocess; out=subprocess.run(['git','grep','-l','-e','hash_history.json','-e','discovery_cache.json','-e','billing_audit_frozen_rows.json','--','pipeline/','billing_audit/','generate_weekly_pdfs.py'],capture_output=True,text=True).stdout.strip(); print('CACHE_REFS:',out or '(none)'); sys.exit(0 if not out else 1)"</automated>
  </verify>
  <done>The three local JSON caches and their six workflow cache steps are gone; `group_state` is the sole skip gate and discovery source; every operator escalation flag still forces a full regeneration; the schedule, budgets and `advanced_options` parser are untouched; the before and after wall-clock figures are recorded against the baseline.</done>
</task>

<task type="auto">
  <name>Task 4: Append the dated Phase 11 Living Ledger entry</name>
  <files>memory-bank/living-ledger.md</files>
  <read_first>
    - `CLAUDE.md` — the "Autonomous Cloud Memory Injection" section: new architectural standards, recurring fixes and operational rules get a dated `[YYYY-MM-DD HH:MM]` entry appended to the BOTTOM of `memory-bank/living-ledger.md`, in the same PR as the code change.
    - `memory-bank/living-ledger.md` — the last three entries, for heading shape (`## [YYYY-MM-DD HH:MM] <title>`) and the level of detail the file's existing entries carry.
    - `.planning/phases/11-incremental-read-affected-group-regeneration/11-CONTEXT.md` — D-01..D-12, the decisions this entry records as binding.
    - The SUMMARY files for plans 11-02, 11-05 and 11-07 — the escalation triggers as shipped, the parity comparator as shipped, and the streak reading that authorised this plan.
  </read_first>
  <action>
    Append one dated entry to the BOTTOM of `memory-bank/living-ledger.md` recording the operational rules Phase 11 establishes. Do not inline it into `CLAUDE.md` and do not edit any earlier entry.

    Heading: `## [YYYY-MM-DD HH:MM] Phase 11 shipped — incremental read + affected-group regeneration; INC-05 retirement` with the real date and time of the commit.

    The entry records, each as a rule a future engineer must not re-litigate:
    - The seven D-02 FULL-read escalation triggers as shipped, and that the safety window alone never self-heals a gap.
    - Capture-time watermark persistence: `last_read_at` is captured immediately before the `rows_modified_since` call and stored as-is; `SAFETY_WINDOW_MINUTES` is subtracted only when building the query. Persist-time subtraction double-subtracts every run.
    - That frequent runs never detect deletions — `rowsModifiedSince` cannot surface an absence — so deletion, formula-only reconciliation and `column_mapping` refresh belong to the weekly deep run.
    - D-06: incremental mode performs zero deletions or prunes for untouched groups; the `keep_historical` gate on attachment cleanup and the full-mode gate on the hash-history prune are why.
    - The shadow-parity comparator: group-key set plus `calculate_data_hash()` equality, verdict in `run_ledger.notes` and never in `run_summary.json`, sub-budgeted, fail-open, never a vacuous `pass`.
    - D-09 streak semantics: derived on demand from `run_ledger` with no counter column; `pass` counts, `fail` resets, `skipped` and absent verdicts are excluded, and a read failure means "cannot confirm", never "satisfied".
    - The INC-05 retirement: what was removed, that `group_state` is now the sole skip gate and discovery source, and the before/after frequent-run wall clock against the 94-minute baseline from run `32743959053`.
    - The rollout ordering rule this phase proves out: a costly-to-revert removal is cut as its own PR strictly after the evidence that authorises it, never bundled with the work that produced the evidence.

    Reference the phase directory and the retirement PR number at the end of the entry so the full diff is traceable. Do not write any token, key, connection string, Supabase project secret, or row-level billing data into the entry.
  </action>
  <acceptance_criteria>
    - `memory-bank/living-ledger.md` gained exactly one new `## [` heading whose text contains `Phase 11`, and it is the last such heading in the file.
    - The entry contains the strings `SAFETY_WINDOW_MINUTES`, `get_parity_streak`, `group_state`, and `32743959053`.
    - No earlier ledger entry was modified: `git diff -- memory-bank/living-ledger.md` shows additions only.
    - `CLAUDE.md`'s Living Ledger section still points at `memory-bank/living-ledger.md` and gained no inlined entry.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import re,sys,pathlib; t=pathlib.Path('memory-bank/living-ledger.md').read_text(encoding='utf-8'); h=re.findall(r'^## \[\d{4}-\d{2}-\d{2} \d{2}:\d{2}\].*$', t, re.M); last=h[-1] if h else ''; ok='Phase 11' in last; print('LEDGER_OK: '+last if ok else 'LEDGER_MISSING_PHASE_11_ENTRY: '+last); sys.exit(0 if ok else 1)"</automated>
    <automated>python -c "import sys,pathlib; t=pathlib.Path('memory-bank/living-ledger.md').read_text(encoding='utf-8'); need=['SAFETY_WINDOW_MINUTES','get_parity_streak','group_state','32743959053']; miss=[n for n in need if n not in t]; print('LEDGER_CONTENT_OK' if not miss else 'MISSING:'+str(miss)); sys.exit(1 if miss else 0)"</automated>
    <automated>python -c "import subprocess,sys; d=subprocess.run(['git','diff','--numstat','--','memory-bank/living-ledger.md'],capture_output=True,text=True).stdout.strip(); dele=d.split('\t')[1] if d else '0'; print('APPEND_ONLY_OK' if dele=='0' else 'DELETIONS:'+dele); sys.exit(0 if dele=='0' else 1)"</automated>
  </verify>
  <done>One dated `[YYYY-MM-DD HH:MM]` Phase 11 entry sits at the bottom of `memory-bank/living-ledger.md` recording the escalation triggers, the capture-time watermark rule, the deep-run reconciliation boundary, the D-06 preservation gates, the parity comparator, the D-09 streak semantics and the INC-05 retirement — additions only, no secrets, nothing inlined into `CLAUDE.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local cache artifacts → operational rollback | The three `if: always()` cache saves are the mechanism by which a failed run keeps its state; removing them removes the recovery path. |
| Editor → protected workflow file | `.github/workflows/weekly-excel-generation.yml` carries the schedule, the runner timeout and the session budget alongside the cache steps. |
| Plan 11-07's recorded decision → this plan's removals | The two plans land in separate PRs, possibly days apart; a `fail` verdict in between withdraws the authorisation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-38 | Tampering | Collateral edit to the protected workflow | critical | mitigate | Task 3 removes exactly six cache steps and nothing else; a verify command asserts the schedule, `timeout-minutes: 180`, `TIME_BUDGET_MINUTES` and the `advanced_options` parser all survive, and the diff is shown in the SUMMARY for line-by-line review. |
| T-11-39 | Denial of service | Session hard-killed by a budget/timeout mismatch | high | mitigate | Task 2 and task 3 both leave `TIME_BUDGET_MINUTES` at 165 and `timeout-minutes` at 180; the 15-minute gap reserved for cache-save and artifact upload is not consumed by this change. |
| T-11-40 | Tampering | Operator escalation flags silently broken by the hash-history removal | high | mitigate | Task 3 adds a test per flag asserting `RESET_HASH_HISTORY`, `REGEN_WEEKS`, `RESET_WR_LIST` and `FORCE_GENERATION` still force a full regeneration against `group_state`. |
| T-11-41 | Tampering | Attachment identity lost with the pre-fetch cache | high | mitigate | Task 2 keeps the existing per-row on-demand fallback in `pipeline/cleanup.py` and prefers `group_state`'s stored attachment ids, whose correctness the flip PR's IN-01 item proved on a real upload. |
| T-11-43 | Repudiation | Stale authorisation — the streak broke between the 11-07 decision and this PR | high | mitigate | Task 1's blocking gate re-runs `get_parity_streak` at execution time and halts if the streak no longer holds or the recorded option was `defer`. |
| T-11-44 | Information disclosure | Secrets or row-level billing data written into the Living Ledger | medium | mitigate | Task 4 names rules, env-var names and run ids only; the action explicitly forbids tokens, keys, connection strings and row data. |
</threat_model>

<verification>
- `python -m pytest tests/test_incremental_read.py -q` — the fast per-task gate (well under the 30-second sampling latency in `11-VALIDATION.md`).
- `python -m pytest tests/ -q` — full suite green.
- `python -m py_compile generate_weekly_pdfs.py` — syntax gate.
- `bash scripts/run_6_gates.sh` — all six gates. **Plan gate: run once before this plan is declared complete**, not after every task; it runs the full pytest suite, the mypy delta and a `TEST_MODE` run, which exceeds the 30-second per-task feedback latency `11-VALIDATION.md` sets.
- Workflow guard: zero `actions/cache/` steps remain while the schedule, `timeout-minutes`, `TIME_BUDGET_MINUTES` and the `advanced_options` parser survive.
- `git diff --exit-code -- pipeline_memory/schema.sql` — zero schema change across the whole phase.
- Ledger guard: the last dated heading in `memory-bank/living-ledger.md` is the Phase 11 entry, and the diff is additions only.

Manual (recorded in `11-VALIDATION.md` Manual-Only Verifications): the frequent-run wall
clock before and after, compared against the 94-minute baseline from run `32743959053`
(ROADMAP success criterion 4), and the re-confirmed streak reading pasted at task 1's gate.
</verification>

<success_criteria>
1. Task 1's blocking gate read back plan 11-07's option id and re-confirmed the streak at execution time.
2. Both attachment pre-fetch phases and their sub-budget constants are removed; attachment identity comes from `group_state` with the existing on-demand fallback intact.
3. The three local JSON caches and their six workflow cache steps are removed; `group_state.content_hash` is the sole skip gate.
4. The schedule, `timeout-minutes`, `TIME_BUDGET_MINUTES` and the `advanced_options` parser are untouched.
5. The before and after frequent-run wall clock is recorded against the 94-minute baseline.
6. `memory-bank/living-ledger.md` carries a dated Phase 11 entry with the phase's operational rules, appended not inlined.
</success_criteria>

## Artifacts this phase produces

Created by this plan:
- The before/after wall-clock section of `docs/run-memory-write-flip-checklist.md`
- The dated Phase 11 entry at the bottom of `memory-bank/living-ledger.md`
- Operator-flag regression tests (`RESET_HASH_HISTORY`, `REGEN_WEEKS`, `RESET_WR_LIST`, `FORCE_GENERATION`) in `tests/test_incremental_read.py`

Removed by this plan (not new symbols, listed so drift verification does not read the absences as regressions): the two attachment pre-fetch phases in `pipeline.orchestrate`; config constants `ATTACHMENT_PREFETCH_MAX_MINUTES`, `ATTACHMENT_PREFETCH_FUTURE_TIMEOUT_SEC`, `DISCOVERY_CACHE_TTL_MIN`, `USE_DISCOVERY_CACHE`; the `generated_docs/hash_history.json`, `generated_docs/discovery_cache.json` and `generated_docs/billing_audit_frozen_rows.json` read/write paths; six `actions/cache/` workflow steps.

Created by other Phase 11 plans (excluded from drift verification): `pipeline_memory/reader.py` with `get_sheet_watermarks`, `get_last_run_ledger_status` (plan 02), `map_affected_to_sheets` (plan 04), `get_row_state_row_ids` (plan 06), `get_parity_streak` (plan 07); `pipeline.fetch._is_abbreviated_response`, `pipeline.fetch.fetch_sheet_delta`, `pipeline.orchestrate.resolve_run_mode`, `RUN_MEMORY_INCREMENTAL_ENABLED`, `SAFETY_WINDOW_MINUTES`, `run_ledger.notes` key `fallback_reason`, `tests/test_incremental_read.py`, `tests/fixtures/incremental/abbreviated_sheet_response.json` (plan 02); `keep_historical` on `pipeline.cleanup.cleanup_untracked_sheet_attachments` (plan 03); the PHASE 2a / PHASE 2b branch (plan 04); `pipeline/parity.py` with `compare_shadow_parity` and `run_shadow_delta_reads`, the `RUN_MEMORY_SHADOW_*` constants, `run_ledger.notes` keys `parity_verdict` and `parity_details`, `tests/test_parity_shadow.py` (plan 05); `pipeline_memory.writer.mark_rows_deleted`, `tests/test_deep_run_reconciliation.py`, `tests/fixtures/incremental/deleted_row.json` and `formula_only_change.json` (plan 06); `__mem_quantity`, `__mem_units_total_price`, `docs/run-memory-write-flip-checklist.md` (plan 01).

<output>
Create `.planning/phases/11-incremental-read-affected-group-regeneration/11-08-SUMMARY.md` when done.
</output>
