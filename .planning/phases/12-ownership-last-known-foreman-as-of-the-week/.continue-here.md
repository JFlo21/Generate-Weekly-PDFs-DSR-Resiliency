---
context: phase
phase: 12-ownership-last-known-foreman-as-of-the-week
task: 4
total_tasks: 4
status: in_progress
last_updated: 2026-09-10T18:05:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: 12-06 Tasks 1–3 are DONE and the apply is ONE-WAY — the live backfill landed 2026-09-05 03:45 UTC
      (1,758 rows updated, 0 errors; `12-06-SUMMARY.md` Task 3 record). Do NOT re-run the dry-run / decision / apply
      workflow. The 12-06 SUMMARY still carries `status: blocked` (the only re-entrant state), so a bare
      `/gsd-execute-phase 12` would resolve 12-06 as runnable FROM TASK 1 — resume at Task 4 only (see next_action).
- [ ] CONSTRAINT: Rubric-pass ≠ safe — a green suite shipped a fix with defective coverage (CR-01: the hash-suffix
      regex was written against a 6-hex test fixture while `pipeline/excel.py:412` emits a 16-hex tail) — every PR that
      touches an attribution writer gets a read-only Opus `production-risk-reviewer` pass before it opens (12-10 Task 1
      pattern), and fixtures for filename parsers derive from the production builder.
- [ ] CONSTRAINT: Living-ledger `## [` headings break the push gate — `tests/test_own04_documentation.py::_newest_ledger_entry`
      asserts D-12-C/D-12-D live in the LAST `## [` heading of `memory-bank/living-ledger.md` (WR-01). Append new entries
      as dated `[YYYY-MM-DD HH:MM]` paragraphs (the ledger's current style), never as a new `## [` heading, and run
      `python -m pytest tests/test_own04_documentation.py -q` after any ledger edit.
- [ ] CONSTRAINT: The harness-boundary hook denies any `gsd-tools` argument containing the token "review" (commit
      messages, `state.add-decision` summaries, `query init.code-review`, `resolve-model gsd-code-reviewer`) — reword
      ("verdict", "code-quality report") and resolve the inputs from `.planning/config.json` and the phase directory.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Re-running a landed one-way apply | `status: blocked` on 12-06 SUMMARY makes `/gsd-execute-phase 12` restart at Task 1 although Tasks 1–3 landed 2026-09-05 | blocking | Resume at Task 4 by hand (orchestrator + `12-06-PLAN.md` Task 4 checklist); never invoke `--apply` again |
| Rubric-pass ≠ safe | 12-07 tests were green while the hash-suffix regex under-matched production's 16-hex tail (CR-01) | blocking | Opus production-risk pass before any attribution-writer PR; fixtures from the production builder |
| Newest-ledger-entry test pin | `test_own04_documentation.py` pins two decisions to the last `## [` ledger heading | blocking | Dated `[..]` paragraph appends only; run the doc test after ledger edits |
| "review" token in gsd-tools args | Harness-boundary hook denied three commands in the 2026-09-04 session | blocking | Reword; read config/phase files instead of the `init.code-review` / `resolve-model` queries |
| Bash-scripted ledger edits | The context-continuity Stop hook reads `.claude/session-delta.jsonl` (Edit/Write only) and kept demanding updates already made via sed/python | advisory | Touch `.claude/project-state.md` and `docs/CHANGELOG_CONTEXT.md` with the Edit tool, commit with Bash |
| Halted SUMMARY satisfies the shared-ID ready gate | The 12-10 executor marked OWN-02/OWN-03 Complete because 12-06's halted SUMMARY counted as done; reverted in `5121c30` | advisory | After any gaps-only run whose target plan is still pending, audit `REQUIREMENTS.md` |
| Executor cannot dispatch agents | `gsd-executor` has no Agent tool, so "dispatch an Opus review" tasks stall inside it | advisory | Orchestrator runs the reviewer and passes the verdict to a continuation executor |
| Executors touching Supabase | Plans forbid executors from the Supabase MCP or DDL | advisory | Only the orchestrator runs owner-named steps, read-only by default, on Juan's explicit instruction |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 12 (Ownership — last known foreman as of the week): gap plans 12-07..12-10 closed G-12-3 (merged to master in
`ba6eeaf`, PR #389, together with Phase 14). Plan 12-06 (owner-run OWN-03 live remediation) has Tasks 1–3 DONE on
2026-09-05 — full-scope dry-run APPROVED (`I approve`), decision `apply-full`, live apply at Juan's `Run it`: 1,758 rows
updated / 0 skipped / 0 errors, 0 differences on the 219,518 untouched rows, provenance total 1,758 — and Task 4
(checkpoint:human-verify, post-apply scheduled-run observation) still PENDING. Phase 14 (Foreman Helper #2) ran to
completion in between and its code-review fix lane is PR #402; the Phase 14 handoff (`.planning/HANDOFF.json` Task 3)
points here for the Task 4 detail. Phase-goal verification and the roadmap update stay skipped until Task 4 is recorded.
</current_state>

<completed_work>

12-06 Tasks 1–3 (2026-09-05, all recorded in `12-06-SUMMARY.md`):
- Task 1 re-run (read-only dry-run, 00:24–00:25 UTC): 207 WRs × 54 weeks, 1,758 proposed rows / 30 WRs / 76 pairs,
  0 conflicts, 4,071 unresolved (no in-week evidence anywhere in Supabase; route to source 5 later); every G-12-3 /
  CR-01 guard 0 (extension-bearing 0, hash-tailed 0); 1,758/1,758 proposals agree with the `public.artifacts`
  filename. Juan APPROVED as-is.
- Task 2 decision: `apply-full` (same `--wr`/`--weeks` scope as the dry-run, no `--include-blank-roles`).
- Task 3 apply (03:40–04:15 UTC): fresh same-UTC-day STEP 1 backup `attribution_snapshot_backup_20260905`
  (221,276 rows = live); `--apply --i-approved-this` → updated 1,758 / skipped_real_name 0 / skipped_no_row 0 /
  errors 0; `backfill_source` {live 692, backfill_artifacts 1,066}; `frozen_helper` / `frozen_vac_crew` untouched;
  no extension or hash tail written. Observation for verify-work: `backfill_run_id` is `''` (not NULL) on all 1,758
  rows because `_compute_run_id()` returns `''` without `GITHUB_RUN_ID` (local operator run) — low-severity note.

Earlier (2026-09-03 → 2026-09-04, gap closure run):
- 12-07: `_extract_claimer_from_filename` strips exactly one document extension when the hash tail is absent and rejects
  any candidate still ending in one; `_build_apply_payload` guards `proposed_value` (commits `8d27e36`…`f14aa5c`).
- 12-08: Juan's decisions D-12-C `defer` and D-12-D `substitute-89829163` recorded in ROADMAP, runbook, ledger (`83aa924`).
- 12-09: RPC extension-refusal guard added to `billing_audit.backfill_attribution` (`is_sentinel_value` byte-identical);
  STEP 4 + STEP 5 + NOTIFY applied via the Supabase MCP (EXECUTE = `service_role` only).
- 12-10: Opus production-risk verdict `pass`; backup `attribution_snapshot_backup_20260904` (220,621 rows); scoped
  dry-run WR 89732091 × 7 weeks → 0 proposed / 235 unresolved, 0 extension-bearing; Juan replied `re-enter-12-06`.
- Close-out: OWN-02/OWN-03 reverted to Pending (`5121c30`); 12-06 SUMMARY `halted` → `blocked` (`7ce962a`);
  code-quality report `12-REVIEW.md` (`634d93b`); ledgers (`ff3efb1`, `45e9458`).
- 2026-09-03 first attempt of Task 1 was REJECTED (source-3 filename parser defect) — preserved as history in the SUMMARY.
</completed_work>

<remaining_work>

- 12-06 Task 4 (checkpoint:human-verify) — `12-06-PLAN.md` Task 4 `how-to-verify`, seven observations on a scheduled
  `weekly-excel-generation.yml` run that completed after 2026-09-05 03:45 UTC (many have by now; the first was
  Sat 2026-09-05 15:00 UTC; Juan's early `Verified` at ~05:40 UTC predates any such run and was not accepted):
  1. `sentinel_claimers_ignored` in the run log / `run_summary.json` vs a pre-apply run — must drop for the 30 WRs.
  2. Attachment swap on `TARGET_SHEET_ID`: the 76 backfilled (WR, week) files regenerate under real `_User_<name>`
     names and the `_User_Unknown_Foreman` counterparts are removed by the sentinel-superseded gate. The plan's
     sample WR 19073866 is UNRESOLVABLE (zero rows in every Supabase store; D-12-D substitute WR 89829163 has
     placeholder-only artifacts) — re-decide ROADMAP SC3's sample first; candidates WR 89746993 (4 weeks),
     89841789, 89848991, 90851321.
  3. Spot-check three other backfilled WRs; a WR that did not regenerate gets a scoped `RESET_WR_LIST` nudge
     (`.claude/skills/force-week-regeneration/SKILL.md`), never `scripts/backfill_attribution_snapshot.py`.
  4. No PPP attachment deleted (reset is `TARGET_SHEET_ID`-scoped; gate is same-week, same-variant).
  5. No real person's historical attachment disappeared (CR-01 blast radius; check punctuation-leading claimers).
  6. Python duration vs `TIME_BUDGET_MINUTES` (165) — a regeneration burst on the first run is expected.
  7. Run conclusion and any new Sentry issue fingerprint.
  Transcribe all seven into `12-06-SUMMARY.md` (Task 4 section), flip its frontmatter to a completion record, and only
  then drop the three backups `attribution_snapshot_backup_20260903` / `_20260904` / `_20260905` (owner-run DDL).
  A failed step 2, 4 or 5 is a phase gap for `/gsd:plan-phase 12 --gaps`, never an inline fix.
- Then `/gsd-verify-work 12` (reconcile G-12-3; file CR-01, WR-01, the `backfill_run_id = ''` note and the SC3 sample
  substitution as gaps) → `phase.complete 12`. Phase 13 (`wr_week_ownership`, D-12-A) only when Juan asks.
</remaining_work>

<decisions_made>

- Task 1 re-run APPROVED as-is; Task 2 `apply-full`; Task 3 executed by the orchestrator at `Run it` (2026-09-05).
- The 4,071 unresolved rows have no in-week evidence in any Supabase store (`smartsheet_unified_history` holds only
  post-remap names) — they route to source 5 (Smartsheet cell history) after the apply; the apply scope was not widened.
- D-12-C `defer`: the 945 `#NO MATCH` rows stay out of OWN-03 (already no-history via `resolve_claimer`).
- D-12-D `substitute-89829163` is itself unresolvable as an SC3 sample — re-decide in Task 4 / verify-work.
- 12-06 re-entered through `status: blocked` (the only re-entrant SUMMARY state), not by deleting the SUMMARY; the
  SUMMARY is rewritten into a completion record only when Task 4 is transcribed.
- `verify_phase_goal` / `update_roadmap` skipped until Task 4 is recorded.
- CR-01 and WR-01 are not fixed inside 12-06: the gate is advisory and the parser regex is protected extraction
  behavior; `/gsd-verify-work 12` files them as gaps.
</decisions_made>

<blockers>
- Task 4 needs Juan at the keyboard (human-verify checkpoint): the observations are made on live GitHub Actions runs,
  `TARGET_SHEET_ID` attachments and Sentry — orchestrator reads only, on Juan's instruction.
- ROADMAP SC3 sample: WR 19073866 and WR 89829163 are both unusable; pick a real backfilled WR before step 2.
- Backups `_20260903` (220,010), `_20260904` (220,621), `_20260905` (221,276): retain until Task 4 is recorded, then
  drop — owner-run DDL only.
- CR-01 (latent): `_FILENAME_HASH_SUFFIX_RE` 6-hex vs production 16-hex; 0 of the `public.artifacts` filenames carry
  a hash tail while `SUPABASE_HASH_STORE_AUTHORITATIVE=1` holds. Fix folds with the six-hex-letter over-match ticket.
- WR-01: newest-`## [`-ledger-heading test pin. Workaround: dated-paragraph appends only.
- Owner security item (not on the 12-06 path): `anon`/`authenticated` hold full DML grants on
  `billing_audit.attribution_snapshot` behind RLS — confirm the policies deny them.
</blockers>

## Required Reading (in order)
1. `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-06-SUMMARY.md` — Task 1–3 evidence (counts,
   backup names, provenance note) and the frontmatter re-entry comment (`status: blocked`)
2. `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-06-PLAN.md` — Task 4 `how-to-verify` (the seven
   observations) and its `resume-signal`
3. `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-REVIEW.md` — CR-01 and WR-01 (do not add a
   `## [` ledger heading)
4. `.claude/project-state.md` § Latest work and § Next — repo-level status (Phase 14 / PR #402 state)
5. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses; apply before any assumption analysis

## Critical Anti-Patterns (do NOT repeat these)
- Re-running Tasks 1–3 → the apply is landed and one-way; resume at Task 4 only.
- Rubric-pass ≠ safe → Opus production-risk pass before any attribution-writer PR; fixtures from `pipeline/excel.py`.
- Ledger `## [` heading with the newest-entry test pin in place → dated-paragraph appends; run the doc test.
- "review" token in gsd-tools arguments → reword; resolve inputs from files.
- Bash-scripted ledger edits → Edit tool for ledgers, Bash for commits.
- Trusting the shared-ID ready gate after a halted plan → audit `REQUIREMENTS.md` after gaps-only runs.
- Executors touching Supabase or DDL → orchestrator only, read-only by default, on Juan's explicit instruction.

## Infrastructure State
- Supabase billing project: `billing_audit.attribution_snapshot` 221,276 rows, 1,758 of them with `backfill_run_id`
  (`''`) and `backfill_provenance` set (2026-09-05 apply); `billing_audit.backfill_attribution` carries the extension
  guard (SECURITY INVOKER; EXECUTE `service_role` only). Backups `attribution_snapshot_backup_20260903` (220,010),
  `_20260904` (220,621), `_20260905` (221,276) all present — retained until Task 4.
- GitHub: Phase 12 gap closure and Phase 14 merged to `master` (`ba6eeaf` and later); `cell-history-backfill.yml`
  dispatch-only (no schedule). Phase 14 code-review fix lane: PR #402 (`fix/phase-14-cr01-cr02-wr03`).
- No background processes, servers, or async jobs. Untracked, deliberately unadded: `.agents/`, `.planning/state.json`,
  `.serena/`, `website/.claude/`.

<context>
The remediation itself is finished: 1,758 sentinel primary claimers were replaced with evidence-backed real names on
2026-09-05 and the untouched population was proven byte-identical to the backup. What remains is observational — proving
that production picked the names up (files regenerated under real names, placeholder files removed, nothing on PPP or any
real person's history touched, run within budget) — and then closing the phase. The resuming session must not treat the
`status: blocked` SUMMARY as "nothing ran": it is the re-entry mechanism from 2026-09-04, and the apply has since landed.
</context>

<next_action>
Do NOT run a bare `/gsd-execute-phase 12` (it resolves 12-06 as runnable from Task 1). Instead, with Juan present: open
`12-06-PLAN.md` Task 4, pick the newest completed `weekly-excel-generation.yml` run after 2026-09-05 03:45 UTC, walk the
seven observations read-only (run log / `run_summary.json`, `TARGET_SHEET_ID` attachments for the chosen sample WRs,
PPP count, Sentry), and transcribe them into `12-06-SUMMARY.md` under Task 4. On `approved`, rewrite the SUMMARY
frontmatter into a completion record, drop the three backups at Juan's instruction, then `/gsd-verify-work 12`.
</next_action>
