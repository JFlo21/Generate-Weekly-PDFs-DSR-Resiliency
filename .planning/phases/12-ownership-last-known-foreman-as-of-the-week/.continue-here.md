---
context: phase
phase: 12-ownership-last-known-foreman-as-of-the-week
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-09-04T07:27:59.690Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Rubric-pass ≠ safe — a green suite shipped a fix with defective coverage (CR-01: the hash-suffix
      regex was written against a 6-hex test fixture while `pipeline/excel.py:412` emits a 16-hex tail) — every PR that
      touches an attribution writer gets a read-only Opus `production-risk-reviewer` pass before it opens (12-10 Task 1
      pattern), fixtures for filename parsers derive from the production builder, and the 12-06 Task 1 dry-run report
      must be checked for BOTH extension-bearing and hash-tailed proposed values (expect 0 and 0).
- [ ] CONSTRAINT: Living-ledger append breaks the push gate — `tests/test_own04_documentation.py::_newest_ledger_entry`
      asserts D-12-C/D-12-D live in the LAST `## [` entry of `memory-bank/living-ledger.md` (WR-01) — do not append
      the ledger until that test searches every entry; run `python -m pytest tests/test_own04_documentation.py -q`
      after any ledger edit.
- [ ] CONSTRAINT: The harness-boundary hook denies any `gsd-tools` argument containing the token "review" (commit
      messages, `state.add-decision` summaries, `query init.code-review`, `resolve-model gsd-code-reviewer`) — reword
      ("verdict", "code-quality report") and resolve the inputs from `.planning/config.json` and the phase directory.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Rubric-pass ≠ safe | 12-07 tests were green while the hash-suffix regex under-matched production's 16-hex tail (CR-01) | blocking | Opus production-risk pass before any attribution-writer PR; fixtures from the production builder; dry-run report asserts 0 hash-tailed proposals |
| Newest-ledger-entry test pin | `test_own04_documentation.py` pins two decisions to the last ledger entry; the next append reddens `pytest` and the pre-push hook | blocking | No ledger append until WR-01 is fixed; run the doc test after ledger edits |
| "review" token in gsd-tools args | Harness-boundary hook denied three commands this session | blocking | Reword; read config/phase files instead of the `init.code-review` / `resolve-model` queries |
| Bash-scripted ledger edits | The context-continuity Stop hook reads `.claude/session-delta.jsonl` (Edit/Write only) and kept demanding updates already made via sed/python | advisory | Touch `.claude/project-state.md` and `docs/CHANGELOG_CONTEXT.md` with the Edit tool, commit with Bash |
| Halted SUMMARY satisfies the shared-ID ready gate | The 12-10 executor marked OWN-02/OWN-03 Complete because 12-06's halted SUMMARY counted as done; reverted in `5121c30` | advisory | After any gaps-only run whose target plan is still pending, audit `REQUIREMENTS.md` |
| `status: halted` is not re-entrant | The plan index treats halted as summarized; only `status: blocked` makes a plan runnable again | advisory | 12-06 SUMMARY now carries `status: blocked` with a YAML comment; do not delete the SUMMARY |
| Executor cannot dispatch agents | `gsd-executor` has no Agent tool, so "dispatch an Opus review" tasks stall inside it | advisory | Orchestrator runs the reviewer and passes the verdict to a continuation executor |
| Executors touching Supabase | Plans forbid executors from the Supabase MCP or DDL | advisory | Only the orchestrator runs owner-named steps, read-only by default, on Juan's explicit instruction |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 12 (Ownership — last known foreman as of the week) on `feat/phase-12-remediation`, HEAD `45e9458`, tree clean,
nothing pushed. `/gsd-execute-phase 12 --gaps-only` CLOSED: gap plans 12-07..12-10 done, gap G-12-3 fixed in code and
live SQL. The plan index lists 12-06 (owner-run OWN-03 live remediation) as the only incomplete, runnable plan; it
resumes at Task 1 of 4 (all four are blocking-human checkpoints). Phase-goal verification and the roadmap update were
skipped on purpose because 12-06 has not applied. The advisory code-quality report `12-REVIEW.md` (`634d93b`) holds
1 blocker (CR-01, latent), 9 warnings, 2 info; none fixed, all routed to `/gsd-verify-work 12`.
</current_state>

<completed_work>

Completed this run (2026-09-03 late night → 2026-09-04 07:30 UTC):
- 12-07: `_extract_claimer_from_filename` strips exactly one document extension when the hash tail is absent and rejects
  any candidate still ending in one; `_build_apply_payload` guards `proposed_value` (commits `8d27e36`…`f14aa5c`).
- 12-08: Juan's decisions D-12-C `defer` and D-12-D `substitute-89829163` recorded in ROADMAP, runbook, ledger (`83aa924`).
- 12-09: RPC extension-refusal guard added to `billing_audit.backfill_attribution` (`is_sentinel_value` byte-identical);
  at Juan's instruction the orchestrator applied STEP 4 + STEP 5 + NOTIFY via the Supabase MCP (05:36–05:37 UTC;
  EXECUTE = `service_role` only; 0 rows carry `backfill_run_id`).
- 12-10: orchestrator-run Opus production-risk verdict `pass`; STEP 1 backup `attribution_snapshot_backup_20260904`
  (220,621 rows = live, 06:07:36 UTC, Juan approved the read-back); scoped read-only dry-run WR 89732091 × 7 weeks →
  0 proposed / 235 unresolved, 0 extension-bearing (was 235/235); Juan replied `re-enter-12-06`.
- Close-out: OWN-02/OWN-03 reverted to Pending (`5121c30`); 12-06 SUMMARY `halted` → `blocked` (`7ce962a`);
  code-quality report (`634d93b`); ledgers (`ff3efb1`, `45e9458`); vault pages + log updated; parked packet applied.
- Wave-3 gates: pytest 2,117 passed / 1 skipped / 441 subtests; `py_compile`; schema-drift, codebase-drift, UI gates
  `block: false`; 6-gate harness ALL PASSED. Doc test after ledger edits: 25 passed.
- 12-06 Task 1: in progress — first attempt rejected 2026-09-03; the full-population dry-run has NOT been re-run yet.
</completed_work>

<remaining_work>

- 12-06 Task 1 (checkpoint:human-verify): cheap read-only checks, then the full-population dry-run (report outside the
  repo; no `--include-blank-roles`). Report must show 0 extension-bearing AND 0 hash-tailed proposed values; the open
  question is how many of the 4,070 previously-placeholder rows now resolve via source 4.
- 12-06 Task 2 (checkpoint:decision): Juan's apply verdict.
- 12-06 Task 3 (checkpoint:human-verify): `--apply` on the same UTC day as the dated backup (re-run STEP 1 if the
  2026-09-04 table has expired); restore the Sunday cron only together with a real candidate source.
- 12-06 Task 4 (checkpoint:human-verify): post-run provenance check.
- Then `/gsd-verify-work 12` (reconcile G-12-3; file CR-01 and WR-01 as gaps) → `phase.complete 12`.
</remaining_work>

<decisions_made>

- D-12-C `defer`: the 945 `#NO MATCH` rows stay out of OWN-03 (already no-history via `resolve_claimer`).
- D-12-D `substitute-89829163`: SC3 sample is WR 89829163 WE 082425/083125/091425/092125 via `backfill_artifacts`;
  WR 19073866 has zero rows in every Supabase store.
- 12-06 re-entered through `status: blocked` (the only re-entrant SUMMARY state), not by deleting the SUMMARY.
- `verify_phase_goal` / `update_roadmap` skipped: the phase is incomplete by design until 12-06 applies.
- CR-01 and WR-01 not fixed in this run: the gate is advisory, both fall outside `--gaps-only`, and the parser regex is
  protected extraction behavior; `/gsd-verify-work 12` files them as gaps.
- No living-ledger entry this session (WR-01); lessons live in the vault synthesis `guardrail-allowlist-and-gsd-ops-lessons` §11.
- Executor context delivered as one scratchpad bundle file (~104 KB) the executor Reads first, per the prompt-caching
  overlay; 7/7 dispatches consumed it correctly. Dispatch stayed sequential (ISOLATION=none) because the worktree
  base-check found HEAD ahead of `origin/HEAD`.
</decisions_made>

<blockers>
- Backup validity: `attribution_snapshot_backup_20260904` satisfies the `--apply` probe only before 2026-09-05 00:00 UTC
  (19:00 CDT 2026-09-04). Workaround: re-run STEP 1 on apply day; never drop or overwrite an existing backup table.
- CR-01 (latent): `_FILENAME_HASH_SUFFIX_RE` 6-hex vs production 16-hex. Read-only count 2026-09-04: 0 of 116,906
  `public.artifacts` filenames carry any hash tail (all rows post-date the 2026-05-28 `SUPABASE_HASH_STORE_AUTHORITATIVE=1`
  flip). Returns only if that flag is reverted. Fix folds with the six-hex-letter over-match ticket (`{16}` tail +
  production-shape fixtures).
- WR-01: newest-ledger-entry test pin. Workaround: no ledger append until fixed.
- Owner security item (not on the 12-06 path): `anon`/`authenticated` hold full DML grants on
  `billing_audit.attribution_snapshot` behind RLS — confirm the policies deny them.
</blockers>

## Required Reading (in order)
1. `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-06-PLAN.md` — the four blocking-human tasks and their exact commands
2. `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-06-SUMMARY.md` — first-attempt evidence and the YAML re-entry comment (`status: blocked`)
3. `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-10-SUMMARY.md` — Opus verdict, backup evidence, scoped dry-run proof, `re-enter-12-06`
4. `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-REVIEW.md` — CR-01 (check the dry-run for hash tails) and WR-01 (do not append the ledger)
5. `.claude/project-state.md` § Latest work and § Next — repo-level status and the backup expiry
6. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses; apply before any assumption analysis

## Critical Anti-Patterns (do NOT repeat these)
- Rubric-pass ≠ safe → Opus production-risk pass before any attribution-writer PR; fixtures from `pipeline/excel.py`; dry-run report asserts 0 hash-tailed proposals.
- Ledger append with the newest-entry test pin in place → no `living-ledger.md` append until WR-01 is fixed; run the doc test after ledger edits.
- "review" token in gsd-tools arguments → reword; resolve inputs from files.
- Bash-scripted ledger edits → Edit tool for ledgers, Bash for commits.
- Trusting the shared-ID ready gate after a halted plan → audit `REQUIREMENTS.md` after gaps-only runs.
- Executors touching Supabase or DDL → orchestrator only, read-only by default, on Juan's explicit instruction.

## Infrastructure State
- Supabase billing project: `billing_audit.backfill_attribution` carries the extension guard (applied 2026-09-04 05:37 UTC; SECURITY INVOKER; EXECUTE `service_role` only; PostgREST schema reloaded). `attribution_snapshot` 220,621 rows, 0 with `backfill_run_id`. Backups `attribution_snapshot_backup_20260903` (220,010) and `_20260904` (220,621) both present. `public.artifacts` 116,906 rows, all hash-less.
- GitHub: PR #387 and #388 MERGED; `master` green; `cell-history-backfill.yml` dispatch-only (no schedule), 0 runs. Local branch `feat/phase-12-remediation` at `45e9458`, not pushed.
- No background processes, servers, or async jobs. Untracked, deliberately unadded: `.agents/`, `.planning/state.json`, `.serena/`, `website/.claude/`.
- Scratchpad artifacts (session-local, disposable): executor context bundle, `12-10-task1-independent-review.md`.

<context>
The G-12-3 story is closed at the code and SQL layers; what remains is the owner-run live remediation that G-12-3
interrupted. The mental model for the resuming session: run 12-06 Task 1 as a fresh read-only dry-run over the full
population, analyse the report in-sandbox (counts only, no names in the repo), and put two numbers in front of Juan —
extension-bearing proposals (expect 0) and hash-tailed proposals (expect 0, CR-01 check) — plus how many rows now
resolve via source 4. Everything after that is Juan's call at blocking checkpoints. Keep the apply on the same UTC day
as the backup. Do not fix CR-01 or WR-01 inside 12-06; they belong to the verify-work pass.
</context>

<next_action>
Start with: `/clear` → `/gsd-execute-phase 12` (the index resolves 12-06 as the only runnable plan; it resumes at Task 1).
Before the executor starts, confirm the backup is still same-UTC-day valid; if not, ask Juan to authorize a fresh STEP 1.
</next_action>
