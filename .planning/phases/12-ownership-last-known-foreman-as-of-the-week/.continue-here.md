---
context: phase
phase: 12-ownership-last-known-foreman-as-of-the-week
task: 1
total_tasks: 6
status: in_progress
last_updated: 2026-09-03T07:14:11.038Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Rubric verifiers are not a safety review — plan-checker, executor self-check, and the haiku must_haves rubric all passed plan 12-01, yet the Opus production-risk review found three HIGH defects (blank roles treated as sentinels because `is_sentinel_claimer(None)` is True, so source 2 would have copied the primary's name into empty `helper`/`vac_crew` roles at `--apply` time; per-row source-1 queries against the plan's batching rule; `with_retry()` returning `None` read as "no evidence") — before any PR that can write `billing_audit.attribution_snapshot`, run the read-only Opus `production-risk-reviewer` on the commit range and apply one fix round; treat `with_retry() is None` as a failure everywhere it is called.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Rubric-pass ≠ safe | All GSD-side checks passed 12-01 while an independent Opus review found three HIGH defects in the attribution write path (see constraint above) | blocking | Opus `production-risk-reviewer` on the commit range before the PR for any plan touching `attribution_snapshot` writers; one fix round; re-verify |
| GSD verbs on local `master` | Running phase verbs on `master` (branching_strategy `none`) left 15 unpushed tracking commits that had to be reset away before PR #385 | advisory | Create a branch off `origin/master` before `/gsd-execute-phase` (wave 1 used `feat/phase-12-ownership`); wave 2 needs a fresh branch off the merged master |
| Retrying a hook-denied write | The harness-boundary hook denies every ClaudeOS write to `AGENTS.md` | advisory | Owner hand step; never retry or route around the hook |
| Stale plan wording (12-02) | `12-02-PLAN.md` line 25 says align WR-01 to "discovery.py's lazy import of the same path"; discovery.py never imports `AttachmentParentType` | advisory | Executor aligns to discovery.py's lazy `Sheet`/`Folder` import pattern; `pipeline/orchestrate.py:43-44` is the only import site |
| Manifest `--files` as one string | `worktree.record-agent --files "a,b,c"` stored one comma-joined entry and `cleanup-wave` warned `scope_out_of_declared` for every file | advisory | Pass the files as the verb expects (separate entries), or expect and dismiss the warnings after confirming the diff matches the plan's `files_modified` |
| Protected areas in wave 2 | 12-03 is Supabase SQL/RPC (owner-deployed); 12-04 adds a GitHub Actions workflow; 12-02 edits the protected cleanup path | advisory | Both `autonomous: false` plans pause for Juan; do not auto-approve; `~/.claude/rules/production-guardrails.md` |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 12 wave 1 (plan 12-01) is complete, reviewed, and on PR #387 (`feat/phase-12-ownership`, 12 commits over `master` `560115f`, mergeable). Waves 2–4 (12-02 … 12-06) are not started; the phase is NOT marked complete and no VERIFICATION.md exists. PR #386 (docs: record the PR #385 merge) is also open; its commit is cherry-picked into #387 so either merge order is clean. Local `master` equals `origin/master`. Working tree is clean (only the untracked `.agents/`, `.serena/`, `.planning/state.json`, `website/.claude/` owner-decision items).
</current_state>

<completed_work>

Completed Tasks:
- Plan 12-01 Task 1: end-to-end dry-run tracer, source 4 only — Done (`d49be6b`)
- Plan 12-01 Task 2: sources 1–3, conflict / unresolved / ordering rules — Done (`2b9f3b3` RED, `7912fd2` GREEN)
- Plan 12-01 Task 3: `--apply` write path — Done (`c1020c7` RED, `e047c02` GREEN); SUMMARY `9607336`
- Worktree merge `c9edd5c`; tracking `9d93963` (ROADMAP/STATE: 12-01 complete)
- Post-merge review fix round `d922d29` + SUMMARY addendum `9607190`; docs write-back `20ac7d7`, `07a2ac6`
- Gates: full suite 1,994 passed; 6-gate harness; GSD wave-post gates (schema drift, codebase drift, UI) clear; haiku rubric 10/10 on the executor commit, 8/9 after the fix round (only line length fails; `python-lint` is warn-only)
</completed_work>

<remaining_work>

- 12-02 (wave 2, autonomous): CR-01 `_SANITIZED_ERROR_IDENTIFIERS` predicate in `pipeline/cleanup.py` + WR-01 lazy import in `pipeline/orchestrate.py`, with tests — protected cleanup path, surgical only
- 12-03 (wave 2, checkpoint): `billing_audit/own03_backfill_attribution.sql` + `schema.sql` contract + SQL contract test — Juan applies; p_rows key list must match the script's (`wr, week_ending, smartsheet_row_id, role, value, backfill_source, backfill_run_id`)
- 12-04 (wave 2, checkpoint): `scripts/backfill_cell_history_attribution.py` + `.github/workflows/cell-history-backfill.yml` — GitHub Actions change, Juan approves
- 12-05 (wave 3): OWN-04 runbook page, sidebars, environment reference, ledger, docs test
- 12-06 (wave 4, checkpoint): owner-run remediation — dry-run report review, `--apply --i-approved-this`, source-5 cron, attachment replacement
- Phase gates after wave 4: code review, regression gate, gsd-verifier VERIFICATION.md, phase.complete
</remaining_work>

<decisions_made>

- Executed wave 1 only, on a dedicated branch off `origin/master`, then PR — waves 2–4 touch protected areas and `branching_strategy` is `none`
- Named-sentinel-only targeting is the script default; `--include-blank-roles` opts back in — Juan confirms before 12-06 (OWN-03 scope = the `Unknown Foreman` defect, not roles that never existed)
- Opus production-risk review is a mandatory gate for attribution-writing code (see constraint)
- `with_retry() is None` → read failure, exit 7 — never "no evidence"
- Deferred to 12-06 / 12-02: `--from-report` approval binding; public `ROLE_BY_VARIANT` export from `billing_audit/writer.py`
</decisions_made>

<blockers>
- PR #386 and #387 unmerged: wave 2 must start from the merged `master`, not from `feat/phase-12-ownership`
- Owner RPC + backup table absent until 12-03 is applied: `--apply` exits 3 against any real environment (by design)
- Juan's confirmation of the targeting default is pending
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — position (12-01 complete, 5 plans remaining)
2. `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-01-SUMMARY.md` — what exists, the review-fix addendum, the p_rows contract 12-03 must match
3. `.claude/project-state.md` § Latest work + § Next — owner items and the WR-01 executor note
4. `memory-bank/living-ledger.md` `[2026-09-03 01:20]` — the review-before-PR rule and why
5. `~/.claude/rules/production-guardrails.md` — 12-02/12-03/12-04 are protected areas
1. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses; apply before any assumption analysis

## Critical Anti-Patterns (do NOT repeat these)
- RUBRIC-PASS-AS-SAFE: verifiers check what the plan names, not what it forgot → Opus production-risk review before the PR on attribution writers
- GSD-ON-MASTER: tracking commits on local `master` → always a branch off `origin/master` first
- SILENT-NONE-READ: `with_retry()` `None` treated as empty → raise, exit 7

## Infrastructure State
- No background processes, servers, or async jobs
- GitHub: PR #387 (wave 1) and PR #386 (docs) open; `master` at `560115f`; 16 Dependabot alerts reported on `master`
- Supabase: no schema/RPC changes made; `billing_audit.backfill_attribution` and `attribution_snapshot_backup_*` do not exist yet
- Local: `feat/phase-12-ownership` == origin; worktree `agent-a33c59c2d0cf4b47b` removed; `.planning/tmp/` gitignored

## Pre-Execution Critique Required
- Not applicable — pausing after an executed, reviewed wave, not between design and execution

<context>
The session ran Juan's five post-#385 recommendations: synced master, opened PR #386 for the merge record, ran `/gsd-execute-phase 12` for wave 1 on a dedicated branch, and surfaced the owner items. The important learning is that the Opus review found real write-path defects after every GSD-side check passed; the fix round hardened the script and made blank-role targeting opt-in. The next session should not start wave 2 until #387 is merged and a fresh branch exists, because wave 2's three plans include two owner checkpoints (Supabase SQL, GitHub Actions) and the CR-01/WR-01 edits in the protected cleanup path.
</context>

<next_action>
Start with: confirm PR #387 (and #386) merged, `git checkout master && git pull`, create `feat/phase-12-wave-2` off `origin/master`, then `/clear` and `/gsd-execute-phase 12` — it resumes at wave 2 (12-02, 12-03, 12-04); expect checkpoints for 12-03 and 12-04 and pass the WR-01 executor note to 12-02.
</next_action>
