---
phase: 12-ownership-last-known-foreman-as-of-the-week
plan: 10
type: execute
wave: 3
depends_on: [12-07, 12-08, 12-09]
files_modified:
  - scripts/backfill_claim_time_attribution.py
  - tests/test_backfill_claim_time_attribution.py
  - billing_audit/own03_backfill_attribution.sql
autonomous: false
requirements: [OWN-02, OWN-03]
gap_closure: true
gap_ids: [G-12-3]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "An independent reviewer with no authoring context has judged the combined 12-07 + 12-09 delta against a production-risk rubric and returned pass / needs-fix / fail with evidence; a needs-fix verdict got exactly one fix round and was re-judged."
    - "A backup table named attribution_snapshot_backup_<today UTC YYYYMMDD> exists, is readable by service_role, and its exact name is recorded — the --apply precondition probe looks for that exact name and exits 3 rather than writing if it is absent."
    - "A scoped, read-only dry-run against live data exits 0 and its report contains ZERO rows with status=proposed whose proposed_value carries a document file extension."
    - "The same report's proposed values are all either real names or absent — the count of proposals equal to a sentinel spelling is zero."
    - "The dry-run report is written outside the repository working tree, so no claimer PII can be committed even accidentally — to a directory resolved inside the task (`${TMPDIR:-/tmp}/own03_verify`, cleared before the run) and passed to the verification check as an argument, never through an undefined environment variable."
    - "12-06 is declared re-entrant from its Task 1 with the invocation posture pinned by 12-08's decisions, and the re-entry conditions are written down."
  artifacts:
    - .planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-10-SUMMARY.md
  key_links:
    - "12-08's #NO MATCH decision determines whether the verification dry-run and the 12-06 re-run carry --include-blank-roles; running the wrong posture invalidates the review Juan is about to give."
    - "12-08's SC3 decision determines which WR the re-run must demonstrate; on juan-supplies the WR must be live-checked across the seven Supabase stores before the dry-run."
    - "The --apply backup precondition compares against TODAY's UTC date, so a backup created on a prior day does not satisfy it — Task 2 must run on the same UTC day as the eventual apply."
    - "12-02's CR-01 sentinel-identifier fix must be on the branch being run, because a large regeneration burst is exactly the condition that turned the old false-positive classification into attachment loss."
  prohibitions:
    - "Never run --apply in this plan — this plan re-opens 12-06, it does not replace it."
    - "Never write the dry-run report into generated_docs/ or anywhere inside the repository working tree."
    - "Never copy a real claimer name into the summary, the ledger, a log line, or any committed file — counts, WR numbers and fictional names only."
    - "Never drop or overwrite the existing dated backup table from a prior day."
    - "Never proceed past a fail verdict from the independent review; halt and route back to a new gap-closure plan."
    - "Never auto-select a checkpoint outcome — every checkpoint here is blocking-human."
---

<objective>
Prove gap G-12-3 is closed against live data, restore the `--apply` precondition, and hand
control back to plan 12-06 at its Task 1.

The rejected run's evidence is the bar: 4,070 of 4,762 proposals were the placeholder string
carrying an `.xlsx` extension, and a scoped re-run over WR 89732091 across seven weeks
produced 235 of 235 rows the same way. After 12-07's parser fix and 12-09's server-side
guard, the same scoped invocation must produce zero extension-bearing proposals. That
delta — measured, not asserted — is what makes a second owner review meaningful.

Purpose: 12-06 halted at its designed stop with a REJECT. Nothing in GSD should quietly
re-run a production write path just because some code changed. This plan supplies the three
things the re-entry needs: independent judgement on the fix, a same-UTC-day backup, and a
live measurement that the defect is gone.

Output: a review verdict, a fresh dated backup table name, a zero-defect dry-run report
summary (counts only), and a written re-entry declaration for 12-06.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-06-PLAN.md
@.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-06-SUMMARY.md
@.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-07-SUMMARY.md
@.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-08-SUMMARY.md
@.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-09-SUMMARY.md
@~/.claude/rules/production-guardrails.md

**Facts already established (do not re-derive):**

- The rejected report (`generated_docs/own03_backfill_report.json`, git-ignored, written
  2026-09-03 17:15): 5,829 rows; proposed 4,762 / conflict 1,066 / unresolved 1; sources
  `backfill_artifacts` 5,136 / `live` 692; fidelity desanitized 4,070 / exact 692 / blank
  1,067. All 4,070 desanitized proposals were the single placeholder string. All 1,066
  conflicts were between extension-suffixed names.
- Reproduction invocation used by the diagnosing session (read-only, exit 0, 235 sentinel
  rows, 235/235 proposed):
  `--wr 89732091 --weeks 071325,072025,072725,080325,081025,090725,091425`
- Report JSON shape: a top-level object whose `rows` key is the list of report rows; each row
  carries `wr`, `week_ending`, `row_id`, `role`, `status`, `current_value`, `proposed_value`,
  `source`, `name_fidelity`, `evidence`.
- `--report-dir` defaults to `generated_docs`. Pointing it elsewhere triggers the script's own
  WARNING from `_warn_if_report_dir_outside_generated_docs` — that warning is EXPECTED here
  and is not a failure.
- `_probe_backup_table_readable` / `_backup_table_name` build the expected backup name from
  TODAY's UTC date; a missing or unreadable table makes `--apply` exit 3.
- Live snapshot on 2026-09-03: `billing_audit.attribution_snapshot` 220,433 rows vs
  `attribution_snapshot_backup_20260903` 220,010 rows — the existing backup is stale by 423
  rows and does not satisfy a later-day apply.
- Sound subset from the rejected run: 692 `live` (source 1) proposals across 7 WRs and 7
  distinct names, fidelity exact — those were never in question.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Independent production-risk review of the combined fix</name>
  <precondition>Plans 12-07 and 12-09 are complete and committed on this branch, and `python -m pytest tests/ -q` is green.</precondition>
  <files>scripts/backfill_claim_time_attribution.py, tests/test_backfill_claim_time_attribution.py, billing_audit/own03_backfill_attribution.sql</files>
  <action>
    Dispatch ONE independent reviewer with no authoring context (an Opus-pinned reviewer per
    the repository's model-routing policy; the reviewer is read-only and returns findings, it
    does not edit). Give it the diff of 12-07 and 12-09 only — `git diff` scoped to
    `scripts/backfill_claim_time_attribution.py`, `tests/test_backfill_claim_time_attribution.py`,
    `billing_audit/own03_backfill_attribution.sql` and `billing_audit/schema.sql` — plus this
    rubric, and nothing else:

    1. Can any input reach `_resolve_single_name` as a candidate while still carrying a
       document extension? Name the path if so.
    2. Does the extension strip ever remove a legitimate part of a human name? Consider
       initialled and suffixed names.
    3. Does the hash-suffixed path behave identically to before the change? Prove it from the
       diff, not from the tests.
    4. Can `_build_apply_payload` still emit a sentinel or extension-bearing value through any
       branch, including `include_blank_roles=True`?
    5. Did the SQL change alter the target set of the three per-role UPDATE statements in any
       way? `billing_audit.is_sentinel_value` must be byte-identical.
    6. Does any new log line, exception message, or test fixture contain a real claimer name?
    7. Is the guard order correct — is the extension check reached before any UPDATE runs?
    8. Does anything in the delta touch `generate_weekly_pdfs.py`, `pipeline/*`,
       `audit_billing_changes.py`, or `billing_audit.lookup_attribution_bulk`?

    Require a verdict of `pass`, `needs-fix` or `fail` with file:line evidence for every
    finding.

    On `needs-fix`: apply exactly ONE fix round addressing the named findings, re-run the full
    suite, and re-submit to the same reviewer. If the second verdict is not `pass`, treat it as
    `fail`.

    On `fail`: halt this plan. Do not proceed to Task 2. Record the findings and return to the
    orchestrator noting that a further gap-closure plan is required.

    Record the verdict, the reviewer's findings, and any fix round in `12-10-SUMMARY.md`.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <automated>python -m py_compile scripts/backfill_claim_time_attribution.py</automated>
    <automated>bash scripts/run_6_gates.sh</automated>
  </verify>
  <done>An independent reviewer returned `pass` (directly, or after one fix round), the findings are recorded, and the full suite plus the 6-gate harness are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Juan creates a fresh same-UTC-day backup table</name>
  <precondition>Task 1 returned `pass`. The existing `attribution_snapshot_backup_20260903` must remain in place — this task adds a new dated table, it never replaces one.</precondition>
  <read_first>
    - `billing_audit/own03_backfill_attribution.sql` § STEP 1 — the `CREATE TABLE ... AS SELECT`, the `GRANT SELECT ... TO service_role`, and the STEP 1 VERIFY query. Both the CREATE and the GRANT carry the `YYYYMMDD` placeholder and both must be substituted.
    - `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-03-SUMMARY.md` line ~248 — the name format the original apply recorded.
  </read_first>
  <what-built>
    `billing_audit/own03_backfill_attribution.sql` STEP 1 — the dated backup step: a
    `CREATE TABLE billing_audit.attribution_snapshot_backup_YYYYMMDD AS SELECT` over the live
    snapshot, a `GRANT SELECT ... TO service_role` against that same name, and a VERIFY query
    listing every backup table that exists. Both the CREATE and the GRANT carry the
    `YYYYMMDD` placeholder. Nothing in this repository executes it; the backfill script only
    probes for the resulting table by name.
  </what-built>
  <how-to-verify>
Run STEP 1 in the Supabase SQL editor. The executor performs none of these steps itself.

1. Substitute today's UTC date in `YYYYMMDD` form into BOTH the `CREATE TABLE` and the
   `GRANT SELECT` — running the GRANT with the placeholder left in is the footgun the file
   warns about, and it silently leaves the new table unreadable by `service_role`.
2. Run the `CREATE TABLE ... AS SELECT`, then the `GRANT SELECT ... TO service_role`.
3. Run the STEP 1 VERIFY query. It must list BOTH `attribution_snapshot_backup_20260903` and
   the new table — the prior-day backup is preserved, never replaced.
4. Confirm the counts: `SELECT count(*) FROM billing_audit.attribution_snapshot;` and
   `SELECT count(*) FROM billing_audit.attribution_snapshot_backup_<today>;` must agree.
5. Report back: the exact new table name, its row count, the live snapshot row count, and the
   UTC timestamp of the apply.

Why a fresh table rather than the existing one: the `--apply` precondition probe builds the
expected name from TODAY's UTC date, so a prior-day table makes `--apply` exit 3 instead of
writing. The 2026-09-03 table is also 423 rows stale against live. This backup is valid only
for an apply performed on the SAME UTC day — if the apply slips past midnight UTC, STEP 1
must be re-run before it.
  </how-to-verify>
  <resume-signal>Reply with the four answers from step 5, then `approved` — or describe what failed and at which step.</resume-signal>
  <action>
Present the `<how-to-verify>` checklist to Juan and wait. Do not execute any SQL and do not
use an MCP write tool.

When he replies, transcribe the exact backup table name, its row count, the live
`attribution_snapshot` row count and the UTC timestamp into `12-10-SUMMARY.md`, along with
the explicit statement that no agent, script or MCP tool executed DDL.

If the two counts in step 4 disagree, halt and ask Juan to re-run STEP 1 — a short backup is
not a backup.
  </action>
  <verify>
    <human-check>Juan confirms the new `billing_audit.attribution_snapshot_backup_<today UTC>` table exists, that `GRANT SELECT ... TO service_role` was run against that exact name, and that the STEP 1 VERIFY query lists it.</human-check>
  </verify>
  <acceptance_criteria>
    - `12-10-SUMMARY.md` records the exact backup table name, its row count, and the live snapshot row count.
    - The prior `attribution_snapshot_backup_20260903` table still exists.
    - No agent, script or MCP tool executed DDL; the summary says so explicitly.
  </acceptance_criteria>
  <done>A readable, same-UTC-day backup table exists under the exact name the `--apply` precondition probe will look for.</done>
  <reversibility rating="costly">Creates a new table in a production, data-team-owned billing schema. Dropping it later is a second coordinated production change.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Zero-defect verification dry-run, then re-entry into 12-06</name>
  <precondition>Task 2 recorded a same-UTC-day backup table name, and 12-08-SUMMARY.md records both decisions — the `--include-blank-roles` posture and the SC3 sample. If 12-08 chose `raw-read-rpc`, this task must not run: plan 12-11 comes first.</precondition>
  <read_first>
    - `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-08-SUMMARY.md` — the two decisions that pin this run's scope.
    - `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-06-PLAN.md` § Task 1 — the seven-step owner review checklist the re-entry resumes at.
  </read_first>
  <what-built>
    The combined G-12-3 fix: 12-07's extension-stripping source-3 parser plus its
    proposed-value guard in `_build_apply_payload`, and 12-09's live RPC validation guard that
    refuses an extension-bearing proposed value server-side. This task measures that fix
    against live data on the exact scope that previously failed, and produces a dry-run report
    written outside the repository working tree.
  </what-built>
  <how-to-verify>
Juan reviews the before/after counts this task presents. What must be true:

1. The dry-run exited 0 on the same scope as the pre-fix reproduction
   (`--wr 89732091 --weeks 071325,072025,072725,080325,081025,090725,091425`).
2. Proposals whose value carries a document file extension: 0 — against 235 of 235 on the
   identical scope before the fix.
3. Proposals whose value is classified as a sentinel by
   `billing_audit.writer.is_sentinel_claimer`: 0.
4. The remaining rows resolve to real names, fall through, or are correctly classified as
   conflict or unresolved — the shape Juan expects from operator knowledge.
5. `git status --porcelain` is clean: the report lives outside the working tree, so no
   claimer PII can be committed.

If all five hold, the re-entry conditions for plan 12-06 are met: parser fix in place, live
RPC guard in place, independent review `pass`, same-UTC-day backup recorded, scope pinned by
12-08. Plan 12-06 then resumes at its own Task 1 — the full-scope owner review against its
seven-step checklist — with its Tasks 2-4 still gated behind Juan's APPROVE / APPROVE WITH
SCOPE / REJECT verdict exactly as originally written.
  </how-to-verify>
  <resume-signal>Reply `re-enter-12-06` to confirm the defect is closed and 12-06 resumes at Task 1, or `hold` with what still looks wrong.</resume-signal>
  <action>
Run the verification dry-run as a read-only operation, then hand the result to Juan.

Resolve the report directory FIRST, in the same shell, so the run and its check read the same
concrete path. `REPORT_DIR` is not inherited from anywhere — define it here, outside the
repository working tree, and clear it so a stale report from an earlier attempt cannot be
mistaken for this run's evidence:

```
REPORT_DIR="${TMPDIR:-/tmp}/own03_verify"
rm -rf "$REPORT_DIR" && mkdir -p "$REPORT_DIR"
```

Invocation — reproduce the diagnosing session's exact scope so the before/after comparison is
like for like, and add `--include-blank-roles` only if 12-08 chose `include-blank-roles`:

```
python scripts/backfill_claim_time_attribution.py \
  --wr 89732091 \
  --weeks 071325,072025,072725,080325,081025,090725,091425 \
  --report-dir "$REPORT_DIR"
```

The verify gate re-derives that same `${TMPDIR:-/tmp}/own03_verify` path and passes the report
file to the checker as a command-line argument, so nothing depends on an environment variable
that may not be set; a missing report makes the check exit non-zero rather than pass silently.
Record the literal resolved `REPORT_DIR` in `12-10-SUMMARY.md`.

Never point `--report-dir` at `generated_docs/` and never at any path inside the repository
working tree: the report carries real claimer names, and Juan's 2026-09-03 report must not be
overwritten. The script's own WARNING about a report directory outside `generated_docs` is
expected here — acknowledge it and confirm `git status` is clean afterwards. Once the counts
below are extracted, delete `$REPORT_DIR` — the derived counts are the deliverable, the
name-bearing report is not. Do not pass `--apply`. Do not pass `--i-approved-this`.

The run must exit 0. Then derive the comparison in-sandbox and report COUNTS ONLY — never a
proposed value, never a claimer name:

- total rows considered, and rows by status
- rows with `status` `proposed` whose `proposed_value` matches
  `\.(xlsx|xlsm|xls|csv|pdf|json)$` case-insensitively — this must be 0, against 235 of 235
  in the pre-fix reproduction
- rows with `status` `proposed` whose `proposed_value` is classified by
  `billing_audit.writer.is_sentinel_claimer` — this must be 0
- rows by source, and rows by `name_fidelity`

Present those counts to Juan alongside the pre-fix figures from `12-06-SUMMARY.md`, then
declare the re-entry conditions and stop:

- 12-07's parser fix and 12-09's live RPC guard are both in place
- an independent reviewer returned `pass`
- a same-UTC-day backup exists under the recorded name
- the scoped dry-run shows zero extension-bearing and zero sentinel proposals
- the `--include-blank-roles` posture and the SC3 sample are pinned by 12-08

State plainly that plan 12-06 is now re-entrant from its Task 1, that its Task 1 is the
full-scope owner review Juan must still perform against the seven-step checklist, and that
Tasks 2-4 of 12-06 remain gated behind his APPROVE / APPROVE WITH SCOPE / REJECT verdict
exactly as written. Do not modify `12-06-PLAN.md`.
  </action>
  <verify>
    <automated>python -c 'import json,re,sys;p=sys.argv[1];d=json.load(open(p,encoding="utf-8"));rows=d["rows"];pat=re.compile(r"[.](?:xlsx|xlsm|xls|csv|pdf|json)$",re.I);bad=[r for r in rows if r.get("status")=="proposed" and pat.search(str(r.get("proposed_value") or ""))];print("extension_bearing_proposals=%d of %d rows" % (len(bad),len(rows)));sys.exit(1 if bad else 0)' "${TMPDIR:-/tmp}/own03_verify/own03_backfill_report.json"</automated>
    <automated>git status --porcelain</automated>
    <human-check>Juan has seen the before/after counts and agrees the source-3 defect is closed, and has acknowledged that 12-06 re-entry starts at its Task 1 with the full-scope owner review still to do.</human-check>
  </verify>
  <acceptance_criteria>
    - The dry-run exited 0 and the extension-bearing proposal count is 0.
    - The sentinel-proposal count is 0.
    - `git status --porcelain` shows no untracked report file — the report lives outside the working tree.
    - `12-10-SUMMARY.md` records the literal resolved `REPORT_DIR` path, the before/after counts, the invocation used including the `--include-blank-roles` posture, and the re-entry declaration.
    - The name-bearing report directory is deleted after the counts are extracted; only counts leave the sandbox.
    - `12-06-PLAN.md` is unmodified.
  </acceptance_criteria>
  <done>Live evidence shows the defect is gone, and 12-06 is formally re-entrant from Task 1 with every precondition recorded.</done>
  <reversibility rating="reversible">A dry-run performs no writes; the re-entry declaration authorizes nothing on its own — 12-06's own checkpoints still gate the apply.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Supabase read -> dry-run report | The report materializes real claimer names onto disk; where it lands determines whether PII can be committed. |
| authoring agent -> review verdict | A reviewer that has seen the authoring rationale is not independent; the verdict must come from fresh context. |
| backup table -> `--apply` precondition | The probe's date match is the only thing standing between a rushed apply and an unrecoverable one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-40 | Information disclosure | dry-run report | high | mitigate | `--report-dir` points outside the working tree; only counts are surfaced; `git status --porcelain` is asserted clean (Task 3). |
| T-12-41 | Repudiation | fix verification | high | mitigate | Independent fresh-context reviewer with a fixed eight-point rubric, one fix round, verdict recorded with file:line evidence (Task 1). |
| T-12-42 | Denial of service | missing backup | critical | mitigate | Same-UTC-day backup created and its exact name recorded before re-entry; the prior-day table is preserved, not replaced (Task 2). |
| T-12-43 | Tampering | premature apply | critical | mitigate | No task passes `--apply` or `--i-approved-this`; 12-06's own decision checkpoint remains the sole authorization path (Task 3). |
| T-12-44 | Spoofing | scope drift | medium | mitigate | The verification run reproduces the diagnosing session's exact `--wr` / `--weeks` scope so the before/after comparison is like for like (Task 3). |
| T-12-45 | Elevation of privilege | agent-executed DDL | high | mitigate | STEP 1 is run by Juan by hand; no agent, script or MCP tool executes DDL (Task 2). |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed by this plan; no new dependency is added. If one becomes necessary, stop and route to the package-legitimacy gate. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` and `bash scripts/run_6_gates.sh` green before the review is dispatched.
- The independent reviewer's verdict is `pass`, recorded with evidence.
- A `billing_audit.attribution_snapshot_backup_<today UTC>` table exists, is service_role
  readable, and its exact name and row count are recorded.
- The verification dry-run exits 0; extension-bearing proposals = 0 (against 235 of 235
  pre-fix on the identical scope); sentinel proposals = 0.
- `git status --porcelain` is clean — no report file inside the working tree.
- `12-06-PLAN.md` is byte-for-byte unmodified.

<human-check>Juan agrees the before/after counts demonstrate the source-3 defect is closed, and acknowledges 12-06 re-entry begins at its Task 1 with the full-scope owner review still outstanding.</human-check>
</verification>

<success_criteria>
- The fix has been judged by someone who did not write it.
- The `--apply` precondition is satisfiable again on the day of the apply.
- Live evidence, not assertion, shows zero placeholder proposals on the scope that
  previously produced 100% placeholder proposals.
- Plan 12-06 resumes from Task 1 with its own checkpoints intact and its semantics unedited.
</success_criteria>

<output>
Create `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-10-SUMMARY.md` when done.
Record: the reviewer's verdict and findings, any fix round, the exact backup table name with
row counts, the verification dry-run invocation and its before/after counts, and the re-entry
declaration for 12-06. Counts, WR numbers and option ids only — no real claimer name.
</output>
