---
phase: 12-ownership-last-known-foreman-as-of-the-week
plan: 05
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-03, 12-04]
files_modified:
  - website/docs/runbook/ownership-attribution.md
  - website/sidebars.ts
  - website/docs/runbook/scripts.md
  - website/docs/runbook/workflows.md
  - website/docs/reference/environment.md
  - memory-bank/living-ledger.md
  - tests/test_own04_documentation.py
autonomous: true
requirements: [OWN-01, OWN-04]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "website/docs/runbook/ownership-attribution.md documents the ownership ladder AS IMPLEMENTED: observed_in_week -> backfill_artifacts -> backfill_hash_history -> operator -> sentinel, with no cross-week rung and no last_known_before_week."
    - "The runbook records D-12-A: Phase 12 ships no wr_week_ownership table; OWN-01's ladder is served by billing_audit.attribution_snapshot plus the backfill_source / backfill_run_id provenance columns, and the table is deferred to Phase 13."
    - "The runbook records D-12-B: source 4 (backfill_hash_history) reads billing_audit.group_content_hash and pipeline_memory.group_state, not a JSON file; there is no --hash-history flag; weeks never seen in a run since 2026-05-25 fall through to sources 3 and 5."
    - "The runbook records the amended Foundation A contract: first-write-wins still holds for real names, but a frozen sentinel is now treated as no-claimer and may be replaced by a provenance-tagged backfilled value."
    - "The runbook distinguishes scripts/backfill_attribution_snapshot.py (freezes the CURRENT Smartsheet value — rejected semantics for claim time) from scripts/backfill_claim_time_attribution.py (claim-time ladder), with an explicit warning not to run the former against the remediated WRs (Pitfall 4)."
    - "The runbook names which component owns each flow: the Python billing pipeline owns regeneration and attachment cleanup; the two backfill scripts and the cell-history workflow are one-time / off-hours remediation jobs; Supabase DDL and RPC deployment is owner-applied."
    - "The runbook documents the rollback path: restore from billing_audit.attribution_snapshot_backup_<YYYYMMDD>."
    - "website/sidebars.ts lists runbook/ownership-attribution inside the Runbook category so the page is reachable, and no existing sidebar entry is removed."
    - "website/docs/reference/environment.md documents the four CELL_HISTORY_BACKFILL_* variables with their defaults and states that no production run reads them."
    - "memory-bank/living-ledger.md gains one dated [YYYY-MM-DD HH:MM] entry at the BOTTOM of the file recording D-12-A, D-12-B, the CR-01 fix and the amended Foundation A contract; CLAUDE.md is not modified."
    - "No real claimer name appears in any documentation file — the only claimer names used are the fictional Avery Example / Pat Example / Sam Sample already used by this repo's tests."
    - statement: "The Docusaurus site still builds after the new page and sidebar entry are added, with no broken cross-links."
      verification: backstop
  artifacts:
    - website/docs/runbook/ownership-attribution.md
    - tests/test_own04_documentation.py
    - memory-bank/living-ledger.md
  key_links:
    - "website/sidebars.ts -> runbook/ownership-attribution — without the sidebar entry the page exists but is unreachable."
    - "website/docs/runbook/scripts.md and workflows.md link to the new ownership page rather than restating the ladder, so there is one source of truth."
    - "memory-bank/living-ledger.md is the durable record; CLAUDE.md stays lean and is not edited (its Living Ledger section already points at the ledger file)."
  prohibitions:
    - "Never document a cross-week ownership rung."
    - "Never put a real claimer name, secret, token or env value in documentation or the ledger."
    - "Never append the ledger entry to CLAUDE.md."
    - "Never append a stale paragraph that contradicts existing runbook content — rewrite the affected passage instead."
    - "Never reference the retired ATTACHMENT_PREFETCH_* or DISCOVERY_CACHE_* variables as live."
---

<objective>
Write OWN-04: document the amended Foundation A contract and the ownership ladder as it was
actually implemented, in the Docusaurus runbook and in the Living Ledger.

Purpose: `REQUIREMENTS.md` OWN-01 still describes a ladder rung
(`last_known_before_week`) that was decided OFF on 2026-09-01 19:55, and names a
`wr_week_ownership` table that Phase 12 deliberately does not build. Two backfill scripts
with confusingly similar names now exist with opposite semantics. Without this plan, the
next operator reading the requirements would implement or run the wrong thing.

Decisions this plan records:
- **D-12-A** — no `wr_week_ownership` table in Phase 12; OWN-01's ladder is satisfied by
  `attribution_snapshot` + `resolve_claimer` + the two provenance columns; the table is
  deferred to Phase 13.
- **D-12-B** — source 4 reads the Supabase hash store, not a JSON file; no
  `--hash-history` flag; the uncovered weeks fall through to sources 3 and 5.

Output: a new runbook page, targeted rewrites of three existing pages, the sidebar entry, a
dated Living Ledger entry, and a structural test that keeps the documentation honest.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-RESEARCH.md
@.claude/rules/documentation-maintenance.md
</context>

## Artifacts this phase produces

This plan creates `website/docs/runbook/ownership-attribution.md` and
`tests/test_own04_documentation.py`. The full phase-wide artifact list is in
`12-01-PLAN.md` § "Artifacts this phase produces" — the runbook page must describe every
symbol on that list.

<assumption_delta_decision>
Same decision as `12-01-PLAN.md`: the noun that is now primary is the *claim-time claimer
with provenance*; decision `promote`. This plan is where the promotion is written down so a
future reader does not re-derive the demoted representation as current. The runbook must
state plainly that the live Smartsheet name at generation time is now a fallback, used only
when no frozen or backfilled value exists — and that a later edit to an already-claimed row
is an audit event, not a re-attribution.
</assumption_delta_decision>

<tasks>

<!-- Comment-text discipline (#429): the literals below are named verbatim in
     <acceptance_criteria> negative greps AND must appear verbatim in the test
     files these tasks author, because those tests ARE the gate. Exempted. -->
<!-- planner-discipline-allow: last_known_before_week -->
<!-- planner-discipline-allow: --hash-history -->
<!-- planner-discipline-allow: ATTACHMENT_PREFETCH_ -->
<!-- planner-discipline-allow: DISCOVERY_CACHE_ -->

<task type="auto">
  <name>Task 1: Write the ownership-attribution runbook page</name>
  <files>website/docs/runbook/ownership-attribution.md, website/sidebars.ts, tests/test_own04_documentation.py</files>
  <read_first>
    - `.claude/rules/documentation-maintenance.md` — the "synthesize, do not dump commits", "rewrite, do not append", and "state which component owns each flow" rules this page must satisfy.
    - `website/docs/runbook/scripts.md` and `website/docs/runbook/workflows.md` — the existing front-matter shape (`id`, `title`, `sidebar_position`), heading depth and voice to match.
    - `website/sidebars.ts` — the `runbookSidebar` `Runbook` category array the new id is appended to.
    - `docs/superpowers/specs/2026-09-01-own-03-claim-time-backfill-design.md` §2, §3, §4, §6, §7 — the ladder, sources, write path, validation and consequences being documented.
    - `billing_audit/writer.py` lines 96-118 and 1058-1146 — `is_sentinel_claimer` and `resolve_claimer`, whose behavior the page describes.
    - `pipeline/cleanup.py` lines 89-116 and 480-520 (as fixed by plan 12-02) — the sentinel-superseded attachment cleanup the page must explain.
    - `scripts/backfill_attribution_snapshot.py` lines 1-10 — the docstring whose "current completed rows in Smartsheet" semantics the page must warn against confusing with the new script.
  </read_first>
  <action>
Create `website/docs/runbook/ownership-attribution.md` with front matter
`id: ownership-attribution`, `title: Ownership and claim-time attribution`,
`sidebar_position: 7` (placing it after `operations`, before `auth-rbac-bootstrap` — renumber
`sidebar_position` on `auth-rbac-bootstrap` and the pages after it only if the existing
numbers collide; check them before choosing).

Organize the page around operator workflows, not code modules, per
`.claude/rules/documentation-maintenance.md`. Required sections:

`## Who owns a (WR, week) file` — the claim-time rule in plain language: the file is named
for, and contains only the rows of, the person who claimed those rows in that week; the
first non-sentinel value observed on the row at or after it became completed for that role.
State the three roles and their "claimed when" conditions from spec §2. State plainly that
a later Smartsheet edit to an already-claimed row is an audit event, never a
re-attribution, and that correcting a real frozen name needs an operator override that is
out of scope for Phase 12.

`## Ownership ladder` — a numbered list of the ladder AS IMPLEMENTED:
`observed_in_week` (source 1, `pipeline_memory.row_event` / `row_state`, tag `live`) ->
source 2 (a non-sentinel value already frozen for another role on the SAME row, tag `live`)
-> `backfill_artifacts` (source 3, `public.artifacts` filenames) -> `backfill_hash_history`
(source 4, the Supabase hash store) -> `operator` (source 5, Smartsheet cell history) ->
sentinel (the row keeps its placeholder). Follow it with a boxed note recording D-12-A and
the 2026-09-01 19:55 decision: there is deliberately NO cross-week rung, and
`REQUIREMENTS.md` OWN-01's wording predates that decision. State D-12-A's second half: no
`wr_week_ownership` table exists; the ladder is served by
`billing_audit.attribution_snapshot` plus the `backfill_source` and `backfill_run_id`
provenance columns, with the table deferred to Phase 13.

`## The amended Foundation A contract` — first-write-wins still holds for a real name: once
a real person is frozen for a row and role, nothing overwrites it. What changed on
2026-08-24's defect repair is that a frozen SENTINEL is now read as "no claimer", so a
provenance-tagged backfilled value may replace it. Name the three enforcement points: the
Python predicate `billing_audit.writer.is_sentinel_claimer`, its SQL twin
`billing_audit.is_sentinel_value`, and the `WHERE` clause inside
`billing_audit.backfill_attribution`.

`## Sources and their limits` — a table of the five sources with their provenance tag,
coverage window and abstention rule. Record D-12-B explicitly: source 4 reads
`billing_audit.group_content_hash` and `pipeline_memory.group_state`, NOT a JSON file;
there is no `--hash-history` flag; a week never seen in a run since 2026-05-25 is not
covered by source 4 and falls through to sources 3 and 5. Record the single-name rule for
sources 3 and 4 — two distinct real names for the same week and role means the sources
abstain and the row is reported as a conflict.

`## Running the backfill` — the operator procedure: dry-run first
(`python scripts/backfill_claim_time_attribution.py`), review
`generated_docs/own03_backfill_report.csv`, then
`--apply --i-approved-this` only after the dated backup table exists. List the script's
exit codes. State that the report is git-ignored because it carries claimer names.

`## Two scripts, opposite semantics` — a prominent warning block (Pitfall 4):
`scripts/backfill_attribution_snapshot.py` freezes whatever Smartsheet shows TODAY for a
target week; `scripts/backfill_claim_time_attribution.py` derives the historically-correct
claimer. Running the former against a remediated WR would overwrite the repair with the
current value. State that the "current always wins" policy was rejected on 2026-09-01
19:45.

`## After the backfill` — what the next scheduled run does: `resolve_claimer` finds the
real frozen name with no grouping-code change; the file regenerates under the real name;
the sentinel-superseded gate in `pipeline/cleanup.py` removes the stale placeholder
attachment in the same run; PPP attachments are never purged by any reset.

`## Rollback` — restore affected rows from
`billing_audit.attribution_snapshot_backup_<YYYYMMDD>`; do not drop that table until the
next scheduled run has been verified.

`## Who owns what` — per `.claude/rules/documentation-maintenance.md`: the Python billing
pipeline (`generate_weekly_pdfs.py` on its GitHub Actions cron) owns regeneration and
attachment cleanup; `scripts/backfill_claim_time_attribution.py` is a one-time operator
remediation; `scripts/backfill_cell_history_attribution.py` runs only from
`.github/workflows/cell-history-backfill.yml`, never inside the billing run; Supabase DDL
and RPC bodies are owner-applied from `billing_audit/own03_backfill_attribution.sql` and
are never executed by repo code.

Use only fictional claimer names (`Avery Example`, `Pat Example`, `Sam Sample`) in every
example. Do not mention the retired `ATTACHMENT_PREFETCH_*` or `DISCOVERY_CACHE_*`
variables as live.

Add `'runbook/ownership-attribution'` to `website/sidebars.ts` inside the `Runbook`
category's `items` array, positioned after `'runbook/operations'`. Remove nothing.

Create `tests/test_own04_documentation.py` following the module shape of
`tests/test_generate_runbook_entry.py`. Assert: the page exists; its non-comment body
contains the headings `## Ownership ladder`, `## The amended Foundation A contract` and
`## Rollback`; it contains all five provenance tags `live`, `backfill_artifacts`,
`backfill_hash_history`, `operator` and the word `sentinel`; it names both
`scripts/backfill_attribution_snapshot.py` and
`scripts/backfill_claim_time_attribution.py`; it contains ZERO occurrences of
`last_known_before_week`, `wr_week_ownership` used as a shipped artifact (allow it only in
a sentence that also contains `Phase 13`), `--hash-history`, `ATTACHMENT_PREFETCH_` and
`DISCOVERY_CACHE_`; and `website/sidebars.ts` contains `'runbook/ownership-attribution'`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_own04_documentation.py -q</automated>
    <fails_when>non-zero exit, or "failed" / "error" appears in the pytest summary line</fails_when>
    <automated>npm --prefix website run typecheck</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `website/docs/runbook/ownership-attribution.md` exists with front matter `id: ownership-attribution`.
    - The page contains the headings `## Ownership ladder`, `## The amended Foundation A contract`, `## Two scripts, opposite semantics`, `## Rollback` and `## Who owns what`.
    - The page contains `D-12-A` and `D-12-B` and zero occurrences of `last_known_before_week`, `--hash-history`, `ATTACHMENT_PREFETCH_` and `DISCOVERY_CACHE_`.
    - Every occurrence of `wr_week_ownership` on the page is in a sentence that also contains `Phase 13`.
    - `website/sidebars.ts` contains `'runbook/ownership-attribution'` and still contains every id it contained before.
    - `python -m pytest tests/test_own04_documentation.py -q` exits 0.
  </acceptance_criteria>
  <done>An operator can read one page and correctly run, interpret, and roll back the ownership backfill without consulting the planning artifacts.</done>
  <reversibility rating="reversible">Documentation only.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Rewrite the affected existing runbook and reference pages</name>
  <files>website/docs/runbook/scripts.md, website/docs/runbook/workflows.md, website/docs/reference/environment.md</files>
  <read_first>
    - `website/docs/runbook/scripts.md` (full) — the section structure the two new scripts join; identify whether an existing sentence about attribution backfill needs rewriting rather than a new paragraph appended.
    - `website/docs/runbook/workflows.md` (full) — how existing workflows are described, so the new one matches; check for any existing statement about which workflows consume the Smartsheet budget that must be updated rather than contradicted.
    - `website/docs/reference/environment.md` (full) — the env-var table format, and whether the retired `ATTACHMENT_PREFETCH_*` / `DISCOVERY_CACHE_*` entries are still listed as live (if so, correct them in the same commit per the documentation-maintenance rule).
    - `.claude/rules/documentation-maintenance.md` §2 — rewrite, do not append; remove or merge outdated paragraphs in the same commit.
  </read_first>
  <action>
`website/docs/runbook/scripts.md`: add a new `## Ownership attribution backfill` section
describing `scripts/backfill_claim_time_attribution.py` and
`scripts/backfill_cell_history_attribution.py` in one or two sentences each, and link to
`ownership-attribution` for the full procedure — do not restate the ladder here. In the
same edit, rewrite whatever the page currently says about
`scripts/backfill_attribution_snapshot.py` so it carries the "freezes the CURRENT Smartsheet
value; do not run against remediated WRs" warning inline; if the page does not mention that
script at all, add it to the same new section with the warning. Do not leave two
descriptions of attribution backfill on the page.

`website/docs/runbook/workflows.md`: add `.github/workflows/cell-history-backfill.yml` to
the workflow inventory with its trigger (`workflow_dispatch`, plus the Sunday 05:00 UTC
cron if plan 12-04 Task 3 selected `approve-cron`), its caps, its distinct concurrency group,
and the backlog gate that makes it a no-op when zero rows remain. State explicitly that it
shares the `SMARTSHEET_API_TOKEN` with the billing run and that this is why it is capped
and scheduled off-hours. If the page currently states or implies that
`weekly-excel-generation.yml` is the only Smartsheet-consuming workflow, rewrite that
sentence rather than contradicting it further down.

`website/docs/reference/environment.md`: add the four
`CELL_HISTORY_BACKFILL_MAX_REQUESTS` (3000), `CELL_HISTORY_BACKFILL_MAX_ROWS` (1200),
`CELL_HISTORY_BACKFILL_PACE_SEC` (0.25) and `CELL_HISTORY_BACKFILL_MAX_MINUTES` (45)
entries in the page's existing table format, each noting that it is read ONLY by
`scripts/backfill_cell_history_attribution.py` and never by the production run. While in
the file, correct any entry that still presents `ATTACHMENT_PREFETCH_MAX_MINUTES`,
`ATTACHMENT_PREFETCH_FUTURE_TIMEOUT_SEC`, `DISCOVERY_CACHE_TTL_MIN` or
`USE_DISCOVERY_CACHE` as having an effect — they were retired in Phase 11 Plan 08 (INC-05)
and are documented no-ops.

Do not introduce a broken cross-link: every reference to the new page uses the Docusaurus
id `ownership-attribution`.
  </action>
  <verify>
    <automated>npm --prefix website run build</automated>
    <fails_when>non-zero exit, or any line containing "Broken link" or "Docusaurus found broken links" in the output</fails_when>
    <automated>python -m pytest tests/test_own04_documentation.py -q</automated>
    <fails_when>non-zero exit, or "failed" / "error" appears in the pytest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `website/docs/runbook/scripts.md` names both new scripts and carries the "freezes the CURRENT Smartsheet value" warning on `scripts/backfill_attribution_snapshot.py`, in exactly one place.
    - `website/docs/runbook/workflows.md` names `cell-history-backfill.yml` with its caps, its own concurrency group and the backlog gate.
    - `website/docs/reference/environment.md` lists all four `CELL_HISTORY_BACKFILL_*` variables with their defaults, each marked as not read by the production run.
    - `website/docs/reference/environment.md` describes `ATTACHMENT_PREFETCH_MAX_MINUTES`, `ATTACHMENT_PREFETCH_FUTURE_TIMEOUT_SEC`, `DISCOVERY_CACHE_TTL_MIN` and `USE_DISCOVERY_CACHE` as retired no-ops, or does not list them at all.
    - `npm --prefix website run build` exits 0 with no broken-link output.
  </acceptance_criteria>
  <done>The runbook has one coherent account of ownership attribution, the new workflow, and the new environment variables, with no contradictory leftovers.</done>
  <reversibility rating="reversible">Documentation only.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Append the dated Living Ledger entry</name>
  <files>memory-bank/living-ledger.md</files>
  <read_first>
    - `memory-bank/living-ledger.md` — the last 60 lines, for the exact `[YYYY-MM-DD HH:MM]` heading format, entry voice and the existing Phase 12 entries `[2026-09-01 18:05]`, `[2026-09-01 18:55]`, `[2026-09-01 19:45]`, `[2026-09-01 19:55]` and `[2026-09-02 00:35]` that this entry follows and must not contradict.
    - `CLAUDE.md` § "Living Ledger (Auto-Updated Context)" — the rule that new entries go to the BOTTOM of `memory-bank/living-ledger.md` and never back into `CLAUDE.md`.
    - `12-01-SUMMARY.md`, `12-02-SUMMARY.md`, `12-03-SUMMARY.md`, `12-04-SUMMARY.md` — what actually shipped, including the live column names Juan reported and the options he selected at the two decision checkpoints.
  </read_first>
  <action>
Append ONE new dated entry to the BOTTOM of `memory-bank/living-ledger.md` using the
existing `[YYYY-MM-DD HH:MM]` heading convention and the file's established voice. Do not
edit `CLAUDE.md`.

The entry synthesizes what shipped and why — not a commit list. It must record, in this
order:

1. **D-12-A** — Phase 12 ships no `wr_week_ownership` table. OWN-01's ladder is served by
   `billing_audit.attribution_snapshot` + `resolve_claimer` + the new `backfill_source` /
   `backfill_run_id` provenance columns; the table is deferred to Phase 13. State that
   `REQUIREMENTS.md` OWN-01's `wr_week_ownership` and cross-week wording is stale and that
   this entry supersedes it, so a future reader does not re-open the question.
2. **D-12-B** — source 4 (`backfill_hash_history`) reads `billing_audit.group_content_hash`
   and `pipeline_memory.group_state` rather than a JSON file; no `--hash-history` flag; the
   single-name rule and the pre-2026-08-24 `updated_at` preference; the weeks it cannot
   cover fall through to sources 3 and 5.
3. **The amended Foundation A contract** — first-write-wins still protects a real name; a
   frozen sentinel is now read as no-claimer and may be replaced by a provenance-tagged
   backfilled value; enforcement lives in `is_sentinel_claimer`, its SQL twin
   `billing_audit.is_sentinel_value`, and `backfill_attribution`'s `WHERE` clause.
4. **CR-01** — the rule that came out of it, stated as a durable engineering rule: a
   sentinel classifier's fail-safe direction is "real name, decline to delete", because a
   sanitized identifier is lossy and a leading underscore can come from a real name's
   leading punctuation. Name `pipeline/cleanup.py` and the sanitized-error allowlist as the
   fix, and the sentinel-superseded attachment gate as the blast radius.
5. **WR-01** — a deep third-party module path used by exactly one helper belongs inside
   that helper, guarded, so an SDK relocation degrades one function instead of breaking a
   production module's import.
6. **Source 5 isolation** — the cell-history backfill runs only in
   `.github/workflows/cell-history-backfill.yml` with its own concurrency group and hard
   caps, never inside `generate_weekly_pdfs.py`, because both share one Smartsheet token and
   one 300 req/min ceiling. Note the structural test that enforces it.
7. **The two-scripts trap** — `scripts/backfill_attribution_snapshot.py` freezes the current
   Smartsheet value and must never be run against a remediated WR.

Reference files and the runbook page rather than pasting diffs. Include no real claimer
name, no secret, no token and no environment value. Use only the fictional
`Avery Example` if an example is needed.
  </action>
  <verify>
    <automated>python -m pytest tests/test_own04_documentation.py -q</automated>
    <fails_when>non-zero exit, or "failed" / "error" appears in the pytest summary line</fails_when>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
  </verify>
  <acceptance_criteria>
    - `memory-bank/living-ledger.md` ends with a new entry whose heading matches the `[YYYY-MM-DD HH:MM]` pattern and whose date is the execution date.
    - The entry contains `D-12-A`, `D-12-B`, `CR-01`, `WR-01`, `backfill_source`, `backfill_run_id` and `cell-history-backfill.yml`.
    - The entry contains zero occurrences of `SUPABASE_SERVICE_ROLE_KEY`, `SMARTSHEET_API_TOKEN` and any value that looks like a credential.
    - `CLAUDE.md` is unmodified (`git diff --name-only` does not list it).
    - `python -m pytest tests/ -q` exits 0.
  </acceptance_criteria>
  <done>The Living Ledger carries one dated, synthesized entry that a future engineer can read instead of re-deriving Phase 12's decisions.</done>
  <reversibility rating="reversible">Documentation only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private billing data -> public repository documentation | Claimer names and Supabase identifiers could leave the private data plane through a committed markdown file |
| documentation -> future operator action | A wrong or stale instruction here becomes a production action later |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-22 | Information Disclosure | Runbook page and Living Ledger entry | high | mitigate | Only fictional claimer names (`Avery Example`, `Pat Example`, `Sam Sample`) are used, matching the existing public-repo rule in `tests/test_sentinel_superseded_cleanup.py`. The acceptance criteria grep the ledger entry for credential-shaped strings. No report content is pasted. |
| T-12-23 | Tampering | Stale documentation causing a wrong production action | high | mitigate | The page explicitly supersedes `REQUIREMENTS.md` OWN-01's stale wording and carries the two-scripts warning; `tests/test_own04_documentation.py` fails if the dropped ladder rung or the non-existent `--hash-history` flag appears. |
| T-12-24 | Repudiation | Decisions with no durable record | medium | mitigate | One dated Living Ledger entry records D-12-A, D-12-B, CR-01, WR-01 and the source-5 isolation rule, per this repo's autonomous-memory convention. |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed or added; `npm --prefix website run build` uses the existing lockfile. RESEARCH.md § Package Legitimacy Audit records zero new dependencies for the phase. |
</threat_model>

<verification>
- `python -m pytest tests/test_own04_documentation.py -q` exits 0.
- `npm --prefix website run typecheck` exits 0.
- `npm --prefix website run build` exits 0 with no broken-link output.
- `python -m pytest tests/ -q` exits 0 with no failed or errored tests.
- `git diff --name-only` does not list `CLAUDE.md`.
</verification>

<success_criteria>
- The runbook documents the ladder as implemented, with no cross-week rung and no shipped `wr_week_ownership` table.
- D-12-A and D-12-B are recorded in both the runbook and the Living Ledger.
- The two similarly-named backfill scripts are impossible to confuse after reading the page.
- The Docusaurus site builds with the new page reachable from the sidebar and no broken links.
- No real claimer name or credential appears anywhere in the documentation.
</success_criteria>

<output>
Create `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-05-SUMMARY.md` when done
</output>
