---
phase: 14-foreman-helper-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-foreman-helper-2/14-PENDING-RESOLUTIONS.md
  - .planning/phases/14-foreman-helper-2/14-DECISIONS.md
autonomous: false
requirements: [HLP-04, HLP-06, HLP-07]

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every item 14-RESEARCH.md marked `pending — resolve in execution task` has a written answer, or a written statement of why it is still open and which later plan must therefore carry the risk."
    - "Whether the `pipeline_memory.group_state` hash-skip path or the `_live_row_attachments` pre-seed can suppress a FIRST Helper #2 generation is answered from the actual read paths, not from the documented contract alone."
    - "What `pipeline/attribution.py`, `pipeline/pricing.py`, and `pipeline/observability.py` need for Helper #2 is answered by reading them, so no later plan silently under-scopes."
    - "The live Smartsheet column state for the six Helper #2 titles, and whether the Resource Analyst `Foreman Helper #2` column is still blank, is confirmed by Juan before fixtures and pilot scope are finalized."
  artifacts:
    - .planning/phases/14-foreman-helper-2/14-PENDING-RESOLUTIONS.md
    - .planning/phases/14-foreman-helper-2/14-DECISIONS.md
  key_links:
    - "The A1/A2 answer feeds plan 14-06: if a first helper2 run CAN be suppressed, 14-06 must add an explicit guard rather than inherit an assumption."
    - "The A3 answer feeds plans 14-03, 14-06, and 14-08: any Helper #2 need found in attribution.py, pricing.py, or observability.py is assigned to a named plan in this document."
    - "Juan's live-column confirmation feeds plans 14-01/14-07 fixtures and plan 14-10 pilot scope: a still-blank Resource Analyst column makes the pilot fixture-only, and the plan must say so."
  prohibitions:
    - statement: "Never write, provision, repair, or reconnect any Smartsheet column, row, or sheet in this plan; the live probe is read-only and owner-run."
      status: active
      verification: unverified
    - statement: "Never modify production source code in this plan; its only outputs are two planning documents."
      status: active
      verification: unverified
    - statement: "Never resolve O-14-A here; this plan records open questions, it does not decide business rules."
      status: active
      verification: unverified
---

<objective>
Close the four evidence gaps 14-RESEARCH.md explicitly deferred with "pending — resolve in
execution task", in Wave 1, before any plan that depends on them makes a design choice.

Purpose: the research was bounded and read-only, and it was honest about what it could not see.
Three of those gaps sit directly under later design decisions — first-run suppression, module
scope, and pilot feasibility. Answering them now costs a few file reads; answering them later costs
a re-plan.

Output: `14-PENDING-RESOLUTIONS.md`, one section per gap, each naming the later plan that depends on
it, plus `14-DECISIONS.md`, the phase's running owner-decision ledger.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-foreman-helper-2/14-RESEARCH.md
@.planning/phases/14-foreman-helper-2/14-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Answer A1 and A2 — can a FIRST Helper #2 generation be suppressed?</name>
  <files>.planning/phases/14-foreman-helper-2/14-PENDING-RESOLUTIONS.md</files>
  <read_first>
    - .planning/phases/14-foreman-helper-2/14-RESEARCH.md sections "Ownership, Timing, and Repeated Runs" and "Assumptions Log" (A1, A2)
    - pipeline_memory/reader.py — the full group_state read path including get_group_state_attachments_by_wr
    - pipeline/change_detection.py lines 815-880 — _resolve_unchanged_for_skip, including the branches past line 848 the research did not read
    - pipeline/orchestrate.py lines 1190-1260 — the _live_row_attachments pre-seed
    - pipeline/parity.py — whether a one-time mass content-hash change disturbs a parity streak (this feeds plan 14-04's HASH_FIELDS decision)
  </read_first>
  <precondition>No Supabase or Smartsheet credentials are needed; every question in this task is answerable by reading repository source.</precondition>
  <action>
Read the four call paths and write, per mechanism, whether a brand-new helper2 variant's FIRST
generation for a (WR, week) can be suppressed, and what key each mechanism uses.

Answer exactly these three questions. First: is the group_state skip keyed on the full
(wr, week_ending, variant, identifier) tuple, so a variant with no stored row falls through to
regenerate? Second: does the _live_row_attachments pre-seed key on that same tuple or on something
coarser such as WR and week only — and if coarser, can a primary attachment's presence be mistaken
for a helper2 attachment's presence? Third: does pipeline/parity.py consume content_hash values or
row_event volume in a way that a one-time mass hash change would disturb?

For each answer, state the evidence as a file plus the line range actually read, and state the
confidence. Then name the consequence: which later plan must add an explicit guard, and what that
guard is. If a mechanism turns out to be able to suppress a first Helper #2 run, say so plainly and
record it as a required task for plan 14-06 rather than softening it.

Write the findings into `.planning/phases/14-foreman-helper-2/14-PENDING-RESOLUTIONS.md` under one
section per assumption id, headed `## A1` and `## A2`, plus a parity sub-finding. Each section opens
with a one-word verdict line: CONFIRMED SAFE, REQUIRES GUARD, or STILL UNKNOWN.
  </action>
  <verify>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('.planning/phases/14-foreman-helper-2/14-PENDING-RESOLUTIONS.md').read_text(encoding='utf-8'); sys.exit(0 if ('## A1' in t and '## A2' in t and 'parity' in t.lower()) else 1)"</automated>
    <fails_when>non-zero exit — the A1 section, the A2 section, or the parity finding is absent from the document</fails_when>
  </verify>
  <acceptance_criteria>
    - `14-PENDING-RESOLUTIONS.md` contains a section headed `## A1` and one headed `## A2`
    - Each of those sections opens with one of the three verdict words and cites at least one concrete file plus line range read during this task
    - Each of those sections names the later plan number that inherits the consequence
    - A parity finding is recorded and names whether plan 14-04's hash decision is affected
  </acceptance_criteria>
  <done>The first-run suppression question is answered from the code, and any required guard is assigned to a named later plan instead of surviving as an assumption.</done>
</task>

<task type="auto">
  <name>Task 2: Answer A3 — what Helper #2 needs from attribution.py, pricing.py, and observability.py</name>
  <files>.planning/phases/14-foreman-helper-2/14-PENDING-RESOLUTIONS.md</files>
  <read_first>
    - .planning/phases/14-foreman-helper-2/14-RESEARCH.md section "Prototype Reference" (the 16-file list and Assumption A3)
    - pipeline/attribution.py — every function that enumerates roles or WR scopes
    - pipeline/pricing.py — any branch keyed on a variant string or on helper metadata
    - pipeline/observability.py — the PII log markers and the redaction helper used by pipeline/upload.py
    - .planning/phases/14-foreman-helper-2/reference/helper2-prototype-2026-07/test_helper2_formula_generation.py.txt — fixture shapes only
    - .planning/phases/14-foreman-helper-2/reference/helper2-prototype-2026-07/test_weekly_excel_helper_split.py.txt — fixture shapes only
  </read_first>
  <action>
Determine by reading whether each of the three modules the 2026-07 prototype touched, but the
research did not read, needs Helper #2 work — and assign any real need to a specific plan.

For pipeline/attribution.py: list every place a role or variant vocabulary is enumerated and state
whether a Helper #2 role must appear there. For pipeline/pricing.py: state whether any pricing
branch reads a helper field or a variant string. If pricing is variant-agnostic, say so explicitly —
that is itself a load-bearing finding, because it is what makes "Helper #2 inherits Helper #1
pricing untouched" true rather than assumed (D-14-03). For pipeline/observability.py: name the exact
marker constant or function that a new Helper #2 log line must route through so a person's name is
never emitted unredacted, and hand that name to plans 14-07 and 14-08.

Separately, skim the two renamed prototype test files for reusable fixture shapes only — the row
dictionaries and the expected-filename assertions. Record which shapes are reusable and which
assertions are stale because the prototype's base predates Phases 10 through 12. Never copy a
prototype assertion into a test without re-validating it against current code.

Append the findings under a section headed `## A3`, with a per-module verdict of either
NO CHANGE NEEDED or CHANGE NEEDED, and for the latter the plan number it is assigned to, plus a
one-line justification each.
  </action>
  <verify>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('.planning/phases/14-foreman-helper-2/14-PENDING-RESOLUTIONS.md').read_text(encoding='utf-8'); need=['## A3','pipeline/attribution.py','pipeline/pricing.py','pipeline/observability.py']; sys.exit(0 if all(k in t for k in need) else 1)"</automated>
    <fails_when>non-zero exit — the A3 section is missing, or one of the three module paths is not mentioned in the document</fails_when>
  </verify>
  <acceptance_criteria>
    - `14-PENDING-RESOLUTIONS.md` contains a section headed `## A3` naming all three modules
    - Each module carries an explicit verdict and, when a change is needed, the plan number that owns it
    - The prototype fixture-shape review lists at least one reusable shape and at least one assertion flagged as stale
  </acceptance_criteria>
  <done>The three unread modules have evidence-backed verdicts, so no later plan can under-scope them by inheriting an unexamined assumption.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Juan confirms the live Helper #2 column state and the pilot data precondition</name>
  <files>.planning/phases/14-foreman-helper-2/14-DECISIONS.md</files>
  <read_first>
    - .planning/phases/14-foreman-helper-2/14-CONTEXT.md sections "Discovery and mapping-cache reuse" (D-14-10) and "Specific Ideas" (the live sheet ids)
    - .planning/phases/14-foreman-helper-2/14-RESEARCH.md section "Active Sources and Field Contracts"
  </read_first>
  <what-built>
Tasks 1 and 2 answered the repository-answerable gaps (A1, A2, A3) and wrote them into
`14-PENDING-RESOLUTIONS.md`. What remains is the one question no repository read can answer: the
CURRENT state of the live Smartsheet columns. The 2026-09-05 observations in 14-CONTEXT.md are a
snapshot taken during context gathering; the fixtures in plans 14-01 and 14-07 and the pilot scope in
plan 14-10 are built on them, and they may have drifted since.

This checkpoint carries `gate="blocking-human"` because no agent may read production Smartsheet and
because auto-approving it would record an unobserved snapshot as an observation. Nothing was built
against live Smartsheet and nothing may be: this task authorizes no write, no column provisioning,
no formula repair, and no reconnection of any sheet.
  </what-built>
  <how-to-verify>
Juan runs a read-only look at the live sheets and answers four questions. Record each answer in
`.planning/phases/14-foreman-helper-2/14-DECISIONS.md` with the date it was observed and the words
"read-only".

1. Main ProMax `3239244454645636` — confirm it still carries all six titles `Foreman Helping? #2`,
   `Foreman Helper #2 Active?`, `Helping Foreman #2 Completed Unit?`, `Helper #2 Dept #`,
   `Helper #2 Job [#]`, and `Foreman Helper #2 Email`.
2. Any other sheet in Intake folder `8815193070299012` or in the ProMax Database folder — does any of
   them now carry a PARTIAL Helper #2 column set? A partial set is a capability-unavailable case that
   plan 14-07's fixtures must cover.
3. Intake ProMax 8 `2244739192541060` and Backup 2 `2230129632694148` — confirm they still have no
   Helper #2 columns. Record this as accepted state under D-14-01, never as a defect and never as work.
4. Resource Analyst `Foreman Helper #2` (column id `1589780186173316`) — is it still blank on every
   row? If it is, plan 14-10's pilot is fixture-only and must say so rather than claim a dry-run over
   real Helper #2 data.

Record column titles, column ids, and counts only — never a row value and never a person's name.
Then write the marker line `LIVE-COLUMN-PROBE: ANSWERED` with the observation date, and one line
stating the pilot-mode consequence: fixture-only, or dry-run over real Helper #2 rows.
  </how-to-verify>
  <resume-signal>Type "approved" once all four answers are recorded in 14-DECISIONS.md with their observation date, or describe what the live read actually showed if it differs from the 2026-09-05 snapshot</resume-signal>
  <verify>
    <human-check>Juan has answered all four questions and the answers are written into 14-DECISIONS.md with an observation date.</human-check>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('.planning/phases/14-foreman-helper-2/14-DECISIONS.md').read_text(encoding='utf-8'); sys.exit(0 if ('LIVE-COLUMN-PROBE: ANSWERED' in t and 'read-only' in t) else 1)"</automated>
    <fails_when>non-zero exit — the answered marker line or the read-only attestation is missing from 14-DECISIONS.md</fails_when>
  </verify>
  <acceptance_criteria>
    - `14-DECISIONS.md` exists and contains a line marking the live column probe as answered, with an observation date
    - All four questions have recorded answers, each attested read-only
    - The pilot-mode consequence is stated in one line: fixture-only, or dry-run over real Helper #2 rows
    - No Smartsheet write of any kind is recorded or performed
  </acceptance_criteria>
  <done>The live column state and the pilot data precondition are confirmed by the owner, dated, and recorded, so plans 14-07 and 14-10 build on observed state rather than a stale snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live Smartsheet → planning document | An owner-run read could copy row-level personal data into a repository file |
| Repository source → planning document | Quoted code excerpts could carry secrets if a module embeds one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02-01 | Information Disclosure | 14-DECISIONS.md live-probe answers | medium | mitigate | Record column titles, column ids, and counts only — never row values, never a person's name, never a token; the probe answers whether a column exists and whether it is blank, not what is in it |
| T-14-02-02 | Tampering | live Smartsheet during the probe | high | mitigate | The task is explicitly read-only and owner-run; it authorizes no write, no column provisioning, and no reconnection, and the acceptance criteria require a read-only attestation |
| T-14-02-03 | Repudiation | pending-resolution verdicts | low | mitigate | Every verdict must cite the file and line range that produced it, so a later reader can re-derive it |
| T-14-02-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages; 14-RESEARCH.md section "Package Legitimacy Audit" records the audit as not applicable |
</threat_model>

## Artifacts this phase produces

- New file `.planning/phases/14-foreman-helper-2/14-PENDING-RESOLUTIONS.md` with sections `## A1`, `## A2`, `## A3`
- New file `.planning/phases/14-foreman-helper-2/14-DECISIONS.md` — the phase's running owner-decision ledger, seeded here and appended to by plans 14-04, 14-07, 14-08, 14-09, and 14-10
- Verdict vocabulary: `CONFIRMED SAFE`, `REQUIRES GUARD`, `STILL UNKNOWN`, `NO CHANGE NEEDED`, `CHANGE NEEDED`, `LIVE-COLUMN-PROBE: ANSWERED`

## Edge Assumptions (spec-less probe fallback)

| Req | Probe category | Adopted answer (assumption) |
|---|---|---|
| HLP-04 | unclassified | Intake ProMax 8 is verified only through a fixture shaped like it — Helper #1 columns present, Helper #2 columns absent. The live probe in Task 3 confirms that shape is still accurate; it never remediates the sheet. |
| HLP-06 | idempotency | Whether a repeated run can suppress a first Helper #2 generation is exactly what Task 1 answers; until answered it is recorded as an open assumption, never as a passing condition. |
| HLP-07 | boundary | If the Resource Analyst Helper #2 column is still blank on every row, the pilot's real-data boundary does not exist yet and the pilot is fixture-only. This is recorded, not worked around. |

<verification>
- Both planning documents exist and every automated check in the tasks above exits 0.
- No file outside `.planning/phases/14-foreman-helper-2/` appears in this plan's diff.
- Each verdict in `14-PENDING-RESOLUTIONS.md` cites a file plus line range.
</verification>

<success_criteria>
Every "pending — resolve in execution task" item from 14-RESEARCH.md is either answered with
evidence or explicitly carried forward with a named owner plan; the owner has confirmed the live
column state read-only; no source file and no Smartsheet object was modified.
</success_criteria>

<output>
Create `.planning/phases/14-foreman-helper-2/14-02-SUMMARY.md` when done. Label evidence
precisely: repository reads are DOCUMENTED EVIDENCE, and the Smartsheet answers are OWNER-OBSERVED
LIVE STATE — neither is a dry-run pass and neither is a production observation of Helper #2
behavior.
</output>
