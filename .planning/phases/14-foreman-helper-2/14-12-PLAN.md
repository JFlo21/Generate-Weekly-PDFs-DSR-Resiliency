---
phase: 14-foreman-helper-2
plan: 12
type: execute
wave: 8
depends_on: ["14-04", "14-07", "14-10"]
gap_closure: true
files_modified:
  - pipeline_memory/schema.sql
  - pipeline_memory/helper2_columns_migration.sql
  - tests/test_upsert_rows_bulk_helper2_contract.py
  - .github/workflows/weekly-excel-generation.yml
  - website/docs/runbook/foreman-helper-2.md
  - website/docs/reference/environment.md
  - .github/prompts/configuration-environment.md
  - .planning/phases/14-foreman-helper-2/14-DECISIONS.md
  - .planning/phases/14-foreman-helper-2/14-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [HLP-06, HLP-07]

estimate:
  tokens: 45000
  raw_tokens: 20000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "pipeline_memory.upsert_rows_bulk names helper2_observed, helper2_completed, helper2_dept, helper2_job in its typed recordset list, its incoming projection, the row_event after_image, the row_state INSERT list and its ON CONFLICT set list, so a run that observes a Helper #2 persists it instead of the RPC silently dropping the four keys (O-14-B)."
    - "A source-pin test keeps every HASH_FIELDS member in every one of those RPC lists; a future hashed field that is missing from the RPC fails the suite instead of failing silently in production."
    - "The five additive columns (row_state helper2_* x4, sheet_registry.mapping_schema) exist in production, applied together with the RPC in one session outside a run window, with the pre-state parked for rollback."
    - "The apply is proven by a read-back: the columns exist, the live function text carries the helper2 fields, service_role's EXECUTE and the search_path pin survived, and a synthetic round-trip through the RPC stores Helper #2 values, carries them in after_image, and adds zero row_event rows on an identical second call."
    - "HELPER2_ENABLED is wired into the scheduled workflow exactly as the runbook proposed (a repo variable with a '0' fallback) and switched on by the owner's instruction, so enabling and disabling is a variable flip with no further code change."
    - "The requirement ledger tells the truth: HLP-06 becomes Complete only after the read-back, and O-14-A Follow-up 1 carries the owner's written confirmation."
  artifacts:
    - pipeline_memory/helper2_columns_migration.sql
    - tests/test_upsert_rows_bulk_helper2_contract.py
  key_links:
    - "pipeline_memory/writer.py::_row_to_payload already emits the four helper2 keys and HASH_FIELDS already hashes them (14-04); this plan only makes the RPC keep them."
    - "pipeline_memory/reader.py::get_sheet_watermarks stops degrading (one WARNING + full validation for every sheet) the moment sheet_registry.mapping_schema exists; discovery writes the marker after the first full validation."
  prohibitions:
    - statement: "Never change the upsert_rows_bulk signature or return type; CREATE OR REPLACE on the identical signature is what keeps the live grants and the search_path pin."
      status: active
      verification: unverified
    - statement: "Never add a DEFAULT, NOT NULL, index, or constraint to the new columns; the migration must stay a metadata-only, reversible ADD COLUMN IF NOT EXISTS."
      status: active
      verification: unverified
    - statement: "Never apply the SQL while a weekly-excel-generation run is in progress; check gh run list at the moment of applying."
      status: active
      verification: unverified
    - statement: "Never hard-code HELPER2_ENABLED to '1' in the workflow; the documented shape is vars.HELPER2_ENABLED with a '0' fallback so the switch stays a variable flip."
      status: active
      verification: unverified
    - statement: "Never touch TIME_BUDGET_MINUTES, timeout-minutes, the advanced_options parser, or any other workflow line."
      status: active
      verification: unverified
---

<objective>
Close O-14-B and finish HLP-06's cached half: make `pipeline_memory.upsert_rows_bulk` carry the four
Helper #2 fields the writer already sends, apply the two pending additive DDLs
(`row_state` helper2_* x4 per D-14-08-APPLIED, `sheet_registry.mapping_schema` per D-14-10-APPLIED)
together with the RPC in one owner-delegated session, prove it with a read-back, then wire and enable
`HELPER2_ENABLED` for the scheduled workflow.

Purpose: 14-VERIFICATION.md (2026-09-09) found HLP-06 only half met. Until the RPC keeps the fields,
the first post-merge run pays the ~217k `row_event` hash churn without persisting a single Helper #2
value, and `sheet_registry.mapping_schema` missing means every run re-validates every sheet. The owner
asked on 2026-09-08 for both DDLs, the O-14-B closure, and the flag enabled once those land.

Output: an RPC text update with a lockstep contract test, an additive migration file, the applied and
verified production change, the workflow wiring, and the decision, requirement, and verification
records that make the phase honest.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-foreman-helper-2/14-DECISIONS.md
@.planning/phases/14-foreman-helper-2/14-VERIFICATION.md

Read `D-14-08-APPLIED`, `D-14-10-APPLIED`, `O-14-B`, and `D-14-12-ROLLOUT` first. The live pre-state
(no helper2 columns, no marker column, RPC md5 5987e5ed…, grants service_role/postgres/PUBLIC EXECUTE,
proconfig search_path="") was captured 2026-09-09 02:03Z into the owner's vault raw folder.

Owner decision of record (Juan, chat, 2026-09-08 evening CDT), quoted in 14-DECISIONS.md: apply both
DDLs, close O-14-B, treat O-14-A Follow-up 1 as confirmed, "and then enable the helper 2 once these
issues are fixed". The 14-10 runbook's "Confirm the budget still holds after the first real Helper #2
pilot run" caution is overridden by that instruction; the Resource Analyst Helper #2 column is blank on
every live row, so enabling changes no workbook until a crew records a second helper.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RPC carries every HASH_FIELDS member; additive migration file</name>
  <files>pipeline_memory/schema.sql, pipeline_memory/helper2_columns_migration.sql, tests/test_upsert_rows_bulk_helper2_contract.py</files>
  <read_first>
    - pipeline_memory/schema.sql lines 270-506 — the RPC: typed jsonb_to_recordset list, incoming projection, after_image, row_state INSERT + ON CONFLICT set list
    - pipeline_memory/writer.py lines 642-670 (HASH_FIELDS) and 756-870 (_row_to_payload already emits helper2_* keys)
    - tests/test_helper2_attribution_sql_contract.py — the _read_source / _strip_sql_comments idiom to mirror
    - tests/test_pipeline_memory_shadow.py lines 446-520 — the existing schema.sql column-set parser
  </read_first>
  <reversibility rating="reversible">Repository-only SQL text and tests; nothing executes until Task 2.</reversibility>
  <action>
RED: `tests/test_upsert_rows_bulk_helper2_contract.py` parses the RPC block out of schema.sql and asserts
every HASH_FIELDS member is present in the typed recordset (with the row_state DDL types for the helper2
columns), the incoming projection, after_image, the INSERT list + select, and the ON CONFLICT set list;
asserts the signature, return type and search_path pin are unchanged; and pins the migration file
(header names the project and OWNER-APPLIED, five idempotent ADD COLUMN IF NOT EXISTS statements, no
DROP/DELETE/TRUNCATE/DEFAULT/NOT NULL/index, no duplicated RPC body). Expect 11 failures.

GREEN: add the four helper2 fields to each RPC list in schema.sql (after vac_completed everywhere, in
HASH_FIELDS order), with a comment block explaining the lockstep pin and the apply order; write
`pipeline_memory/helper2_columns_migration.sql` with STEP 1 (four row_state columns), STEP 2
(mapping_schema), STEP 3 pointer to the schema.sql block, and the read-back queries. Keep schema.sql CRLF.
  </action>
  <verify>PYTHONUTF8=1 python -m pytest tests/test_upsert_rows_bulk_helper2_contract.py tests/test_pipeline_memory_shadow.py -q; then the full suite and bash scripts/run_6_gates.sh.</verify>
  <done>Contract test green (12 passed), full suite green, six gates pass, schema.sql diff touches only the RPC block and its comment.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Apply the five columns and the RPC in one session, read back, record</name>
  <files>.planning/phases/14-foreman-helper-2/14-DECISIONS.md, pipeline_memory/helper2_columns_migration.sql, pipeline_memory/schema.sql</files>
  <decision>Apply the DDL + RPC to production now, owner-delegated to the session via the Supabase MCP (resolved 2026-09-08: Juan approved both DDLs and the O-14-B closure in one message).</decision>
  <options>
    - apply-delegated: the session applies migration STEP 1-2 plus the schema.sql RPC block verbatim as ONE Supabase migration, outside a run window, then reads back (chosen).
    - owner-by-hand: Juan pastes the two files into the SQL Editor; the session only reads back.
    - defer: leave the columns absent; the RPC change alone would fail (INSERT references missing columns), so the RPC must wait too.
  </options>
  <action>
1. Check `gh run list --workflow weekly-excel-generation.yml --limit 1`; if a run is in progress, wait
   for it (runs take 32-55 min) — the ALTER takes an ACCESS EXCLUSIVE lock and the RPC replace should
   not race a writing run.
2. Apply, as one migration named `helper2_row_state_columns_marker_and_rpc`: the five ALTER TABLE
   statements from the migration file, then the CREATE OR REPLACE FUNCTION block copied verbatim from
   schema.sql (CRLF stripped), then the GRANT line (a no-op on an unchanged signature, kept for parity).
3. Read back: (a) four helper2 columns on row_state, one mapping_schema on sheet_registry;
   (b) pg_get_functiondef contains helper2_observed and still `SET search_path TO ''`;
   (c) role_routine_grants still lists service_role EXECUTE; proconfig unchanged;
   (d) synthetic round-trip on `sheet_id = -14012` with two rows via the RPC: call 1 returns the
   affected pairs and row_state holds the helper2 values, row_event.after_image carries them;
   call 2 with identical payload returns zero pairs and adds zero row_event rows; then delete the
   synthetic row_state and row_event rows and confirm zero remain.
4. Record `D-14-13-DDL-APPLIED` and `D-14-13-VERIFIED` in 14-DECISIONS.md with DB-stamped times,
   mark `O-14-B` RESOLVED, add an APPLIED note to the migration file header and the schema.sql RPC
   comment.
  </action>
  <resume-signal>Read-back (a)-(d) all pass and the records are committed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Wire and enable HELPER2_ENABLED; make the ledgers truthful</name>
  <files>.github/workflows/weekly-excel-generation.yml, website/docs/runbook/foreman-helper-2.md, website/docs/reference/environment.md, .github/prompts/configuration-environment.md, .planning/phases/14-foreman-helper-2/14-DECISIONS.md, .planning/phases/14-foreman-helper-2/14-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - website/docs/runbook/foreman-helper-2.md lines 193-215 — the proposed env line and its rationale
    - .github/workflows/weekly-excel-generation.yml lines 213-245 — the "Generate reports" step env block where RUN_MEMORY_WRITE_ENABLED lives
  </read_first>
  <reversibility rating="reversible">One env line with a '0' fallback; rollback is `gh variable set HELPER2_ENABLED --body 0` or deleting the line.</reversibility>
  <action>
1. Add `HELPER2_ENABLED: ${{ vars.HELPER2_ENABLED || '0' }}` to the "Generate reports" step env
   block next to RUN_MEMORY_WRITE_ENABLED with a four-line comment (owner decision, rollback, where the
   variable lives). Touch nothing else in the workflow; the existing workflow structural tests must pass.
2. Runbook: retitle "Workflow wiring (not yet applied)" to "Workflow wiring (applied)", state the
   variable flip as the on/off switch and that the pilot caution was overridden by the owner on
   2026-09-08; environment.md and configuration-environment.md say the flag is wired and variable-driven.
3. 14-DECISIONS.md: `D-14-14-ENABLE` (Juan's quoted instruction, the variable mechanism, expected
   first-enabled-run behaviour: counters present, no degrade warning, no Helper #2 workbook until the
   RA column carries a name) and `O-14-A-FOLLOWUP-1 — CONFIRMED` (quote).
4. REQUIREMENTS.md: HLP-06 back to Complete with the read-back reference; 14-VERIFICATION.md: an
   addendum section marking the HLP-06 gap closed with evidence and the status line updated.
5. After the PR merges: `gh variable set HELPER2_ENABLED --body 1 --repo JFlo21/Generate-Weekly-PDFs-DSR-Resiliency`,
   then confirm the next scheduled run's log shows the Helper #2 counters and no degrade warning.
  </action>
  <verify>pytest tests/ -q (workflow structural tests included); cd website && npm run typecheck && npm run build; gh variable list shows HELPER2_ENABLED=1 after merge.</verify>
  <done>Workflow line present and variable-driven, docs consistent, records written, PR merged, variable set, first enabled run observed or handed off with the exact log lines to look for.</done>
</task>

</tasks>

<threat_model>
- T-14-12-01 (integrity): a hashed field missing from the RPC silently drops data — mitigated by the lockstep test.
- T-14-12-02 (availability): ALTER TABLE lock during a writing run — mitigated by the run-window check.
- T-14-12-03 (privilege): losing service_role EXECUTE on the RPC — mitigated by keeping the signature (CREATE OR REPLACE) and reading grants back.
- T-14-12-04 (rollback): the pre-state is parked verbatim in the vault raw folder; every DDL is droppable.
</threat_model>
