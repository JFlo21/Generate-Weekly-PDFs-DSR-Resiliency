---
phase: 14-foreman-helper-2
plan: 09
type: execute
wave: 5
depends_on: ["14-03", "14-08"]
files_modified:
  - billing_audit/helper2_attribution.sql
  - billing_audit/schema.sql
  - tests/test_helper2_attribution_sql_contract.py
  - .planning/phases/14-foreman-helper-2/14-DECISIONS.md
autonomous: false
requirements: [HLP-06]

estimate:
  tokens: 44000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A single owner-deployed SQL file adds the two Helper #2 attribution columns, extends the freeze function's parameters, and recreates BOTH lookup functions with the new return columns."
    - "Both lookup functions are dropped before being recreated, because Postgres cannot change a function's return columns with a replace — the failure mode that silently never deployed on 2026-05-27."
    - "The stale operator comment above the bulk lookup function, which tells the reader to use a plain replace, is corrected in the same change."
    - "Freezing a Helper #2 role writes only the Helper #2 columns; the existing primary, helper, and VAC columns of that row are unchanged."
    - "After the migration, the Python capability degrade added in plan 14-03 no longer fires, and that is confirmed by observation, not assumed."
    - "The backfill function and the Phase 12 provenance columns are untouched."
  artifacts:
    - billing_audit/helper2_attribution.sql
    - tests/test_helper2_attribution_sql_contract.py
  key_links:
    - "billing_audit/writer.py freeze parameters and role keys (plan 14-03) must exactly match the parameter names and return column names in this SQL file; a mismatch degrades silently rather than erroring."
    - "The drop-then-create sequence is the only thing standing between this migration and the 2026-05-27 silent non-deploy; the contract test is what keeps it there."
  prohibitions:
    - statement: "Never use a plain create-or-replace to change either lookup function's return columns; drop first, exactly as the single-row function's migration already does."
      status: active
      verification: unverified
    - statement: "Never modify billing_audit.backfill_attribution, the backfill_source column, or the backfill_run_id column in this migration."
      status: active
      verification: unverified
    - statement: "Never apply this SQL from an agent session; the owner applies it in the Supabase SQL editor."
      status: active
      verification: unverified
    - statement: "Never let a Helper #2 freeze clear or overwrite frozen_primary, frozen_helper, or frozen_vac_crew."
      status: active
      verification: unverified
    - statement: "Never claim the migration succeeded without a post-migration read-back; a migration that errors nowhere and deploys nothing is this file's documented failure mode."
      status: active
      verification: unverified
---

<objective>
Give Helper #2 its own frozen attribution role in Supabase: two additive nullable columns on the
attribution snapshot, two new freeze parameters, and both lookup functions recreated with the new
return columns — deployed by the owner, with a read-back that proves it actually landed.

Purpose: this is the phase's protected-area change. It carries a specific, documented failure mode:
Postgres cannot change a function's return columns with a plain replace, so a migration written the
obvious way succeeds with no error and deploys nothing. That happened here before, on 2026-05-27,
and the surviving comment above the bulk lookup function still tells the next reader to do it the way
that fails.

Output: one owner-deployed SQL file, a contract test that pins its shape, the corrected comment, and
an owner-observed read-back.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-foreman-helper-2/14-CONTEXT.md
@.planning/phases/14-foreman-helper-2/14-RESEARCH.md
@.planning/phases/14-foreman-helper-2/14-PATTERNS.md
@.planning/phases/14-foreman-helper-2/14-DECISIONS.md
@.planning/phases/14-foreman-helper-2/14-03-SUMMARY.md
@.planning/ROADMAP.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the owner-deployed Helper #2 attribution SQL and its contract test</name>
  <files>billing_audit/helper2_attribution.sql, billing_audit/schema.sql, tests/test_helper2_attribution_sql_contract.py</files>
  <read_first>
    - .planning/phases/14-foreman-helper-2/14-PATTERNS.md section "billing_audit/schema.sql" — the drop-first idiom and both function bodies are quoted there and are the copy source
    - billing_audit/schema.sql lines 175-220 — the freeze function's documented parameter contract and the statement that the table DDL and function bodies live in Supabase, not here
    - billing_audit/schema.sql lines 325-375 — the single-row lookup function, its drop-first comment, and the 2026-05-27 incident note
    - billing_audit/schema.sql lines 375-415 — the bulk lookup function and the stale operator comment above it
    - billing_audit/own03_backfill_attribution.sql — the Phase 12 precedent for an owner-deployed SQL file in this repository
    - tests/test_own03_backfill_sql_contract.py — the Phase 12 precedent for a SQL contract test
    - billing_audit/writer.py lines 600-680 and 1040-1140 — the exact parameter and role names plan 14-03 sends and reads
  </read_first>
  <reversibility rating="reversible">This task only authors files in the repository — a SQL migration script, its contract test, and a comment correction. Nothing is executed against any database here, so the whole task is undone by reverting a commit. The one-way step is APPLYING this file, which is Task 2 and is gated there.</reversibility>
  <action>
Write a single owner-deployed SQL file, `billing_audit/helper2_attribution.sql`, following the shape
of the Phase 12 backfill SQL file: a header stating who applies it, where, when, and what to verify
afterwards, then numbered steps a person can run in order.

Step one: add the two Helper #2 columns to the attribution snapshot table, additive and nullable, so
old code ignores them and new code tolerates their absence. Step two: recreate the freeze function
with the two new Helper #2 parameters appended, preserving every existing parameter's name and type
exactly, so plan 14-03's payload matches. State in the file that the function must write only the
named per-role columns and must not clear the other roles — first-write-wins per role, per Phase 12
decision D-12-A. Step three: drop and recreate the single-row lookup function with the two new return
columns, copying the existing drop-first idiom verbatim including its sentinel-nulling case
expressions. Step four: do exactly the same for the bulk lookup function — it has the identical
return-table restriction, and its own comment is wrong about that. Step five: reload the schema cache
and read back both functions.

In `billing_audit/schema.sql`, correct the stale operator comment above the bulk lookup function so
it names the drop-first requirement, and add the two new columns and parameters to the documented
contracts. That comment being wrong is exactly why the same failure could repeat.

Write `tests/test_helper2_attribution_sql_contract.py` on the Phase 12 contract-test pattern: parse
the SQL file as text and assert its shape. Assert both lookup functions have a drop statement
preceding their create statement; assert neither lookup function uses a plain create-or-replace;
assert the freeze parameter names in the SQL exactly equal the parameter names `billing_audit/writer.py`
sends; assert the lookup return column names exactly equal the role column names the writer reads;
and assert the file contains no statement touching the backfill function or the provenance columns.
This test is the only automated defense this migration has, because the deployed function bodies live
in Supabase and cannot be inspected from the repository.
  </action>
  <verify>
    <automated>python -m pytest tests/test_helper2_attribution_sql_contract.py -q</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>python -m pytest tests/test_billing_audit_shadow.py -q</automated>
    <fails_when>non-zero exit, or any "failed"/"error" count above 0 in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `billing_audit/helper2_attribution.sql` exists with a header naming the applier, the target project, and the post-application verification
    - Each of the two lookup functions in that file has a drop statement at a lower line number than its create statement
    - The contract test asserts parameter-name equality between the SQL file and `billing_audit/writer.py`, and return-column-name equality with the role names the writer reads
    - The contract test asserts the file contains no reference to the backfill function or the provenance columns
    - The stale operator comment in `billing_audit/schema.sql` above the bulk lookup function now names the drop-first requirement
    - `python -m pytest tests/test_helper2_attribution_sql_contract.py -q` exits 0
  </acceptance_criteria>
  <done>A single reviewable SQL file exists that cannot silently fail to deploy, and a test fails the moment its shape or its names drift from the Python that calls it.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Juan reviews and applies the attribution migration</name>
  <files>.planning/phases/14-foreman-helper-2/14-DECISIONS.md</files>
  <read_first>
    - billing_audit/helper2_attribution.sql — the file authored in Task 1, in full
    - billing_audit/schema.sql lines 325-375 — the 2026-05-27 incident note, so the risk is stated in the owner's own repository language
    - .planning/phases/14-foreman-helper-2/14-DECISIONS.md — the prior records from plans 14-02, 14-04, 14-07, and 14-08
    - .github/workflows/weekly-excel-generation.yml — inspect only, to identify the cron windows the migration must avoid
  </read_first>
  <reversibility rating="one-way">Dropping and recreating a live lookup function removes it for the duration of the transaction; a scheduled billing run calling it inside that window fails. Adding the two snapshot columns is reversible, but the function replacement is not something to discover mid-run, which is why this checkpoint gates the apply on an owner-chosen quiet window.</reversibility>
  <decision>Approve applying `billing_audit/helper2_attribution.sql` to the production Supabase project, and choose the deployment order relative to the code merge and the quiet window it runs in.</decision>
  <context>
Stop for Juan. This is a protected-area production change and only he applies it — this checkpoint
carries `gate="blocking-human"` because an auto-selected answer here would authorize a live schema
and function replacement no human reviewed. Do not run any SQL from this session.

Present four things. The SQL file itself, in full, since it is short enough to read. The blast
radius: dropping and recreating the two lookup functions removes them for the duration of the
transaction, so a scheduled billing run that calls them inside that window fails — identify the cron
schedule from the workflow file, read-only, and propose a quiet window. The rollback: the two columns
can be dropped and both functions recreated from the current definitions in `billing_audit/schema.sql`,
and no data is destroyed because the migration only adds. And the verification he will run afterwards,
which is Task 3 — this migration's documented failure mode produces no error at apply time, so
"the SQL ran without complaint" is never the evidence.

The options below are the deployment order. Both are safe by construction — plan 14-03 degrades
cleanly against an un-migrated function, and old code ignores the new columns — but the order must be
a stated choice rather than an accident, because it decides whether the first post-merge run writes
Helper #2 attribution or logs one degrade warning.
  </context>
  <options>
    <option id="sql-first">
      <name>Apply the SQL in a quiet window first, then merge the code</name>
      <pros>The first post-merge run writes Helper #2 attribution immediately, with no degrade warning. The migration is verified by Task 3 before any code depends on it.</pros>
      <cons>The new columns and function signatures sit unused in production until the merge lands, so a rollback of the code leaves the schema ahead of it — harmless but worth recording.</cons>
    </option>
    <option id="code-first">
      <name>Merge the code first, then apply the SQL in a quiet window</name>
      <pros>The Python degrade path from plan 14-03 is exercised against a real un-migrated function, which proves the degrade rather than assuming it.</pros>
      <cons>Every run between the merge and the apply logs the Helper #2 capability-degrade warning and writes no Helper #2 attribution — expected, but it must be recorded as expected so nobody treats it as an incident.</cons>
    </option>
    <option id="defer">
      <name>Defer the migration; ship the phase with the Python degrade active</name>
      <pros>No protected-area change now; nothing in production is touched.</pros>
      <cons>Helper #2 attribution is never frozen, so the role exists in code and nowhere in the data. Plan 14-10's rollout has no attribution to observe, and Assumption A4 stays open.</cons>
    </option>
  </options>
  <resume-signal>Select: sql-first, code-first, or defer — and state the quiet window (outside the cron schedule read from the workflow file). Record the approval, the applied timestamp, the chosen window, the deployment order, and the rollback steps in 14-DECISIONS.md as `D-14-07-APPLIED` before Task 3 starts.</resume-signal>
  <verify>
    <human-check>Juan has read the SQL file, chosen a window, applied it in the Supabase SQL editor, and recorded the result.</human-check>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('.planning/phases/14-foreman-helper-2/14-DECISIONS.md').read_text(encoding='utf-8'); sys.exit(0 if 'D-14-07-APPLIED' in t else 1)"</automated>
    <fails_when>non-zero exit — the applied record is absent, which means Task 3 has nothing to verify and must not report success</fails_when>
  </verify>
  <acceptance_criteria>
    - 14-DECISIONS.md contains a `D-14-07-APPLIED` record with the applied timestamp, the chosen window, and the deployment order relative to the code merge
    - The record names the rollback steps
    - No SQL was executed from an agent session
  </acceptance_criteria>
  <done>The protected-area migration is owner-reviewed, owner-applied in a chosen window, and recorded with its rollback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Read back what actually deployed</name>
  <files>.planning/phases/14-foreman-helper-2/14-DECISIONS.md</files>
  <read_first>
    - billing_audit/helper2_attribution.sql — its final verification step
    - billing_audit/schema.sql lines 325-375 — the documented symptom of a silent non-deploy: the migration succeeds and the new column always reads as absent
    - billing_audit/writer.py lines 600-680 — the degrade path from plan 14-03, whose silence is now the signal
  </read_first>
  <what-built>
Task 1 authored `billing_audit/helper2_attribution.sql` and its contract test; Task 2 is where Juan
applied it to the production Supabase project in his chosen window. What exists now is a claim, not a
fact: this migration's documented failure mode — a plain replace that cannot change a function's
return columns — succeeds with no error and deploys nothing. That is exactly what happened here on
2026-05-27.

This checkpoint therefore carries `gate="blocking-human"`. Auto-approving it would record an
unobserved read-back as observed and close 14-RESEARCH.md Assumption A4 on nothing. There is no
automated substitute: the deployed function bodies live in Supabase and cannot be inspected from the
repository, so the only evidence available is Juan reading them back.
  </what-built>
  <how-to-verify>
Verify by observation, not by inference. "The SQL ran without complaint" is not evidence here.

Juan confirms four things, read-only, and records each in
`.planning/phases/14-foreman-helper-2/14-DECISIONS.md` with its observation timestamp, labelled
OWNER-OBSERVED PRODUCTION:

1. Both lookup functions now return the two Helper #2 columns — query one known row through each
   function and confirm the columns are present in the result shape, even when their values are null.
   A column that is absent rather than null is the silent non-deploy.
2. The attribution snapshot table has both new columns.
3. Freezing a Helper #2 role on a test row writes only the Helper #2 columns and leaves that row's
   existing `frozen_primary`, `frozen_helper`, and `frozen_vac_crew` values byte-identical. This is
   the D-14-07 per-role guarantee the repository cannot prove on its own, and it is 14-RESEARCH.md
   Assumption A4 being closed by observation.
4. The next real run logs no Helper #2 capability-degrade warning — the end-to-end confirmation that
   the Python from plan 14-03 and the deployed function now agree.

Then write the `D-14-07-VERIFIED` record. If any check fails, do NOT proceed to plan 14-10: roll back
per the Task 2 record and re-open Task 1, because a half-deployed attribution contract is worse than
none. If check 3 is not performed, record Assumption A4 as still open in those words rather than
closing it.
  </how-to-verify>
  <resume-signal>Type "approved" once all four read-backs are recorded in 14-DECISIONS.md as `D-14-07-VERIFIED` with timestamps, or describe which check failed so the rollback in the Task 2 record is run instead</resume-signal>
  <verify>
    <human-check>Juan has run all four read-back checks and recorded the results with timestamps.</human-check>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('.planning/phases/14-foreman-helper-2/14-DECISIONS.md').read_text(encoding='utf-8'); sys.exit(0 if 'D-14-07-VERIFIED' in t else 1)"</automated>
    <fails_when>non-zero exit — the read-back record is absent, so the migration must be treated as unverified regardless of whether the SQL appeared to run</fails_when>
  </verify>
  <acceptance_criteria>
    - 14-DECISIONS.md contains a `D-14-07-VERIFIED` record with four dated observations
    - The record explicitly states that a Helper #2 freeze did not alter the other three role columns on the observed row
    - The record states whether the plan 14-03 degrade warning appeared in the following run
    - Assumption A4 from 14-RESEARCH.md is marked closed by observation, or the record says plainly that it remains open
  </acceptance_criteria>
  <done>The deployed attribution contract is confirmed by reading it back, and the one assumption the repository could never prove is either closed by observation or still openly flagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository SQL text → live Supabase production project | A protected-area schema and function change |
| deployed function → Python attribution reads and writes | The contract that decides who owns a billing row |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-09-01 | Tampering | lookup function replacement | critical | mitigate | Drop-then-create for BOTH functions, pinned by a contract test, applied in an owner-chosen window outside the cron schedule, with the rollback recorded before the change |
| T-14-09-02 | Repudiation | silent non-deploy | critical | mitigate | The failure produces no error, so the only defense is the Task 3 read-back; the plan refuses to report success without a recorded observation |
| T-14-09-03 | Tampering | a Helper #2 freeze clearing another role | critical | mitigate | The SQL states the per-role write requirement explicitly, and Task 3 observes a real row before and after to confirm the other three columns are unchanged |
| T-14-09-04 | Denial of Service | a scheduled run during the drop window | high | mitigate | The cron schedule is read from the workflow file and a quiet window is chosen at the Task 2 checkpoint |
| T-14-09-05 | Elevation of Privilege | applying production SQL from an agent session | high | mitigate | The plan authorizes authoring only; every apply step is owner-executed, and both checkpoints carry `gate="blocking-human"`, so neither is auto-resolved in auto-mode — a plain `gate="blocking"` would let an unattended run auto-select the first deployment option and auto-approve the read-back |
| T-14-09-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed; 14-RESEARCH.md section "Package Legitimacy Audit" records the audit as not applicable |
</threat_model>

## Artifacts this phase produces

- New file `billing_audit/helper2_attribution.sql` — the owner-deployed migration, in numbered steps
- New file `tests/test_helper2_attribution_sql_contract.py` — the SQL shape and name-equality contract test
- `billing_audit/schema.sql` — the corrected bulk-lookup operator comment and the extended documented contracts
- `.planning/phases/14-foreman-helper-2/14-DECISIONS.md` — records `D-14-07-APPLIED` and `D-14-07-VERIFIED`

## Edge Assumptions (spec-less probe fallback)

| Req | Probe category | Adopted answer (assumption) |
|---|---|---|
| HLP-06 | empty | A row with no Helper #2 attribution returns null for both new columns, and null is treated as no history, not as a claim. |
| HLP-06 | idempotency | Applying the migration twice is safe: the column additions use if-not-exists semantics and the function drops use if-exists, so a re-run is a no-op. |
| HLP-06 | adjacency | A row already frozen with primary or Helper #1 attribution gains Helper #2 values in its own columns without those existing values changing. |
| HLP-06 | ordering | Deployment order relative to the code merge is a stated choice; both orders are safe because the Python degrades against an un-migrated function and old code ignores new columns. |
| HLP-06 | concurrency | The function replacement is transactional; a concurrent run either sees the old definition or the new one, never a partial one — which is why the window matters rather than the ordering within it. |

## Assumption A4, closed here or stated open

14-RESEARCH.md Assumption A4 — that the freeze function's upsert semantics keep per-role columns
independent — cannot be proven from the repository, because the function body lives in Supabase.
Task 3 closes it by observation on a real row. If the observation is not performed, this plan does not
report the assumption as closed.

<verification>
- `python -m pytest tests/test_helper2_attribution_sql_contract.py tests/test_billing_audit_shadow.py -q` exits 0.
- `python -m pytest tests/ -q` exits 0.
- 14-DECISIONS.md carries both `D-14-07-APPLIED` and `D-14-07-VERIFIED`.
- `git diff` shows no change to the backfill function or the Phase 12 provenance columns.
</verification>

<success_criteria>
Helper #2 has its own frozen attribution role in production, deployed through a migration that cannot
silently fail, confirmed by reading it back, with the other three roles observably untouched.
</success_criteria>

<output>
Create `.planning/phases/14-foreman-helper-2/14-09-SUMMARY.md` when done. Label evidence
precisely: the contract test is FIXTURE PASS; the migration is OWNER-APPLIED LIVE STATE; the read-back
is OWNER-OBSERVED PRODUCTION. Do not merge these labels.
</output>