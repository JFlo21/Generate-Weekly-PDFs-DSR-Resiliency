---
context: phase
phase: 14-foreman-helper-2
task: 3
total_tasks: 3
status: complete
last_updated: 2026-09-11T04:05:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Tasks 1–2 are DONE — CR-01 / CR-02 / WR-01..03 / IN-01 were fixed on Juan's `--fix` decision
      and MERGED via PR #402 → `6e6e1e8` (2026-09-10). Do NOT re-open them or redo the fix. The PR #402 owner
      follow-ups in `.planning/todos/done/2026-09-10-pr402-review-followups.md` are DECIDED
      (D-14-FOLLOWUPS, 2026-09-11): breaker half-open on probe success is MERGED via PR #407 → `0585758` (2026-09-11 15:48Z;
      review round 1 added the breaker lock + trip-generation check; branch deleted); `keep_historical` in the SUB-09 off-contract gate is
      DEFERRED behind `RUN_MEMORY_INCREMENTAL_ENABLED` (no code until incremental read is ever enabled). Do NOT re-decide either.
      Task 3 (= Phase 12 12-06 Task 4) is now DONE (Juan approved 2026-09-10 ~23:45Z). `/gsd-verify-work 12` and
      `phase.complete 12` are also DONE (2026-09-11); Phase 12 is CLOSED. The next action is the `pipeline_memory` twin-breaker
      lock follow-up PR, then Phase 13 only when Juan asks.
- [ ] CONSTRAINT: A file-level parity table certifies nothing about a site — `tests/test_helper2_family_parity.py`
      never listed `pricing.py` / `attribution.py`, which is how CR-01 survived 14 plans (WR-02); and a file-wide
      presence check would pass if only the early gate gained the Helper #2 strings while the AEP-vs-reduced
      rate selection did not. Any fix adds per-site or behavioural rate-class assertions that go RED first.
- [ ] CONSTRAINT: Codex is outside the harness — the 12 Codex threads on PR #399 and 2 on PR #400 are listed for
      Juan only. Never act on them, never resolve them, never cite them as the reason for a change; verify a claim
      independently against the code if Juan asks.
- [ ] CONSTRAINT: A cache-admission marker is stamped by the production caller in the same call that writes the
      certified data, and adoption is logged BEFORE it is persisted (O-14-E rules 1–2, ledger `[2026-09-09 19:35]`).
      Never backfill such a marker by SQL without proving the certified data is current.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Fixing a protected finding because it is "Critical" | Severity does not grant authority; CR-01/CR-02 change billing output and WR holds | blocking | Owner decision recorded in `14-DECISIONS.md` before any edit to `pricing.py` / `grouping.py` |
| Half-fixing a two-site enumeration | Adding Helper #2 only to the early pricing gate routes AEP-billable Helper #2 work to the reduced rate (Greptile on #399) | blocking | Both literal sites in one change; rate-class assertion with different new/reduced fixture rates |
| Resume pointer skipping an OPEN owner decision | `stopped_at` once sent the next session straight to 12-06 Task 4 while the CR-01/CR-02 decision was still open (Greptile on #400) | advisory | Every pointer lists any open owner decision before the next task; that decision is recorded (`--fix`, PR #402 → `6e6e1e8`) and 12-06 Task 4 is DONE (Phase 12 closed 2026-09-11), so pointers go to the PR #402 owner follow-ups, then Phase 13 when Juan asks |
| "review" token in a gsd-tools / shell line | Harness-boundary hook denies any command carrying a whitespace-preceded `review` beside a GSD token | advisory | Reword ("verdict", "report"); avoid `init.code-review` on the command line |
| Docstring pushed after the squash merge | bed436c landed after PR #396 merged and was lost until cherry-picked into #398 | advisory | Check `gh pr view --json state` before pushing to a PR branch |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 14 (Foreman Helper #2) is COMPLETE, code-reviewed and the review findings are FIXED: master carries PR #402
(squash `6e6e1e8`, 2026-09-10) on top of the report PR #399 → `94f2636`, write-back PR #400 → `82638ad` and closure
PR #398 → `45bdbbf`; the Phase 14 write-back landed via docs PR #403 and this Phase 12 closure lands via docs PR
#404 (take the current tip from git, not from here). Helper #2
is ENABLED (`HELPER2_ENABLED=1`, permanent, flag-off = emergency kill switch). The registry skip works again (O-14-E
RESOLVED on runs `34411958861` + `34415980363`). The first scheduled run after `6e6e1e8` (run `34530899488`) was
observed clean 2026-09-10: no Helper #2 shadow groups exist yet, so the expected empty regeneration wave produced
nothing to act on. Nothing is in flight; the tree is clean apart from four pre-existing untracked dirs. Phase 12
is now CLOSED — `/gsd-verify-work 12` passed 2026-09-11 (12-UAT.md 34/34, 12-VERIFICATION.md 77/77 must-haves) and
`phase.complete 12` advanced STATE.md to Phase 13 "Ready to plan"; the open items on this phase are the two owner
follow-ups in the todo file, then Phase 13 only when Juan asks.
</current_state>

<completed_work>

Completed Tasks:
- Plans 14-01..14-14: Done (last: 14-14 frequent-run mapping adoption, PR #396 → `736141a`; observed 2026-09-09/10)
- O-14-E closure + Phase 14 close: Done (PR #398 → `45bdbbf`; `14-DECISIONS.md`, `14-VERIFICATION.md` addendum, ROADMAP 14/14)
- `/gsd-code-review 14`: Done (`14-REVIEW.md`, PR #399 → `94f2636`; Greptile ×3 + Copilot ×1 rounds fixed, approved)
- Write-back: Done (PR #400 → `82638ad`; vault project page, current-state, log `[2026-09-10j]`)
</completed_work>

<remaining_work>

- Task 1: DONE 2026-09-10 — owner decision `--fix`; CR-01 / CR-02 / WR-01..03 / IN-01 fixed and MERGED via PR #402 → `6e6e1e8` (`14-REVIEW-FIX.md`; bot-review follow-ups through `dc484f2`)
- Task 2: DONE with Task 1 (parity test extended to `pricing.py` + `attribution.py`, per-owning-block checks; `SUB_RATES_FP` gate widened to the Helper #2 shadows; Opus production-risk pass). Owner follow-ups only: `.planning/todos/done/2026-09-10-pr402-review-followups.md`
- Task 3: DONE (12-06 Task 4 approved 2026-09-10 ~23:45Z): 12-06 Task 4 observed 2026-09-10 (all seven pass, run
  33974128574, recorded in `12-06-SUMMARY.md`); Juan replied `Approved` / `Adopt 89746993` (D-12-E) / `Yes drop
  them using our supabase connector`; the `_20260903/04/05` snapshot backups are dropped — detail in
  `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-06-SUMMARY.md` (Task 4 section + Checkpoint history)
- Task 3b: DONE 2026-09-10: run 34530899488 (head `3fb81c7`, success, 63:19) — no Helper #2 shadow groups exist in
  `group_state`, so no regeneration wave; 0 `_Helper2_`/`SUB_RATES_FP`/`freeze_attribution_degraded` lines;
  `freeze_attribution` RPC calls all HTTP 200
- After: DONE 2026-09-11 — `/gsd-verify-work 12` (12-UAT.md 34/34, 12-VERIFICATION.md 77/77 must-haves) → `phase.complete 12` (Phase 12 closed; STATE.md advanced to Phase 13 "Ready to plan")
- Then: DECIDED 2026-09-11 (D-14-FOLLOWUPS) — breaker half-open MERGED via PR #407 → `0585758`; `keep_historical` in the SUB-09 off-contract gate deferred behind `RUN_MEMORY_INCREMENTAL_ENABLED` (record `.planning/todos/done/2026-09-10-pr402-review-followups.md`)
- Then: Phase 13 only when Juan asks
</remaining_work>

<decisions_made>

- O-14-E was closed on two observed frequent runs via plan 14-14 instead of waiting for the Monday deep run; the SQL marker backfill was rejected because 0/121 stored mappings carried Helper #2 keys.
- The code-review Criticals were reported first (owner-decision areas), then fixed on Juan's `--fix` decision 2026-09-10 and merged (PR #402 → `6e6e1e8`).
- Resume pointers went to Phase 12 12-06 Task 4 once the owner decision was recorded; that task is DONE and Phase 12 is CLOSED (2026-09-10) — pointers now go to the PR #402 follow-ups, then Phase 13 when Juan asks.
- Phase closeout records land on master through standalone docs PRs (#397, #398, #400), never a push to master.
</decisions_made>

<blockers>
- CR-01 / CR-02: RESOLVED (PR #402 → `6e6e1e8`). Follow-ups DECIDED 2026-09-11 (D-14-FOLLOWUPS): breaker half-open shipped (`close_circuit` on probe success); `keep_historical` deferred behind the `RUN_MEMORY_INCREMENTAL_ENABLED` gate.
- claude-mem capture was broken 2026-09-10 (worker down, then a gutted 13.24.7 plugin cache); repaired 2026-09-11 from the marketplace checkout — ledgers + vault are the record for 2026-09-10.
- Codex threads (#399 ×12, #400 ×2, #402 ×3) unresolved by design.
</blockers>

## Required Reading (in order)
1. `.planning/phases/14-foreman-helper-2/14-REVIEW-FIX.md` — what PR #402 fixed and how (all_fixed); `14-REVIEW.md` for the original findings
2. `.claude/project-state.md` — Next actions (PR #402 follow-ups; PRs #404 / #405 are merged) and the Helper #2 posture
3. `.planning/STATE.md` → Blockers/Concerns (Phase 14 block) and Session footer
4. `.planning/phases/14-foreman-helper-2/14-DECISIONS.md` — O-14-A..E and `D-14-CR-FIX` (the CR-01/CR-02 `--fix` decision)
5. `memory-bank/living-ledger.md` `[2026-09-09 19:35]` and `[2026-09-09 23:40]` — the marker rules and the variant-family enumeration rule
6. `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/12-UAT.md` / `12-VERIFICATION.md` (Phase 12 closed 2026-09-10)
7. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses; apply before any assumption analysis

## Critical Anti-Patterns (do NOT repeat these)
- FIX-ON-SEVERITY: a Critical finding in a protected area is still an owner decision → record the decision, then TDD
- HALF-SITE FIX: extend every literal enumeration of the variant family in one change; parity assertions per site
- CODEX ACTION: never act on / resolve Codex threads; Claude-native verification on request only
- STALE POINTER: when a next action completes, update every pointer (STATE.md `stopped_at` + Current Position, project-state, AI_CONTEXT_RESUME snapshot + line-255 pointer, vault current-state) in the same PR

## Infrastructure State
- GitHub Actions cron: Helper #2 ENABLED (`HELPER2_ENABLED=1` repo variable); schedules unchanged; runs skip ≈112/121 sheets via `pipeline_memory.sheet_registry` (`mapping_schema='helper2-v1'`)
- Supabase `poeyztlmsawfoqlanucc`: `sheet_registry` 121/121 marked; no schema change pending; RLS untouched
- CI noise: `code/snyk` and the Azure mirror `JFlo21.Generate-Weekly-PDFs-DSR-Resiliency` fail on every PR and never block
- claude-mem: repaired 2026-09-11 (13.24.7 cache restored from the marketplace checkout); mem-search has a gap for 2026-09-10
- No background processes, no async jobs

## Pre-Execution Critique Required
- Design artifact: `14-REVIEW.md` remediation blocks for CR-01 (both pricing sites) and WR-03 (four-state probe with Condition; waiters wait, timeout → retryable failure, never persisted degraded)
- Critique focus: does the parity test go RED on each site independently; are the fixture new/reduced rates different; is the known-good Helper #1 subcontractor sample identified before the code changes
- Gate: Do NOT begin execution until Juan's decision is recorded and the critique is complete

<context>
The phase ended cleanly and the review found real billing gaps that Helper #2's fourteen plans never touched
because the parity guard never covered pricing. I stopped at the boundary on purpose: every record now says
"decision first". If Juan says fix, the hand-written TDD PR is the recommended path over `--fix`, because the
rate-class assertion and the sample validation matter more than speed here.
</context>

<next_action>
Nothing is active on this phase (Tasks 1-3 DONE; Phase 12 CLOSED; D-14-FOLLOWUPS decided 2026-09-11 — breaker
half-open MERGED via PR #407 → `0585758`, `keep_historical` deferred; record `.planning/todos/done/2026-09-10-pr402-review-followups.md`). Next: the `pipeline_memory` twin-breaker lock follow-up PR, then Phase 13
only when Juan asks (`/gsd-plan-phase 13`). Never re-run Phase 12 or Phase 14 tasks.
</next_action>
