---
context: phase
phase: 14-foreman-helper-2
task: 1
total_tasks: 3
status: paused
last_updated: 2026-09-10T05:31:44.744Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Protected areas need an owner decision, not a fix — `14-REVIEW.md` CR-01 (`pipeline/pricing.py`
      rate-matrix variant tuples) and CR-02 (`pipeline/grouping.py` `EXCLUDE_WRS` / `WR_FILTER` matchers) are a
      billing formula and the WR hold controls. Ask Juan first; record the answer in `14-DECISIONS.md`; only then
      write code, TDD, both pricing sites in the same change, validated against a known-good Helper #1
      subcontractor sample, with an Opus `production-risk-reviewer` pass before the PR opens.
- [ ] CONSTRAINT: A file-level parity table certifies nothing about a site — `tests/test_helper2_family_parity.py`
      never listed `pricing.py` / `attribution.py`, which is how CR-01 survived 14 plans (WR-02); and a file-wide
      presence check would pass if only the early gate gained the Helper #2 strings while the AEP-vs-reduced
      rate selection did not. Any fix adds per-site or behavioural rate-class assertions that go RED first.
- [ ] CONSTRAINT: Codex is outside the harness — the 12 Codex threads on PR #399 and 2 on PR #400 are listed for
      Juan only. Never act on them, never resolve them, never cite them as the reason for a change; verify a claim
      independently against the code if Juan asks.
- [ ] CONSTRAINT: A cache-admission marker is stamped by the production caller in the same call that writes the
      certified data, and adoption is logged BEFORE it is persisted (O-14-E rules 1–2, ledger `[2026-09-09 19:35]`).
      Never backfill such a marker by SQL without proving the certified data is current.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Fixing a protected finding because it is "Critical" | Severity does not grant authority; CR-01/CR-02 change billing output and WR holds | blocking | Owner decision recorded in `14-DECISIONS.md` before any edit to `pricing.py` / `grouping.py` |
| Half-fixing a two-site enumeration | Adding Helper #2 only to the early pricing gate routes AEP-billable Helper #2 work to the reduced rate (Greptile on #399) | blocking | Both literal sites in one change; rate-class assertion with different new/reduced fixture rates |
| Resume pointer skipping the owner decision | `stopped_at` sent the next session straight to 12-06 Task 4 (Greptile on #400) | advisory | Every pointer (STATE.md, project-state, AI_CONTEXT_RESUME, this file) lists the decision first |
| "review" token in a gsd-tools / shell line | Harness-boundary hook denies any command carrying a whitespace-preceded `review` beside a GSD token | advisory | Reword ("verdict", "report"); avoid `init.code-review` on the command line |
| Docstring pushed after the squash merge | bed436c landed after PR #396 merged and was lost until cherry-picked into #398 | advisory | Check `gh pr view --json state` before pushing to a PR branch |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 14 (Foreman Helper #2) is COMPLETE and code-reviewed. Master is at `d08420f` (PR #400 → `82638ad`, the
write-back for the code-review report PR #399 → `94f2636`, on top of closure PR #398 → `45bdbbf`). Helper #2 is
ENABLED (`HELPER2_ENABLED=1`, permanent, flag-off = emergency kill switch). The registry skip works again (O-14-E
RESOLVED on runs `34411958861` + `34415980363`). Nothing is in flight; the tree is clean apart from four
pre-existing untracked dirs. The only open item on this phase is Juan's decision on the two Critical findings.
</current_state>

<completed_work>

Completed Tasks:
- Plans 14-01..14-14: Done (last: 14-14 frequent-run mapping adoption, PR #396 → `736141a`; observed 2026-09-09/10)
- O-14-E closure + Phase 14 close: Done (PR #398 → `45bdbbf`; `14-DECISIONS.md`, `14-VERIFICATION.md` addendum, ROADMAP 14/14)
- `/gsd-code-review 14`: Done (`14-REVIEW.md`, PR #399 → `94f2636`; Greptile ×3 + Copilot ×1 rounds fixed, approved)
- Write-back: Done (PR #400 → `82638ad`; vault project page, current-state, log `[2026-09-10j]`)
</completed_work>

<remaining_work>

- Task 1: Juan's decision on CR-01 / CR-02 (fix via `/gsd-code-review 14 --fix`, hand-written TDD PR, or accept as-is) — record in `14-DECISIONS.md`
- Task 2 (only if fixing): parity test RED first (add `pricing.py` + `attribution.py`, per-site / rate-class assertions), both pricing sites, `_HELPER2_` matcher shapes, WR-01/WR-02/WR-03/IN-01, sample validation, Opus production-risk pass, PR
- Task 3: Phase 12 12-06 Task 4 (post-apply scheduled-run check) then drop the `_20260903/04/05` snapshot backups — detail in `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/.continue-here.md`
- After: `/gsd-verify-work 12` → phase.complete 12; Phase 13 only when Juan asks
</remaining_work>

<decisions_made>

- O-14-E was closed on two observed frequent runs via plan 14-14 instead of waiting for the Monday deep run; the SQL marker backfill was rejected because 0/121 stored mappings carried Helper #2 keys.
- The code-review Criticals were reported, not fixed: billing formula and WR hold controls are owner-decision areas.
- Every resume pointer lists the owner decision before 12-06 Task 4 (Greptile on #400).
- Phase closeout records land on master through standalone docs PRs (#397, #398, #400), never a push to master.
</decisions_made>

<blockers>
- CR-01 / CR-02 owner decision: pending Juan. Until fixed, `*_Helper2_*` subcontractor workbook totals are unverified and `EXCLUDE_WRS` does not hold Helper #2 files.
- claude-mem worker DOWN during the closing session (port 37777 refused): nothing after ~04:30Z 2026-09-10 was captured; ledgers + vault are the record.
- 14 Codex threads (#399 ×12, #400 ×2) unresolved by design.
</blockers>

## Required Reading (in order)
1. `.planning/phases/14-foreman-helper-2/14-REVIEW.md` — Critical Issues CR-01 / CR-02 with the two-site remediation and the WR-03 four-state probe design
2. `.claude/project-state.md` — Next actions (decision first) and the Helper #2 posture
3. `.planning/STATE.md` → Blockers/Concerns (Phase 14 block) and Session footer
4. `.planning/phases/14-foreman-helper-2/14-DECISIONS.md` — O-14-A..E; where the CR-01/CR-02 decision gets recorded
5. `memory-bank/living-ledger.md` `[2026-09-09 19:35]` and `[2026-09-09 23:40]` — the marker rules and the variant-family enumeration rule
6. `.planning/phases/12-ownership-last-known-foreman-as-of-the-week/.continue-here.md` — before touching 12-06 Task 4
7. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses; apply before any assumption analysis

## Critical Anti-Patterns (do NOT repeat these)
- FIX-ON-SEVERITY: a Critical finding in a protected area is still an owner decision → record the decision, then TDD
- HALF-SITE FIX: extend every literal enumeration of the variant family in one change; parity assertions per site
- CODEX ACTION: never act on / resolve Codex threads; Claude-native verification on request only
- STALE POINTER: when a next action completes, update every pointer (STATE.md `stopped_at` + Current Position, project-state, AI_CONTEXT_RESUME snapshot + line-255 pointer, vault current-state) in the same PR

## Infrastructure State
- GitHub Actions cron: Helper #2 ENABLED (`HELPER2_ENABLED=1` repo variable); schedules unchanged; runs skip ≈112/121 sheets via `pipeline_memory.sheet_registry` (`mapping_schema='helper2-v1'`)
- Supabase `poeyztlmsawfoqlanucc`: `sheet_registry` 121/121 marked; no schema change pending; RLS untouched
- CI noise: `code/snyk` and the Azure mirror `JFlo21.Generate-Weekly-PDFs-DSR-Resiliency` fail on every PR and never block
- claude-mem worker: DOWN at session end (repair before relying on mem-search for 2026-09-10)
- No background processes, no async jobs

## Pre-Execution Critique Required
- Design artifact: `14-REVIEW.md` remediation blocks for CR-01 (both pricing sites) and WR-03 (four-state probe with Condition; waiters wait, timeout → retryable failure, never persisted degraded)
- Critique focus: does the parity test go RED on each site independently; are the fixture new/reduced rates different; is the known-good Helper #1 subcontractor sample identified before the code changes
- Gate: Do NOT begin execution until Juan's decision is recorded and the critique is complete

<context>
The phase ended cleanly and the review found real billing gaps that Helper #2's fourteen plans never touched
because the parity guard never covered pricing. I stopped at the boundary on purpose: every record now says
"decision first". If Juan says fix, the hand-written TDD PR is the recommended path over `--fix`, because the
rate-class assertion and the sample validation matter more than speed here.
</context>

<next_action>
Start with: read `14-REVIEW.md` Critical Issues, then ask Juan for the CR-01/CR-02 decision and record it in `14-DECISIONS.md` before any other Phase 12/13 work.
</next_action>
