---
phase: 14-foreman-helper-2
plan: 14
type: execute
wave: 9
depends_on: ["14-13"]
gap_closure: true
files_modified:
  - pipeline/orchestrate.py
  - tests/test_frequent_run_mapping_adoption.py
  - .planning/phases/14-foreman-helper-2/14-DECISIONS.md
autonomous: false
requirements: [HLP-06]

estimate:
  tokens: 20000
  raw_tokens: 8000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "A frequent run writes the freshly validated column_mapping (and therefore the helper2-v1 marker) for every sheet it fully validated this run, not only for brand-new sheets."
    - "A sheet admitted from the discovery skip index keeps echoing its stored mapping and is never re-stamped (14-07 / 14-13 contracts preserved)."
    - "Adopting a fresh mapping on a frequent run is never silent: the same drift log + Sentry breadcrumb the deep run emits fires for each adopted sheet whose mapping changed."
    - "The default call shape of _compute_registry_mapping_sheets is unchanged (existing Phase 11 tests pass untouched)."
  artifacts:
    - tests/test_frequent_run_mapping_adoption.py
  key_links:
    - "Evidence 2026-09-09: 0 of 121 sheet_registry.column_mapping values carry any Helper #2 key (last deep run 34086148733 ran 6696a46, pre-Helper-#2), so a marker-only SQL backfill would admit every sheet from cache WITHOUT Helper #2 columns and silently disable detection on 114 sheets. Refusing that path is what forces this plan."
    - "pipeline/orchestrate.py::_compute_registry_marker_sheets (14-13) already stamps marker = not skip-admitted AND mapping written this call; widening the written set is the whole change."
  prohibitions:
    - statement: "Never stamp the marker on an echoed stored mapping; never backfill mapping_schema by SQL while stored mappings lack the Helper #2 keys."
      status: active
      verification: unverified
    - statement: "Never change the deep-run behaviour (column_mapping_sheets None) or the skip-index admission conditions."
      status: active
      verification: unverified
    - statement: "No schema, RPC, workflow, or flag change."
      status: active
      verification: unverified
---

<objective>
Clear O-14-E without waiting for the Monday deep run: a frequent run that fully validates a registered
sheet adopts that freshly validated mapping (drift logged) and so earns the marker on the same run. The
next scheduled run after merge stamps all 121 sheets; the run after that is admitted from cache.

Owner decision (Juan, 2026-09-09, chat): "the 121 sheets do run and I cannot wait for that deep run so I
need this to be cleared another way" — chose the code change over the unsafe SQL backfill.
</objective>

<context>
@.planning/phases/14-foreman-helper-2/14-DECISIONS.md — `O-14-E`, `D-14-10-APPLIED`.
@.planning/phases/14-foreman-helper-2/14-13-PLAN.md — the marker helper this plan feeds.

Phase 11 D-03 said a frequent run must never *silently* adopt a drifted mapping. A sheet that took a full
validation this run is not silent adoption: the mapping was just validated against live columns and is
the mapping this very run processed with; the deep run's drift log now fires for it too. The stored
mapping was, in practice, staler than the one in use (Helper #2 keys missing) — echoing it was the risk.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: frequent runs adopt fully-validated mappings, drift logged</name>
  <files>pipeline/orchestrate.py, tests/test_frequent_run_mapping_adoption.py</files>
  <read_first>
    - pipeline/orchestrate.py `_compute_registry_mapping_sheets`, `_log_column_mapping_drift`, and the pass-1 / pass-2 registry blocks
    - tests/test_deep_run_reconciliation.py `ColumnMappingRefreshTests` (arity + fixtures)
    - tests/test_mapping_schema_marker_caller.py (writer-payload idiom)
  </read_first>
  <reversibility rating="reversible">Additive kwarg with a backward-compatible default; a registry row can always be re-validated.</reversibility>
  <action>
RED: `tests/test_frequent_run_mapping_adoption.py` — helper includes a registered sheet present in
`fully_validated_sids`, excludes a skip-admitted one, keeps the old new-sheets-only behaviour when the
kwarg is omitted, still returns None on a deep run; `_log_column_mapping_drift(..., label=...)` prefixes
the warning with the label; `orch.main` source pin: `fully_validated_sids=_registry_fully_validated_sids`
is passed and the drift logger is called on the frequent path; writer integration: a fully-validated
registered sheet's payload carries the FRESH mapping plus the marker, a skip-admitted sheet echoes the
stored mapping with no marker.

GREEN: add `fully_validated_sids: set[int] | None = None` to `_compute_registry_mapping_sheets` (union
into the frequent-run set); add `label: str = "Deep-run"` to `_log_column_mapping_drift`; in `main`
read the skip sids once, derive `_registry_fully_validated_sids`, pass it, and on pass 2 log drift for
the fully-validated subset on frequent runs with label "Frequent-run full-validation".
  </action>
  <verify>PYTHONUTF8=1 python -m pytest tests/test_frequent_run_mapping_adoption.py tests/test_mapping_schema_marker_caller.py tests/test_deep_run_reconciliation.py tests/test_incremental_read.py -q; then `pytest tests/ -q` and `bash scripts/run_6_gates.sh`.</verify>
  <done>New tests green, full suite green, six gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: observe and close O-14-E</name>
  <files>.planning/phases/14-foreman-helper-2/14-DECISIONS.md</files>
  <action>After the first scheduled run on the merge: `sheet_registry.mapping_schema = 'helper2-v1'` on all 121 rows and every `column_mapping` carries the Helper #2 keys; the following run logs `skipped via sheet_registry` ≈ 121 and the Helper #2 counters are unchanged (114 capable / 7 unavailable). Then mark O-14-E RESOLVED.</action>
  <verify>Supabase read + run log grep.</verify>
  <done>O-14-E closed with the observed counts.</done>
</task>

</tasks>

<threat_model>
- A frequent run adopting a wrong mapping: impossible without a full validation of that sheet this run, and drift is logged + breadcrumbed. The mapping adopted is the one the run already processed with.
- Marker on a mapping lacking Helper #2 keys: only if the live sheet lacks them (then it is correct); a later column addition bumps the sheet version and forces re-validation.
</threat_model>
