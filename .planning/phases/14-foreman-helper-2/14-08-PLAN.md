---
phase: 14-foreman-helper-2
plan: 08
type: execute
wave: 4
depends_on: ["14-06", "14-07"]
files_modified:
  - pipeline/grouping.py
  - pipeline/orchestrate.py
  - tests/golden/run_summary_baseline.json
  - tests/test_foreman_helper_2.py
  - .planning/phases/14-foreman-helper-2/14-DECISIONS.md
autonomous: false
requirements: [HLP-03, HLP-05]

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A source row with BOTH helper completion checkboxes checked, both names real, and both depts present is handled by the rule Juan chooses at the Task 1 checkpoint — and by no rule at all until he chooses."
    - "Whatever the chosen rule, one such row never stops the run; the 2026-07 prototype behavior of aborting before workbook generation is rejected."
    - "A conflicted row is counted and logged with its own distinct reason, so an operator can find and correct it rather than learning of it from a billing dispute."
    - "The run summary distinguishes four states: excluded source, eligible source without Helper #2 capability, eligible source with capability but no qualifying completion, and rows held for a helper conflict."
    - "The run_summary.json key set and tests/golden/run_summary_baseline.json stay equal, so Gate 6 passes."
  artifacts:
    - pipeline/grouping.py
    - pipeline/orchestrate.py
    - tests/golden/run_summary_baseline.json
    - .planning/phases/14-foreman-helper-2/14-DECISIONS.md
  key_links:
    - "The reason strings from plans 14-01 and 14-07 feed the run_summary counters, which feed tests/golden/run_summary_baseline.json, which Gate 6 compares for exact key-set equality."
    - "The O-14-A decision is applied at the single point where both slots would otherwise emit keys, and at the matching shadow site, so the rule lives in one place rather than scattered."
  prohibitions:
    - statement: "Never invent a conflict rule — do not choose a winner, do not split the row, do not duplicate the charge, and do not deduplicate silently, until Juan records the decision."
      status: active
      verification: unverified
    - statement: "Never abort the run because of a single conflicted row; the run continues and the row is surfaced."
      status: active
      verification: unverified
    - statement: "Never resolve a conflict by emitting the same physical unit into two customer-facing files."
      status: active
      verification: unverified
    - statement: "Never add a run_summary key without updating tests/golden/run_summary_baseline.json in the same commit."
      status: active
      verification: unverified
---

<objective>
Handle the one case Phase 14 cannot decide for itself — a source row where both helper completion
checkboxes are checked with real names and depts — and make the whole Helper #2 path legible in the
run summary.

Purpose: O-14-A is a business decision about money. Three separate flows collide on that row —
internal production credit, subcontractor payment, and customer billing — and the unit must appear in
exactly one customer-facing file. Four options exist, one is recommended, and none may be adopted by
an agent. Everything else in this phase ships regardless; only this rule waits.

Output: the recorded owner decision, an implementation of exactly that decision, and run-summary
counters that make the Helper #2 conditions visible to whoever reads a run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-foreman-helper-2/14-CONTEXT.md
@.planning/phases/14-foreman-helper-2/14-RESEARCH.md
@.planning/phases/14-foreman-helper-2/14-DECISIONS.md
@.planning/phases/14-foreman-helper-2/14-06-SUMMARY.md
@.planning/phases/14-foreman-helper-2/14-07-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: O-14-A — Juan decides what happens when both helper slots are valid on one row</name>
  <files>.planning/phases/14-foreman-helper-2/14-DECISIONS.md</files>
  <read_first>
    - .planning/phases/14-foreman-helper-2/14-CONTEXT.md — the O-14-A entry in full, including the three money flows and the four options
    - .planning/phases/14-foreman-helper-2/14-RESEARCH.md Open Questions item 1
    - pipeline/grouping.py lines 605-750 and 940-1200 — the two emission sites where a conflicted row would otherwise produce keys for both slots
    - memory-bank/living-ledger.md — grep the 2026-05-19 header only, for the duplicate-billing precedent
  </read_first>
  <decision>O-14-A: what happens to a source row where BOTH helper completion checkboxes are checked, both names are real, and both depts are present — one physical unit of work with two people claiming completion.</decision>
  <context>
Stop and put this to Juan. Do not proceed past this task without a recorded answer, and never adopt
the recommended option unilaterally — that is why this checkpoint carries `gate="blocking-human"`.
O-14-A is a business decision about money and is deliberately still OPEN by owner instruction; an
auto-selected first option would be an agent inventing a billing rule.

Frame it the way CONTEXT.md frames it: three affected flows are not the same question. Internal
production credit decides whose file shows the line item. Subcontractor payment flows through the
reduced-sub and AEP-billable shadow files. Customer billing requires the unit to appear in exactly
one customer-facing file.

The recommendation is option `hold`, and its cost is stated plainly below rather than hidden: the
unit is not billed at all this run, so a conflicted row nobody corrects is revenue delayed until
someone corrects it.

One thing is decided regardless of which option he picks: the 2026-07 prototype raised an exception
and aborted before workbook generation, and that behavior is rejected. One bad row must never stop a
production billing run.

Two follow-ups to ask while he is deciding — both affect operator documentation only and neither
blocks anything. First, whether the same person appearing in slot 1 on some rows and slot 2 on others
within one work request and week should keep producing two files, one per slot, as D-14-06 accepts.
Second, whether the conflict signal should reach Sentry or only the run summary.
  </context>
  <options>
    <option id="hold">
      <name>Hold the row out of every file this run, with visibility (recommended)</name>
      <pros>Nothing is billed wrongly. The row is logged once with a distinct reason, counted in the run summary, and surfaced so a human can correct the source row. Invents no priority rule.</pros>
      <cons>The unit is not billed at all this run — a conflicted row nobody corrects is revenue delayed until someone does.</cons>
    </option>
    <option id="helper1-wins">
      <name>Helper #1 wins; the Helper #2 claim is dropped for that row</name>
      <pros>Simple, deterministic, the unit is billed once and on time.</pros>
      <cons>Invents a slot-priority rule nobody has stated, and silently drops a real person's claim to credit with no signal that it happened.</cons>
    </option>
    <option id="both-files">
      <name>Emit the row into both helper files</name>
      <pros>Neither person loses credit.</pros>
      <cons>Double credit — the same physical unit reaches two customer-facing files, which violates exactly-once billing. The 2026-05-19 duplicate-billing precedent in the living ledger is what this repository already paid for that shape.</cons>
    </option>
    <option id="primary-keeps">
      <name>The primary foreman keeps the unit; both helper claims are dropped</name>
      <pros>The unit is billed once, on time, by the person who owns the work request.</pros>
      <cons>Drops the helper credit entirely, which is the exact attribution this phase exists to capture.</cons>
    </option>
  </options>
  <resume-signal>Select: hold, helper1-wins, both-files, or primary-keeps — and answer the two follow-ups (per-slot file duplication; Sentry versus run-summary visibility). Record the choice in 14-DECISIONS.md as an `O-14-A ... RESOLVED` record with a date and one sentence of rationale in Juan's own terms. Task 2 cannot start until that record exists.</resume-signal>
  <verify>
    <human-check>Juan has chosen one of the four options and the choice is recorded with a date and a rationale.</human-check>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('.planning/phases/14-foreman-helper-2/14-DECISIONS.md').read_text(encoding='utf-8'); sys.exit(0 if ('O-14-A' in t and 'RESOLVED' in t) else 1)"</automated>
    <fails_when>non-zero exit — the resolution record is absent, which means Task 2 is still blocked and must not be started</fails_when>
  </verify>
  <acceptance_criteria>
    - 14-DECISIONS.md contains a resolution record for O-14-A naming exactly one of the four options, with a date and a one-sentence rationale
    - The record answers the two non-blocking follow-ups about per-slot file duplication and Sentry visibility
    - No conflict-handling code was written before this record existed
  </acceptance_criteria>
  <done>The business rule for a doubly-claimed row is stated by its owner and written down, so the implementation has one source rather than an inference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exactly the decided conflict rule — BLOCKED on Task 1</name>
  <files>pipeline/grouping.py, tests/test_foreman_helper_2.py</files>
  <read_first>
    - .planning/phases/14-foreman-helper-2/14-DECISIONS.md — the O-14-A record; if it is absent, stop and return to Task 1
    - pipeline/grouping.py lines 605-750 — the plain-leg emission for both slots after plans 14-01 and 14-06
    - pipeline/grouping.py lines 940-1200 — the shadow-leg emission for both slots after plan 14-06
    - pipeline/grouping.py lines 355-395 — the pre-pass exclusions, which must agree with whatever the rule decides
    - pipeline/observability.py — the reason-code and redaction helpers named by plan 14-02's A3 finding
    - tests/test_subcontractor_helper_shadow_rescue.py — plan 14-06's placeholder test asserting this phase took no position, which this task replaces
  </read_first>
  <behavior>
    - A row with both slots valid is handled by the recorded option and by nothing else.
    - If the recorded option is hold: the row produces no group, no workbook, and no attachment for either slot this run; it is logged once with the conflict reason and counted; the run continues and every other row is unaffected.
    - If the recorded option is a winner rule: the winning slot emits exactly one key, the losing slot emits none, and the unit appears in exactly one customer-facing file.
    - The pre-pass exclusions agree with the emission decision, so a conflicted row is not partially processed by one stage and skipped by another.
    - A row with only one valid slot is completely unaffected, in both the plain and the shadow legs.
    - The subcontractor variant of a conflicted row follows the same recorded rule, applied at the shadow emission site.
    - No conflicted row raises, and no conflicted row stops workbook generation for any other group.
  </behavior>
  <action>
Read the O-14-A record first. If it is not present, stop — this task is blocked and must not be
started on an inferred rule.

Implement the recorded option at the single point where both slots would otherwise emit keys, so the
rule lives in one place rather than being scattered across the module. Apply the same rule at the
shadow emission site, so a subcontractor conflicted row is not handled differently by accident. Make
the pre-pass exclusions agree with the emission decision.

Whatever the option, three things hold. The run continues — no exception, no abort, and no early
return that skips other groups. The row is logged once with the conflict reason string locked in plan
14-01, routed through the PII-safe logging helper. And the decision is traceable from the code with a
comment naming the O-14-A record, so the next reader finds the authority instead of re-deriving it.

Replace plan 14-06's placeholder test with tests covering the behaviors above for the recorded
option, including the subcontractor conflicted row and the same-person-in-both-slots row, which
collapses into this same case.
  </action>
  <verify>
    <automated>python -m pytest tests/test_foreman_helper_2.py -q -k "conflict or both_slots"</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>python -m pytest tests/test_subcontractor_helper_shadow_rescue.py -q</automated>
    <fails_when>non-zero exit, or any "failed"/"error" count above 0 in the summary line</fails_when>
    <automated>python -m py_compile pipeline/grouping.py</automated>
    <fails_when>non-zero exit, or a SyntaxError traceback in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `pipeline/grouping.py` contains the conflict handling at one plain-leg site and one shadow site, each with a comment naming the O-14-A record
    - A test asserts a both-slots-valid row is handled by the recorded option, and a second test asserts the run continues and other groups still generate
    - A test asserts a subcontractor both-slots-valid row follows the same rule
    - Plan 14-06's placeholder no-position test is removed rather than left contradicting the new behavior
    - `python -m pytest tests/test_subcontractor_helper_shadow_rescue.py -q` exits 0
  </acceptance_criteria>
  <done>Exactly one recorded rule governs a doubly-claimed row, applied identically to primary and subcontractor rows, and one bad row can never stop a billing run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the Helper #2 conditions visible in the run summary</name>
  <files>pipeline/orchestrate.py, tests/golden/run_summary_baseline.json, tests/test_foreman_helper_2.py</files>
  <read_first>
    - pipeline/orchestrate.py lines 5080-5225 — the run_summary assembly, the frozen key-set comment, and the billing_audit counter merge
    - scripts/check_run_summary_structure.py — the whole file; the comparison is key-set equality, not a superset check
    - tests/golden/run_summary_baseline.json — the current frozen key set, including the counter added by plan 14-03
    - pipeline/fetch.py lines 545-600 — the capability reasons emitted by plans 14-01 and 14-07
    - pipeline/grouping.py — the conflict reason emitted by Task 2
  </read_first>
  <behavior>
    - The run summary reports how many sources were eligible but lacked Helper #2 capability.
    - The run summary reports how many sources had the capability but produced no qualifying Helper #2 completion.
    - The run summary reports how many rows were held or reassigned by the O-14-A rule.
    - The run summary reports how many Helper #2 groups were generated.
    - Every one of those keys is present on every run, including a run with the feature flag off, with a zero value — so the key set never varies.
    - The golden baseline and the emitted summary have identical key sets.
  </behavior>
  <action>
Add the Helper #2 counters to the run summary so the four conditions D-14-02 requires to stay
distinguishable are visible without reading a log file, and so the pilot in plan 14-10 has something
to compare.

Gate 6 compares key sets for exact equality, so every new key must be present on every run and must
be added to `tests/golden/run_summary_baseline.json` in the same commit with a zero-valued integer.
Pre-seed the counters the way the existing counters are pre-seeded, so a flag-off run emits the same
key set as a flag-on run. A key that appears only when Helper #2 data exists would fail Gate 6 on
every quiet run, which is worse than no counter at all.

Add a test that builds a run summary for a flag-off run and asserts its key set equals the golden
baseline's key set — the same comparison Gate 6 makes, but reachable from the unit suite so a
mismatch is caught before the gate.
  </action>
  <verify>
    <automated>python -m pytest tests/test_foreman_helper_2.py -q -k "run_summary or counter"</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>TEST_MODE=true python generate_weekly_pdfs.py</automated>
    <fails_when>non-zero exit, or no generated_docs/run_summary.json written by the run</fails_when>
    <automated>python scripts/check_run_summary_structure.py</automated>
    <fails_when>non-zero exit, or a line beginning with FAIL naming missing or extra keys</fails_when>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>non-zero exit, or any "failed"/"error" count above 0 in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `pipeline/orchestrate.py` emits four new Helper #2 counters, all pre-seeded so they appear on every run
    - `tests/golden/run_summary_baseline.json` contains the same four keys with zero values
    - `python scripts/check_run_summary_structure.py` exits 0 after a TEST_MODE run
    - A unit test asserts the emitted key set equals the golden key set for a flag-off run
    - `python -m pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>A reader of run_summary.json can tell excluded, capability-absent, capability-present-but-idle, conflicted, and generated apart — on every run, flag on or off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conflicted source row → billing artifact | The decision determines whether a unit is billed once, twice, or not at all |
| run summary → operator and Gate 6 | A structural contract other tooling and humans depend on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-08-01 | Repudiation | conflicted-row handling | critical | mitigate | The rule is owner-decided and recorded before any code exists; the code carries a comment naming that record, so the billing outcome is attributable to a stated decision rather than to an inference |
| T-14-08-02 | Tampering | double emission of one unit | critical | mitigate | The rule is applied at a single emission point per leg, and a test asserts the unit reaches exactly one customer-facing file |
| T-14-08-03 | Denial of Service | one bad row aborting the run | high | mitigate | No exception path is introduced; a test asserts other groups still generate when a conflicted row is present — the explicit rejection of the prototype's abort-on-conflict |
| T-14-08-04 | Information Disclosure | conflict log lines naming two people | medium | mitigate | Reason code and identifiers only, routed through the existing PII-safe logging helper |
| T-14-08-05 | Repudiation | run summary key drift | medium | mitigate | Counters are pre-seeded and the golden baseline is updated in the same commit; a unit test performs the same key-set comparison Gate 6 performs |
| T-14-08-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed; 14-RESEARCH.md section "Package Legitimacy Audit" records the audit as not applicable |
</threat_model>

## Artifacts this phase produces

- `pipeline/grouping.py`: the O-14-A conflict handling at the plain-leg and shadow emission sites, using the conflict reason string locked in plan 14-01
- `pipeline/orchestrate.py`: four pre-seeded Helper #2 run-summary counters
- `tests/golden/run_summary_baseline.json`: the same four keys with zero values
- `.planning/phases/14-foreman-helper-2/14-DECISIONS.md`: the O-14-A resolution record

## Edge Assumptions (spec-less probe fallback)

| Req | Probe category | Adopted answer (assumption) |
|---|---|---|
| HLP-05 | adjacency | Both slots naming the SAME person on one row collapses into the same conflict case; if the recorded rule credits once, the person is credited once. |
| HLP-05 | empty | A row where one slot is valid and the other is blank, unchecked, or a formula-error value is not a conflict and takes the ordinary single-slot path. |
| HLP-05 | idempotency | A conflicted row produces the same outcome on every run until the source row is corrected; no state accumulates. |
| HLP-05 | concurrency | The conflict decision is per-row and stateless, so the bounded worker pool cannot produce a different outcome under parallelism. |
| HLP-03 | empty | A run with no Helper #2 data anywhere still emits every Helper #2 counter with a zero value, so the key set is invariant. |

## Blocked-work statement

Task 2 is BLOCKED on Task 1 and Task 1 is BLOCKED on Juan. This is the only blocked work in Phase 14.
If the decision has not been made when this plan is reached, the correct action is to stop this plan
and let plans 14-09 and 14-10 proceed — they do not depend on it — rather than to adopt the
recommended option unilaterally. Task 3 does not depend on Task 1 except for the conflict counter's
value, which is zero until the rule exists; if Task 2 is blocked, Task 3 may still ship with the
conflict counter pre-seeded and always zero, and that must be stated in the summary rather than
presented as coverage.

<verification>
- `python -m pytest tests/ -q` exits 0.
- `TEST_MODE=true python generate_weekly_pdfs.py` followed by `python scripts/check_run_summary_structure.py` exits 0.
- `bash scripts/run_6_gates.sh` reports all gates passed.
- The O-14-A record exists in 14-DECISIONS.md and the code comment in `pipeline/grouping.py` names it.
</verification>

<success_criteria>
The doubly-claimed row has exactly one owner-stated rule, applied in one place per leg, that never
stops a run; and every Helper #2 condition is countable from run_summary.json on any run.
</success_criteria>

<output>
Create `.planning/phases/14-foreman-helper-2/14-08-SUMMARY.md` when done. Label evidence
precisely: FIXTURE PASS plus a TEST_MODE synthetic run — that is a dry-run pass over synthetic data,
not a controlled upload and not a production observation. If Task 2 remained blocked, say so in the
first line of the summary rather than reporting the plan as complete.
</output>