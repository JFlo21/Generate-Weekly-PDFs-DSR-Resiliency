---
milestone: v1.0
milestone_name: Subcontractor Rate Logic
audited: 2026-05-19T00:00:00Z
status: tech_debt        # was gaps_found; sole requirement gap (MIG-01) descoped to v1.1 on 2026-05-19 (see Update below)
scores:
  requirements: 12/12      # SUB-01..07 (Phase 1) + SUB-08..12 (Phase 1.1) all verified code-side; MIG-01 descoped to v1.1
  phases: 2/2              # both gating phases have VERIFICATION.md, no code-side gaps
  integration: 12/12       # all SUB-IDs wired cross-phase (integration checker PASS)
  flows: 1/1               # subcontractor E2E flow complete end-to-end
gaps:
  requirements:
    - id: "MIG-01"
      status: "resolved (descoped to v1.1 on 2026-05-19)"
      phase: "Phase 2 (Railway -> Render Pre-Migration ADR) — DEFERRED"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "n/a (descoped)"
      evidence: >-
        ORIGINAL FINDING: No memory-bank/adr/0001-railway-to-render.md exists;
        Phase 2 has no directory/plans/VERIFICATION.md. REQUIREMENTS.md mapped
        MIG-01 -> Phase 2 'Pending' while ROADMAP.md marked Phase 2 DEFERRED and
        'does NOT gate v1.0 completion' — a documentation inconsistency.
        RESOLUTION (2026-05-19): MIG-01 moved from v1 Requirements to the
        v1.1-deferred section of REQUIREMENTS.md; traceability row updated to
        'Deferred -> v1.1'. It no longer counts as an unsatisfied v1.0
        requirement. The ADR ships in v1.1 alongside the migration execution.
  integration: []
  flows: []
tech_debt:
  - phase: 01-subcontractor-rate-logic-modification
    items:
      - "OPERATOR ACTION: apply billing_audit/schema.sql to Supabase (ADD COLUMN IF NOT EXISTS variant TEXT on billing_audit.pipeline_run) before first scheduled prod run, or emit_run_fingerprint upsert fails on the variant write"
      - "DEFERRED VERIFICATION: first scheduled GHA prod run after merge (ROADMAP SC#5 production portion) - byte-identical primary/helper/vac_crew/ORIG hashes + new variant attachments on both target sheets"
      - "DEFERRED VERIFICATION: Step B real-data SKIP_UPLOAD price-write spot-check (needs SMARTSHEET_API_TOKEN; deferred in Plan 01-06)"
  - phase: 01.1-subcontractor-helper-shadow-rescue
    items:
      - "WARNING WR-01: Bug A pre-acceptance rescue has only a source-grep guard, no behavioral e2e test driving _fetch_and_process_sheet (the exact 'mirror passes but production reverted' trap the new e2e file was meant to close)"
      - "WARNING WR-02: whitespace-only helper from reader produces an empty-suffix group key (e.g. ..._REDUCEDSUB_HELPER_) that the PPP whitelist accepts as in-contract"
      - "WARNING WR-03/04/05/06 + INFO IN-01..IN-04: documented follow-ups in 01.1-REVIEW.md (non-blocking)"
      - "OPERATOR ACTION: data team must deploy lookup_attribution(p_wr,p_week_ending,p_smartsheet_row_id) RPC + attribution_snapshot read access in Supabase before flipping SUBCONTRACTOR_HELPER_CLAIM_ATTRIBUTION_ENABLED=1 (fail-safe D-12 fall-back makes merge safe before this)"
      - "PENDING HUMAN-UAT (4 items, live-cron observable): shadow-variant emission for subcontractor WRs; Bug C frozen-helper after a real mid-week foreman swap; PPP cleanup whitelist deletes pre-existing off-contract orphans; hash-history prune fires once then idempotent"
  - phase: planning-hygiene
    items:
      - "TRACEABILITY GAP: SUB-08..SUB-12 (Phase 01.1 reqs) are absent from REQUIREMENTS.md traceability table AND empty in all five 01.1 SUMMARY 'requirements_completed' frontmatter ([]). They are verified only in the 01.1-VERIFICATION.md body table. Formalize before/at close."
      - "STATE DRIFT: STATE.md prose body says '50% / Phase 2 ready to plan' while its frontmatter (percent:100, completed_phases:2) and git history correctly show both gating phases complete. Reconciles automatically at milestone close."
nyquist:
  compliant_phases: 0
  partial_phases: 1        # 01.1 (VALIDATION.md is a draft, nyquist_compliant:false, Per-Task map never filled)
  missing_phases: 1        # 01 (no VALIDATION.md)
  overall: non-compliant
---

# Milestone v1.0 "Subcontractor Rate Logic" — Audit Report

**Audited:** 2026-05-19
**Status:** `gaps_found` (one unsatisfied requirement: MIG-01, deferred-by-design)

## Scope

Two gating phases. Phase 2 (MIG-01) is DEFERRED per ROADMAP and does not gate v1.0.

| Phase | VERIFICATION | Code-side result | Notes |
|-------|--------------|------------------|-------|
| 01 — Subcontractor Rate Logic Modification | present | `human_needed` — 7/7 ROADMAP SC + 12/12 gap-closure findings VERIFIED; 0 code-side gaps | 14/14 plans; 3 deferred operator-action items |
| 01.1 — Helper-Shadow Rescue (INSERTED) | present | `human_needed` — 6/7 ROADMAP SC verified/human-deferred; 2 BLOCKERs (CR-01/CR-02) RESOLVED 2026-05-20 | 5/5 plans; 4 pending HUMAN-UAT; WR/IN follow-ups deferred |
| 02 — Railway→Render Pre-Migration ADR | absent | DEFERRED — not started, out of v1.0 scope | MIG-01 lives here |

## Requirements Coverage (3-source cross-reference)

Sources: phase VERIFICATION.md status/evidence · SUMMARY `requirements_completed` frontmatter · REQUIREMENTS.md traceability `[ ]`/`[x]`.

| REQ-ID | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final status |
|--------|--------------|---------------------|-----------------|--------------|
| SUB-01 | VERIFIED (code-side) | listed (01-02, 01-03) | `[ ]` "Implemented (pending operator verification)" | **partial** — code-verified, awaiting live verification |
| SUB-02 | VERIFIED (code-side) | listed (01-02, 01-03) | `[ ]` | **partial** |
| SUB-03 | VERIFIED (code-side) | listed (01-04) | `[ ]` | **partial** |
| SUB-04 | VERIFIED (code-side) | listed (01-01, 01-03) | `[ ]` | **partial** |
| SUB-05 | VERIFIED (code-side) | listed (01-02/03/04) | `[ ]` | **partial** |
| SUB-06 | VERIFIED (code-side) | listed (01-01..04) | `[ ]` | **partial** |
| SUB-07 | VERIFIED (code-side) | listed (01-05) | `[ ]` | **partial** — also needs Supabase schema apply |
| MIG-01 | missing (no Phase 2) | missing | `[ ]` "Pending" | **unsatisfied** — deferred-by-design |
| SUB-08 | VERIFIED code-side (01.1 VERIF body) | **missing (`[]`)** | **not present** | **partial** + traceability gap |
| SUB-09 | VERIFIED code-side | **missing (`[]`)** | **not present** | **partial** + traceability gap |
| SUB-10 | VERIFIED code-side | **missing (`[]`)** | **not present** | **partial** + traceability gap |
| SUB-11 | VERIFIED code-side | **missing (`[]`)** | **not present** | **partial** + traceability gap (RPC deploy pending) |
| SUB-12 | VERIFIED code-side | **missing (`[]`)** | **not present** | **partial** + traceability gap |

**Coverage:** 7/8 mapped v1 requirements verified code-side; **1 unsatisfied (MIG-01, deferred)**. 5 Phase-01.1 requirements (SUB-08..12) verified code-side but **not formalized** in REQUIREMENTS.md or SUMMARY frontmatter — a traceability gap, not a code gap. **0 fully "satisfied"** in the strict sense because every SUB requirement's production-observable success criteria are deferred to live cron / operator deploy.

## Cross-Phase Integration

**PASS — 0 wiring gaps.** Phase 01.1 consumes Phase 01's exports correctly:
- `lookup_attribution` defined at `billing_audit/writer.py:673`, imported + called at `generate_weekly_pdfs.py:4792-4793`.
- Bug B1 partitioning gate reads the same hoisted `is_subcontractor_row` flag (gate at L4614).
- Variant vocabulary consistent end-to-end: group keys (`_REDUCEDSUB`/`_AEPBILLABLE`/`_*_HELPER_*`) → internal strings (`reduced_sub`/`aep_billable`/`*_helper`) → `build_group_identity` round-trip (L2414-2432) → filter matchers (in sync) → `pipeline_run.variant` → PPP whitelist `{'reduced_sub','reduced_sub_helper'}` (L7831).
- Bug C schema contract present in `billing_audit/schema.sql` (L157-199): `attribution_snapshot` columns + `lookup_attribution` RPC param contract. Prior CR-01 BLOCKER confirmed resolved (DDL bodies intentionally data-team-owned, same pattern as `freeze_attribution`).
- **E2E flow complete:** subcontractor row → rescue price → partitioned variant emission → dual-target upload → PPP cleanup whitelist → attribution recorded. No broken steps.
- **Tests:** `pytest tests/ -q` → **682 passed / 26 skipped / 58 subtests** (matches claimed baseline).

## Nyquist Coverage

| Phase | VALIDATION.md | nyquist_compliant | Action |
|-------|---------------|-------------------|--------|
| 01 | **missing** | — | `/gsd-validate-phase 1` (optional — retroactive) |
| 01.1 | present (draft) | `false` (Per-Task Verification Map never populated) | `/gsd-validate-phase 1.1` (optional — retroactive) |

Discovery only — these do not block close. The pytest suite (682 green, +59 net for Phase 01.1) provides strong de-facto coverage; the formal Nyquist VALIDATION docs were never completed.

## Bottom line

The **code is done and integration-clean** — both gating phases verified code-side, all 12 SUB-IDs wired, 682 tests green, both Phase-01.1 BLOCKERs resolved. The audit is `gaps_found` for exactly one reason: **MIG-01 is a v1-mapped requirement whose phase (2) was deliberately deferred.** Everything else is either deferred *live-production verification* (4 HUMAN-UAT items + 3 operator-action items) or *planning hygiene* (SUB-08..12 untracked in REQUIREMENTS.md; STATE.md prose drift; missing Nyquist docs) — none of which is a broken implementation.

### Recommended path to a clean close
1. **Resolve MIG-01** — formally move it to v1.1 (it is already in STATE.md Deferred Items + the v1.1 plan). This is a one-line REQUIREMENTS.md/ROADMAP edit, not a build task.
2. **Formalize SUB-08..12** in REQUIREMENTS.md traceability so Phase 01.1's coverage is recorded.
3. Then `/gsd-complete-milestone v1.0` — accept the deferred live-cron UAT + operator-deploy items as documented known gaps (they validate post-merge on the next scheduled run).

## Update — 2026-05-19 (gap closure)

Steps 1 and 2 of the recommended path are **done**. Status flipped `gaps_found` → `tech_debt`.

- **MIG-01 descoped to v1.1.** Moved from v1 Requirements to the v1.1-deferred section of REQUIREMENTS.md; traceability row now reads `Deferred → v1.1`. It is no longer an unsatisfied v1.0 requirement. ROADMAP.md already marked Phase 2 DEFERRED.
- **SUB-08..SUB-12 formalized.** Added as a Phase 1.1 requirements block in REQUIREMENTS.md with traceability rows (status: Implemented, pending operator verification). ROADMAP.md Phase 1.1 "Requirements" note updated to drop "to be formalized".
- **Requirements score:** now 12/12 mapped v1 requirements verified code-side (was 7/8 with MIG-01 as the lone unsatisfied).

**Remaining = tech_debt, not blockers** (accepted at close, validated post-merge):
- 4 pending HUMAN-UAT items (live-cron observable) + 3 operator-action items (apply `billing_audit/schema.sql`; data team deploys `lookup_attribution` RPC; Step B price-write spot-check).
- Phase 01.1 WARNING/INFO follow-ups (WR-01..06, IN-01..04) — non-blocking.
- Nyquist VALIDATION docs incomplete (Phase 01 missing, Phase 01.1 draft) — 682 passing tests give de-facto coverage.

**Next:** `/gsd-complete-milestone v1.0` (pre-flight now sees `tech_debt`, routes to "complete & accept debt").
