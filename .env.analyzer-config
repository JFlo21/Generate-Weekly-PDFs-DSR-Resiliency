# Code Analyzer Agent Configuration
# ==================================
# This file configures the automated code analysis agent that scans your
# Python code for errors and sends notifications.

# =============================================================================
# NOTIFICATION OPTIONS (Choose one or both)
# =============================================================================

# OPTION 1: GitHub Actions Notifications (RECOMMENDED - No Setup Required!)
# --------------------------------------------------------------------------
# The Code Analyzer automatically writes detailed reports to GitHub Actions
# workflow summaries. To receive notifications:
#
# 1. Go to the repository on GitHub
# 2. Click "Watch" (bell icon) and select "All Activity" or "Custom" → "Workflows"
# 3. You'll receive GitHub notifications when analysis finds issues
#
# This works automatically - no SMTP server required!

# OPTION 2: Email Notifications (OPTIONAL - Requires SMTP Setup)
# ---------------------------------------------------------------
# Only needed if you want direct email notifications in addition to GitHub.
# Most users can skip this section and use GitHub notifications instead.

# Email Notification Settings (Optional)
# --------------------------------------
# Comma-separated list of email addresses to receive error notifications
# Leave empty to use GitHub notifications only
ANALYZER_EMAIL_RECIPIENTS=

# Set to 'true' to send emails even when no issues are found (status reports)
ANALYZER_EMAIL_ALWAYS=false

# SMTP Configuration (only required if using email notifications)
# For Gmail, use smtp.gmail.com with an App Password
# For Office 365, use smtp.office365.com
# ANALYZER_SMTP_SERVER=smtp.gmail.com
# ANALYZER_SMTP_PORT=587
# ANALYZER_SMTP_USERNAME=your-email@company.com
# ANALYZER_SMTP_PASSWORD=your-app-password
# ANALYZER_SMTP_USE_TLS=true
# ANALYZER_SENDER_EMAIL=code-analyzer@linetecservices.com

# =============================================================================
# ANALYSIS CONFIGURATION
# =============================================================================

# Minimum severity level to report: 'info', 'warning', or 'error'
# - info: Reports all issues including informational messages
# - warning: Reports warnings and errors only
# - error: Reports only critical errors
ANALYZER_MIN_SEVERITY=warning

# Include fix suggestions in reports (recommended)
ANALYZER_INCLUDE_SUGGESTIONS=true

# Patterns to exclude from analysis (comma-separated)
# Default excludes: __pycache__, .git, venv, env, .env, node_modules
ANALYZER_EXCLUDE_PATTERNS=

# Report Output Settings
# ----------------------
# Directory where analysis reports are saved
ANALYZER_REPORT_DIR=generated_docs/code_analysis

# Brand color for HTML reports (optional)
ANALYZER_BRAND_COLOR=#C00000

# =============================================================================
# INTEGRATIONS (OPTIONAL)
# =============================================================================

# Sentry Integration
# ------------------
# If SENTRY_DSN is configured in main .env, errors from the analyzer
# will also be sent to Sentry for centralized error tracking
